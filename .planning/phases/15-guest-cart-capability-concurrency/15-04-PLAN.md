---
phase: 15-guest-cart-capability-concurrency
plan: 04
type: execute
wave: 3
depends_on: [15-03]
files_modified:
  - apps/backend/medusa-config.ts
  - apps/backend/src/infrastructure/__tests__/medusa-config.unit.spec.ts
  - apps/backend/src/links/guest-cart-capability-cart.ts
  - apps/backend/src/modules/guest-cart-capability/migrations/
  - apps/backend/src/modules/guest-cart-capability/__tests__/guest-cart-capability.postgres.spec.ts
autonomous: false
requirements: [CART-01, CART-03]
must_haves:
  truths:
    - "Migration CLI nasce depois do model e aplica só em PostgreSQL descartável."
    - "token_hash UNIQUE e uma capability active por cart_id são autoridade PostgreSQL."
    - "Module link Cart ↔ guestCartCapability existe sem FK cross-module."
  artifacts:
    - path: apps/backend/src/modules/guest-cart-capability/migrations/
      provides: CLI-generated migration + snapshot
    - path: apps/backend/src/links/guest-cart-capability-cart.ts
      provides: defineLink isList true
    - path: apps/backend/src/modules/guest-cart-capability/__tests__/guest-cart-capability.postgres.spec.ts
      provides: UNIQUE/hash-only/one-active
  key_links:
    - from: apps/backend/src/links/guest-cart-capability-cart.ts
      to: apps/backend/src/links/payment-attempt-cart.ts
      via: defineLink CartModule.linkable.cart
      pattern: "defineLink|isList"
  prohibitions:
    - "Não usar supabase db push nem DB remoto."
    - "Não copiar bigint de 13-VALIDATION.md."
    - "Não promover rotas HTTP neste plano."
---

<objective>
Registrar o módulo, gerar migration Medusa e provar constraints PostgreSQL (CART-01).

Purpose: P15-D01 persistido com generate+migrate local descartável.
Output: medusa-config, link, migration CLI, suite postgres.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-guest-cart-capability-concurrency/15-03-PLAN.md
@.planning/phases/13-storefront-contract-foundation-surface-lockdown/13-05-SUMMARY.md
@apps/backend/medusa-config.ts
@apps/backend/src/links/payment-attempt-cart.ts
@apps/backend/src/modules/store-resource-version/migrations/Migration20260809201808.ts
</context>

<tasks>

<task type="auto">
  <name>Task 15-04-01: Registrar módulo, link e regressão de config</name>
  <files>apps/backend/medusa-config.ts, apps/backend/src/infrastructure/__tests__/medusa-config.unit.spec.ts, apps/backend/src/links/guest-cart-capability-cart.ts</files>
  <read_first>apps/backend/medusa-config.ts módulos tracking_access_token e store_resource_version; apps/backend/src/infrastructure/__tests__/medusa-config.unit.spec.ts expectedLocalModules; apps/backend/src/links/payment-attempt-cart.ts; apps/backend/src/modules/guest-cart-capability/index.ts</read_first>
  <action>Adicionar { key: guest_cart_capability, resolve: ./src/modules/guest-cart-capability } ao array de módulos, uma vez, junto dos custom modules existentes. Atualizar expectedLocalModules na unit de config para incluir o resolve exatamente uma vez. Criar defineLink(CartModule.linkable.cart, { linkable: GuestCartCapabilityModule.linkable.guestCartCapability, isList: true }). Não importar o service de outro módulo. Config regression deve PASS antes do generate.</action>
  <verify>
    <automated>npm run test:unit -w @dtc/backend -- --runTestsByPath src/infrastructure/__tests__/medusa-config.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>Módulo aparece uma vez; link isList true; unit de config verde.</acceptance_criteria>
  <done>Registro pronto para o CLI generate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15-04-02: [BLOCKING] medusa db:generate + migrate descartável + provas PG</name>
  <files>apps/backend/src/modules/guest-cart-capability/migrations/, apps/backend/src/modules/guest-cart-capability/__tests__/guest-cart-capability.postgres.spec.ts</files>
  <read_first>apps/backend/src/modules/store-resource-version/migrations/Migration20260809201808.ts; .planning/phases/13-storefront-contract-foundation-surface-lockdown/13-05-SUMMARY.md P13-13-05-HCD-01 integer; apps/backend/src/modules/store-resource-version/__tests__/store-resource-version.postgres.spec.ts; apps/backend/integration-tests/helpers/guest-cart-postgres.ts; .planning/phases/14-customer-auth-verification/14-07-PLAN.md db:generate pattern</read_first>
  <behavior>
  - token_hash UNIQUE; segunda linha active para o mesmo cart_id falha; plaintext canário ausente.
  - Redis down não altera o resultado das constraints.
  </behavior>
  <action>Somente após a unit de config PASS: do diretório apps/backend executar npx medusa db:generate guest_cart_capability. Preservar filename/class/snapshot gerados. Complementar CHECK de status e UNIQUE parcial active por cart_id somente pelo mecanismo autorizado do projeto (como 13-05/14-07), sem reescrever a migration à mão de forma incompatível com o snapshot. Aplicar somente no PostgreSQL descartável via o runner existente (migrateDisposableMedusaSchema ou equivalente local da Phase 14) — nunca supabase db push, nunca DATABASE_URL remoto. Spec postgres: hash-only inspect do 15-01, UNIQUE token_hash, one-active-per-cart, statuses consumed/expired/revoked. Um spec por processo. Version columns neste módulo não existem; não copiar bigint. Se generate falhar, BLOCKED — não inventar DDL paralelo.</action>
  <verify>
    <automated>node apps/backend/scripts/run-disposable-postgres-tests.mjs -- npm run test:integration:modules -w @dtc/backend -- --runTestsByPath src/modules/guest-cart-capability/__tests__/guest-cart-capability.postgres.spec.ts</automated>
  </verify>
  <acceptance_criteria>CLI generate exit 0; snapshot/model alinhados; PG descartável prova UNIQUE/hash-only/one-active; cleanup loopback.</acceptance_criteria>
  <done>CART-01 persistido com autoridade PostgreSQL.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 15-04-03: Revisar migration/link</name>
  <files>None (checkpoint)</files>
  <read_first>apps/backend/src/modules/guest-cart-capability/migrations/; apps/backend/src/links/guest-cart-capability-cart.ts; apps/backend/medusa-config.ts; output sanitizado do generate e do spec PG</read_first>
  <action>Apresentar identidade CLI, constraints e prova de que a migration não foi aplicada em alvo persistente/remoto. Não promover rotas.</action>
  <verify>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>Humano PASS ou BLOCKED; confirma generate local-only.</acceptance_criteria>
  <done>Gate manual da persistência.</done>
</task>

</tasks>

## Manual gate / NO DEPLOY

Parar após `15-04-SUMMARY.md`. **NO DEPLOY.** Sem migrate remoto.

## Artifacts this phase produces

- medusa-config key `guest_cart_capability`
- `defineLink` Cart ↔ `guestCartCapability`
- CLI migration under `apps/backend/src/modules/guest-cart-capability/migrations/`
- postgres proofs UNIQUE / one-active / hash-only

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI generate → local files | generated SQL must not target remote DSN |
| module → Cart | association via link, not FK into cart table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-01 | Tampering | token_hash UNIQUE | mitigate | PostgreSQL UNIQUE + spec concorrente |
| T-15-04-02 | Elevation of Privilege | cart_id | mitigate | sem FK; uma active por cart via partial UNIQUE |
| T-15-04-03 | Information Disclosure | migration SQL | mitigate | nenhuma coluna plaintext no DDL gerado |
| T-15-04-SC | Tampering | npm | accept | nenhum package install; só CLI Medusa já instalado |
</threat_model>

<verification>Config unit + um processo PG disposable; revisão humana.</verification>
<success_criteria>Módulo migrável localmente com constraints CART-01.</success_criteria>
<output>Create `15-04-SUMMARY.md` e pare. NO DEPLOY.</output>
