---
phase: 15-guest-cart-capability-concurrency
plan: 13
type: execute
wave: 12
depends_on: [15-12]
files_modified:
  - apps/backend/src/api/store/carts/[id]/line-items/[line_id]/route.ts
  - apps/backend/src/api/store-surface/manifest.ts
  - apps/backend/src/api/store-surface/__tests__/manifest.unit.spec.ts
  - apps/backend/src/api/middlewares.ts
  - apps/backend/integration-tests/http/guest-cart-line-item-delete.spec.ts
autonomous: false
requirements: [CART-05, CART-07, CART-08]
must_haves:
  truths:
    - "DELETE /store/carts/{id}/line-items/{line_id} chama deleteLineItemsWorkflow com o id único."
    - "If-Match, CAS, idempotência P15-D07 e 412 permanecem iguais às outras mutações."
  artifacts:
    - path: apps/backend/src/api/store/carts/[id]/line-items/[line_id]/route.ts
      provides: DELETE remove local
    - path: apps/backend/integration-tests/http/guest-cart-line-item-delete.spec.ts
      provides: delete + 412 + replay
  key_links:
    - from: apps/backend/src/api/store/carts/[id]/line-items/[line_id]/route.ts
      to: node_modules/@medusajs/core-flows
      via: deleteLineItemsWorkflow
      pattern: "deleteLineItemsWorkflow"
  prohibitions:
    - "Não duplicar origin local."
    - "Não tratar DELETE sem If-Match como best-effort."
---

<objective>
Expor delete por item M1 via deleteLineItemsWorkflow (CART-05).

Purpose: terceira key nativa promovida a native+local_extension.
Output: DELETE by line_id + HTTP.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@apps/backend/src/api/store/carts/[id]/line-items/[line_id]/route.ts
@apps/backend/src/api/store-surface/manifest.ts
</context>

## Staged counts

M1 10→11, DENY 48→47, nativeLocalExtension 4→5, total 63, native identity 51. Após este plano as 3 line-item keys nativas estão M1; DENY de cart nativo restante permanece 47 no recorte RESEARCH final (ainda sem a row nova de clear).

<tasks>

<task type="auto" tdd="true">
  <name>Task 15-13-01: DELETE by line_id + promoção</name>
  <files>apps/backend/src/api/store/carts/[id]/line-items/[line_id]/route.ts, apps/backend/src/api/store-surface/manifest.ts, apps/backend/src/api/store-surface/__tests__/manifest.unit.spec.ts, apps/backend/src/api/middlewares.ts</files>
  <read_first>apps/backend/src/api/store/carts/[id]/line-items/[line_id]/route.ts POST update; manifest DELETE line_id entry; core-flows deleteLineItemsWorkflow({ ids })</read_first>
  <behavior>
  - DELETE remove o item, bumpa ETag, invalida PA + SHP no-op.
  </behavior>
  <action>Export DELETE no mesmo arquivo local. Input ids: [line_id]. Ordem P15-D07. Promover DELETE .../{line_id} para M1_ENABLED native+local_extension. Middleware method DELETE no matcher :line_id. Counts: m1 11, deny 47, nativeLocalExtension 5.</action>
  <verify>
    <automated>npm run test:unit -w @dtc/backend -- --runTestsByPath src/api/store-surface/__tests__/manifest.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>M1=11; deny=47; nativeLocalExtension=5; native identity 51.</acceptance_criteria>
  <done>Delete-per-item M1 ligado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15-13-02: HTTP delete, 412 e replay</name>
  <files>apps/backend/integration-tests/http/guest-cart-line-item-delete.spec.ts</files>
  <read_first>apps/backend/integration-tests/http/guest-cart-line-item-update.spec.ts</read_first>
  <behavior>
  - Delete 200 cart sem o line; stale 412; same-key replay não 412; sem capability 404.
  </behavior>
  <action>Add then delete. Leakage. Zero Order. Wrong line_id fail-closed sem enumerar carts alheios.</action>
  <verify>
    <automated>npm run test:integration:http -w @dtc/backend -- --runTestsByPath integration-tests/http/guest-cart-line-item-delete.spec.ts</automated>
  </verify>
  <acceptance_criteria>FE-CART-003 delete PASS com CART-08.</acceptance_criteria>
  <done>Delete comprovado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 15-13-03: Revisar delete</name>
  <files>None (checkpoint)</files>
  <read_first>apps/backend/src/api/store/carts/[id]/line-items/[line_id]/route.ts; apps/backend/src/api/store-surface/manifest.ts</read_first>
  <action>Confirmar as 3 keys nativas como native+local_extension e identity 51.</action>
  <verify>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>Humano PASS ou BLOCKED.</acceptance_criteria>
  <done>Gate manual delete.</done>
</task>

</tasks>

## Manual gate / NO DEPLOY

Parar após `15-13-SUMMARY.md`. **NO DEPLOY.**

## Artifacts this phase produces

- DELETE `deleteLineItemsWorkflow` per-item handler
- M1 key DELETE /store/carts/{id}/line-items/{line_id}

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DELETE path params → workflow | untrusted cart id / line id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-13-01 | Elevation of Privilege | {id}/{line_id} | mitigate | capability + cart ativo do ator |
| T-15-13-02 | Tampering | If-Match | mitigate | CAS + 412 |
| T-15-13-SC | Tampering | npm | accept | nenhum install |
</threat_model>

<verification>Manifest unit + HTTP delete; revisão humana.</verification>
<success_criteria>Delete-per-item M1 sem segundo engine.</success_criteria>
<output>Create `15-13-SUMMARY.md` e pare. NO DEPLOY.</output>
