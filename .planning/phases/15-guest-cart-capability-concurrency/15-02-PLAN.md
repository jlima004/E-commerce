---
phase: 15-guest-cart-capability-concurrency
plan: 02
type: execute
wave: 1
depends_on: [15-01]
files_modified:
  - docs/DB_MODEL_v1.22.md
  - apps/backend/src/modules/guest-cart-capability/types.ts
  - apps/backend/src/modules/guest-cart-capability/hash.ts
  - apps/backend/src/modules/guest-cart-capability/models/guest-cart-capability.ts
  - apps/backend/src/modules/guest-cart-capability/__tests__/guest-cart-capability-hash.unit.spec.ts
autonomous: false
requirements: [CART-01, CART-02]
must_haves:
  truths:
    - "DB_MODEL descreve GuestCartCapability hash-only antes de existir migration."
    - "CSPRNG de 32 bytes vira header base64url; persistência lógica é só SHA-256 hex."
    - "O model Medusa não declara coluna de plaintext nem nonce/HKDF."
  artifacts:
    - path: docs/DB_MODEL_v1.22.md
      provides: entidade GuestCartCapability e constraints P15-D01
    - path: apps/backend/src/modules/guest-cart-capability/types.ts
      provides: status active|expired|revoked|consumed e mint result
    - path: apps/backend/src/modules/guest-cart-capability/hash.ts
      provides: generate/hash/compare SHA-256
    - path: apps/backend/src/modules/guest-cart-capability/models/guest-cart-capability.ts
      provides: model.define guest_cart_capability prefix gccap
  key_links:
    - from: apps/backend/src/modules/guest-cart-capability/models/guest-cart-capability.ts
      to: apps/backend/src/modules/tracking-access-token/models/tracking-access-token.ts
      via: model.define + UNIQUE token_hash; statuses extra consumed
      pattern: "token_hash|consumed"
  prohibitions:
    - "Não persistir plaintext, nonce, Idempotency-Key raw, cookie ou JWT."
    - "Não copiar HMAC-pepper de tracking nem HKDF de auth."
    - "Não executar medusa db:generate neste plano."
    - "Não emitir header HTTP nem promover rotas."
---

<objective>
Fixar o contrato de persistência hash-only (CART-01) em DB_MODEL, types, hash e model DML antes do service e da migration.

Purpose: P15-D01/P15-D02 e D15-02 ficam tipados sem DDL ainda.
Output: documentação + primitives + model; plaintext só no tipo de mint result.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-guest-cart-capability-concurrency/15-01-PLAN.md
@.planning/phases/15-guest-cart-capability-concurrency/15-RESEARCH.md
@docs/DB_MODEL_v1.22.md
@apps/backend/src/modules/tracking-access-token/types.ts
@apps/backend/src/modules/tracking-access-token/models/tracking-access-token.ts
@apps/backend/src/modules/customer-auth/bff-service-auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 15-02-01: Documentar GuestCartCapability no DB_MODEL</name>
  <files>docs/DB_MODEL_v1.22.md</files>
  <read_first>docs/DB_MODEL_v1.22.md changelog e seções StoreIdempotencyRecord/StoreResourceVersion; 15-RESEARCH.md Q-01; PLAN locks P15-D01/P15-D08 em 15-01-PLAN.md</read_first>
  <action>Atualizar changelog para registrar GuestCartCapability nesta revisão sem inventar major. Declarar owner module guest_cart_capability, campos id, cart_id lógico (texto do cart Medusa, não FK SQL), token_hash SHA-256 hex UNIQUE, status active|expired|revoked|consumed, expires_at, consumed_at nullable, revoked_at nullable, last_used_at nullable, timestamps Medusa. Cardinalidade: no máximo uma linha active por cart_id via UNIQUE parcial. Vínculo Cart via module link, nunca FK cross-module. TTL 7d rolling / teto 30d desde created_at. Negativas: plaintext, nonce, pepper HMAC, HKDF, Redis como autoridade. Não escrever DDL SQL neste documento além do contrato lógico.</action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('docs/DB_MODEL_v1.22.md','utf8'); for (const x of ['GuestCartCapability','guest_cart_capability','token_hash','consumed','gccap','7d','30d']) if (!s.includes(x)) throw new Error('missing '+x)"</automated>
  </verify>
  <acceptance_criteria>DB_MODEL nomeia a entidade, UNIQUE de hash, consumed, link sem FK, TTL 7d/30d e negativas de plaintext.</acceptance_criteria>
  <done>P15-D01 está no DB_MODEL antes do service.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15-02-02: Types, hash SHA-256 e model DML</name>
  <files>apps/backend/src/modules/guest-cart-capability/types.ts, apps/backend/src/modules/guest-cart-capability/hash.ts, apps/backend/src/modules/guest-cart-capability/models/guest-cart-capability.ts, apps/backend/src/modules/guest-cart-capability/__tests__/guest-cart-capability-hash.unit.spec.ts</files>
  <read_first>apps/backend/src/modules/tracking-access-token/types.ts; apps/backend/src/modules/tracking-access-token/models/tracking-access-token.ts; apps/backend/src/modules/tracking-access-token/service.ts generateTrackingAccessToken e compare; apps/backend/src/modules/customer-auth/bff-service-auth.ts digestSecret SHA-256; apps/backend/src/modules/store-resource-version/models/store-resource-version.ts indexes where deleted_at</read_first>
  <behavior>
  - generateGuestCartCapability(randomBytesFn) pede 32 bytes e devolve base64url; hashGuestCartCapability é hex SHA-256 estável; compareGuestCartCapabilityHash usa timingSafeEqual e recusa buffers de tamanho diferente.
  - GuestCartCapabilityRecord não possui campo de plaintext; MintGuestCartCapabilityResult tem plaintext_token somente no retorno de mint.
  </behavior>
  <action>Criar GUEST_CART_CAPABILITY_STATUS com ACTIVE, EXPIRED, REVOKED, CONSUMED. GUEST_CART_CAPABILITY_RANDOM_BYTES = 32. Header constant GUEST_CART_CAPABILITY_HEADER = x-indicio-guest-cart-token. hash.ts: generate com toString base64url; hash com createHash sha256 utf8 hex; compare timingSafeEqual; dummy hash SHA-256 de um literal fixo interno (sem HMAC). Model define guest_cart_capability, prefix gccap, token_hash text, cart_id text, status enum default active, expires_at, consumed_at nullable, revoked_at nullable, last_used_at nullable. Indexes: UNIQUE token_hash; UNIQUE parcial (cart_id) WHERE status = active AND deleted_at IS NULL se o DML aceitar where; caso o DML não aceite o predicado de status, deixar o predicado para a migration CLI no plano 04 e documentar no types. Não incluir coluna plaintext. Testes unitários cobrem encoding, hash, compare e ausência de campos proibidos no Record type via expect(Object.keys).</action>
  <verify>
    <automated>npm run test:unit -w @dtc/backend -- --runTestsByPath src/modules/guest-cart-capability/__tests__/guest-cart-capability-hash.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>32 bytes base64url; SHA-256 hex; timingSafeEqual; model sem plaintext/nonce; CONSUMED existe; header constant bate com CART-02.</acceptance_criteria>
  <done>Contrato DML/hash está pronto para o service do 15-03.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 15-02-03: Revisar contrato hash-only</name>
  <files>None (checkpoint)</files>
  <read_first>docs/DB_MODEL_v1.22.md; apps/backend/src/modules/guest-cart-capability/types.ts; apps/backend/src/modules/guest-cart-capability/hash.ts; apps/backend/src/modules/guest-cart-capability/models/guest-cart-capability.ts</read_first>
  <action>Apresentar campos, UNIQUE, encoding base64url e negativas. Não gerar migration.</action>
  <verify>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>Humano PASS ou BLOCKED; confirma P15-D01/P15-D02 no diff.</acceptance_criteria>
  <done>Gate manual do contrato de persistência.</done>
</task>

</tasks>

## Manual gate / NO DEPLOY

Parar após `15-02-SUMMARY.md`. **NO DEPLOY.**

## Artifacts this phase produces

- `GUEST_CART_CAPABILITY_STATUS`, `GUEST_CART_CAPABILITY_HEADER`, `GUEST_CART_CAPABILITY_RANDOM_BYTES`
- `generateGuestCartCapability`, `hashGuestCartCapability`, `compareGuestCartCapabilityHash`
- `GuestCartCapabilityRecord`, `MintGuestCartCapabilityResult`
- model `guest_cart_capability` prefix `gccap`
- DB_MODEL entity `GuestCartCapability`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mint return → persisted record | plaintext must not cross into the record/table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-01 | Information Disclosure | GuestCartCapabilityRecord | mitigate | plaintext only on MintGuestCartCapabilityResult |
| T-15-02-02 | Tampering | token_hash | mitigate | SHA-256 hex UNIQUE; timingSafeEqual |
| T-15-02-03 | Spoofing | generateGuestCartCapability | mitigate | 32 CSPRNG bytes base64url |
| T-15-02-SC | Tampering | npm | accept | nenhum install |
</threat_model>

<verification>Node scan do DB_MODEL + unit hash; revisão humana.</verification>
<success_criteria>CART-01 contrato DML/hash existe sem migration nem HTTP.</success_criteria>
<output>Create `15-02-SUMMARY.md` e pare. NO DEPLOY.</output>
