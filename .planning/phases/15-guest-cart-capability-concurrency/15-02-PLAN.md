---
phase: 15-guest-cart-capability-concurrency
plan: 02
type: execute
wave: 1
depends_on: [15-01]
files_modified:
  - docs/DB_MODEL_v1.22.md
  - apps/backend/src/modules/guest-cart-capability/types.ts
  - apps/backend/src/modules/guest-cart-capability/hash.ts
  - apps/backend/src/modules/guest-cart-capability/models/guest-cart-capability.ts
  - apps/backend/src/modules/guest-cart-capability/service.ts
  - apps/backend/src/modules/guest-cart-capability/lookup.ts
  - apps/backend/src/modules/guest-cart-capability/index.ts
  - apps/backend/medusa-config.ts
  - apps/backend/src/infrastructure/__tests__/medusa-config.unit.spec.ts
  - apps/backend/src/links/guest-cart-capability-cart.ts
  - apps/backend/src/modules/guest-cart-capability/migrations/
  - apps/backend/src/modules/guest-cart-capability/__tests__/guest-cart-capability-hash.unit.spec.ts
  - apps/backend/src/modules/guest-cart-capability/__tests__/guest-cart-capability-service.unit.spec.ts
  - apps/backend/src/modules/guest-cart-capability/__tests__/guest-cart-capability.postgres.spec.ts
autonomous: false
requirements: [CART-01, CART-02, CART-03]
must_haves:
  truths:
    - "DB_MODEL descreve GuestCartCapability hash-only antes de existir migration."
    - "CSPRNG de 32 bytes vira header base64url; persistência lógica é só SHA-256 hex."
    - "O model Medusa não declara coluna de plaintext nem nonce/HKDF."
    - "mint persiste somente token_hash e devolve plaintext uma vez."
    - "lookup de miss executa compare dummy antes do throw uniforme."
    - "expire/revoke/consume são transições de service; Redis não concede validade."
    - "Migration CLI nasce depois do model e aplica só em PostgreSQL descartável."
    - "token_hash UNIQUE e uma capability active por cart_id são autoridade PostgreSQL."
    - "Module link Cart ↔ guestCartCapability existe sem FK cross-module."
  artifacts:
    - path: docs/DB_MODEL_v1.22.md
      provides: entidade GuestCartCapability e constraints P15-D01
    - path: apps/backend/src/modules/guest-cart-capability/hash.ts
      provides: generate/hash/compare SHA-256 de produção
    - path: apps/backend/src/modules/guest-cart-capability/service.ts
      provides: mint/expire/revoke/consume/touchRollingTtl
    - path: apps/backend/src/modules/guest-cart-capability/lookup.ts
      provides: lookupByPresentedToken dummy-on-miss
    - path: apps/backend/src/modules/guest-cart-capability/migrations/
      provides: CLI-generated migration + snapshot
    - path: apps/backend/src/links/guest-cart-capability-cart.ts
      provides: defineLink isList true
  key_links:
    - from: apps/backend/src/modules/guest-cart-capability/hash.ts
      to: apps/backend/src/modules/customer-auth/bff-service-auth.ts
      via: SHA-256 utf8 hex unpepered
      pattern: "createHash\\(\"sha256\"\\)"
    - from: apps/backend/src/modules/guest-cart-capability/lookup.ts
      to: apps/backend/src/modules/tracking-access-token/lookup.ts
      via: dummy compare then uniform throw
      pattern: "performDummy|compareGuestCartCapabilityHash"
    - from: apps/backend/src/links/guest-cart-capability-cart.ts
      to: apps/backend/src/links/payment-attempt-cart.ts
      via: defineLink CartModule.linkable.cart
      pattern: "defineLink|isList"
  prohibitions:
    - "Não persistir plaintext, nonce, Idempotency-Key raw, cookie ou JWT."
    - "Não copiar HMAC-pepper de tracking nem HKDF de auth."
    - "Não usar supabase db push nem DB remoto."
    - "Não emitir header HTTP nem promover rotas."
    - "Não inventar DDL paralelo se medusa db:generate falhar — BLOCKED."
---

<objective>
Entregar o domínio guest-capability completo e persistível: contrato, primitives CSPRNG/SHA-256, service, lookup, módulo, link e provas PostgreSQL.

Purpose: CART-01/CART-03 em um único estado testável, sem três cortes intermediários.
Output: módulo migrável localmente com UNIQUE/hash-only/one-active; nenhuma rota M1 ainda.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-guest-cart-capability-concurrency/15-01-PLAN.md
@.planning/phases/15-guest-cart-capability-concurrency/15-RESEARCH.md
@.planning/phases/15-guest-cart-capability-concurrency/15-PATTERNS.md
@docs/DB_MODEL_v1.22.md
@apps/backend/src/modules/tracking-access-token/types.ts
@apps/backend/src/modules/tracking-access-token/models/tracking-access-token.ts
@apps/backend/src/modules/tracking-access-token/service.ts
@apps/backend/src/modules/tracking-access-token/lookup.ts
@apps/backend/src/modules/tracking-access-token/index.ts
@apps/backend/src/modules/customer-auth/bff-service-auth.ts
@apps/backend/medusa-config.ts
@apps/backend/src/links/payment-attempt-cart.ts
@apps/backend/src/modules/store-resource-version/migrations/Migration20260809201808.ts
</context>

Honor P15-D01, P15-D02, P15-D08 from 15-01-PLAN.md. Clock/entropy of 15-01 may be injected into mint tests; production generate/hash/compare live in `hash.ts`.

Task order inside this plan (do not reorder):

1. DB_MODEL
2. constants/types
3. CSPRNG generate
4. SHA-256 hash/compare
5. Medusa model
6. service
7. lookup dummy-on-miss
8. lifecycle primitives
9. module index
10. medusa-config
11. module link Cart ↔ capability
12. `medusa db:generate`
13. disposable PostgreSQL migrate
14. unit + PostgreSQL proof
15. human checkpoint

<tasks>

<task type="auto" tdd="true">
  <name>Task 15-02-01: DB_MODEL, types, generate/hash/compare e model</name>
  <files>docs/DB_MODEL_v1.22.md, apps/backend/src/modules/guest-cart-capability/types.ts, apps/backend/src/modules/guest-cart-capability/hash.ts, apps/backend/src/modules/guest-cart-capability/models/guest-cart-capability.ts, apps/backend/src/modules/guest-cart-capability/__tests__/guest-cart-capability-hash.unit.spec.ts</files>
  <read_first>docs/DB_MODEL_v1.22.md changelog e seções StoreIdempotencyRecord/StoreResourceVersion; 15-RESEARCH.md Q-01; PLAN locks P15-D01/P15-D02/P15-D08 em 15-01-PLAN.md; apps/backend/src/modules/tracking-access-token/types.ts; apps/backend/src/modules/tracking-access-token/models/tracking-access-token.ts; apps/backend/src/modules/customer-auth/bff-service-auth.ts digestSecret SHA-256; apps/backend/src/modules/store-resource-version/models/store-resource-version.ts indexes where deleted_at</read_first>
  <behavior>
  - generateGuestCartCapability(randomBytesFn) pede 32 bytes e devolve base64url; hashGuestCartCapability é hex SHA-256 estável; compareGuestCartCapabilityHash usa timingSafeEqual e recusa buffers de tamanho diferente.
  - GuestCartCapabilityRecord não possui campo de plaintext; MintGuestCartCapabilityResult tem plaintext_token somente no retorno de mint.
  </behavior>
  <action>Atualizar changelog do DB_MODEL para registrar GuestCartCapability nesta revisão sem inventar major. Declarar owner module guest_cart_capability, campos id, cart_id lógico (texto do cart Medusa, não FK SQL), token_hash SHA-256 hex UNIQUE, status active|expired|revoked|consumed, expires_at, consumed_at nullable, revoked_at nullable, last_used_at nullable, timestamps Medusa. Cardinalidade: no máximo uma linha active por cart_id via UNIQUE parcial. Vínculo Cart via module link, nunca FK cross-module. TTL 7d rolling / teto 30d desde created_at. Negativas: plaintext, nonce, pepper HMAC, HKDF, Redis como autoridade. Criar GUEST_CART_CAPABILITY_STATUS com ACTIVE, EXPIRED, REVOKED, CONSUMED. GUEST_CART_CAPABILITY_RANDOM_BYTES = 32. Header constant GUEST_CART_CAPABILITY_HEADER = x-indicio-guest-cart-token. hash.ts: generate com toString base64url; hash com createHash sha256 utf8 hex; compare timingSafeEqual; dummy hash SHA-256 de um literal fixo interno (sem HMAC). Model define guest_cart_capability, prefix gccap, token_hash text, cart_id text, status enum default active, expires_at, consumed_at nullable, revoked_at nullable, last_used_at nullable. Indexes: UNIQUE token_hash; UNIQUE parcial (cart_id) WHERE status = active AND deleted_at IS NULL se o DML aceitar where; caso o DML não aceite o predicado de status, deixar o predicado para a migration CLI na task 15-02-03 e documentar no types. Não incluir coluna plaintext. Testes unitários cobrem encoding, hash, compare e ausência de campos proibidos no Record type via expect(Object.keys). Entropy/clock do 15-01 podem injetar bytes; este plano é o dono do algoritmo.</action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('docs/DB_MODEL_v1.22.md','utf8'); for (const x of ['GuestCartCapability','guest_cart_capability','token_hash','consumed','gccap','7d','30d']) if (!s.includes(x)) throw new Error('missing '+x)" && npm run test:unit -w @dtc/backend -- --runTestsByPath src/modules/guest-cart-capability/__tests__/guest-cart-capability-hash.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>DB_MODEL nomeia a entidade, UNIQUE de hash, consumed, link sem FK, TTL 7d/30d; 32 bytes base64url; SHA-256 hex; timingSafeEqual; model sem plaintext/nonce; CONSUMED existe; header constant bate com CART-02.</acceptance_criteria>
  <done>Contrato DML/hash de produção existe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15-02-02: Service, lookup dummy-on-miss, lifecycle e module index</name>
  <files>apps/backend/src/modules/guest-cart-capability/service.ts, apps/backend/src/modules/guest-cart-capability/lookup.ts, apps/backend/src/modules/guest-cart-capability/index.ts, apps/backend/src/modules/guest-cart-capability/__tests__/guest-cart-capability-service.unit.spec.ts</files>
  <read_first>apps/backend/src/modules/tracking-access-token/service.ts mintTrackingAccessToken, compare, PLAINTEXT_FORBIDDEN; apps/backend/src/modules/tracking-access-token/lookup.ts dummy miss; apps/backend/src/modules/guest-cart-capability/hash.ts; apps/backend/src/modules/guest-cart-capability/types.ts; apps/backend/src/modules/tracking-access-token/index.ts; apps/backend/src/modules/store-resource-version/index.ts; 15-01-PLAN.md P15-D08 TTL</read_first>
  <behavior>
  - mintGuestCartCapability grava record sem plaintext; plaintext_token só no retorno; construir record com plaintext lança GUEST_CART_CAPABILITY_PLAINTEXT_FORBIDDEN.
  - lookupByPresentedToken em miss faz dummy compare e lança o mesmo erro público que expired/revoked/consumed/wrong hash.
  - touchRollingTtl em uso válido estende expires_at para min(now+7d, created_at+30d) e nunca ultrapassa o teto.
  </behavior>
  <action>MedusaService sobre o model. mint usa generate+hash de hash.ts, persiste token_hash, cart_id, status active, expires_at = now+7d. expire/revoke/consume atualizam status e timestamps; consume seta consumed_at. assertRecordHasNoPlaintext recusa chaves plaintext_token/token/capability. lookup: hash do candidato, listByHash; se vazio, dummy SHA-256 compare; throw uniforme GUEST_CART_CAPABILITY_LOOKUP_INVALID (mapeado depois para 404 HTTP). Não distinguir miss vs mismatch no erro. Clock injetável para testes de TTL. last_used_at atualizado em lookup válido. Não usar createHmac. Export GUEST_CART_CAPABILITY_MODULE = guest_cart_capability e default Module com GuestCartCapabilityModuleService. Reexportar types/hash públicos necessários às rotas posteriores. Não registrar medusa-config nesta task (próxima).</action>
  <verify>
    <automated>npm run test:unit -w @dtc/backend -- --runTestsByPath src/modules/guest-cart-capability/__tests__/guest-cart-capability-service.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>Mint hash-only; dummy miss; transições consumed/expired/revoked; rolling 7d com cap 30d; erro uniforme; module key exportado.</acceptance_criteria>
  <done>Service/lookup cobrem CART-01 e CART-03 em unit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15-02-03: [BLOCKING] medusa-config, link, db:generate e PostgreSQL descartável</name>
  <files>apps/backend/medusa-config.ts, apps/backend/src/infrastructure/__tests__/medusa-config.unit.spec.ts, apps/backend/src/links/guest-cart-capability-cart.ts, apps/backend/src/modules/guest-cart-capability/migrations/, apps/backend/src/modules/guest-cart-capability/__tests__/guest-cart-capability.postgres.spec.ts</files>
  <read_first>apps/backend/medusa-config.ts módulos tracking_access_token e store_resource_version; apps/backend/src/infrastructure/__tests__/medusa-config.unit.spec.ts expectedLocalModules; apps/backend/src/links/payment-attempt-cart.ts; apps/backend/src/modules/guest-cart-capability/index.ts; apps/backend/src/modules/store-resource-version/migrations/Migration20260809201808.ts; .planning/phases/13-storefront-contract-foundation-surface-lockdown/13-05-SUMMARY.md P13-13-05-HCD-01 integer; apps/backend/src/modules/store-resource-version/__tests__/store-resource-version.postgres.spec.ts; apps/backend/integration-tests/helpers/guest-cart-postgres.ts; .planning/phases/14-customer-auth-verification/14-07-PLAN.md db:generate pattern</read_first>
  <behavior>
  - token_hash UNIQUE; segunda linha active para o mesmo cart_id falha; plaintext canário ausente.
  - Redis down não altera o resultado das constraints.
  </behavior>
  <action>Adicionar { key: guest_cart_capability, resolve: ./src/modules/guest-cart-capability } ao array de módulos, uma vez, junto dos custom modules existentes. Atualizar expectedLocalModules na unit de config para incluir o resolve exatamente uma vez. Criar defineLink(CartModule.linkable.cart, { linkable: GuestCartCapabilityModule.linkable.guestCartCapability, isList: true }). Não importar o service de outro módulo. Config regression deve PASS antes do generate. Somente após a unit de config PASS: do diretório apps/backend executar npx medusa db:generate guest_cart_capability. Preservar filename/class/snapshot gerados. Complementar CHECK de status e UNIQUE parcial active por cart_id somente pelo mecanismo autorizado do projeto (como 13-05/14-07), sem reescrever a migration à mão de forma incompatível com o snapshot. Aplicar somente no PostgreSQL descartável via o runner existente (migrateDisposableMedusaSchema ou equivalente local da Phase 14) — nunca supabase db push, nunca DATABASE_URL remoto. Spec postgres: hash-only inspect do 15-01, UNIQUE token_hash, one-active-per-cart, statuses consumed/expired/revoked. Um spec por processo. Version columns neste módulo não existem; não copiar bigint. Se generate falhar, BLOCKED — não inventar DDL paralelo. Exact-set permanece 63/51/12/15/50/7/6.</action>
  <verify>
    <automated>npm run test:unit -w @dtc/backend -- --runTestsByPath src/infrastructure/__tests__/medusa-config.unit.spec.ts && node apps/backend/scripts/run-disposable-postgres-tests.mjs -- npm run test:integration:modules -w @dtc/backend -- --runTestsByPath src/modules/guest-cart-capability/__tests__/guest-cart-capability.postgres.spec.ts</automated>
  </verify>
  <acceptance_criteria>Módulo aparece uma vez; link isList true; CLI generate exit 0; snapshot/model alinhados; PG descartável prova UNIQUE/hash-only/one-active; cleanup loopback.</acceptance_criteria>
  <done>Capability domain completo e persistível com autoridade PostgreSQL.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 15-02-04: Revisar o domínio persistível</name>
  <files>None (checkpoint)</files>
  <read_first>docs/DB_MODEL_v1.22.md; apps/backend/src/modules/guest-cart-capability/hash.ts; apps/backend/src/modules/guest-cart-capability/service.ts; apps/backend/src/modules/guest-cart-capability/migrations/; apps/backend/src/links/guest-cart-capability-cart.ts; apps/backend/medusa-config.ts; outputs sanitizados de unit e PG</read_first>
  <action>Apresentar encoding 32 bytes base64url, SHA-256 hash-only, dummy-on-miss, UNIQUE/one-active, identidade CLI e prova de que a migration não foi aplicada em alvo persistente/remoto. Não promover rotas. Não iniciar 15-03 sem PASS humano.</action>
  <verify>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>Humano PASS ou BLOCKED; confirma P15-D01/P15-D02, generate local-only, e ausência de HTTP/capability header.</acceptance_criteria>
  <done>Gate manual do domínio persistível.</done>
</task>

</tasks>

## Constraints / negative proofs

- Exact-set Store permanece o da Phase 14. Este plano não promove cart.
- Sem plaintext em record, logs, migration SQL ou fixtures persistentes.

## Stop conditions

BLOCKED se generate falhar, DSN não-loopback, HMAC/HKDF copiados, coluna plaintext, ou FK cross-module.

## Rollback / local recovery

Reverter arquivos deste plano; encerrar somente PIDs/containers registrados; remover apenas databases temporários.

## Manual gate / NO DEPLOY

Parar após `15-02-SUMMARY.md`. **NO DEPLOY.** Sem migrate remoto.

## Artifacts this phase produces

- `GUEST_CART_CAPABILITY_STATUS`, `GUEST_CART_CAPABILITY_HEADER`, `GUEST_CART_CAPABILITY_RANDOM_BYTES`
- `generateGuestCartCapability`, `hashGuestCartCapability`, `compareGuestCartCapabilityHash`
- mint/lookup/lifecycle service
- medusa-config key `guest_cart_capability`
- `defineLink` Cart ↔ `guestCartCapability`
- CLI migration under `apps/backend/src/modules/guest-cart-capability/migrations/`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mint return → persisted record | plaintext must not cross into the record/table |
| CLI generate → local files | generated SQL must not target remote DSN |
| module → Cart | association via link, not FK into cart table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-01 | Information Disclosure | GuestCartCapabilityRecord | mitigate | plaintext only on MintGuestCartCapabilityResult |
| T-15-02-02 | Tampering | token_hash UNIQUE | mitigate | PostgreSQL UNIQUE + spec; SHA-256 hex; timingSafeEqual |
| T-15-02-03 | Spoofing | generateGuestCartCapability | mitigate | 32 CSPRNG bytes base64url |
| T-15-02-04 | Elevation of Privilege | cart_id | mitigate | sem FK; uma active por cart via partial UNIQUE |
| T-15-02-SC | Tampering | npm | accept | nenhum package install; só CLI Medusa já instalado |
</threat_model>

<verification>DB_MODEL scan + hash unit + service unit + config unit + um processo PG disposable; revisão humana.</verification>
<success_criteria>Capability domain completo e persistível, não três estados intermediários. CART-01 persistido com autoridade PostgreSQL.</success_criteria>
<output>Create `15-02-SUMMARY.md` e pare. NO DEPLOY.</output>
