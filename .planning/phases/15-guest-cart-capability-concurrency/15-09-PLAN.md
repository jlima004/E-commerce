---
phase: 15-guest-cart-capability-concurrency
plan: 09
type: execute
wave: 8
depends_on: [15-08]
files_modified:
  - apps/backend/src/api/store/carts/concurrency.ts
  - apps/backend/src/api/store/carts/active/route.ts
  - apps/backend/src/api/store-surface/errors.ts
  - apps/backend/src/api-docs/components/errors.ts
  - apps/backend/src/api/store/carts/__tests__/concurrency.unit.spec.ts
  - apps/backend/src/api/store-surface/__tests__/errors.unit.spec.ts
autonomous: false
requirements: [CART-07, CART-08]
must_haves:
  truths:
    - "Create/GET active inicializam StoreResourceVersion resource_type cart com integer e devolvem ETag quoted."
    - "Replay do POST active (mesma Idempotency-Key) devolve cart_id+DTO stored no 15-08 mais o ETag atual lido de StoreResourceVersion; a allowlist de metadata não ganha chave etag."
    - "Envelope público aceita code CART_VERSION_MISMATCH e cart = PublicStoreCartPreOrder sem capability nem version no body."
    - "isStoreErrorResponse deixa de rejeitar a chave cart quando o snapshot é o DTO allowlisted."
  artifacts:
    - path: apps/backend/src/api/store/carts/concurrency.ts
      provides: parseIfMatchHeader, formatCartEtag, requireIfMatch
    - path: apps/backend/src/api/store-surface/errors.ts
      provides: STORE_ERROR_CODES.CART_VERSION_MISMATCH + snapshot allowlist
  key_links:
    - from: apps/backend/src/api/store/carts/active/route.ts
      to: apps/backend/src/modules/store-resource-version/service.ts
      via: initialize resourceType cart
      pattern: "initialize\\(|formatCartEtag"
    - from: apps/backend/src/api/store-surface/errors.ts
      to: apps/backend/src/api/store/carts/serializers.ts
      via: cart snapshot PublicStoreCartPreOrder
      pattern: "CART_VERSION_MISMATCH|serializeStoreCartPreOrder"
  prohibitions:
    - "Não usar bigint de 13-VALIDATION.md."
    - "Não colocar version no JSON do cart."
    - "Não ligar If-Match obrigatório no POST active get-or-create (não é mutação estrutural)."
    - "Não implementar line-item CAS neste plano (consumidores em 15-11+)."
---

<objective>
Wire StoreResourceVersion ao cart ativo e materializar o envelope 412 CART_VERSION_MISMATCH com snapshot seguro (D15-09/D15-10/D15-11).

Purpose: primitive pública de concorrência pronta antes das mutações de line-item.
Output: ETag no GET/POST active, parser If-Match, envelope com cart DTO.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@apps/backend/src/modules/store-resource-version/service.ts
@apps/backend/src/api/store-surface/errors.ts
@apps/backend/src/api/store/carts/serializers.ts
@apps/backend/src/api-docs/components/errors.ts
@.planning/phases/13-storefront-contract-foundation-surface-lockdown/13-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 15-09-01: ETag quoted, parse If-Match e initialize no active</name>
  <files>apps/backend/src/api/store/carts/concurrency.ts, apps/backend/src/api/store/carts/active/route.ts, apps/backend/src/api/store/carts/__tests__/concurrency.unit.spec.ts</files>
  <read_first>apps/backend/src/modules/store-resource-version/service.ts initialize e compareAndSwapWithMutation; apps/backend/src/api-docs/components/headers.ts ETag; apps/backend/src/api-docs/components/parameters.ts IfMatch; apps/backend/src/api/store/carts/active/route.ts</read_first>
  <behavior>
  - formatCartEtag(1) === '"1"'; parseIfMatchHeader aceita '"1"' e rejeita vazio, W/, token sem aspas, não-inteiro.
  - GET/POST active setam ETag a partir de initialize/load; GET não incrementa; replay do POST active anexa o ETag atual (não stored).
  </behavior>
  <action>concurrency.ts: formatCartEtag, parseIfMatchHeader, requireIfMatch (falha VALIDATION_ERROR se ausente/malformed — usado só nas mutações posteriores). Active route: após refetch, initialize(resourceType: cart, resourceId: cart.id) em transação Medusa existente do request se disponível; responder ETag quoted. No replay idempotente do 15-08, ler a versão atual via StoreResourceVersion e anexar formatCartEtag ao response (safe context = cart_id + DTO stored + ETag current); não persistir string etag em STORE_IDEMPOTENCY_SAFE_METADATA_KEYS. Não chamar compareAndSwapWithMutation no get-or-create. Integer only.</action>
  <verify>
    <automated>npm run test:unit -w @dtc/backend -- --runTestsByPath src/api/store/carts/__tests__/concurrency.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>Parser/format quoted; initialize cart version 1; GET não bumpa.</acceptance_criteria>
  <done>CART-07 baseline no active cart.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15-09-02: Envelope CART_VERSION_MISMATCH + snapshot DTO</name>
  <files>apps/backend/src/api/store-surface/errors.ts, apps/backend/src/api/store-surface/__tests__/errors.unit.spec.ts, apps/backend/src/api-docs/components/errors.ts</files>
  <read_first>apps/backend/src/api/store-surface/errors.ts STORE_ERROR_CODES, isStoreErrorResponse, toStoreErrorResponse void options.cart; apps/backend/src/api/store/carts/serializers.ts PublicStoreCartPreOrder serializeStoreCartPreOrder; apps/backend/src/api-docs/components/errors.ts StoreErrorResponse.cart placeholder</read_first>
  <behavior>
  - toStoreErrorResponse com code CART_VERSION_MISMATCH, retryable false, cart serializeStoreCartPreOrder(source), ETag header da versão atual.
  - isStoreErrorResponse aceita chave cart somente se passar um predicate isPublicStoreCartPreOrderSnapshot (allowlist de campos; rejeita guestCartToken/version/capability).
  </behavior>
  <action>Adicionar STORE_ERROR_CODES.CART_VERSION_MISMATCH. Mapear classify 412 para esse code quando o erro de domínio for CART_VERSION_MISMATCH; manter PRECONDITION_FAILED para outros 412. Parar de void options.cart; sanitizar via serializer. Atualizar StoreErrorResponse type. No registry components/errors.ts, trocar o oneOf primitive por $ref PublicStoreCartPreOrder — este plano só toca o TypeScript do component; não rodar openapi:generate (15-17). Testes unitários: snapshot sem token; chave extra falha closed; cart omitido continua válido para erros sem snapshot.</action>
  <verify>
    <automated>npm run test:unit -w @dtc/backend -- --runTestsByPath src/api/store-surface/__tests__/errors.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>412 CART_VERSION_MISMATCH + DTO allowlisted; token/version ausentes do body; errors.unit.spec cobre o allowlist.</acceptance_criteria>
  <done>CART-08 envelope pronto para mutações.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 15-09-03: Revisar ETag/envelope</name>
  <files>None (checkpoint)</files>
  <read_first>apps/backend/src/api/store/carts/concurrency.ts; apps/backend/src/api/store-surface/errors.ts</read_first>
  <action>Confirmar integer ETag quoted, snapshot DTO e que POST active não exige If-Match.</action>
  <verify>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>Humano PASS ou BLOCKED.</acceptance_criteria>
  <done>Gate manual CART-07/08 foundation.</done>
</task>

</tasks>

## Manual gate / NO DEPLOY

Parar após `15-09-SUMMARY.md`. **NO DEPLOY.**

## Artifacts this phase produces

- `formatCartEtag`, `parseIfMatchHeader`, `requireIfMatch`
- `STORE_ERROR_CODES.CART_VERSION_MISMATCH`
- ETag on GET/POST active
- StoreErrorResponse.cart → PublicStoreCartPreOrder (registry TS)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| If-Match header → parser | untrusted version string |
| 412 body → client | snapshot must not leak capability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-09-01 | Tampering | version | mitigate | integer StoreResourceVersion; Redis não decide |
| T-15-09-02 | Information Disclosure | 412 cart | mitigate | serializer allowlist; sem token/version |
| T-15-09-03 | Elevation of Privilege | bigint schema | mitigate | HCD 13-05 integer only |
| T-15-09-SC | Tampering | npm | accept | nenhum install |
</threat_model>

<verification>Units concurrency + errors; revisão humana.</verification>
<success_criteria>ETag/412 envelope existem antes das mutações.</success_criteria>
<output>Create `15-09-SUMMARY.md` e pare. NO DEPLOY.</output>
