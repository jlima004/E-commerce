---
phase: 15-guest-cart-capability-concurrency
plan: 15
type: execute
wave: 14
depends_on: [15-14]
files_modified:
  - apps/backend/src/api/store/carts/active/route.ts
  - apps/backend/src/api/store/carts/[id]/line-items/route.ts
  - apps/backend/src/api/store/carts/[id]/line-items/[line_id]/route.ts
  - apps/backend/src/modules/checkout/shipping-invalidation.ts
  - apps/backend/src/modules/checkout/__tests__/shipping-invalidation.unit.spec.ts
  - apps/backend/src/modules/payment-attempt/__tests__/cart-invalidation-cart-m1.unit.spec.ts
  - apps/backend/integration-tests/http/guest-cart-native-deny.spec.ts
  - apps/backend/integration-tests/http/customer-cart-line-items.spec.ts
autonomous: false
requirements: [CART-09, CART-05]
must_haves:
  truths:
    - "Toda mutação estrutural M1 chama invalidateActivePaymentAttemptForCartChange e os no-ops invalidateShippingQuote/invalidateShippingSelection."
    - "HTTP nativo create-by-id, read-by-id, complete, attach e shipping-methods permanece DENY 404."
    - "Customer autenticado (BFF + customerAuthAccessGuard context) muta o próprio active cart sem guest capability e sem merge."
    - "Guest capability não autoriza cart de Customer; JWT cru/session nativo não autoriza M1."
    - "Nenhuma mutação cria Order; complete continua stub 404."
  artifacts:
    - path: apps/backend/src/modules/checkout/shipping-invalidation.ts
      provides: no-op SHP hooks com invocação testável
    - path: apps/backend/integration-tests/http/guest-cart-native-deny.spec.ts
      provides: anti-bypass D15-08
    - path: apps/backend/integration-tests/http/customer-cart-line-items.spec.ts
      provides: Q-07 dual path
  key_links:
    - from: apps/backend/src/api/store/carts/[id]/line-items/route.ts
      to: apps/backend/src/modules/payment-attempt/cart-invalidation.ts
      via: invalidateActivePaymentAttemptForCartChange
      pattern: "invalidateActivePaymentAttemptForCartChange"
    - from: apps/backend/src/api/store/carts/[id]/line-items/route.ts
      to: apps/backend/src/modules/customer-auth/access-guard.ts
      via: customerId do contexto autorizado, nunca JWT hash
      pattern: "customerId|customerAuthAccessGuard"
  prohibitions:
    - "Não implementar Gelato quote/select."
    - "Não endurecer PaymentAttempt M1 (Phase 19)."
    - "Não habilitar attach/merge (Phase 16)."
    - "Não usar authenticate(customer) session/bearer como posse M1."
    - "Não tratar JWT hash como actorScope."
---

<objective>
Fechar CART-09 (PA real + SHP no-op + anti-bypass) e Q-07/D15-16 (mesmas ops, prova Customer distinta).

Purpose: invalidação estrutural e dual-path sem puxar Phases 16–19.
Output: hooks invocados, DENY nativo, HTTP Customer line-items/active.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@apps/backend/src/modules/payment-attempt/cart-invalidation.ts
@apps/backend/src/modules/customer-auth/access-guard.ts
@apps/backend/src/api/store/carts/[id]/complete/route.ts
@apps/backend/src/api/store/customers/me/cart/attach/route.ts
@apps/backend/src/api/store/carts/idempotency-scope.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 15-15-01: Provar invocação PA + SHP no-op em toda mutação</name>
  <files>apps/backend/src/modules/checkout/shipping-invalidation.ts, apps/backend/src/modules/checkout/__tests__/shipping-invalidation.unit.spec.ts, apps/backend/src/modules/payment-attempt/__tests__/cart-invalidation-cart-m1.unit.spec.ts, apps/backend/src/api/store/carts/[id]/line-items/route.ts, apps/backend/src/api/store/carts/[id]/line-items/[line_id]/route.ts</files>
  <read_first>apps/backend/src/modules/payment-attempt/cart-invalidation.ts invalidateActivePaymentAttemptForCartChange; apps/backend/src/modules/checkout/shipping-invalidation.ts; line-item handlers; 15-RESEARCH.md Q-06 three layers</read_first>
  <behavior>
  - Cada add/update/delete/clear (quando ids.length>0) incrementa contadores de quote e selection no-op e chama invalidateActivePaymentAttemptForCartChange com o cartId.
  - No-ops não fazem fetch HTTP nem leem env Gelato.
  </behavior>
  <action>Centralizar um helper applyStructuralCartInvalidation(cartId, at) usado pelos quatro handlers. Unit do helper com spies. Não criar tabela placeholder. FE-PAY-006 rastreado aqui como invocação, não como PaymentAttempt M1.</action>
  <verify>
    <automated>npm run test:unit -w @dtc/backend -- --runTestsByPath src/modules/checkout/__tests__/shipping-invalidation.unit.spec.ts src/modules/payment-attempt/__tests__/cart-invalidation-cart-m1.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>Helper único; PA real; SHP no-op; zero Gelato.</acceptance_criteria>
  <done>CART-09 camadas 1–2 fechadas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15-15-02: Dual-path Customer + DENY nativo HTTP</name>
  <files>apps/backend/src/api/store/carts/active/route.ts, apps/backend/src/api/store/carts/[id]/line-items/route.ts, apps/backend/src/api/store/carts/[id]/line-items/[line_id]/route.ts, apps/backend/integration-tests/http/customer-cart-line-items.spec.ts, apps/backend/integration-tests/http/guest-cart-native-deny.spec.ts</files>
  <read_first>apps/backend/src/modules/customer-auth/access-guard.ts customerId; apps/backend/src/api/middlewares.ts createCustomerAuthAccessGuardMiddleware; apps/backend/src/api/store/customers/me/route.ts requireAuthenticatedContext; 15-01-PLAN.md P15-D04 customerActorScope; complete/attach routes; store-surface-lockdown.spec.ts analog</read_first>
  <behavior>
  - Com BFF + Authorization + customerAuthAccessGuard customerId e sem guest header, Customer GET/POST active e line-items operam o cart customer_id; actorScope usa customerId.
  - Com guest header em cart de Customer: 404 uniforme. Sem capability e sem Authorization: 404. JWT/session nativo sem access guard: nunca posse M1.
  - POST /store/carts, GET/POST /store/carts/{id}, complete, attach, shipping-methods: 404 STORE_SURFACE_DENY envelope.
  </behavior>
  <action>Resolver ator XOR nesta ordem no handler (nunca authenticate nativo session/bearer como M1): (1) se o request apresenta o header de capability, lookup guest — válido segue guest path; inválido/expirado/revogado/consumido/wrong-cart → 404 uniforme P15-D05, sem cair no ramo Customer; (2) senão se Authorization está presente, chamar customerAuthAccessGuard e usar customerId do contexto PostgreSQL — Customer path, actorScope customerActorScope({ customerId }); (3) senão (guest-without-Authorization e sem capability) → 404 uniforme. Não colocar access-guard obrigatório no matcher das rotas de cart (quebraria o passo 1). Customer POST active não emite guest header. HTTP Customer: register/login Phase 14 helpers, create active, add item. HTTP DENY: lista D15-08. Order count zero. D14-08: após revoke/expiry do access, o cart guest persistido continua GET-able com capability. Casos Customer skipped em cart-checkout-store.spec.ts permanecem skipped; este spec é o sucessor.</action>
  <verify>
    <automated>npm run test:integration:http -w @dtc/backend -- --runTestsByPath integration-tests/http/customer-cart-line-items.spec.ts integration-tests/http/guest-cart-native-deny.spec.ts</automated>
  </verify>
  <acceptance_criteria>Q-07 PASS; D15-08 DENY PASS; D14-08 cart preservado; JWT/session não são M1; zero Order.</acceptance_criteria>
  <done>CART-09 camada 3 + dual-path fechados.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 15-15-03: Revisar invalidação e dual-path</name>
  <files>None (checkpoint)</files>
  <read_first>apps/backend/src/modules/checkout/shipping-invalidation.ts; apps/backend/integration-tests/http/guest-cart-native-deny.spec.ts; apps/backend/integration-tests/http/customer-cart-line-items.spec.ts</read_first>
  <action>Confirmar no-op SHP, PA real, DENY nativo e Customer path sem merge.</action>
  <verify>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>Humano PASS ou BLOCKED.</acceptance_criteria>
  <done>Gate manual CART-09/Q-07.</done>
</task>

</tasks>

## Manual gate / NO DEPLOY

Parar após `15-15-SUMMARY.md`. **NO DEPLOY.**

## Artifacts this phase produces

- `applyStructuralCartInvalidation`
- `invalidateShippingQuote`, `invalidateShippingSelection` (no-op)
- Customer XOR actor resolution on cart M1 handlers
- HTTP native DENY spec
- HTTP customer line-items spec

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native Medusa HTTP → guard | must remain DENY |
| Customer bearer → cart | access context PG, not JWT-as-scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-15-01 | Elevation of Privilege | native cart routes | mitigate | persist DENY + HTTP proofs |
| T-15-15-02 | Spoofing | JWT/session | mitigate | customerAuthAccessGuard / capability XOR |
| T-15-15-03 | Tampering | money path | mitigate | PA invalidation; SHP no-op until Phase 18 |
| T-15-15-04 | Elevation of Privilege | Order birth | mitigate | complete DENY; zero Order |
| T-15-15-SC | Tampering | npm | accept | nenhum install |
</threat_model>

<verification>Units invalidation + dois HTTP specs; revisão humana.</verification>
<success_criteria>CART-09 e Q-07 observáveis sem Gelato/merge.</success_criteria>
<output>Create `15-15-SUMMARY.md` e pare. NO DEPLOY.</output>
