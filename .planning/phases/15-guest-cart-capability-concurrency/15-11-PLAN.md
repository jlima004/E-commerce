---
phase: 15-guest-cart-capability-concurrency
plan: 11
type: execute
wave: 10
depends_on: [15-10]
files_modified:
  - apps/backend/src/api/store/carts/[id]/line-items/route.ts
  - apps/backend/src/api/store-surface/manifest.ts
  - apps/backend/src/api/store-surface/__tests__/manifest.unit.spec.ts
  - apps/backend/src/api/middlewares.ts
  - apps/backend/src/modules/checkout/shipping-invalidation.ts
  - apps/backend/integration-tests/http/guest-cart-line-item-add.spec.ts
autonomous: false
requirements: [CART-05, CART-06, CART-07, CART-08]
must_haves:
  truths:
    - "POST /store/carts/{id}/line-items local chama addToCartWorkflow; origin vira native+local_extension; HTTP nativo continua o mesmo path identity."
    - "Mutação exige capability (guest) ou será expandida em 15-15 para Customer; If-Match; Idempotency-Key; CAS incrementa versão."
    - "Retry da mesma Idempotency-Key após sucesso não devolve 412 só porque o ETag avançou (P15-D07)."
    - "Stale If-Match devolve 412 CART_VERSION_MISMATCH + snapshot sem token."
  artifacts:
    - path: apps/backend/src/api/store/carts/[id]/line-items/route.ts
      provides: POST add local
    - path: apps/backend/integration-tests/http/guest-cart-line-item-add.spec.ts
      provides: add + 412 + replay
  key_links:
    - from: apps/backend/src/api/store/carts/[id]/line-items/route.ts
      to: node_modules/@medusajs/core-flows
      via: addToCartWorkflow
      pattern: "addToCartWorkflow"
    - from: apps/backend/src/api/store/carts/[id]/line-items/route.ts
      to: apps/backend/src/modules/store-resource-version/service.ts
      via: compareAndSwapWithMutation after claim
      pattern: "compareAndSwapWithMutation"
  prohibitions:
    - "Não criar segunda entrada origin local para este path (quebraria native identity 51)."
    - "Não reabilitar POST /store/carts nativo."
    - "Não implementar Gelato."
    - "Não criar Order."
---

<objective>
Expor add line-item M1 reusando addToCartWorkflow atrás de rota local, com posse, If-Match, CAS e idempotência (CART-05..08).

Purpose: primeira mutação estrutural do RESEARCH wave line-items.
Output: POST collection local + promoção native+local_extension + HTTP.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@apps/backend/src/api/store/carts/active/route.ts
@apps/backend/src/api/store-surface/manifest.ts
@apps/backend/src/modules/store-resource-version/service.ts
@apps/backend/src/modules/store-idempotency/service.ts
@apps/backend/src/api/store/carts/line-items/validators.ts
@apps/backend/src/api/store/carts/concurrency.ts
</context>

## Handler order (P15-D07) — lock for 15-11..14

1. BFF already at middleware.
2. Resolve actor: lookupByPresentedToken (guest). `{id}` must be that actor's cart or 404 uniforme.
3. Idempotency-Key required; claim with guestCartCapabilityActorScope + cartResourceScope operation POST /store/carts/{id}/line-items.
4. If claim replay: return stored DTO + current ETag; do not parse If-Match as a 412 gate.
5. If claimed: requireIfMatch; compareAndSwapWithMutation wrapping addToCartWorkflow; on stale → 412 CART_VERSION_MISMATCH + snapshot + ETag atual, mark claim as failed/not-success so retry with new intent needs new key+If-Match.
6. Validators CART-06 before workflow.
7. invalidate hooks: chamar a função de CART-09 se já existir no 15-15; até lá, chamar um import estável de cart-invalidation.ts invalidateActivePaymentAttemptForCartChange (as-built) para não deixar add sem invalidação de PA. SHP no-op pode ser stub import de 15-15 ou função local temporária no mesmo módulo checkout se o arquivo shipping-invalidation.ts ainda não existe — criar o no-op mínimo neste plano somente se necessário para não adiar CART-09 em add; preferir criar `apps/backend/src/modules/checkout/shipping-invalidation.ts` com no-ops exportados (testados de verdade no 15-15).
8. Refetch, assertNoPaymentOrOrderFields, serializeStoreCartPreOrder, ETag.

Staged counts after this plan: nativeLocalExtension 2→3, DENY 50→49, M1 8→9. Native identity still 51 (this key stays native-like). total 63. Update validateStoreSurfaceManifest and PHASE15 cart enabled ops concat this POST.

<tasks>

<task type="auto" tdd="true">
  <name>Task 15-11-01: Rota local add + promoção manifest</name>
  <files>apps/backend/src/api/store/carts/[id]/line-items/route.ts, apps/backend/src/api/store-surface/manifest.ts, apps/backend/src/api/store-surface/__tests__/manifest.unit.spec.ts, apps/backend/src/api/middlewares.ts, apps/backend/src/modules/checkout/shipping-invalidation.ts</files>
  <read_first>apps/backend/src/api/store/carts/active/route.ts; apps/backend/src/api/store/carts/[id]/complete/route.ts last-writer-wins local; apps/backend/src/api/store-surface/manifest.ts POST line-items entry; node_modules/@medusajs/core-flows package exports for addToCartWorkflow; apps/backend/src/modules/payment-attempt/cart-invalidation.ts; apps/backend/src/api/middlewares.ts</read_first>
  <behavior>
  - Manifest origin native+local_extension, runtime_policy M1_ENABLED, m1_enablement enabled for POST line-items collection.
  - Native identity count remains 51; local-only remains 12.
  </behavior>
  <action>Implementar POST export na rota local last-writer-wins. Import addToCartWorkflow from @medusajs/core-flows. Seguir a ordem P15-D07 acima. Criar shipping-invalidation.ts com invalidateShippingQuote e invalidateShippingSelection no-op que incrementam um contador testável in-process (sem I/O Gelato). Ligar query/response cart middlewares no matcher /store/carts/:id/line-items POST. Não adicionar row extra no manifest. Atualizar counts: m1EnabledPolicy 9, deny 49, nativeLocalExtension 3, preserveLegacy 5, total 63. STORE_SURFACE_PHASE15_CART_ENABLED_OPERATIONS inclui o POST collection.</action>
  <verify>
    <automated>npm run test:unit -w @dtc/backend -- --runTestsByPath src/api/store-surface/__tests__/manifest.unit.spec.ts src/api/store/carts/line-items/__tests__/validators.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>origin native+local_extension; M1=9; native identity 51; handler export POST existe.</acceptance_criteria>
  <done>Add M1 compilável com exact-set staged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15-11-02: HTTP add, 412 stale e replay sem false 412</name>
  <files>apps/backend/integration-tests/http/guest-cart-line-item-add.spec.ts</files>
  <read_first>apps/backend/integration-tests/http/guest-cart-tracer.spec.ts; apps/backend/integration-tests/http/guest-cart-idempotency.spec.ts; apps/backend/src/modules/catalog fixtures used by cart-checkout-store.spec.ts</read_first>
  <behavior>
  - Add válido 200/201 com ETag incrementado; qty 100/decimal 400; sem If-Match 400; If-Match stale 412 + cart snapshot + ETag atual sem token.
  - Repeat same Idempotency-Key + If-Match antigo: 200 replay, não 412.
  - Session-alone add 404. Native identity path não abre segundo engine.
  </behavior>
  <action>Usar variante vendável BRL existente nos testes de catálogo. Provar P15-D07 replay. Leakage. Zero Order. Variant inválida → erro de validação/domínio sem vazar internals.</action>
  <verify>
    <automated>npm run test:integration:http -w @dtc/backend -- --runTestsByPath integration-tests/http/guest-cart-line-item-add.spec.ts</automated>
  </verify>
  <acceptance_criteria>FE-CART-003 add + FE-CART-005/008 412 + P15-D07 replay PASS.</acceptance_criteria>
  <done>Primeira mutação estrutural comprovada.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 15-11-03: Revisar add</name>
  <files>None (checkpoint)</files>
  <read_first>apps/backend/src/api/store/carts/[id]/line-items/route.ts; apps/backend/integration-tests/http/guest-cart-line-item-add.spec.ts; apps/backend/src/api/store-surface/manifest.ts</read_first>
  <action>Confirmar workflow reuse, CAS, replay e native identity 51.</action>
  <verify>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>Humano PASS ou BLOCKED.</acceptance_criteria>
  <done>Gate manual add.</done>
</task>

</tasks>

## Manual gate / NO DEPLOY

Parar após `15-11-SUMMARY.md`. **NO DEPLOY.**

## Artifacts this phase produces

- POST `addToCartWorkflow` local handler
- `invalidateShippingQuote` / `invalidateShippingSelection` no-op stubs
- M1 key POST /store/carts/{id}/line-items (native+local_extension)
- HTTP add/412/replay spec

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| If-Match + body → mutation | stale writer and extra fields |
| Idempotency-Key retry → CAS | must not false-412 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-11-01 | Tampering | lost update | mitigate | If-Match + compareAndSwapWithMutation |
| T-15-11-02 | Elevation of Privilege | native HTTP | mitigate | origin extension, not a second native row; other natives DENY |
| T-15-11-03 | Spoofing | session | mitigate | capability required |
| T-15-11-04 | Replay | Idempotency-Key | mitigate | claim before CAS; replay safe; key ≠ ownership |
| T-15-11-05 | Tampering | Order | mitigate | assertNoPaymentOrOrderFields |
| T-15-11-SC | Tampering | npm | accept | nenhum install |
</threat_model>

<verification>Manifest unit + HTTP add; revisão humana.</verification>
<success_criteria>Add M1 reusa Medusa, é concorrente e idempotente.</success_criteria>
<output>Create `15-11-SUMMARY.md` e pare. NO DEPLOY.</output>
