---
phase: 15-guest-cart-capability-concurrency
plan: 06
type: execute
wave: 5
depends_on: [15-05]
files_modified:
  - apps/backend/src/api/store/carts/active/route.ts
  - apps/backend/src/modules/checkout/active-cart.ts
  - apps/backend/src/api/store-surface/manifest.ts
  - apps/backend/src/api/store-surface/__tests__/manifest.unit.spec.ts
  - apps/backend/src/api/middlewares.ts
  - apps/backend/src/modules/checkout/__tests__/active-cart.unit.spec.ts
  - apps/backend/integration-tests/http/guest-cart-tracer.spec.ts
  - apps/backend/integration-tests/http/cart-checkout-store.spec.ts
autonomous: false
requirements: [CART-01, CART-02, CART-04]
must_haves:
  truths:
    - "POST /store/carts/active guest com BFF cria cart BRL, devolve 201, ETag opcional ainda pode faltar, e emite x-indicio-guest-cart-token uma vez."
    - "GET /store/carts/active com a capability devolve 200 e não reemite o header; GET sem capability devolve 404 e não cria cart."
    - "Sessão sozinha, mesmo com active_cart_id, não autoriza GET/POST M1."
    - "POST/GET /store/carts/active sem credencial BFF devolvem 404 e não emitem o header de capability."
    - "token_hash está no PostgreSQL; plaintext do header não está."
    - "GET/POST /store/carts/active são M1_ENABLED; as 6 Auth M1 permanecem; PRESERVE_LEGACY perde só essas duas."
  artifacts:
    - path: apps/backend/src/api/store/carts/active/route.ts
      provides: posse por capability; emit-once no 201
    - path: apps/backend/integration-tests/http/guest-cart-tracer.spec.ts
      provides: tracer HTTP CART-01/02/04
    - path: apps/backend/src/api/store-surface/manifest.ts
      provides: active M1_ENABLED staged counts
  key_links:
    - from: apps/backend/src/api/store/carts/active/route.ts
      to: apps/backend/src/modules/guest-cart-capability/service.ts
      via: mint no 201; lookup no GET
      pattern: "mintGuestCartCapability|lookupByPresentedToken|x-indicio-guest-cart-token"
  prohibitions:
    - "Não colocar o token no JSON, URL, query ou logs."
    - "Não reabrir POST /store/carts, GET/POST /store/carts/{id}, complete, attach, shipping-methods."
    - "Não implementar line-items neste plano."
    - "Não reemitir o header em GET ou replay (replay idempotente completo é 15-08; GET já prova emit-once)."
    - "Não criar Order."
---

<objective>
Entregar o tracer de produção: guest POST/GET /store/carts/active com capability opaca, persistência hash-only, negação de sessão-só e header emit-once.

Purpose: verificar CART-01/02/04 no caminho feliz antes de expandir lifecycle, CAS e line-items.
Output: rotas active M1 + manifest staged + spec HTTP tracer.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-guest-cart-capability-concurrency/15-01-PLAN.md
@apps/backend/src/api/store/carts/active/route.ts
@apps/backend/src/modules/checkout/active-cart.ts
@apps/backend/src/api/store-surface/manifest.ts
@apps/backend/src/api/middlewares.ts
@apps/backend/integration-tests/http/cart-checkout-store.spec.ts
@apps/backend/src/api/store/carts/serializers.ts
</context>

## Tracer contract (FE-CART-001, FE-CART-002)

POST guest (BFF + Idempotency-Key ainda pode ser wired de forma mínima neste plano; wiring completo P15-D04/P15-D09 é 15-08, mas o 201 já deve mintar):
- 201, body `{ cart: PublicStoreCartPreOrder }` via serializer existente, currency brl
- header `x-indicio-guest-cart-token` presente só neste 201
- `assertNoPaymentOrOrderFields`
- opcional: gravar `req.session.active_cart_id` como hint se session existir (P15-D10)

GET guest com o mesmo header: 200, mesmo cart id, header de capability ausente.
GET sem header / com header lixo: 404 NOT_FOUND, zero cart criado.
POST/GET com session.active_cart_id e sem capability: 404 (GET) ou create novo guest (POST) sem tratar a sessão como posse — POST sem capability e com BFF é o create lazy.
POST/GET /store/carts/active sem credencial BFF: 404 uniforme e a response não emite o header de capability (prova M1 deste plano; 15-05 já ligou o guard e tem HTTP missing-BFF no spec de wiring).

Staged manifest (P15-D06 parcial): total 63, native identity 51, local 12, EXTENDED 15, DENY 50, PRESERVE_LEGACY 5, M1_ENABLED 8, m1EnablementEnabled 8. `STORE_SURFACE_PHASE14_ENABLED_OPERATIONS` vira subset; nova constante `STORE_SURFACE_PHASE15_CART_ENABLED_OPERATIONS` começa com GET/POST active. Reescrever o check PHASE14_EXACT_SURFACE para: as 6 auth continuam M1_ENABLED e o restante M1 é exatamente o subset Phase 15 já promovido.

<tasks>

<task type="auto" tdd="true">
  <name>Task 15-06-01: Promover active no manifest e reescrever posse</name>
  <files>apps/backend/src/api/store-surface/manifest.ts, apps/backend/src/api/store-surface/__tests__/manifest.unit.spec.ts, apps/backend/src/modules/checkout/active-cart.ts, apps/backend/src/modules/checkout/__tests__/active-cart.unit.spec.ts, apps/backend/src/api/middlewares.ts, apps/backend/src/api/store/carts/active/route.ts</files>
  <read_first>apps/backend/src/api/store-surface/manifest.ts GET/POST active e validateStoreSurfaceManifest; apps/backend/src/api/store-surface/__tests__/manifest.unit.spec.ts PRESERVE_LEGACY_KEYS e M1 exact-set; apps/backend/src/api/store/carts/active/route.ts createCartWorkflow; apps/backend/src/modules/checkout/active-cart.ts resolveActiveCartIdentity; apps/backend/src/api/middlewares.ts matcher /store/carts/active</read_first>
  <behavior>
  - Manifest: active GET/POST runtime_policy M1_ENABLED e m1_enablement enabled; PRESERVE_LEGACY_KEYS deixa de incluir as duas; M1 count 8; Phase 14 six still present.
  - resolveActiveCartIdentity M1 guest não usa session.activeCartId como prova; helper separado resolveGuestCartFromCapability.
  </behavior>
  <action>Promover as duas entradas owner_phase 15 de PRESERVE_LEGACY para M1_ENABLED. Atualizar validateStoreSurfaceManifest: total 63, extended 15, m1EnabledPolicy 8, m1EnablementEnabled 8, preserveLegacy 5, deny 50. Substituir igualdade rígida às 6 auth por: enabled set = Phase14 six concat Phase15 cart enabled ops (agora 2). Atualizar manifest.unit.spec.ts na mesma mudança. Em active-cart.ts, extrair resolveM1GuestCartPossession(header) vs hint de sessão. Em middlewares.ts, no matcher /store/carts/active, remover authenticate(...) como autoridade M1; manter storeCartPreOrder query/response middlewares e o BFF já ligado no 15-05. POST handler: se lookup de capability válido, reutilizar o cart ligado; senão createCartWorkflow currency_code brl, mintGuestCartCapability, set header só no 201, serializer PublicStoreCartPreOrder. GET: lookup obrigatório; miss → MedusaError NOT_FOUND uniforme P15-D05; não criar. Dual-run hint P15-D10 somente após mint se req.session existir. Nunca json.guestCartToken. Line-items permanecem DENY.</action>
  <verify>
    <automated>npm run test:unit -w @dtc/backend -- --runTestsByPath src/api/store-surface/__tests__/manifest.unit.spec.ts src/modules/checkout/__tests__/active-cart.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>M1=8 com 6 auth + 2 active; sessão não é posse; GET não cria; header só no código do 201.</acceptance_criteria>
  <done>Superfície active M1 está ligada à capability.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15-06-02: Tracer HTTP + inspeção hash-only</name>
  <files>apps/backend/integration-tests/http/guest-cart-tracer.spec.ts, apps/backend/integration-tests/http/cart-checkout-store.spec.ts</files>
  <read_first>apps/backend/integration-tests/http/cart-checkout-store.spec.ts; apps/backend/integration-tests/helpers/guest-cart-leakage.ts; apps/backend/integration-tests/helpers/guest-cart-postgres.ts; apps/backend/src/api/store/carts/serializers.ts PublicStoreCartPreOrder</read_first>
  <behavior>
  - POST BFF+publishable cria 201 com header; GET com o header 200 sem o header; GET sem header 404; request só com session cookie/active_cart_id sem capability 404 no GET.
  - POST/GET /store/carts/active sem credencial BFF: 404; response não emite o header de capability.
  - Após POST, SQL hash-only: plaintext do header ausente; token_hash presente.
  </behavior>
  <action>HTTP integration com credencial BFF de teste já usada na Phase 14. Casos: create, get, missing header, garbage header, session-alone GET, GET does not create (count carts), missing-BFF POST e GET (404 uniforme; capability header não emitido). Leakage collector no response JSON, Location, logs da request. assertNoPaymentOrOrderFields no cart. Order count zero. Não chamar Stripe. Não usar watch mode. Migrar ou quarentenar os casos herdados de GET/POST /store/carts/active em cart-checkout-store.spec.ts que ainda tratam sessão como posse: reescrever os casos guest de identidade para BFF+capability (sucessor tracer) ou describe.skip com ponteiro explícito para guest-cart-tracer.spec.ts; casos Customer active ficam skipped com ponteiro para customer-cart-line-items.spec.ts (15-15). Casos de payment/complete que usam session só como hint P15-D10 podem permanecer se não afirmarem posse M1 em GET/POST active.</action>
  <verify>
    <automated>npm run test:integration:http -w @dtc/backend -- --runTestsByPath integration-tests/http/guest-cart-tracer.spec.ts integration-tests/http/cart-checkout-store.spec.ts</automated>
  </verify>
  <acceptance_criteria>Tracer PASS: emit-once, 404 uniforme, sessão insuficiente, missing-BFF sem header de capability, hash-only, zero Order, token ausente do JSON. Inherited active-cart session tests migrados ou quarentenados.</acceptance_criteria>
  <done>Tracer de produção comprovado; expansão pode começar.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 15-06-03: Revisar o tracer</name>
  <files>None (checkpoint)</files>
  <read_first>apps/backend/integration-tests/http/guest-cart-tracer.spec.ts; apps/backend/src/api/store/carts/active/route.ts; apps/backend/src/api/store-surface/manifest.ts</read_first>
  <action>Apresentar 201+header, GET sem header, 404, hash-only e M1=8. Não iniciar line-items sem PASS.</action>
  <verify>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>Humano PASS ou BLOCKED no tracer CART-01/02/04.</acceptance_criteria>
  <done>Gate manual do tracer.</done>
</task>

</tasks>

## Manual gate / NO DEPLOY

Parar após `15-06-SUMMARY.md`. **NO DEPLOY.**

## Artifacts this phase produces

- M1 routes `GET /store/carts/active`, `POST /store/carts/active`
- `STORE_SURFACE_PHASE15_CART_ENABLED_OPERATIONS`
- `resolveM1GuestCartPossession`
- HTTP tracer `guest-cart-tracer.spec.ts`
- migrate/quarantine of inherited `/store/carts/active` session cases in `cart-checkout-store.spec.ts`
- header emission only on 201

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BFF → POST active | first issuance of guest capability |
| client → GET active | presented header is untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06-01 | Spoofing | session.active_cart_id | mitigate | sessão não autoriza M1 |
| T-15-06-02 | Information Disclosure | 201 header | mitigate | header-only; ausente de JSON/GET/replay futuro |
| T-15-06-03 | Elevation of Privilege | native cart HTTP | mitigate | create-by-id/read-by-id permanecem DENY |
| T-15-06-04 | Tampering | Order birth | mitigate | assertNoPaymentOrOrderFields; complete DENY |
| T-15-06-SC | Tampering | npm | accept | nenhum install |
</threat_model>

<verification>Units manifest/active-cart + HTTP tracer; revisão humana.</verification>
<success_criteria>O tracer prova posse opaca, hash-only, emit-once e 404 GET vazio.</success_criteria>
<output>Create `15-06-SUMMARY.md` e pare. NO DEPLOY.</output>
