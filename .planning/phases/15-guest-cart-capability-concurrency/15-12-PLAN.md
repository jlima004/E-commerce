---
phase: 15-guest-cart-capability-concurrency
plan: 12
type: execute
wave: 11
depends_on: [15-11]
files_modified:
  - apps/backend/src/api/store/carts/[id]/line-items/[line_id]/route.ts
  - apps/backend/src/api/store-surface/manifest.ts
  - apps/backend/src/api/store-surface/__tests__/manifest.unit.spec.ts
  - apps/backend/src/api/middlewares.ts
  - apps/backend/integration-tests/http/guest-cart-line-item-update.spec.ts
autonomous: false
requirements: [CART-05, CART-06, CART-07, CART-08]
must_haves:
  truths:
    - "POST /store/carts/{id}/line-items/{line_id} chama updateLineItemInCartWorkflow; quantity 0 remove o item."
    - "Mesma ordem P15-D07 de claim → If-Match → CAS; 412 snapshot seguro."
    - "origin native+local_extension; native identity permanece 51."
  artifacts:
    - path: apps/backend/src/api/store/carts/[id]/line-items/[line_id]/route.ts
      provides: POST update local
    - path: apps/backend/integration-tests/http/guest-cart-line-item-update.spec.ts
      provides: qty 1/99/0/-1/100/decimal HTTP
  key_links:
    - from: apps/backend/src/api/store/carts/[id]/line-items/[line_id]/route.ts
      to: node_modules/@medusajs/core-flows
      via: updateLineItemInCartWorkflow
      pattern: "updateLineItemInCartWorkflow"
  prohibitions:
    - "Não inventar segundo motor de qty 0."
    - "Não duplicar a row do manifest como origin local."
---

<objective>
Expor update de line-item M1 com qty 0 = remove (CART-05/06) via updateLineItemInCartWorkflow.

Purpose: D15-07 no runtime instalado 2.16.0.
Output: POST by line_id + HTTP matrix.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@apps/backend/src/api/store/carts/[id]/line-items/route.ts
@apps/backend/src/api/store/carts/line-items/validators.ts
</context>

## Staged counts

M1 9→10, DENY 49→48, nativeLocalExtension 3→4, total 63, native identity 51.

<tasks>

<task type="auto" tdd="true">
  <name>Task 15-12-01: Rota local update + promoção</name>
  <files>apps/backend/src/api/store/carts/[id]/line-items/[line_id]/route.ts, apps/backend/src/api/store-surface/manifest.ts, apps/backend/src/api/store-surface/__tests__/manifest.unit.spec.ts, apps/backend/src/api/middlewares.ts</files>
  <read_first>apps/backend/src/api/store/carts/[id]/line-items/route.ts POST add order; 15-11-PLAN.md Handler order; core-flows updateLineItemInCartWorkflow; manifest POST line_id entry</read_first>
  <behavior>
  - Update qty 2 bumpa versão; qty 0 remove o line e bumpa versão.
  </behavior>
  <action>POST handler: UpdateCartLineItemBodySchema; se quantity === 0, ainda usar updateLineItemInCartWorkflow (qty 0 remove nativo 2.16.0) — não deletar por outro engine. Copiar ordem P15-D07. Promover a entry POST .../{line_id} para M1_ENABLED native+local_extension. Matcher middleware /store/carts/:id/line-items/:line_id POST + BFF já no tuple. Invalidar PA + SHP no-op. Serializer + ETag.</action>
  <verify>
    <automated>npm run test:unit -w @dtc/backend -- --runTestsByPath src/api/store-surface/__tests__/manifest.unit.spec.ts src/api/store/carts/line-items/__tests__/validators.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>M1=10; origin extension; handler POST exportado.</acceptance_criteria>
  <done>Update M1 ligado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15-12-02: HTTP qty matrix e 412</name>
  <files>apps/backend/integration-tests/http/guest-cart-line-item-update.spec.ts</files>
  <read_first>apps/backend/integration-tests/http/guest-cart-line-item-add.spec.ts</read_first>
  <behavior>
  - 1 e 99 passam; 0 remove; -1, 100, decimal 400; stale If-Match 412; same-key replay não 412.
  </behavior>
  <action>Reusar fixtures de add. Provar item ausente após qty 0. Leakage. Zero Order. {id} de outro cart 404 uniforme.</action>
  <verify>
    <automated>npm run test:integration:http -w @dtc/backend -- --runTestsByPath integration-tests/http/guest-cart-line-item-update.spec.ts</automated>
  </verify>
  <acceptance_criteria>FE-CART-004 HTTP PASS no update; CART-08 412 PASS.</acceptance_criteria>
  <done>Update comprovado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 15-12-03: Revisar update</name>
  <files>None (checkpoint)</files>
  <read_first>apps/backend/src/api/store/carts/[id]/line-items/[line_id]/route.ts; apps/backend/integration-tests/http/guest-cart-line-item-update.spec.ts</read_first>
  <action>Confirmar qty 0 = remove via workflow nativo.</action>
  <verify>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>Humano PASS ou BLOCKED.</acceptance_criteria>
  <done>Gate manual update.</done>
</task>

</tasks>

## Manual gate / NO DEPLOY

Parar após `15-12-SUMMARY.md`. **NO DEPLOY.**

## Artifacts this phase produces

- POST `updateLineItemInCartWorkflow` local handler
- M1 key POST /store/carts/{id}/line-items/{line_id}
- HTTP qty matrix

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| update body → workflow | qty 0 is delete; extras rejected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-12-01 | Tampering | quantity | mitigate | wrapper CART-06 antes do workflow |
| T-15-12-02 | Tampering | stale aba | mitigate | If-Match + 412 snapshot |
| T-15-12-03 | Elevation of Privilege | wrong cart id | mitigate | capability ownership |
| T-15-12-SC | Tampering | npm | accept | nenhum install |
</threat_model>

<verification>Manifest unit + HTTP update; revisão humana.</verification>
<success_criteria>Update M1 com 0=remove e concorrência.</success_criteria>
<output>Create `15-12-SUMMARY.md` e pare. NO DEPLOY.</output>
