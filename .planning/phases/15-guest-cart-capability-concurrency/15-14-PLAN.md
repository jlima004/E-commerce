---
phase: 15-guest-cart-capability-concurrency
plan: 14
type: execute
wave: 13
depends_on: [15-13]
files_modified:
  - apps/backend/src/api/store/carts/[id]/line-items/route.ts
  - apps/backend/src/api/store-surface/manifest.ts
  - apps/backend/src/api/store-surface/__tests__/manifest.unit.spec.ts
  - apps/backend/src/api/middlewares.ts
  - apps/backend/integration-tests/http/guest-cart-line-item-clear.spec.ts
autonomous: false
requirements: [CART-05, CART-07, CART-08]
must_haves:
  truths:
    - "DELETE /store/carts/{id}/line-items é origin local nova; COUNT_TOTAL 63→64; local-only 12→13; EXTENDED 15→16."
    - "Clear lista items e chama deleteLineItemsWorkflow uma vez com todos os ids; vazio 200 idempotente ainda exige ownership, If-Match e Idempotency-Key."
    - "Native identity permanece 51; Auth M1 permanece 6; M1_ENABLED total 12."
  artifacts:
    - path: apps/backend/src/api/store/carts/[id]/line-items/route.ts
      provides: DELETE clear-all
    - path: apps/backend/src/api/store-surface/manifest.ts
      provides: COUNT_TOTAL 64 e validateStoreSurfaceManifest atualizado
  key_links:
    - from: apps/backend/src/api/store-surface/manifest.ts
      to: apps/backend/src/api/store-surface/__tests__/manifest.unit.spec.ts
      via: COUNT_TOTAL 64
      pattern: "64|COUNT_TOTAL"
  prohibitions:
    - "Não inventar rota nativa faltante no pacote Medusa."
    - "Não silenciar as 6 Auth M1 no check PHASE14."
    - "Não tratar PRESERVE_LEGACY como M1."
---

<objective>
Materializar clearCartLineItems local (Q-05) e fechar a aritmética exact-set 64 (P15-D06).

Purpose: CART-05 complete + PRD DELETE collection.
Output: nova key local + counts finais de cart M1 (Customer path ainda 15-15).
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@apps/backend/src/api/store/carts/[id]/line-items/route.ts
@apps/backend/src/api/store-surface/manifest.ts
@apps/backend/src/api/store-surface/__tests__/manifest.unit.spec.ts
@.planning/phases/15-guest-cart-capability-concurrency/15-RESEARCH.md Exact-Set Projection
</context>

## Final cart-surface counts after this plan (Phase 15 cart keys only)

```text
total: 64
native identity: 51
local-only: 13
EXTENDED: 16
DENY: 47
PRESERVE_LEGACY: 5
M1_ENABLED: 12
m1_enablement enabled: 12
nativeLocalExtension: 5
```

M1 12 = 6 Auth Phase 14 + GET/POST active + 3 line-item extensions + DELETE collection.

`validateStoreSurfaceManifest` hardcodes devem virar esses números. `STORE_SURFACE_PHASE14_ENABLED_OPERATIONS` continua o subset de 6. `STORE_SURFACE_PHASE15_CART_ENABLED_OPERATIONS` tem 6 cart keys. Enabled set exact concat.

<tasks>

<task type="auto" tdd="true">
  <name>Task 15-14-01: DELETE collection local + COUNT_TOTAL 64</name>
  <files>apps/backend/src/api/store/carts/[id]/line-items/route.ts, apps/backend/src/api/store-surface/manifest.ts, apps/backend/src/api/store-surface/__tests__/manifest.unit.spec.ts, apps/backend/src/api/middlewares.ts</files>
  <read_first>apps/backend/src/api/store/carts/[id]/line-items/route.ts POST add; 15-RESEARCH.md Q-05; manifest validateStoreSurfaceManifest COUNT_*; PRESERVE_LEGACY_KEYS no spec</read_first>
  <behavior>
  - Nova entry origin local, classification EXTENDED, runtime_policy M1_ENABLED, owner_phase 15, path DELETE /store/carts/{id}/line-items.
  - Empty cart: 200 mesmo DTO, versão bumpa somente se houve delete real; RESEARCH: empty 200 idempotente with ownership+If-Match+key — se não há items, não chamar workflow; PLAN lock: empty still requires If-Match and claim; version does not increment when zero items deleted (no structural change). Document this in the handler comment-free via test name.
  </behavior>
  <action>Adicionar a row no manifest (única key nova). Export DELETE no route.ts da collection. Ordem obrigatória P15-D07 / Q-09 / CART-08 mesmo no carrinho vazio: (1) claim Idempotency-Key; (2) requireIfMatch; (3) remoteQuery ids. Se ids.length===0, serializar o DTO atual e devolver ETag corrente sem compareAndSwapWithMutation e sem deleteLineItemsWorkflow. Se ids.length&gt;0, CAS wrapping um deleteLineItemsWorkflow({ ids }) e só então bump de versão. Atualizar validateStoreSurfaceManifest: total 64, extended 16, local 13 via summarize, m1 12, deny 47, preserveLegacy 5, blocked 17, outside 31. Manifest unit: 64 = 51 native-like + 13 local; nativeLocalExtension 5; M1 keys = Phase14 six + Phase15 six. Middleware DELETE /store/carts/:id/line-items. Não criar 3 rows origin local extras. Não retornar cedo antes de claim/If-Match.</action>
  <verify>
    <automated>npm run test:unit -w @dtc/backend -- --runTestsByPath src/api/store-surface/__tests__/manifest.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>COUNT_TOTAL 64; local 13; M1 12; Auth 6 subset; native identity 51; DELETE collection origin local.</acceptance_criteria>
  <done>Exact-set 64 no manifest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15-14-02: HTTP clear-all e empty idempotent</name>
  <files>apps/backend/integration-tests/http/guest-cart-line-item-clear.spec.ts</files>
  <read_first>apps/backend/integration-tests/http/guest-cart-line-item-add.spec.ts; 15-RESEARCH.md Q-05 empty 200</read_first>
  <behavior>
  - Clear com items: 200 items []; ETag avançou; PA invalidation path exercitado indiretamente.
  - Clear vazio: 200; mesma versão se lock de no-increment; still 400 without If-Match on first claim.
  - Replay same key não 412.
  </behavior>
  <action>Add dois items, clear, assert vazio. Second clear empty. Session-alone 404. Leakage. Zero Order. Native HTTP bypass still DENY (covered also in 15-15).</action>
  <verify>
    <automated>npm run test:integration:http -w @dtc/backend -- --runTestsByPath integration-tests/http/guest-cart-line-item-clear.spec.ts</automated>
  </verify>
  <acceptance_criteria>FE-CART-003 clear PASS; empty 200 com posse/If-Match/key.</acceptance_criteria>
  <done>Clear-all comprovado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 15-14-03: Revisar clear e exact-set 64</name>
  <files>None (checkpoint)</files>
  <read_first>apps/backend/src/api/store-surface/manifest.ts; apps/backend/src/api/store-surface/__tests__/manifest.unit.spec.ts; apps/backend/src/api/store/carts/[id]/line-items/route.ts</read_first>
  <action>Confirmar 64/13/16/12/47/5/51 e a única key nova DELETE collection.</action>
  <verify>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>Humano PASS ou BLOCKED em P15-D06.</acceptance_criteria>
  <done>Gate manual clear/exact-set.</done>
</task>

</tasks>

## Manual gate / NO DEPLOY

Parar após `15-14-SUMMARY.md`. **NO DEPLOY.**

## Artifacts this phase produces

- DELETE /store/carts/{id}/line-items (origin local)
- `validateStoreSurfaceManifest` COUNT_TOTAL 64
- `STORE_SURFACE_PHASE15_CART_ENABLED_OPERATIONS` length 6
- HTTP clear spec

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DELETE collection → all line ids | must not clear another actor's cart |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-14-01 | Elevation of Privilege | clear-all | mitigate | capability + {id} == active cart |
| T-15-14-02 | Tampering | exact-set | mitigate | unit locks 64/51/13/12 e Auth 6 |
| T-15-14-03 | Denial of Service | empty clear | mitigate | no workflow on zero ids; still authz |
| T-15-14-SC | Tampering | npm | accept | nenhum install |
</threat_model>

<verification>Manifest unit + HTTP clear; revisão humana.</verification>
<success_criteria>CART-05 completo com exact-set 64.</success_criteria>
<output>Create `15-14-SUMMARY.md` e pare. NO DEPLOY.</output>
