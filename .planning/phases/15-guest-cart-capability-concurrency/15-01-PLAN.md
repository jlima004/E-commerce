---
phase: 15-guest-cart-capability-concurrency
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/backend/src/modules/guest-cart-capability/__tests__/support/deterministic-guest-cart.ts
  - apps/backend/integration-tests/helpers/guest-cart-postgres.ts
  - apps/backend/integration-tests/helpers/guest-cart-cas.ts
  - apps/backend/integration-tests/helpers/guest-cart-leakage.ts
  - apps/backend/integration-tests/helpers/guest-cart-exact-set.ts
  - apps/backend/src/modules/guest-cart-capability/__tests__/guest-cart-validation-foundation.unit.spec.ts
  - apps/backend/integration-tests/modules/guest-cart-validation-foundation.postgres.spec.ts
autonomous: false
requirements: [CART-01, CART-02, CART-03, CART-04, CART-05, CART-06, CART-07, CART-08, CART-09]
must_haves:
  truths:
    - "Clock e fonte de entropia de 32 bytes são reproduzíveis em teste sem copiar HKDF, nonce ou recovery de auth."
    - "O harness NÃO implementa generate/hash/compare de produção; esses primitivos nascem no 15-02."
    - "Inspeção PostgreSQL descartável prova ausência de capability em texto e UNIQUE de token_hash em tabela de probe."
    - "Helper de CAS consome StoreResourceVersion integer e nunca o schema bigint supersedido."
    - "Canários do header x-indicio-guest-cart-token são procurados em DB, Redis, logs, Sentry, analytics, OpenAPI e snapshots."
    - "Helper de exact-set afirma as 6 rotas Auth M1 da Phase 14 e trata promoções de cart como explícitas."
  artifacts:
    - path: apps/backend/src/modules/guest-cart-capability/__tests__/support/deterministic-guest-cart.ts
      provides: clock congelável e entropy source de exatamente 32 bytes só em teste
    - path: apps/backend/integration-tests/helpers/guest-cart-postgres.ts
      provides: inspeção hash-only e cleanup loopback
    - path: apps/backend/integration-tests/helpers/guest-cart-cas.ts
      provides: CAS helper resource_type cart
    - path: apps/backend/integration-tests/helpers/guest-cart-leakage.ts
      provides: collectors multi-sink do token
    - path: apps/backend/integration-tests/helpers/guest-cart-exact-set.ts
      provides: assertions Auth M1 = 6 e cart promotions explícitas
  key_links:
    - from: apps/backend/integration-tests/helpers/guest-cart-cas.ts
      to: apps/backend/src/modules/store-resource-version/service.ts
      via: compareAndSwapWithMutation resourceType cart
      pattern: "compareAndSwapWithMutation|resourceType"
    - from: apps/backend/integration-tests/helpers/guest-cart-postgres.ts
      to: apps/backend/integration-tests/postgres/disposable-postgres-harness.ts
      via: DSN loopback descartável
      pattern: "DATABASE_URL|loopback"
  prohibitions:
    - "Não copiar customer-auth/security/capabilities.ts (HKDF, nonce, recovery 45s)."
    - "Não implementar generateGuestCartCapability, hashGuestCartCapability nem compareGuestCartCapabilityHash neste plano (produção = 15-02)."
    - "Não apontar harness para DB/Redis remoto, provider real, frontend ou deploy."
    - "Não promover rotas nem emitir x-indicio-guest-cart-token neste plano."
    - "Não usar 13-VALIDATION.md bigint como schema de versão."
    - "Este plano não autoriza execução de produto fora de /gsd-execute-phase após auth humana separada."
---

<objective>
Criar a Wave 0 de validação Nyquist e consolidar os PLAN locks P15-D01..P15-D10 que todos os slices seguintes honram.

Purpose: tornar CART-01..CART-09 demonstráveis antes de persistência e rotas, reusando o harness descartável das Phases 13/14, sem antecipar o algoritmo de produção da capability.
Output: clock, entropy de 32 bytes, fixtures, inspectors (PostgreSQL, CAS, leakage, exact-set) + locks escritos.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-guest-cart-capability-concurrency/15-CONTEXT.md
@.planning/phases/15-guest-cart-capability-concurrency/15-RESEARCH.md
@.planning/phases/15-guest-cart-capability-concurrency/15-VALIDATION.md
@.planning/phases/15-guest-cart-capability-concurrency/15-PATTERNS.md
@apps/backend/integration-tests/postgres/disposable-postgres-harness.ts
@apps/backend/integration-tests/helpers/auth-leakage.ts
@apps/backend/src/modules/store-resource-version/service.ts
@apps/backend/src/api/store-surface/manifest.ts
</context>

## PLAN locks (P15-D01..P15-D10) — honor in every later plan

These close RESEARCH "Must Be Decided in PLAN" plus human-approved RESEARCH defaults. Do not reopen as Qs. Human PLAN review may still adjust TTL and Q-11; until then they are locks.

- **P15-D01 module/table/link (D15-01/D15-02):** Medusa module key `guest_cart_capability`, table/model `guest_cart_capability`, id prefix `gccap`, export `GUEST_CART_CAPABILITY_MODULE`. Link file `apps/backend/src/links/guest-cart-capability-cart.ts`: `defineLink(CartModule.linkable.cart, { linkable: GuestCartCapabilityModule.linkable.guestCartCapability, isList: true })`. In-module `cart_id` is opaque text of the Medusa cart id, not a SQL FK. Partial UNIQUE one `status = active` per `cart_id` where `deleted_at IS NULL`.
- **P15-D02 encoding (CART-01):** 32 CSPRNG bytes encoded `base64url` in header `x-indicio-guest-cart-token`. Persist `createHash("sha256").update(token, "utf8").digest("hex")` only. Compare with `timingSafeEqual` on equal-length buffers. Dummy SHA-256 compare on miss. No HMAC pepper, no HKDF. Production `generate` / `hash` / `compare` land in plan 15-02, not in this harness.
- **P15-D03 BFF list (D13-01, residual risk 2):** sibling exact tuple `STORE_CART_BFF_PROTECTED_OPERATIONS` — do not append cart ops onto `CUSTOMER_AUTH_BFF_PROTECTED_OPERATIONS`. Reuse `customerAuthBffServiceGuardMiddleware` / `CUSTOMER_AUTH_BFF_SERVICE_SECRET`. Tuple of six: `GET /store/carts/active`, `POST /store/carts/active`, `POST /store/carts/{id}/line-items`, `POST /store/carts/{id}/line-items/{line_id}`, `DELETE /store/carts/{id}/line-items/{line_id}`, `DELETE /store/carts/{id}/line-items`.
- **P15-D04 idempotency scopes (D13-18):** `hashStoreIdempotencyScope` only. Guest create (pre-mint): actor `{ kind: "guest_cart_create", bff_key_hash }` where `bff_key_hash` is SHA-256 of the configured BFF secret, never the Idempotency-Key. Guest mutations: actor `{ kind: "guest_cart_capability", token_hash }` using stored hex hash, never plaintext. Customer mutations: actor `{ kind: "customer", customer_id }` from `customerAuthAccessGuard` context (`customerId`). Resource `{ kind: "cart", cart_id, operation }` after cart exists. Forbidden actor inputs: JWT, JWT hash, Medusa session id.
- **P15-D05 GET miss (FE-CART-001, D13-12):** `GET /store/carts/active` without capability, or with missing/invalid/expired/revoked/consumed/wrong-cart capability, returns HTTP 404 `STORE_ERROR_CODES.NOT_FOUND` uniform envelope. GET never creates a cart.
- **P15-D06 manifest arithmetic (human RESEARCH lock):** final `COUNT_TOTAL` 64, native identity 51, local-only 13, EXTENDED 16, DENY 47, PRESERVE_LEGACY 5, M1_ENABLED 12, nativeLocalExtension 5. Stage counts when promoting; `STORE_SURFACE_PHASE14_ENABLED_OPERATIONS` remains an intact subset of six Auth keys. M1 = 6 Auth Phase 14 + 6 Cart Phase 15. Only `DELETE /store/carts/{id}/line-items` is a new `origin: local` identity. The three existing native line-item identities become `native+local_extension`. Never duplicate them as local rows.
- **P15-D07 Idempotency-Key × If-Match:** (1) first successful mutation increments version via `compareAndSwapWithMutation`; (2) retry of same intent / same Idempotency-Key must `claim` first and on `replay` return current canonical cart + current ETag — never a false 412 solely because the first execution advanced ETag; (3) key never replaces ownership or If-Match on first execution; (4) a new intent after conflict still requires a fresh If-Match matching current version. Missing/malformed If-Match on first claim → 400 `VALIDATION_ERROR`. Stale If-Match on claimed path → 412 `CART_VERSION_MISMATCH`. **Replan addendum (B15-P-HR-03):** body/path validation happens **before** `claim`; `claim` still happens **before** `If-Match`. Do not invent a new idempotency state machine. Use existing `processing` / `completed` / `failed_retryable` / `failed_terminal` / `reconciliation_required` / `reconciliation_unresolved` via `markCompleted` / `markFailedRetryable` / `markFailedTerminal`. Stale 412 must not leave the claim abandoned in `processing`.
- **P15-D08 TTL (RESEARCH lock, D15-04/CART-03):** 7-day rolling on valid GET/mutation; absolute cap 30 days from `created_at`. BFF cookie Max-Age must be ≤ capability TTL (document only; do not set browser cookies).
- **P15-D09 Q-11 Option A (RESEARCH lock, D15-05):** after perda B (and perda A if cookie never written), orphan empty/inaccessible cart until TTL. Replay of the same Idempotency-Key returns safe context and does not re-emit `x-indicio-guest-cart-token`. Rotation-on-replay is not the default. **Replan addendum (B15-P-HR-02):** mint success = HTTP **201**. Replay = HTTP **200**. Do not persist a full response DTO on `StoreIdempotencyRecord`. Safe context is `result_id`/`cart_id` plus **refetch** of current canonical `PublicStoreCartPreOrder` and current `StoreResourceVersion` ETag. Never write "stored DTO". Never say "200 ou status stored".
- **P15-D10 dual-run (D15-02/Q-03):** M1 guest possession is only the capability. `req.session.active_cart_id` may be written as a payment hint on create if a Medusa session exists; it is never M1 proof. Do not delete session (Phase 19 payment still reads it).

### POST / GET active contract (B15-P-HR-01 / B15-P-HR-04) — honor from plan 15-03 onward

GET `/store/carts/active`:

- valid guest capability → guest cart 200
- missing capability + no valid Customer context → 404
- presented invalid / expired / revoked / consumed capability → uniform 404
- valid Phase-14 Customer context → customer active cart

POST `/store/carts/active`:

- no capability + guest/BFF → create guest cart
- valid capability → reuse guest cart
- capability PRESENT but invalid/terminal → uniform 404; MUST NOT silently create a new cart
- valid Phase-14 Customer context → create/reuse Customer cart; no guest capability emitted

Customer active ships in the same cut as guest active M1. No `describe.skip` bridge for a regression caused by the plan.

D15-17 is superseded by STATE (PLAN authorized). D15-18 is satisfied by encoding these locks rather than agent discretion.

## Dependencies

- Wave 0 lógica e GSD; nenhum outro plano da fase começa antes do PASS humano deste plano.
- Sem pacote novo, provider real, DB/Redis remoto, Docker persistente ou deploy.

<tasks>

<task type="auto" tdd="true">
  <name>Task 15-01-01: Clock, entropy de 32 bytes, CAS, exact-set e canários</name>
  <files>apps/backend/src/modules/guest-cart-capability/__tests__/support/deterministic-guest-cart.ts, apps/backend/integration-tests/helpers/guest-cart-cas.ts, apps/backend/integration-tests/helpers/guest-cart-leakage.ts, apps/backend/integration-tests/helpers/guest-cart-exact-set.ts, apps/backend/src/modules/guest-cart-capability/__tests__/guest-cart-validation-foundation.unit.spec.ts</files>
  <read_first>.planning/phases/15-guest-cart-capability-concurrency/15-VALIDATION.md Wave 0; .planning/phases/15-guest-cart-capability-concurrency/15-RESEARCH.md Don't Hand-Roll and Residual Risks; apps/backend/src/modules/customer-auth/__tests__/support/deterministic-auth.ts; apps/backend/integration-tests/helpers/auth-leakage.ts; apps/backend/src/modules/store-resource-version/service.ts; apps/backend/src/api/store-surface/manifest.ts; apps/backend/src/api/store-surface/__tests__/manifest.unit.spec.ts</read_first>
  <behavior>
  - Mesma seed/clock produz a mesma sequência de 32 bytes; seed diferente diverge.
  - Canário sintético do header não aparece em collectors de DB plaintext, Redis, logs, Sentry, OpenAPI, snapshots ou analytics.
  - Exact-set helper falha se as 6 chaves de STORE_SURFACE_PHASE14_ENABLED_OPERATIONS sumirem ou se native identity cair abaixo de 51.
  </behavior>
  <action>Escrever primeiro os testes da Wave 0. Exportar createDeterministicGuestCartClock (congelável/avançável) e createDeterministicGuestCartEntropy que devolve exatamente 32 bytes via randomBytesFn injetável. Fornecer fixtures de canário (string base64url fixa + hash hex sintético) para leakage/PG — NÃO exportar generateGuestCartCapability, hashGuestCartCapability nem compareGuestCartCapabilityHash; esses símbolos nascem em apps/backend/src/modules/guest-cart-capability/hash.ts no 15-02. Falhar se NODE_ENV não for test; não exportar esses hooks de produção. O CAS helper deve chamar StoreResourceVersionModuleService.compareAndSwapWithMutation com resourceType cart e expectedVersion number inteiro positivo; rejeitar qualquer conversão bigint. O leakage collector procura o literal do header e um canário de token sintético nos sinks listados em 15-VALIDATION.md. O exact-set helper lê STORE_SURFACE_MANIFEST e afirma as 6 Auth M1, DENY de create-by-id/read-by-id/complete/attach/shipping-methods, e que promoções de cart só passam quando o caller declara as chaves esperadas. Nunca importar hkdfSync nem capabilities.ts de customer-auth.</action>
  <verify>
    <automated>npm run test:unit -w @dtc/backend -- --runTestsByPath src/modules/guest-cart-capability/__tests__/guest-cart-validation-foundation.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>O spec unitário passa em menos de 30 s; clock/entropy são determinísticos; o helper de exact-set conhece as 6 Auth M1; collectors detectam o canário do token; nenhum símbolo de generate/hash/compare de produção entra neste plano; nenhum símbolo HKDF/nonce/recovery entra nos arquivos deste plano.</acceptance_criteria>
  <done>V15-W0 possui primitives reutilizáveis sem copiar o primitivo de auth nem o algoritmo de produção da capability.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15-01-02: Inspeção PostgreSQL hash-only descartável</name>
  <files>apps/backend/integration-tests/helpers/guest-cart-postgres.ts, apps/backend/integration-tests/modules/guest-cart-validation-foundation.postgres.spec.ts</files>
  <read_first>apps/backend/integration-tests/postgres/disposable-postgres-harness.ts; apps/backend/scripts/run-disposable-postgres-tests.mjs; apps/backend/integration-tests/helpers/auth-postgres.ts; apps/backend/src/modules/store-resource-version/__tests__/store-resource-version.postgres.spec.ts</read_first>
  <behavior>
  - SELECT sanitizado em tabela temporária prova que o plaintext do canário está ausente e que token_hash UNIQUE rejeita duplicata.
  - Cleanup remove somente o database/prefixo registrado e recusa DSN não-loopback.
  </behavior>
  <action>Reutilizar o harness descartável existente com prefixo guest_cart explícito e allowlist loopback. Helper assertGuestCartHashOnlyPersistence(conn, { plaintextCanary, tokenHash }) faz SELECT que falha se o canário textual aparecer em qualquer coluna text/json e passa quando só o hash existe. Helper assertTokenHashUnique tenta inserir o mesmo hash duas vezes e espera violação UNIQUE. Este plano cria só tabela de probe temporária; não gera migration Medusa. Um spec por processo disposable. Redis não participa.</action>
  <verify>
    <automated>node apps/backend/scripts/run-disposable-postgres-tests.mjs -- npm run test:integration:modules -w @dtc/backend -- --runTestsByPath integration-tests/modules/guest-cart-validation-foundation.postgres.spec.ts</automated>
  </verify>
  <acceptance_criteria>Um processo disposable prova hash-only e UNIQUE, recusa alvo não-loopback e limpa o database registrado mesmo após falha.</acceptance_criteria>
  <done>Inspeção PostgreSQL de CART-01 está pronta antes do módulo real.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 15-01-03: Revisar a Wave 0 e os PLAN locks</name>
  <files>None (checkpoint de revisão)</files>
  <read_first>apps/backend/src/modules/guest-cart-capability/__tests__/guest-cart-validation-foundation.unit.spec.ts; apps/backend/integration-tests/modules/guest-cart-validation-foundation.postgres.spec.ts; seção PLAN locks deste arquivo; outputs sanitizados dos dois comandos automatizados</read_first>
  <action>Apresentar harnesses, prova de isolamento loopback, resultado dos canários, e a lista P15-D01..P15-D10 incluindo os addenda B15-P-HR-02/03. Confirmar que generate/hash/compare de produção NÃO existem neste plano. Confirmar TTL 7d/30d e Q-11 Option A como locks de PLAN. Não iniciar 15-02 sem aprovação humana explícita. Não implementar produto neste checkpoint.</action>
  <verify>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>Revisão humana registra PASS ou BLOCKED; confirma ausência de provider/infra real, ausência de algoritmo de produção neste plano, e aceita os PLAN locks ou devolve ajuste explícito de TTL/Q-11 somente.</acceptance_criteria>
  <done>Gate manual da Wave 0 concluído.</done>
</task>

</tasks>

## Constraints / negative proofs

- Provider real, deploy, DB/Redis remoto, package/lock e runtime produtivo são proibidos.
- Canários não podem persistir em texto puro.
- Auth exact-set de 6 rotas permanece a autoridade atual; este plano não promove cart.

## Stop conditions

Parar como BLOCKED se o harness alcançar alvo não-loopback, cleanup não for exato, canário vazar, test hook for importável em produção, se generate/hash/compare de produção nascerem aqui, ou se HKDF/nonce de auth for copiado.

## Rollback / local recovery

Encerrar somente PIDs/containers registrados, remover apenas databases temporários e reverter os arquivos deste plano.

## Manual gate / NO DEPLOY

Execução estritamente serial; parar após `15-01-SUMMARY.md`. **NO DEPLOY.** Este PLAN.md não autoriza execução até `/gsd-execute-phase` com auth humana separada.

## Artifacts this phase produces

- `createDeterministicGuestCartClock`, `createDeterministicGuestCartEntropy`
- fixtures de canário (não o algoritmo de produção)
- helpers `guest-cart-postgres`, `guest-cart-cas`, `guest-cart-leakage`, `guest-cart-exact-set`
- header name lock `x-indicio-guest-cart-token`
- module key lock `guest_cart_capability`
- PLAN locks P15-D01..P15-D10

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → disposable Postgres | untrusted DSN must not escape loopback |
| test collectors → logs/Sentry/OpenAPI | canary token must not leak into fixtures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | Information Disclosure | leakage collectors | mitigate | canários multi-sink; plaintext ausente no probe PG |
| T-15-01-02 | Tampering | disposable DSN | mitigate | allowlist loopback + nomes exatos |
| T-15-01-03 | Elevation of Privilege | exact-set helper | mitigate | Auth M1 de 6 rotas é invariante; cart promotions explícitas |
| T-15-01-04 | Spoofing | entropy helper | mitigate | CSPRNG ≥ 32 bytes; teste injeta bytes, produção usará randomBytes no 15-02 |
| T-15-01-SC | Tampering | npm installs | accept | nenhum package install neste plano |
</threat_model>

<verification>Os dois comandos focados passam; git diff --check; revisão humana bloqueante.</verification>
<success_criteria>Validation infrastructure + PLAN locks prontos. Nenhum plano dependente precisa inventar clock, entropy, CAS, leakage ou exact-set. Algoritmo de produção da capability permanece ausente até 15-02.</success_criteria>
<output>Create `15-01-SUMMARY.md` e pare para revisão humana. NO DEPLOY. EXECUTION permanece não autorizada por este arquivo.</output>
