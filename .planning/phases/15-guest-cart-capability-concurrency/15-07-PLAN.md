---
phase: 15-guest-cart-capability-concurrency
plan: 07
type: execute
wave: 6
depends_on: [15-06]
files_modified:
  - apps/backend/src/modules/guest-cart-capability/service.ts
  - apps/backend/src/modules/guest-cart-capability/lookup.ts
  - apps/backend/src/api/store/carts/active/route.ts
  - apps/backend/src/modules/guest-cart-capability/__tests__/guest-cart-capability-lifecycle.unit.spec.ts
  - apps/backend/integration-tests/http/guest-cart-lifecycle.spec.ts
autonomous: false
requirements: [CART-03]
must_haves:
  truths:
    - "Capability expirada, revogada ou consumida produz o mesmo 404 que miss, sem enumerar o motivo."
    - "Uso válido de GET/POST reutilizando o cart rola expires_at em 7d sem ultrapassar created_at+30d."
    - "Carrinho com completed_at encerra a capability (consume); merge consume permanece Phase 16."
    - "Expiry de sessão Customer (D14-08/D15-15) não apaga o cart persistido nem a capability guest."
  artifacts:
    - path: apps/backend/src/modules/guest-cart-capability/__tests__/guest-cart-capability-lifecycle.unit.spec.ts
      provides: TTL rolling/cap e transições
    - path: apps/backend/integration-tests/http/guest-cart-lifecycle.spec.ts
      provides: 404 uniforme expired/revoked/consumed
  key_links:
    - from: apps/backend/src/api/store/carts/active/route.ts
      to: apps/backend/src/modules/guest-cart-capability/lookup.ts
      via: fail-closed lifecycle before read
      pattern: "lookupByPresentedToken|completed_at"
  prohibitions:
    - "Não copiar TTL 30 min de verificação auth."
    - "Não implementar consume-on-merge (Phase 16)."
    - "Não destruir cart quando o access JWT expira."
---

<objective>
Fechar CART-03 no tracer já promovido: ownership, expiração, revogação, consumo e TTL 7d/30d.

Purpose: D15-04 e P15-D08 observáveis em HTTP.
Output: lifecycle unit + HTTP matrix uniforme.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-guest-cart-capability-concurrency/15-06-PLAN.md
@apps/backend/src/modules/guest-cart-capability/service.ts
@apps/backend/integration-tests/http/guest-cart-tracer.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 15-07-01: TTL rolling, cap 30d e consume-on-complete</name>
  <files>apps/backend/src/modules/guest-cart-capability/service.ts, apps/backend/src/modules/guest-cart-capability/lookup.ts, apps/backend/src/api/store/carts/active/route.ts, apps/backend/src/modules/guest-cart-capability/__tests__/guest-cart-capability-lifecycle.unit.spec.ts</files>
  <read_first>apps/backend/src/modules/guest-cart-capability/service.ts; 15-01-PLAN.md P15-D08; apps/backend/src/api/store/carts/active/route.ts</read_first>
  <behavior>
  - Clock now = created_at+6d touch → expires_at ~= now+7d; now = created_at+29d+23h touch não passa de created_at+30d; now > expires_at lookup falha uniforme.
  - Cart completed_at setado implica consume; lookup seguinte uniforme inválido.
  </behavior>
  <action>Usar o clock determinístico da Wave 0. Constants GUEST_CART_CAPABILITY_ROLLING_TTL_MS = 7d e GUEST_CART_CAPABILITY_ABSOLUTE_TTL_MS = 30d. GET/POST reuse chama touchRollingTtl somente após lookup válido. Se o cart refetch mostrar completed_at, chamar consumeGuestCartCapability e responder 404 uniforme. Não expor status no body. D14-08: nenhum code path de expiry JWT chama delete cart ou revoke guest capability.</action>
  <verify>
    <automated>npm run test:unit -w @dtc/backend -- --runTestsByPath src/modules/guest-cart-capability/__tests__/guest-cart-capability-lifecycle.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>7d rolling, cap 30d, consume on completed_at, erro uniforme, zero delete-cart on auth expiry.</acceptance_criteria>
  <done>CART-03 domain fechado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15-07-02: HTTP matrix expired/revoked/consumed</name>
  <files>apps/backend/integration-tests/http/guest-cart-lifecycle.spec.ts</files>
  <read_first>apps/backend/integration-tests/http/guest-cart-tracer.spec.ts; apps/backend/integration-tests/helpers/guest-cart-leakage.ts</read_first>
  <behavior>
  - Três estados terminais devolvem o mesmo 404 NOT_FOUND e o mesmo message estável; timing/body não revelam qual estado.
  - Token continua ausente de JSON/logs.
  </behavior>
  <action>Reusar BFF+tracer fixtures. Forçar expires_at no passado via service/clock de teste, revoke explícito, consume explícito. Comparar status/code/message entre os três. Incluir header de outro cart (wrong) no mesmo 404. Leakage canary. Não usar watch mode.</action>
  <verify>
    <automated>npm run test:integration:http -w @dtc/backend -- --runTestsByPath integration-tests/http/guest-cart-lifecycle.spec.ts</automated>
  </verify>
  <acceptance_criteria>Matrix FE-CART-002 HTTP PASS com 404 uniforme e zero leakage.</acceptance_criteria>
  <done>CART-03 evidenciado em HTTP.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 15-07-03: Revisar lifecycle</name>
  <files>None (checkpoint)</files>
  <read_first>apps/backend/src/modules/guest-cart-capability/__tests__/guest-cart-capability-lifecycle.unit.spec.ts; apps/backend/integration-tests/http/guest-cart-lifecycle.spec.ts</read_first>
  <action>Confirmar TTL 7d/30d, 404 uniforme e D14-08. Não puxar merge.</action>
  <verify>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>Humano PASS ou BLOCKED.</acceptance_criteria>
  <done>Gate manual CART-03.</done>
</task>

</tasks>

## Manual gate / NO DEPLOY

Parar após `15-07-SUMMARY.md`. **NO DEPLOY.**

## Artifacts this phase produces

- `GUEST_CART_CAPABILITY_ROLLING_TTL_MS`, `GUEST_CART_CAPABILITY_ABSOLUTE_TTL_MS`
- consume-on-complete path in active route
- HTTP lifecycle matrix

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| presented token → lifecycle | expired/revoked/consumed must not oracle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07-01 | Information Disclosure | error body | mitigate | 404 uniforme D13-12 |
| T-15-07-02 | Spoofing | expired token | mitigate | lookup fail-closed |
| T-15-07-03 | Tampering | rolling TTL | mitigate | cap 30d |
| T-15-07-SC | Tampering | npm | accept | nenhum install |
</threat_model>

<verification>Unit lifecycle + HTTP matrix; revisão humana.</verification>
<success_criteria>CART-03 observável sem enumeração.</success_criteria>
<output>Create `15-07-SUMMARY.md` e pare. NO DEPLOY.</output>
