---
phase: 15-guest-cart-capability-concurrency
plan: 08
type: execute
wave: 7
depends_on: [15-07]
files_modified:
  - apps/backend/src/api/store/carts/active/route.ts
  - apps/backend/src/api/store/carts/idempotency-scope.ts
  - apps/backend/src/modules/store-idempotency/service.ts
  - apps/backend/src/api/store/carts/__tests__/idempotency-scope.unit.spec.ts
  - apps/backend/integration-tests/http/guest-cart-idempotency.spec.ts
autonomous: false
requirements: [CART-04]
must_haves:
  truths:
    - "POST /store/carts/active exige Idempotency-Key; a mesma key devolve o mesmo cart_id e DTO sem reemitir x-indicio-guest-cart-token (ETag de replay entra no 15-09)."
    - "Actor scope do create é hash da identidade BFF, nunca a própria key nem JWT/session."
    - "Idempotency metadata continua sem chave capability; replay não é posse."
    - "Q-11 Option A: órfão após perda B; nova key cria novo cart; o anterior permanece inacessível até TTL."
  artifacts:
    - path: apps/backend/src/api/store/carts/idempotency-scope.ts
      provides: guestCartCreateActorScope / guestCartCapabilityActorScope / customerActorScope
    - path: apps/backend/integration-tests/http/guest-cart-idempotency.spec.ts
      provides: replay sem header + orphan new-key
  key_links:
    - from: apps/backend/src/api/store/carts/active/route.ts
      to: apps/backend/src/modules/store-idempotency/service.ts
      via: claim then mint or replay safe context
      pattern: "claim\\(|hashStoreIdempotencyScope"
  prohibitions:
    - "Não persistir plaintext no record de idempotência."
    - "Não usar Idempotency-Key como credencial de recuperação."
    - "Não fazer rotation-on-replay."
    - "Não usar JWT hash ou session id como actorScope."
---

<objective>
Ligar store-idempotency ao POST active com scopes P15-D04 e replay Q-11 Option A (P15-D09).

Purpose: CART-04 create/get lazy idempotente sem reemissão do secret.
Output: scope helpers + HTTP replay/orphan.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-guest-cart-capability-concurrency/15-01-PLAN.md
@apps/backend/src/modules/store-idempotency/service.ts
@apps/backend/src/api/store/carts/active/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 15-08-01: Scopes canônicos e claim no POST active</name>
  <files>apps/backend/src/api/store/carts/idempotency-scope.ts, apps/backend/src/api/store/carts/active/route.ts, apps/backend/src/api/store/carts/__tests__/idempotency-scope.unit.spec.ts, apps/backend/src/modules/store-idempotency/service.ts</files>
  <read_first>apps/backend/src/modules/store-idempotency/service.ts ClaimInput, hashStoreIdempotencyScope, STORE_IDEMPOTENCY_SAFE_METADATA_KEYS, forbiddenKeys; 15-01-PLAN.md P15-D04 e P15-D07/P15-D09; apps/backend/src/api-docs/components/parameters.ts IdempotencyKey</read_first>
  <behavior>
  - guestCartCreateActorScope({ bffKeyHash }) canônico e estável; mudando o secret muda o hash; a raw Idempotency-Key não entra no actor object.
  - Replay claim type replay devolve cart_id e DTO stored e não chama mint; não persiste nem devolve ETag neste plano.
  </behavior>
  <action>Implementar guestCartCreateActorScope, guestCartCapabilityActorScope({ tokenHash }), customerActorScope({ customerId }), cartResourceScope({ cartId, operation }). POST active: exigir header Idempotency-Key; claim com operation store.carts.active.create. On claimed: create+mint, persistir metadata allowlisted (operation, result_id=cart_id, response_status=201) sem capability e sem chave etag. On replay: 200 (ou o status stored) com cart_id + DTO only; omitir x-indicio-guest-cart-token; não formatar nem persistir ETag (15-09 anexa o ETag atual a partir de StoreResourceVersion). On conflict: 409 IDEMPOTENCY_KEY_REUSE_CONFLICT. GET não chama claim. Se forbiddenKeys ainda não inclui guest_cart_token, alinhar o nome do header na denylist de metadata. Não alterar o algoritmo de hashStoreIdempotencyScope. Não estender STORE_IDEMPOTENCY_SAFE_METADATA_KEYS com etag.</action>
  <verify>
    <automated>npm run test:unit -w @dtc/backend -- --runTestsByPath src/api/store/carts/__tests__/idempotency-scope.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>Scopes sem JWT/session/plaintext; replay path não mint; metadata allowlist sem capability e sem etag.</acceptance_criteria>
  <done>P15-D04 aplicado ao create.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 15-08-02: HTTP replay emit-once e órfão nova key</name>
  <files>apps/backend/integration-tests/http/guest-cart-idempotency.spec.ts</files>
  <read_first>apps/backend/integration-tests/http/guest-cart-tracer.spec.ts; 15-RESEARCH.md Q-11 Option A</read_first>
  <behavior>
  - Mesma Idempotency-Key: mesmo cart_id; segundo response sem x-indicio-guest-cart-token.
  - Nova key após o primeiro 201 cria outro cart; o primeiro permanece inacessível sem o secret original (órfão).
  </behavior>
  <action>Dois POSTs same-key: primeiro 201 com header, segundo replay sem header. POST different-key: 201 novo cart. GET com o primeiro token ainda funciona se o teste conservou o secret em memória (não é perda B). Simular perda B: descartar o secret e replay same-key → contexto sem header, GET sem token 404. Leakage scan. Zero Order.</action>
  <verify>
    <automated>npm run test:integration:http -w @dtc/backend -- --runTestsByPath integration-tests/http/guest-cart-idempotency.spec.ts</automated>
  </verify>
  <acceptance_criteria>Q-11 Option A HTTP PASS; key ≠ posse; emit-once no replay.</acceptance_criteria>
  <done>CART-04 idempotente sem reemissão.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 15-08-03: Revisar idempotência Q-11</name>
  <files>None (checkpoint)</files>
  <read_first>apps/backend/src/api/store/carts/idempotency-scope.ts; apps/backend/integration-tests/http/guest-cart-idempotency.spec.ts</read_first>
  <action>Confirmar Option A, scopes e ausência de rotation-on-replay. Não ligar If-Match de mutação ainda (15-09/11).</action>
  <verify>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>Humano PASS ou BLOCKED.</acceptance_criteria>
  <done>Gate manual CART-04/Q-11.</done>
</task>

</tasks>

## Manual gate / NO DEPLOY

Parar após `15-08-SUMMARY.md`. **NO DEPLOY.**

## Artifacts this phase produces

- `guestCartCreateActorScope`, `guestCartCapabilityActorScope`, `customerActorScope`, `cartResourceScope`
- POST active `store-idempotency.claim`
- HTTP replay without capability header

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Idempotency-Key → claim | untrusted key must not become ownership |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-08-01 | Elevation of Privilege | Idempotency-Key | mitigate | key ≠ posse; guest mutations ainda exigirão header (15-11+) |
| T-15-08-02 | Information Disclosure | replay body | mitigate | safe context only; sem token |
| T-15-08-03 | Spoofing | actorScope | mitigate | BFF key hash / token_hash / customerId; nunca JWT/session |
| T-15-08-SC | Tampering | npm | accept | nenhum install |
</threat_model>

<verification>Unit scopes + HTTP idempotency; revisão humana.</verification>
<success_criteria>Create lazy idempotente com emit-once e órfão Q-11.</success_criteria>
<output>Create `15-08-SUMMARY.md` e pare. NO DEPLOY.</output>
