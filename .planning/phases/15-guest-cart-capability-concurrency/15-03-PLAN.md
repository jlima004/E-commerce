---
phase: 15-guest-cart-capability-concurrency
plan: 03
type: execute
wave: 2
depends_on: [15-02]
files_modified:
  - apps/backend/src/modules/guest-cart-capability/service.ts
  - apps/backend/src/modules/guest-cart-capability/lookup.ts
  - apps/backend/src/modules/guest-cart-capability/index.ts
  - apps/backend/src/modules/guest-cart-capability/__tests__/guest-cart-capability-service.unit.spec.ts
autonomous: false
requirements: [CART-01, CART-03]
must_haves:
  truths:
    - "mint persiste somente token_hash e devolve plaintext uma vez."
    - "lookup de miss executa compare dummy antes do throw uniforme."
    - "expire/revoke/consume são transições de service; Redis não concede validade."
  artifacts:
    - path: apps/backend/src/modules/guest-cart-capability/service.ts
      provides: mint/expire/revoke/consume/touchRollingTtl
    - path: apps/backend/src/modules/guest-cart-capability/lookup.ts
      provides: lookupByPresentedToken dummy-on-miss
    - path: apps/backend/src/modules/guest-cart-capability/index.ts
      provides: GUEST_CART_CAPABILITY_MODULE
  key_links:
    - from: apps/backend/src/modules/guest-cart-capability/lookup.ts
      to: apps/backend/src/modules/tracking-access-token/lookup.ts
      via: dummy compare then uniform throw
      pattern: "performDummy|compareGuestCartCapabilityHash"
  prohibitions:
    - "Não copiar HKDF/nonce/recovery."
    - "Não registrar o módulo em medusa-config neste plano (isso é 15-04)."
    - "Não logar plaintext_token."
---

<objective>
Implementar mint, lookup anti-enumeração e transições de lifecycle no service, ainda sem migration.

Purpose: CART-01/CART-03 domain API existe com testes unitários antes do CLI generate.
Output: service + lookup + Module export.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-guest-cart-capability-concurrency/15-02-PLAN.md
@apps/backend/src/modules/tracking-access-token/service.ts
@apps/backend/src/modules/tracking-access-token/lookup.ts
@apps/backend/src/modules/store-resource-version/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 15-03-01: Service mint/lifecycle e lookup dummy-miss</name>
  <files>apps/backend/src/modules/guest-cart-capability/service.ts, apps/backend/src/modules/guest-cart-capability/lookup.ts, apps/backend/src/modules/guest-cart-capability/__tests__/guest-cart-capability-service.unit.spec.ts</files>
  <read_first>apps/backend/src/modules/tracking-access-token/service.ts mintTrackingAccessToken, compare, PLAINTEXT_FORBIDDEN; apps/backend/src/modules/tracking-access-token/lookup.ts dummy miss; apps/backend/src/modules/guest-cart-capability/hash.ts; apps/backend/src/modules/guest-cart-capability/types.ts; 15-01-PLAN.md P15-D08 TTL</read_first>
  <behavior>
  - mintGuestCartCapability grava record sem plaintext; plaintext_token só no retorno; construir record com plaintext lança GUEST_CART_CAPABILITY_PLAINTEXT_FORBIDDEN.
  - lookupByPresentedToken em miss faz dummy compare e lança o mesmo erro público que expired/revoked/consumed/wrong hash.
  - touchRollingTtl em uso válido estende expires_at para min(now+7d, created_at+30d) e nunca ultrapassa o teto.
  </behavior>
  <action>MedusaService sobre o model. mint usa generate+hash de hash.ts, persiste token_hash, cart_id, status active, expires_at = now+7d. expire/revoke/consume atualizam status e timestamps; consume seta consumed_at. assertRecordHasNoPlaintext recusa chaves plaintext_token/token/capability. lookup: hash do candidato, listByHash; se vazio, dummy SHA-256 compare; throw uniforme GUEST_CART_CAPABILITY_LOOKUP_INVALID (mapeado depois para 404 HTTP). Não distinguir miss vs mismatch no erro. Clock injetável para testes de TTL. last_used_at atualizado em lookup válido. Não usar createHmac.</action>
  <verify>
    <automated>npm run test:unit -w @dtc/backend -- --runTestsByPath src/modules/guest-cart-capability/__tests__/guest-cart-capability-service.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>Mint hash-only; dummy miss; transições consumed/expired/revoked; rolling 7d com cap 30d; erro uniforme.</acceptance_criteria>
  <done>Service/lookup cobrem CART-01 e CART-03 em unit.</done>
</task>

<task type="auto">
  <name>Task 15-03-02: Exportar o Module key</name>
  <files>apps/backend/src/modules/guest-cart-capability/index.ts</files>
  <read_first>apps/backend/src/modules/tracking-access-token/index.ts; apps/backend/src/modules/store-resource-version/index.ts</read_first>
  <action>Export GUEST_CART_CAPABILITY_MODULE = guest_cart_capability e default Module com GuestCartCapabilityModuleService. Reexportar types/hash públicos necessários aos rotas posteriores. Não registrar medusa-config ainda.</action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('apps/backend/src/modules/guest-cart-capability/index.ts','utf8'); for (const x of ['GUEST_CART_CAPABILITY_MODULE','guest_cart_capability','GuestCartCapabilityModuleService']) if (!s.includes(x)) throw new Error('missing '+x)"</automated>
  </verify>
  <acceptance_criteria>index.ts exporta o module key idêntico a P15-D01.</acceptance_criteria>
  <done>Módulo está importável para o generate do 15-04.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 15-03-03: Revisar mint/lookup</name>
  <files>None (checkpoint)</files>
  <read_first>apps/backend/src/modules/guest-cart-capability/service.ts; apps/backend/src/modules/guest-cart-capability/lookup.ts; apps/backend/src/modules/guest-cart-capability/index.ts</read_first>
  <action>Confirmar hash-only, dummy miss, TTL 7d/30d e ausência de HMAC. Não gerar migration.</action>
  <verify>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>Humano PASS ou BLOCKED.</acceptance_criteria>
  <done>Gate manual do service.</done>
</task>

</tasks>

## Manual gate / NO DEPLOY

Parar após `15-03-SUMMARY.md`. **NO DEPLOY.**

## Artifacts this phase produces

- `mintGuestCartCapability`, `expireGuestCartCapability`, `revokeGuestCartCapability`, `consumeGuestCartCapability`, `touchRollingTtl`
- `lookupByPresentedToken`
- `GUEST_CART_CAPABILITY_MODULE`
- `GUEST_CART_CAPABILITY_PLAINTEXT_FORBIDDEN`, `GUEST_CART_CAPABILITY_LOOKUP_INVALID`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| presented header → lookup | attacker-controlled token; miss must not time-oracle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-01 | Spoofing | lookup.ts | mitigate | dummy compare + erro uniforme |
| T-15-03-02 | Information Disclosure | mint | mitigate | plaintext só no retorno; throw se entrar no record |
| T-15-03-03 | Tampering | TTL | mitigate | teto 30d impede rolling infinito |
| T-15-03-SC | Tampering | npm | accept | nenhum install |
</threat_model>

<verification>Unit service + scan do index; revisão humana.</verification>
<success_criteria>Mint/lookup/lifecycle unitários PASS sem DDL.</success_criteria>
<output>Create `15-03-SUMMARY.md` e pare. NO DEPLOY.</output>
