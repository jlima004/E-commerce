---
phase: "04-stripe-payments-payment-attempt"
plan: 05
type: execute
wave: 4
depends_on:
  - 04-01
  - 04-02
  - 04-03
files_modified:
  - apps/backend/src/api/store/carts/payment-attempts/pix/route.ts
  - apps/backend/src/modules/payment-attempt/pix.ts
  - apps/backend/src/modules/payment-attempt/__tests__/pix-initiation.unit.spec.ts
  - apps/backend/integration-tests/http/payment-attempt-store.spec.ts
autonomous: false
requirements:
  - PAY-02
  - PAY-03
  - PAY-04
requirements_addressed:
  - PAY-02
  - PAY-03
  - PAY-04
must_haves:
  truths:
    - "Pix so pode ser executado depois que 04-01 declarar `PIX_NATIVE_SAFE=true` ou depois de replanejamento/aprovacao explicita de custom provider/camada propria."
    - "Pix inicia PaymentIntent/Sessao em BRL e retorna QR/instrucoes imediatas com `expires_at` efetivo."
    - "`expires_at` efetivo e persistido; QR/copia-e-cola integral nao e persistido salvo aprovacao explicita do gate."
    - "`awaiting_pix_payment`, `pix_expired`, `payment_failed` e `payment_canceled` nunca criam Order."
    - "Pix pendente/expirado/falho permanece estado local ate webhook futuro."
  prohibitions:
    - "Nao assumir Pix native-first puro antes do gate 04-01."
    - "Nao persistir QR/copia-e-cola Pix integral sem justificativa e aprovacao."
    - "Nao persistir `client_secret`."
    - "Nao criar Order."
    - "Nao implementar webhook Stripe."
    - "Nao criar WebhookEventLog ou CheckoutCompletionLog."
    - "Nao emitir purchase_completed."
    - "Nao tocar em Gelato."
---

<objective>
Implementar futuramente a iniciacao de Pix em BRL, com QR/instrucoes imediatas, `expires_at` efetivo e estados assincros locais em `PaymentAttempt`.

Purpose: cumprir PAY-02/PAY-03 sem transformar Pix pendente, expirado, cancelado ou falho em verdade financeira nem criar Order antes do webhook canonico.
Output: rota/helper/testes de Pix com persistencia parcial segura e gate obrigatorio contra native-first inseguro.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
@.planning/phases/04-stripe-payments-payment-attempt/04-01-SUMMARY.md
@.planning/phases/04-stripe-payments-payment-attempt/04-02-SUMMARY.md
@.planning/phases/04-stripe-payments-payment-attempt/04-03-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-stripe-payments-payment-attempt/04-CONTEXT.md
@.planning/phases/04-stripe-payments-payment-attempt/04-RESEARCH.md
@docs/PRD_Backend_v1.1.md
@docs/DB_MODEL_v1.21.md
@apps/backend/src/observability/sanitize.ts
</context>

## Phase Goal

Permitir Pix pre-Order em BRL com UX assincra e persistencia minima segura, sem antecipar webhook/Order.

## Artifacts this phase produces

- `POST /store/carts/:id/payment-attempts/pix`.
- `startPixPaymentAttempt`.
- `PixPaymentAttemptResponse`.
- `extractSafePixInstructions`.
- `maskOrHashPixPayload`.
- Integration tests HTTP para Pix sem Order.

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| REQ | PAY-02 | Pix Stripe BRL assincro | 04-05 | COVERED | QR/instrucoes/`expires_at` e status local. |
| REQ | PAY-03 | Pix pendente/falho nao cria Order | 04-05 | COVERED | State machine + integration negative. |
| REQ | PAY-04 | Tentativa auditavel | 04-05 | COVERED | PaymentAttempt pix com IDs seguros. |
| CONTEXT | D-32..D-39 | Pix assincro e persistencia parcial | 04-05 | COVERED | QR imediato, `expires_at`, sem payload integral por default. |

## Decision Coverage

- D-25: `payment_instructions_displayed`, `awaiting_pix_payment` e `pix_expired` sao estados locais validos.
- D-32: Pix e fluxo assincro, com QR/copia-e-cola e tentativa aguardando pagamento.
- D-33: backend pode retornar QR/instrucoes Pix na resposta imediata.
- D-34: persistencia Pix e parcial: IDs seguros, `expires_at`, status, amount/currency e metodo.
- D-35: QR code, copia-e-cola e instrucoes completas nao sao persistidos integralmente por default.
- D-36: reexibicao Pix avalia refetch/derive seguro da sessao Stripe.
- D-37: `expires_at` efetivo do provider vence preferencia local de TTL.
- D-38: backend persiste o `expires_at` efetivo.
- D-39: Pix pendente, expirado, cancelado ou falho nunca cria Order.
- D-44: confirmacao financeira canonica fica para webhook nas Phases 05/06.
- D-45: Phase 05 cuida de raw-body, assinatura Stripe e `WebhookEventLog`.
- D-46: Phase 06 cuida de criacao idempotente de Order via webhook aprovado.
- D-47: `purchase_completed`, email e Gelato continuam reservados para fases futuras.

<tasks>
<task id="04-05-01" type="manual">
  <name>Task 04-05-01: Aplicar decisao do gate Pix antes de implementar</name>
  <files>.planning/phases/04-stripe-payments-payment-attempt/04-05-SUMMARY.md</files>
  <read_first>
    @.planning/phases/04-stripe-payments-payment-attempt/04-01-SUMMARY.md
    @.planning/phases/04-stripe-payments-payment-attempt/04-RESEARCH.md
  </read_first>
  <action>Antes de editar runtime, ler `04-01-SUMMARY.md`. Se `PIX_NATIVE_SAFE=true`, seguir o caminho aprovado ali. Se `CUSTOM_PROVIDER_OR_LAYER_REQUIRED=true` ou `PIX_GATE_INCONCLUSIVE=true`, parar este plano e replanejar a arquitetura Pix antes de implementar. Registrar a decisao no summary de 04-05.</action>
  <verify>
    <automated>rg -n "PIX_NATIVE_SAFE=true|CUSTOM_PROVIDER_OR_LAYER_REQUIRED=true|PIX_GATE_INCONCLUSIVE=true|STOP_BEFORE_04_05=true" .planning/phases/04-stripe-payments-payment-attempt/04-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>Nenhuma implementacao Pix inicia sem resultado de gate legivel; native-first puro so segue com `PIX_NATIVE_SAFE=true`.</acceptance_criteria>
  <done>Plano Pix preserva a exigencia estrutural do usuario.</done>
</task>

<task id="04-05-02" type="auto" tdd="true">
  <name>Task 04-05-02: Criar helper Pix com persistencia parcial segura</name>
  <files>apps/backend/src/modules/payment-attempt/pix.ts, apps/backend/src/modules/payment-attempt/__tests__/pix-initiation.unit.spec.ts</files>
  <read_first>
    @.planning/phases/04-stripe-payments-payment-attempt/04-01-SUMMARY.md
    @apps/backend/src/modules/payment-attempt/eligibility.ts
    @apps/backend/src/modules/payment-attempt/service.ts
    @apps/backend/src/modules/payment-attempt/state-machine.ts
    @apps/backend/src/observability/sanitize.ts
  </read_first>
  <action>Implementar `startPixPaymentAttempt` conforme caminho aprovado pelo gate. O helper deve derivar amount/currency do cart, criar/associar PaymentCollection/PaymentSession ou camada propria aprovada, criar tentativa `pix`, persistir `provider_payment_intent_id`, `provider_payment_session_id`, `expires_at`, status local e metadata segura. Retornar QR/instrucoes imediatas ao cliente, mas persistir no maximo hash/preview mascarado se aprovado; por default nao gravar copia-e-cola completo, QR completo ou `client_secret`.</action>
  <verify>
    <automated>cd apps/backend && TMPDIR=/tmp npm run test:unit -- --runTestsByPath src/modules/payment-attempt/__tests__/pix-initiation.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>Unit tests provam `expires_at` persistido, QR/instrucoes presentes na resposta imediata, QR/copia-e-cola integral ausente de metadata/persistencia, e status `awaiting_pix_payment` sem Order.</acceptance_criteria>
  <done>Pix tem inicio assincro seguro e auditavel.</done>
</task>

<task id="04-05-03" type="auto" tdd="true">
  <name>Task 04-05-03: Expor rota Store API Pix sem Order</name>
  <files>apps/backend/src/api/store/carts/payment-attempts/pix/route.ts, apps/backend/integration-tests/http/payment-attempt-store.spec.ts</files>
  <read_first>
    @apps/backend/src/api/store/carts/payment-attempts/card/route.ts
    @apps/backend/integration-tests/http/payment-attempt-store.spec.ts
    @apps/backend/src/api/store/carts/serializers.ts
  </read_first>
  <action>Criar `POST /store/carts/:id/payment-attempts/pix`, validando posse/session/customer do cart e eligibility. Resposta deve conter `payment_attempt_id`, IDs seguros de collection/session, `payment_method_type="pix"`, `status`, `amount`, `currency_code`, `expires_at` e instrucoes imediatas aprovadas pelo gate (`qr_code`, `copy_paste`, `hosted_instructions_url` ou representacao segura equivalente). Resposta nao deve conter `order` ou `order_id`.</action>
  <verify>
    <automated>cd apps/backend && TMPDIR=/tmp npm run test:integration:http -- --runTestsByPath integration-tests/http/payment-attempt-store.spec.ts -t "pix"</automated>
  </verify>
  <acceptance_criteria>Integration HTTP inicia Pix em cart completo, retorna `expires_at`, nao retorna Order, e falha se body tentar definir amount/currency ou se resposta/persistencia vazar `client_secret`.</acceptance_criteria>
  <done>PAY-02 exposto como contrato HTTP pre-Order.</done>
</task>

<task id="04-05-04" type="auto" tdd="true">
  <name>Task 04-05-04: Modelar expiracao/falha/cancelamento Pix locais sem Order</name>
  <files>apps/backend/src/modules/payment-attempt/pix.ts, apps/backend/src/modules/payment-attempt/__tests__/pix-initiation.unit.spec.ts</files>
  <read_first>
    @apps/backend/src/modules/payment-attempt/state-machine.ts
    @docs/DB_MODEL_v1.21.md
  </read_first>
  <action>Implementar helpers locais para marcar `pix_expired`, `payment_failed` e `payment_canceled` quando a Phase 04 puder inferir UX/local timeout ou cancelamento local, sem webhook e sem Order. Qualquer verdade financeira final permanece deferida para Phase 05/06. Os helpers devem manter `order_id=null` e preservar `provider_payment_intent_id` para correlacao futura.</action>
  <verify>
    <automated>cd apps/backend && TMPDIR=/tmp npm run test:unit -- --runTestsByPath src/modules/payment-attempt/__tests__/pix-initiation.unit.spec.ts -t "expired|failed|canceled|Order"</automated>
  </verify>
  <acceptance_criteria>`awaiting_pix_payment`, `pix_expired`, `payment_failed` e `payment_canceled` nunca preenchem `order_id`, nao chamam completion e nao iniciam fulfillment.</acceptance_criteria>
  <done>PAY-03 fica protegido no escopo pre-webhook.</done>
</task>
</tasks>

<verification>
Comandos futuros para o executor deste plano:
- `cd apps/backend && TMPDIR=/tmp npm run test:unit -- --runTestsByPath src/modules/payment-attempt/__tests__/pix-initiation.unit.spec.ts`
- `cd apps/backend && TMPDIR=/tmp npm run test:integration:http -- --runTestsByPath integration-tests/http/payment-attempt-store.spec.ts -t "pix"`
- `rg -n "expires_at|awaiting_pix_payment|pix_expired|payment_failed|payment_canceled" apps/backend/src/modules/payment-attempt apps/backend/src/api/store/carts/payment-attempts/pix`
- `! rg -n "client_secret|pi_.*_secret|copy_paste.*metadata|pix_display_qr_code.*metadata|completeCartWorkflow|/store/carts/.*/complete|WebhookEventLog|CheckoutCompletionLog|purchase_completed|gelato|order\\.gelatoapis\\.com" apps/backend/src/modules/payment-attempt apps/backend/src/api/store/carts/payment-attempts`
</verification>

<success_criteria>
- PAY-02 e PAY-03 estao cobertos por unit + integration HTTP.
- Pix retorna QR/instrucoes imediatas e `expires_at` efetivo.
- QR/copia-e-cola integral nao e persistido por default; qualquer excecao depende de gate e justificativa.
- Pix pendente/expirado/falho/cancelado nunca cria Order.
- Nenhum webhook Stripe, WebhookEventLog, CheckoutCompletionLog, purchase_completed ou Gelato e introduzido.
</success_criteria>

<manual_review_gate>
Se 04-01 nao aprovar Pix native-first seguro, este plano deve parar antes de runtime code e virar replanejamento de custom provider/camada propria. Se a solucao exigir secret/config Stripe novo, registrar setup futuro e nao criar nem alterar secrets/config vars.
</manual_review_gate>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stripe Pix instructions -> client | Instrucoes podem ser mostradas imediatamente ao cliente. |
| Stripe Pix instructions -> storage/logs | Persistencia integral e proibida salvo gate aprovado. |
| Pix pending/failure -> Order | Estados locais nao podem criar Order. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-I | Information Disclosure | Pix QR/copia-e-cola | mitigate/block | Persistir apenas `expires_at`, IDs seguros e hash/preview quando aprovado. |
| T-04-05-T | Tampering | Pix amount/currency | mitigate | Eligibility server-side de 04-03. |
| T-04-05-E | Elevation of Privilege | Pix state -> Order | mitigate | State machine e provas negativas impedem Order/fulfillment. |
</threat_model>

<output>
Criar `.planning/phases/04-stripe-payments-payment-attempt/04-05-SUMMARY.md` ao concluir a execucao futura deste plano.
</output>
