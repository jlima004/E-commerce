---
phase: "06-idempotent-webhook-driven-order-creation"
plan: 02
type: implementation
wave: 2
depends_on:
  - 06-01
files_modified:
  - apps/backend/src/modules/payment-attempt/state-machine.ts
  - apps/backend/src/modules/payment-attempt/service.ts
  - apps/backend/src/modules/payment-attempt/__tests__/payment-attempt-order-eligibility.unit.spec.ts
  - apps/backend/src/workflows/order/webhook-order-entrypoint.ts
  - apps/backend/src/workflows/order/__tests__/webhook-order-entrypoint.unit.spec.ts
  - apps/backend/src/api/hooks/stripe/route.ts
  - apps/backend/integration-tests/http/stripe-webhook-order-creation.spec.ts
  - .planning/phases/06-idempotent-webhook-driven-order-creation/06-02-SUMMARY.md
autonomous: false
requirements:
  - ORD-01
  - ORD-02
requirements_addressed:
  - ORD-01
must_haves:
  truths:
    - "There is exactly one internal post-webhook Order creation entrypoint."
    - "Eligibility accepts only `payment_confirmed_by_webhook` with `order_id = null`."
    - "No storefront/checkout endpoint creates Order."
  prohibitions:
    - "Do not create real Orders in this plan."
    - "Do not persist `PaymentAttempt.order_id` yet."
    - "Do not implement `purchase_completed`, Gelato, email, analytics or refund."
    - "Do not run Stripe CLI smoke."
---

<objective>
Define the single internal post-webhook entrypoint and exact `PaymentAttempt` eligibility guard before enabling Order persistence.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-idempotent-webhook-driven-order-creation/06-CONTEXT.md
@.planning/phases/06-idempotent-webhook-driven-order-creation/06-RESEARCH.md
@.planning/phases/06-idempotent-webhook-driven-order-creation/06-VALIDATION.md
@apps/backend/src/api/hooks/stripe/route.ts
@apps/backend/src/modules/payment-attempt/service.ts
@apps/backend/src/modules/payment-attempt/state-machine.ts
</context>

## Artifacts this plan produces

- Exact eligibility helper for Order creation from `PaymentAttempt`.
- Internal workflow/service skeleton that can be called only after webhook confirmation.
- Hook integration point that can invoke the entrypoint after Phase 05 processing, still using a stub/no-op Order creator in this slice.
- Tests proving invalid states cannot create Order and no Store endpoint exists.

<tasks>
<task id="06-02-01" type="code">
  <name>Task 06-02-01: Harden PaymentAttempt Order eligibility</name>
  <files>apps/backend/src/modules/payment-attempt/service.ts, apps/backend/src/modules/payment-attempt/state-machine.ts, apps/backend/src/modules/payment-attempt/__tests__/payment-attempt-order-eligibility.unit.spec.ts</files>
  <read_first>
    @apps/backend/src/modules/payment-attempt/types.ts
    @apps/backend/src/modules/payment-attempt/service.ts
    @apps/backend/src/modules/payment-attempt/state-machine.ts
  </read_first>
  <action>Add or replace the future-order guard with `assertPaymentAttemptEligibleForOrderCreation`. It must require `status === "payment_confirmed_by_webhook"`, `order_id === null`, `provider === "stripe"`, non-empty `provider_payment_intent_id`, positive `amount`, and `currency_code === "brl"`. Test every other status as rejected. Keep pre-Order helpers that force `order_id = null`; add a separate narrow helper later for linking to an order instead of weakening all guards.</action>
  <verify>
    <automated>cd apps/backend && TMPDIR=/tmp npm run test:unit -- --runTestsByPath src/modules/payment-attempt/__tests__/payment-attempt-order-eligibility.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>Only the accepted Phase 05 handoff shape passes; existing order link or invalid status cannot pass.</acceptance_criteria>
</task>

<task id="06-02-02" type="code">
  <name>Task 06-02-02: Create internal entrypoint skeleton</name>
  <files>apps/backend/src/workflows/order/webhook-order-entrypoint.ts, apps/backend/src/workflows/order/__tests__/webhook-order-entrypoint.unit.spec.ts</files>
  <read_first>
    @.planning/phases/06-idempotent-webhook-driven-order-creation/06-RESEARCH.md
    @apps/backend/src/api/hooks/stripe/route.ts
    @apps/backend/src/modules/checkout-completion/service.ts
  </read_first>
  <action>Create an internal-only workflow/service skeleton such as `createOrderFromConfirmedPaymentAttemptWorkflow` that accepts `payment_attempt_id`, `payment_intent_id`, optional `stripe_event_id` and `correlation_id`. In this slice it should claim/validate inputs and expose a stubbed result contract, but not call Medusa Order creation yet. It must not be exported through Store routes.</action>
  <verify>
    <automated>cd apps/backend && TMPDIR=/tmp npm run test:unit -- --runTestsByPath src/workflows/order/__tests__/webhook-order-entrypoint.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>Tests prove direct invalid input/status fails before any Order creation dependency is called.</acceptance_criteria>
</task>

<task id="06-02-03" type="code">
  <name>Task 06-02-03: Wire webhook integration point without storefront completion</name>
  <files>apps/backend/src/api/hooks/stripe/route.ts, apps/backend/integration-tests/http/stripe-webhook-order-creation.spec.ts</files>
  <read_first>
    @apps/backend/src/api/hooks/stripe/route.ts
    @apps/backend/integration-tests/http/stripe-webhook-store.spec.ts
  </read_first>
  <action>Wire the post-webhook internal call only after `payment_intent.succeeded` has produced `payment_confirmed_by_webhook`. In this slice, use the skeleton/no-op Order creator so tests can prove routing and guards without creating an Order. Unsupported, failed, canceled or stale webhooks must not call the entrypoint.</action>
  <verify>
    <automated>cd apps/backend && TMPDIR=/tmp npm run test:integration:http -- --runTestsByPath integration-tests/http/stripe-webhook-order-creation.spec.ts -t "entrypoint|eligibility|invalid status"</automated>
  </verify>
  <acceptance_criteria>Only successful confirmed webhook path reaches the internal entrypoint; Store endpoints remain absent.</acceptance_criteria>
</task>

<task id="06-02-04" type="validation">
  <name>Task 06-02-04: Prove no public completion route</name>
  <files>.planning/phases/06-idempotent-webhook-driven-order-creation/06-02-SUMMARY.md</files>
  <read_first>
    @.planning/phases/06-idempotent-webhook-driven-order-creation/06-VALIDATION.md
  </read_first>
  <action>Run focused unit/HTTP tests and `git grep` proofs that prove no Store route or checkout endpoint creates Order. Record sanitized evidence in summary.</action>
  <verify>
    <automated>bash -lc 'cd apps/backend && git grep -n -E "POST.*/store/carts/.*/complete|/store/carts/\\[id\\]/complete" -- src/api/store; status=$?; test $status -eq 1'</automated>
  </verify>
  <acceptance_criteria>`06-02-SUMMARY.md` states Order creation is still not enabled; only the internal entrypoint/guards exist.</acceptance_criteria>
</task>
</tasks>

<verification>
- `cd apps/backend && TMPDIR=/tmp npm run test:unit -- --runTestsByPath src/modules/payment-attempt/__tests__/payment-attempt-order-eligibility.unit.spec.ts src/workflows/order/__tests__/webhook-order-entrypoint.unit.spec.ts`
- `cd apps/backend && TMPDIR=/tmp npm run test:integration:http -- --runTestsByPath integration-tests/http/stripe-webhook-order-creation.spec.ts -t "entrypoint|eligibility|invalid status"`
- `bash -lc 'cd apps/backend && git grep -n -E "POST.*/store/carts/.*/complete|/store/carts/\\[id\\]/complete" -- src/api/store; status=$?; test $status -eq 1'`
</verification>

<success_criteria>
- Only the canonical post-webhook path can reach the internal Order creation entrypoint.
- Invalid PaymentAttempt states cannot reach Order creation.
- No runtime Order creation or downstream effects exist yet.
</success_criteria>

<manual_review_gate>
STOP after `06-02-SUMMARY.md`. Review the entrypoint and guard before enabling Order persistence.
</manual_review_gate>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-E | Elevation of Privilege | Store/API boundary | mitigate | No public Store completion route; internal-only workflow. |
| T-06-02-T | Tampering | PaymentAttempt state | mitigate | Exact status/order_id/provider/amount/currency guard. |
| T-06-02-R | Repudiation | webhook-to-order handoff | mitigate | Correlation id and PaymentIntent id in internal contract. |
</threat_model>
