---
phase: "06-idempotent-webhook-driven-order-creation"
plan: 05
type: validation
wave: 5
depends_on:
  - 06-04
files_modified:
  - apps/backend/integration-tests/http/stripe-webhook-order-creation.spec.ts
  - apps/backend/src/modules/checkout-completion/__tests__/checkout-completion-log.unit.spec.ts
  - apps/backend/src/modules/payment-attempt/__tests__/payment-attempt-order-eligibility.unit.spec.ts
  - apps/backend/src/workflows/order/__tests__/webhook-order-creation.unit.spec.ts
  - apps/backend/src/workflows/order/__tests__/webhook-order-gelato-snapshot.unit.spec.ts
  - .planning/phases/06-idempotent-webhook-driven-order-creation/06-05-SUMMARY.md
autonomous: false
requirements:
  - ORD-01
  - ORD-02
  - ORD-03
requirements_addressed:
  - ORD-01
  - ORD-02
  - ORD-03
must_haves:
  truths:
    - "Final validation proves the whole Phase 06 invariant under unit, HTTP, build and negative `git grep` proofs."
    - "Phase 07 remains not started."
    - "Stripe CLI smoke remains manual/not executed unless separately approved."
  prohibitions:
    - "Do not start Phase 07."
    - "Do not implement `purchase_completed`."
    - "Do not implement Gelato, email, analytics or refund."
    - "Do not run real Stripe CLI smoke."
    - "Do not apply migrations automatically."
---

<objective>
Close Phase 06 execution with full validation evidence and a manual gate before any downstream phase.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-idempotent-webhook-driven-order-creation/06-VALIDATION.md
@.planning/phases/06-idempotent-webhook-driven-order-creation/06-01-SUMMARY.md
@.planning/phases/06-idempotent-webhook-driven-order-creation/06-02-SUMMARY.md
@.planning/phases/06-idempotent-webhook-driven-order-creation/06-03-SUMMARY.md
@.planning/phases/06-idempotent-webhook-driven-order-creation/06-04-SUMMARY.md
</context>

## Artifacts this plan produces

- Final Phase 06 validation matrix evidence.
- Negative `git grep` evidence proving Phase 07+ is absent.
- `06-05-SUMMARY.md` with explicit manual gate before Phase 07.

<tasks>
<task id="06-05-01" type="test">
  <name>Task 06-05-01: Run complete unit matrix</name>
  <files>apps/backend/src/modules/checkout-completion/__tests__/checkout-completion-log.unit.spec.ts, apps/backend/src/modules/payment-attempt/__tests__/payment-attempt-order-eligibility.unit.spec.ts, apps/backend/src/workflows/order/__tests__/webhook-order-creation.unit.spec.ts, apps/backend/src/workflows/order/__tests__/webhook-order-gelato-snapshot.unit.spec.ts</files>
  <read_first>
    @.planning/phases/06-idempotent-webhook-driven-order-creation/06-VALIDATION.md
  </read_first>
  <action>Ensure unit tests cover idempotency key, log states, invalid statuses, exact eligibility, replay, concurrency, transaction/correlation, decoupled statuses, snapshot success/failure and downstream absence.</action>
  <verify>
    <automated>cd apps/backend && TMPDIR=/tmp npm run test:unit -- --runTestsByPath src/modules/checkout-completion/__tests__/checkout-completion-log.unit.spec.ts src/modules/payment-attempt/__tests__/payment-attempt-order-eligibility.unit.spec.ts src/workflows/order/__tests__/webhook-order-creation.unit.spec.ts src/workflows/order/__tests__/webhook-order-gelato-snapshot.unit.spec.ts src/modules/catalog/__tests__/gelato-snapshot.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>All listed unit suites pass without watch mode and test names map to `06-VALIDATION.md`.</acceptance_criteria>
</task>

<task id="06-05-02" type="test">
  <name>Task 06-05-02: Run HTTP integration matrix</name>
  <files>apps/backend/integration-tests/http/stripe-webhook-order-creation.spec.ts</files>
  <read_first>
    @apps/backend/integration-tests/http/stripe-webhook-store.spec.ts
    @.planning/phases/06-idempotent-webhook-driven-order-creation/06-VALIDATION.md
  </read_first>
  <action>Ensure integration coverage proves signed webhook -> confirmed PaymentAttempt -> one Order, duplicate and concurrent replay no-op, invalid PaymentAttempt states no Order, correlation records, decoupled statuses and Gelato snapshots.</action>
  <verify>
    <automated>cd apps/backend && TMPDIR=/tmp npm run test:integration:http -- --runTestsByPath integration-tests/http/stripe-webhook-store.spec.ts integration-tests/http/stripe-webhook-order-creation.spec.ts</automated>
  </verify>
  <acceptance_criteria>Phase 05 webhook tests remain green and Phase 06 HTTP tests prove one Order under replay/concurrency.</acceptance_criteria>
</task>

<task id="06-05-03" type="validation">
  <name>Task 06-05-03: Run build and negative proofs</name>
  <files>.planning/phases/06-idempotent-webhook-driven-order-creation/06-05-SUMMARY.md</files>
  <read_first>
    @.planning/phases/06-idempotent-webhook-driven-order-creation/06-VALIDATION.md
  </read_first>
  <action>Run build with Admin disabled plus required negative `git grep` proofs. Record sanitized command/result evidence only; do not include secrets, raw Stripe payload, Pix QR/copia-e-cola or `client_secret`.</action>
  <verify>
    <automated>cd apps/backend && HOME=/tmp XDG_CONFIG_HOME=/tmp TMPDIR=/tmp ADMIN_DISABLED=true npm run build</automated>
    <automated>bash -lc 'cd apps/backend && git grep -n -E "POST.*/store/carts/.*/complete|/store/carts/\\[id\\]/complete" -- src/api/store; status=$?; test $status -eq 1'</automated>
    <automated>bash -lc 'cd apps/backend && git grep -n -E "purchase_completed|AnalyticsEventLog|posthog|EmailDeliveryLog|resend|order\\.gelatoapis\\.com|gelato_order_id|create.*Fulfillment|refund|Refund" -- src/workflows/order src/modules/checkout-completion src/api/hooks/stripe/route.ts integration-tests/http/stripe-webhook-order-creation.spec.ts; status=$?; test $status -eq 1'</automated>
    <automated>bash -lc 'cd apps/backend && git grep -n -E "stripe listen|stripe trigger|whsec_|sk_test_|sk_live_|client_secret|pi_[A-Za-z0-9_]+_secret_|pix_display_qr_code|copy_paste|hosted_instructions_url|Authorization|cookies" -- src/modules/checkout-completion src/workflows/order src/api/hooks/stripe integration-tests/http/stripe-webhook-order-creation.spec.ts; status=$?; test $status -eq 1'</automated>
    <automated>bash -lc 'git grep -n -E "whsec_[A-Za-z0-9]{8,}|sk_(test|live)_[A-Za-z0-9]{8,}|pi_[A-Za-z0-9]+_secret_[A-Za-z0-9]{8,}|00020126[0-9A-Z]{20,}" -- .planning/phases/06-idempotent-webhook-driven-order-creation; status=$?; test $status -eq 1'</automated>
  </verify>
  <acceptance_criteria>Build and all negative `git grep` proofs are green, with any unavoidable false positives documented and scoped outside runtime if present.</acceptance_criteria>
</task>

<task id="06-05-04" type="manual">
  <name>Task 06-05-04: Document manual gate before Phase 07</name>
  <files>.planning/phases/06-idempotent-webhook-driven-order-creation/06-05-SUMMARY.md</files>
  <read_first>
    @.planning/ROADMAP.md
    @.planning/STATE.md
  </read_first>
  <action>Write final summary stating Phase 06 stops here, Phase 07 is not started, `purchase_completed` remains absent, and Stripe CLI smoke/migration application remain manual-only unless explicitly approved.</action>
  <verify>
    <automated>git grep -n -E "manual gate|Phase 07|purchase_completed|not started|Stripe CLI" -- .planning/phases/06-idempotent-webhook-driven-order-creation/06-05-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>Summary gives a clear human review gate and no downstream execution instructions are carried out automatically.</acceptance_criteria>
</task>
</tasks>

<verification>
- `cd apps/backend && TMPDIR=/tmp npm run test:unit -- --runTestsByPath src/modules/checkout-completion/__tests__/checkout-completion-log.unit.spec.ts src/modules/payment-attempt/__tests__/payment-attempt-order-eligibility.unit.spec.ts src/workflows/order/__tests__/webhook-order-creation.unit.spec.ts src/workflows/order/__tests__/webhook-order-gelato-snapshot.unit.spec.ts src/modules/catalog/__tests__/gelato-snapshot.unit.spec.ts`
- `cd apps/backend && TMPDIR=/tmp npm run test:integration:http -- --runTestsByPath integration-tests/http/stripe-webhook-store.spec.ts integration-tests/http/stripe-webhook-order-creation.spec.ts`
- `cd apps/backend && HOME=/tmp XDG_CONFIG_HOME=/tmp TMPDIR=/tmp ADMIN_DISABLED=true npm run build`
- Negative `git grep` proofs from `06-VALIDATION.md`
</verification>

<success_criteria>
- ORD-01, ORD-02 and ORD-03 are proven by tests and `git grep` proofs.
- Phase 05 webhook behavior remains green.
- No Phase 07+ runtime effects are present.
- Execution stops at manual gate before Phase 07.
</success_criteria>

<manual_review_gate>
PARAR AQUI. A proxima acao e revisao humana de `06-05-SUMMARY.md`; Phase 07 nao deve iniciar automaticamente.
</manual_review_gate>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-R | Repudiation | acceptance evidence | mitigate | Summary records sanitized commands/results and manual gate. |
| T-06-05-E | Elevation of Privilege | phase boundary | mitigate | Negative greps block Phase 07+ code paths. |
| T-06-05-I | Information Disclosure | summaries/tests | mitigate | Secret/payload greps and sanitized evidence only. |
</threat_model>
