---
phase: "06-idempotent-webhook-driven-order-creation"
plan: 04
type: implementation
wave: 4
depends_on:
  - 06-03
files_modified:
  - apps/backend/src/workflows/order/webhook-order-entrypoint.ts
  - apps/backend/src/workflows/order/steps/build-order-line-item-gelato-snapshots.ts
  - apps/backend/src/workflows/order/__tests__/webhook-order-gelato-snapshot.unit.spec.ts
  - apps/backend/integration-tests/http/stripe-webhook-order-creation.spec.ts
  - .planning/phases/06-idempotent-webhook-driven-order-creation/06-04-SUMMARY.md
autonomous: false
requirements:
  - ORD-01
  - ORD-02
  - CAT-04
requirements_addressed:
  - ORD-01
  - ORD-02
  - CAT-04
must_haves:
  truths:
    - "Plan 06-03 already made snapshots mandatory at Order birth."
    - "This slice hardens snapshot immutability, failure, replay and edge-case coverage."
    - "Invalid snapshot input still blocks Order creation and records a sanitized completion failure."
  prohibitions:
    - "Do not call Gelato API."
    - "Do not create fulfillment."
    - "Do not persist `gelato_order_id`."
    - "Do not implement purchase_completed, email, analytics or refund."
---

<objective>
Harden immutable Gelato snapshot behavior after Order creation was enabled in 06-03, covering edge cases, failure behavior and immutability without starting Gelato fulfillment.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-idempotent-webhook-driven-order-creation/06-CONTEXT.md
@.planning/phases/06-idempotent-webhook-driven-order-creation/06-RESEARCH.md
@.planning/phases/06-idempotent-webhook-driven-order-creation/06-VALIDATION.md
@docs/contracts/gelato-snapshot-v1.md
@apps/backend/src/modules/catalog/gelato-snapshot.ts
@apps/backend/src/api/store/carts/query-config.ts
@apps/backend/src/workflows/order/webhook-order-entrypoint.ts
</context>

## Artifacts this plan produces

- Expanded snapshot unit tests for malformed variants, multi-item carts and metadata merge behavior.
- HTTP tests proving snapshot immutability after catalog edits and failed completion when snapshot input is invalid.
- Negative `git grep` proofs that this remains snapshot persistence only, not Gelato fulfillment.

<tasks>
<task id="06-04-01" type="code">
  <name>Task 06-04-01: Harden snapshot helper edge cases</name>
  <files>apps/backend/src/workflows/order/steps/build-order-line-item-gelato-snapshots.ts, apps/backend/src/workflows/order/__tests__/webhook-order-gelato-snapshot.unit.spec.ts</files>
  <read_first>
    @docs/contracts/gelato-snapshot-v1.md
    @apps/backend/src/modules/catalog/gelato-snapshot.ts
    @apps/backend/src/modules/catalog/gelato-metadata.ts
  </read_first>
  <action>Extend the snapshot helper/step from 06-03 with edge-case coverage: multi-line carts, metadata merge without clobbering safe existing fields, missing variant, missing SKU, invalid metadata, invalid captured timestamp and deterministic one-timestamp-per-order behavior. Keep the exact `GelatoSnapshot` v1 shape.</action>
  <verify>
    <automated>cd apps/backend && TMPDIR=/tmp npm run test:unit -- --runTestsByPath src/workflows/order/__tests__/webhook-order-gelato-snapshot.unit.spec.ts -t "edge|metadata|captured_at|invalid"</automated>
  </verify>
  <acceptance_criteria>Tests prove the 06-03 required snapshot path handles edge cases without weakening the no-Order-without-snapshot invariant.</acceptance_criteria>
</task>

<task id="06-04-02" type="code">
  <name>Task 06-04-02: Harden transactional failure behavior</name>
  <files>apps/backend/src/workflows/order/webhook-order-entrypoint.ts, apps/backend/src/workflows/order/steps/build-order-line-item-gelato-snapshots.ts</files>
  <read_first>
    @apps/backend/src/workflows/order/webhook-order-entrypoint.ts
    @.planning/phases/06-idempotent-webhook-driven-order-creation/06-RESEARCH.md
  </read_first>
  <action>Harden the 06-03 transaction so every known snapshot failure path marks `CheckoutCompletionLog.failed` with sanitized error and leaves no completed Order correlation. Re-run the Order creation tests against failures before and after the Medusa completion call boundary to prove no retry can create a snapshot-less Order.</action>
  <verify>
    <automated>cd apps/backend && TMPDIR=/tmp npm run test:unit -- --runTestsByPath src/workflows/order/__tests__/webhook-order-creation.unit.spec.ts src/workflows/order/__tests__/webhook-order-gelato-snapshot.unit.spec.ts -t "snapshot failure|rollback|retry|transaction"</automated>
  </verify>
  <acceptance_criteria>Failure to persist any snapshot rolls back or marks completion failed without a completed Order correlation, and retry never creates a second/snapshot-less Order.</acceptance_criteria>
</task>

<task id="06-04-03" type="test">
  <name>Task 06-04-03: Prove snapshot immutability and failure behavior</name>
  <files>apps/backend/integration-tests/http/stripe-webhook-order-creation.spec.ts, apps/backend/src/workflows/order/__tests__/webhook-order-gelato-snapshot.unit.spec.ts</files>
  <read_first>
    @docs/contracts/gelato-snapshot-v1.md
    @apps/backend/src/modules/catalog/__tests__/gelato-snapshot.unit.spec.ts
  </read_first>
  <action>Add or extend tests proving valid orders have snapshot metadata from 06-03, later ProductVariant metadata edits do not mutate existing Order LineItems, and invalid/missing Gelato metadata blocks Order creation with sanitized `CheckoutCompletionLog.failed`.</action>
  <verify>
    <automated>cd apps/backend && TMPDIR=/tmp npm run test:unit -- --runTestsByPath src/workflows/order/__tests__/webhook-order-gelato-snapshot.unit.spec.ts src/modules/catalog/__tests__/gelato-snapshot.unit.spec.ts && TMPDIR=/tmp npm run test:integration:http -- --runTestsByPath integration-tests/http/stripe-webhook-order-creation.spec.ts -t "gelato_snapshot|immutability|snapshot failure|edge"</automated>
  </verify>
  <acceptance_criteria>Tests prove exact shape, immutability after catalog edits, and no partial Order without snapshots.</acceptance_criteria>
</task>

<task id="06-04-04" type="validation">
  <name>Task 06-04-04: Prove no Gelato fulfillment starts</name>
  <files>.planning/phases/06-idempotent-webhook-driven-order-creation/06-04-SUMMARY.md</files>
  <read_first>
    @.planning/phases/06-idempotent-webhook-driven-order-creation/06-VALIDATION.md
  </read_first>
  <action>Run focused tests and negative `git grep` proofs showing this slice only hardens persisted snapshots and does not call Gelato API, create fulfillment, or persist Gelato order identifiers. Record sanitized evidence.</action>
  <verify>
    <automated>bash -lc 'cd apps/backend && git grep -n -E "order\\.gelatoapis\\.com|X-API-KEY|gelato_order_id|create.*Fulfillment|Fulfillment" -- src/workflows/order src/modules/checkout-completion integration-tests/http/stripe-webhook-order-creation.spec.ts; status=$?; test $status -eq 1'</automated>
  </verify>
  <acceptance_criteria>`06-04-SUMMARY.md` states Phase 09 remains responsible for Gelato fulfillment.</acceptance_criteria>
</task>
</tasks>

<verification>
- `cd apps/backend && TMPDIR=/tmp npm run test:unit -- --runTestsByPath src/workflows/order/__tests__/webhook-order-gelato-snapshot.unit.spec.ts src/modules/catalog/__tests__/gelato-snapshot.unit.spec.ts`
- `cd apps/backend && TMPDIR=/tmp npm run test:integration:http -- --runTestsByPath integration-tests/http/stripe-webhook-order-creation.spec.ts -t "gelato_snapshot|immutability|snapshot failure|edge"`
- `bash -lc 'cd apps/backend && git grep -n -E "order\\.gelatoapis\\.com|X-API-KEY|gelato_order_id|create.*Fulfillment|Fulfillment" -- src/workflows/order src/modules/checkout-completion integration-tests/http/stripe-webhook-order-creation.spec.ts; status=$?; test $status -eq 1'`
</verification>

<success_criteria>
- All Order LineItems have exact `metadata.gelato_snapshot`.
- Snapshot capture is immutable and independent from future variant metadata edits.
- Snapshot failure prevents partial Order completion.
- No Gelato fulfillment/API work exists.
</success_criteria>

<manual_review_gate>
STOP after `06-04-SUMMARY.md`. Review snapshot persistence before final validation.
</manual_review_gate>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-T | Tampering | product metadata after order | mitigate | Persist immutable snapshot on Order LineItem. |
| T-06-04-R | Repudiation | fulfillment input | mitigate | Snapshot carries source variant id/sku and captured timestamp. |
| T-06-04-D | Denial of Service | invalid catalog metadata | mitigate | Fail closed and record sanitized completion failure. |
</threat_model>
