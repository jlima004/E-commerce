---
phase: "06-idempotent-webhook-driven-order-creation"
plan: 01
type: implementation
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/checkout-completion/index.ts
  - apps/backend/src/modules/checkout-completion/models/checkout-completion-log.ts
  - apps/backend/src/modules/checkout-completion/service.ts
  - apps/backend/src/modules/checkout-completion/types.ts
  - apps/backend/src/modules/checkout-completion/migrations/Migration20260702000000.ts
  - apps/backend/src/modules/checkout-completion/__tests__/checkout-completion-log.unit.spec.ts
  - .planning/phases/06-idempotent-webhook-driven-order-creation/06-01-SUMMARY.md
autonomous: false
requirements:
  - ORD-02
requirements_addressed:
  - ORD-02
must_haves:
  truths:
    - "`CheckoutCompletionLog` exists as the internal idempotency/audit record for Order creation."
    - "`CheckoutCompletionLog.idempotency_key` is unique and deterministic."
    - "`WebhookEventLog` remains external-event idempotency and is not replaced by this module."
  prohibitions:
    - "Do not create Order in this plan."
    - "Do not call `completeCartWorkflow` or `createOrderWorkflow` in this plan."
    - "Do not update `PaymentAttempt.order_id` in this plan."
    - "Do not emit `purchase_completed`."
    - "Do not call Gelato, email, analytics or refund flow."
    - "Do not apply migration automatically."
---

<objective>
Create the schema, contract and pure idempotency helpers for `CheckoutCompletionLog`, without creating Orders yet.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-idempotent-webhook-driven-order-creation/06-CONTEXT.md
@.planning/phases/06-idempotent-webhook-driven-order-creation/06-RESEARCH.md
@.planning/phases/06-idempotent-webhook-driven-order-creation/06-VALIDATION.md
@docs/DB_MODEL_v1.21.md
@apps/backend/src/modules/webhooks/types.ts
@apps/backend/src/modules/payment-attempt/models/payment-attempt.ts
</context>

## Artifacts this plan produces

- `CheckoutCompletionLog` model, service, types and migration draft.
- Pure helpers for idempotency key, safe metadata and status transitions.
- Unit tests for deterministic key generation, uniqueness contract and sensitive metadata rejection.

<tasks>
<task id="06-01-01" type="code">
  <name>Task 06-01-01: Model CheckoutCompletionLog and migration draft</name>
  <files>apps/backend/src/modules/checkout-completion/models/checkout-completion-log.ts, apps/backend/src/modules/checkout-completion/migrations/Migration20260702000000.ts</files>
  <read_first>
    @docs/DB_MODEL_v1.21.md
    @apps/backend/src/modules/payment-attempt/migrations/Migration20260629000000.ts
    @apps/backend/src/modules/webhooks/migrations/Migration20260701000000.ts
  </read_first>
  <action>Create `checkout_completion_log` with fields: `id`, `operation`, `idempotency_key`, `cart_id`, `payment_intent_id`, `payment_attempt_id`, `order_id`, `status`, `error_code`, `error_message`, `metadata`, `locked_at`, `completed_at`, `failed_at`, timestamps and `deleted_at`. The migration draft must include unique `idempotency_key`, index `payment_intent_id`, index `cart_id`, index `payment_attempt_id`, index `order_id`, index `status, locked_at`, and check constraints for operation/status. Do not run migrations.</action>
  <verify>
    <automated>cd apps/backend && TMPDIR=/tmp npm run test:unit -- --runTestsByPath src/modules/checkout-completion/__tests__/checkout-completion-log.unit.spec.ts -t "model|migration|indexes"</automated>
  </verify>
  <acceptance_criteria>DDL contains a unique constraint/index for `idempotency_key`, no raw payload fields, and no downstream tables/events.</acceptance_criteria>
</task>

<task id="06-01-02" type="code">
  <name>Task 06-01-02: Define idempotency helpers and status vocabulary</name>
  <files>apps/backend/src/modules/checkout-completion/service.ts, apps/backend/src/modules/checkout-completion/types.ts, apps/backend/src/modules/checkout-completion/__tests__/checkout-completion-log.unit.spec.ts</files>
  <read_first>
    @docs/DB_MODEL_v1.21.md
    @apps/backend/src/modules/webhooks/service.ts
    @apps/backend/src/modules/payment-attempt/types.ts
  </read_first>
  <action>Define operation `complete_checkout_create_order`, statuses `processing | completed | failed`, and `buildCheckoutCompletionIdempotencyKey`. Default key is `payment_intent_id`; if a composite key is chosen, encode it as `cart_id:payment_intent_id` and test it. Add allowlist-only metadata with `stripe_event_id`, `payment_method_type`, `correlation_id`; reject secrets, raw payload, QR/copia-e-cola, `client_secret`, Authorization/cookies and full PII.</action>
  <verify>
    <automated>cd apps/backend && TMPDIR=/tmp npm run test:unit -- --runTestsByPath src/modules/checkout-completion/__tests__/checkout-completion-log.unit.spec.ts -t "idempotency|metadata|status"</automated>
  </verify>
  <acceptance_criteria>Tests prove deterministic key derivation, missing PaymentIntent rejection and sensitive metadata rejection.</acceptance_criteria>
</task>

<task id="06-01-03" type="test">
  <name>Task 06-01-03: Prove no Order side effects in schema slice</name>
  <files>apps/backend/src/modules/checkout-completion/__tests__/checkout-completion-log.unit.spec.ts, .planning/phases/06-idempotent-webhook-driven-order-creation/06-01-SUMMARY.md</files>
  <read_first>
    @.planning/phases/06-idempotent-webhook-driven-order-creation/06-VALIDATION.md
  </read_first>
  <action>Run focused unit tests and negative `git grep` proofs for Order creation/downstream strings in the new module. Record sanitized evidence in `06-01-SUMMARY.md`.</action>
  <verify>
    <automated>bash -lc 'cd apps/backend && git grep -n -E "completeCartWorkflow|createOrderWorkflow|purchase_completed|AnalyticsEventLog|EmailDeliveryLog|order\\.gelatoapis\\.com|refund" -- src/modules/checkout-completion; status=$?; test $status -eq 1'</automated>
  </verify>
  <acceptance_criteria>Summary states this slice created only schema/contract and stopped before Order creation.</acceptance_criteria>
</task>
</tasks>

<verification>
- `cd apps/backend && TMPDIR=/tmp npm run test:unit -- --runTestsByPath src/modules/checkout-completion/__tests__/checkout-completion-log.unit.spec.ts`
- `bash -lc 'cd apps/backend && git grep -n -E "completeCartWorkflow|createOrderWorkflow|purchase_completed|AnalyticsEventLog|EmailDeliveryLog|order\\.gelatoapis\\.com|refund" -- src/modules/checkout-completion; status=$?; test $status -eq 1'`
</verification>

<success_criteria>
- `CheckoutCompletionLog` schema and helper contract exist.
- Idempotency key is deterministic and unique at DB level.
- No Order creation, PaymentAttempt linking or downstream effects are implemented.
</success_criteria>

<manual_review_gate>
STOP after `06-01-SUMMARY.md`. Review schema/idempotency contract before creating any Order entrypoint.
</manual_review_gate>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-R | Repudiation | CheckoutCompletionLog | mitigate | Auditable operation/status/error fields and idempotency key. |
| T-06-01-D | Denial of Service | replay/concurrency | mitigate | Unique `idempotency_key` at DB level. |
| T-06-01-I | Information Disclosure | metadata | mitigate | Allowlist-only metadata and sensitive-field tests. |
</threat_model>
