---
phase: 17-authenticated-br-checkout-privacy
plan: 02
type: execute
wave: 1
depends_on: ["17-01-PLAN.md"]
files_modified:
  - apps/backend/src/modules/checkout-privacy/models/protected-checkout-data.ts
  - apps/backend/src/modules/checkout-privacy/models/legal-receipt.ts
  - apps/backend/src/modules/checkout-privacy/models/protected-order-snapshot.ts
  - apps/backend/src/modules/checkout-privacy/models/sanitized-purge-ledger.ts
  - apps/backend/src/modules/checkout-privacy/migrations/Migration20260910_checkout_privacy.ts
  - apps/backend/src/modules/store-idempotency/migrations/Migration20260910120000_store_idempotency_keyrings.ts
  - apps/backend/integration-tests/modules/checkout-privacy.postgres.spec.ts
  - apps/backend/scripts/run-disposable-postgres-modules.mjs
autonomous: false
requirements: [CHK-06, CHK-09]
orchestrator_selected_effort: "low | medium | high"
recommended_default_effort: "high"
estimate:
  tokens: 52000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "Quatro entidades materializam o domínio Checkout Privacy: protected_checkout_data, legal_receipt, protected_order_snapshot, sanitized_purge_ledger."
    - "A separação ortogonal de estados em protected_checkout_data é estrita: lifecycle_state ('draft' | 'ready' | 'snapshot_prepared' | 'purged') e purge_state ('active' | 'freeze_suspended' | 'due_now' | 'purged' | 'manual_intervention_required'), validados pela constraint CK_protected_checkout_data_purge_coherence: ((lifecycle_state = 'purged' AND purge_state = 'purged') OR (lifecycle_state != 'purged' AND purge_state != 'purged'))."
    - "A suspensão financeira durável é rastreada na coluna deferred_financial_authority boolean NOT NULL DEFAULT false, coordenada com purge_state via constraint CK_protected_checkout_data_deferred_fin_auth: freeze_suspended exige true; active, due_now e purged exigem false; manual_intervention_required permite true ou false dependendo de pendência financeira."
    - "A unicidade do carrinho ativo é garantida por índice único parcial: UQ_protected_checkout_data_active_cart WHERE lifecycle_state != 'purged' AND deleted_at IS NULL."
    - "A atomicidade do envelope e o suporte a rascunho parcial sem CPF são assegurados por constraint CHECK CK_protected_checkout_data_envelope_atomicidade com 3 ramos estritos: em 'draft', todas as 4 colunas de envelope (encrypted_cpf, cpf_nonce, cpf_tag, cpf_wrapped_dek) DEVEM ser NULL (draft sem CPF) OU todas as 4 DEVEM ser NOT NULL (draft com CPF); em 'ready' e 'snapshot_prepared', todas as 4 colunas DEVEM ser NOT NULL; em 'purged', todas as 4 colunas DEVEM ser NULL."
    - "O modelo ProtectedOrderSnapshot define os 23 campos lógicos mandatórios (com logical snapshot_id mapeado para a coluna física 'id' com prefixo 'posnap_...'), materializando exatamente 26 colunas físicas no PostgreSQL ao incluir os metadados de framework do Medusa v2 (created_at, updated_at, deleted_at); consultas e ORM operam sobre a chave primária física 'id'."
    - "A cardinalidade exata de um snapshot por CCL e PaymentAttempt é assegurada por UQ_protected_order_snapshot_ccl_id, UQ_protected_order_snapshot_pa_id e índice único parcial UQ_protected_order_snapshot_order_id WHERE order_id IS NOT NULL; violações relacionais de unicidade disparam SQLSTATE 23505, estritamente diferenciado de 55000 (imutabilidade/lifecycle); inserção direta com lifecycle_state = 'bound' é suportada para recuperação Option B-R."
    - "Os recibos legais são estritamente imutáveis/append-only via trigger PostgreSQL trg_legal_receipt_immutable e constraint CK_legal_receipt_deleted_at_null: DELETE físico é proibido incondicionalmente com SQLSTATE 55000; mutação de deleted_at (soft delete) é proibida incondicionalmente com SQLSTATE 55000; UPDATE em atos que não sejam consent é proibido com 55000; para consent, permite-se apenas transitar revoked_at de NULL para não-NULL uma vez, exigindo que todos os outros campos históricos (incluindo expressamente superseded_by_receipt_id) satisfaçam IS NOT DISTINCT FROM OLD; violações lançam SQLSTATE 55000."
    - "A migração store-idempotency Migration20260910120000_store_idempotency_keyrings.ts relaxa pepper_version para CHECK (pepper_version >= 1) e adiciona fingerprint_scheme (default 'rfc8785-hmac-sha256-v1') e fingerprint_key_version (integer >= 1) para suportar rotação de chaves."
    - "A migração checkout-privacy contém rollback guard no down() lançando exceção 55000 se existir QUALQUER snapshot com lifecycle_state != 'purged' (incluindo expressamente 'reconciliation_required'), se existirem recibos legais, OU se protected_checkout_data possuir registros ativos (lifecycle_state != 'purged' AND deleted_at IS NULL)."
    - "Existe um Human DDL Checkpoint explícito e bloqueante (P17-02-DDL-HR-01) antes da aplicação irreversível da migração em banco real."
  artifacts:
    - path: apps/backend/src/modules/checkout-privacy/models/protected-checkout-data.ts
      provides: "Definição do modelo Medusa v2 para dados atuais de checkout protegido com lifecycle_state, purge_state, deferred_financial_authority, purged_at e purge_reason"
      exports: [ProtectedCheckoutData]
    - path: apps/backend/src/modules/checkout-privacy/models/legal-receipt.ts
      provides: "Definição do modelo Medusa v2 para recibos legais imutáveis com trigger de append-only e validação IS NOT DISTINCT FROM"
      exports: [LegalReceipt]
    - path: apps/backend/src/modules/checkout-privacy/models/protected-order-snapshot.ts
      provides: "Definição do modelo Medusa v2 para snapshot pré-Order com 23 campos lógicos mandatórios mapeados a 26 colunas físicas no PostgreSQL (incluindo metadados de framework Medusa) e DEK própria"
      exports: [ProtectedOrderSnapshot]
    - path: apps/backend/src/modules/checkout-privacy/models/sanitized-purge-ledger.ts
      provides: "Definição do modelo Medusa v2 para trilha de auditoria saneada de expurgo sem dados pessoais"
      exports: [SanitizedPurgeLedger]
    - path: apps/backend/src/modules/checkout-privacy/migrations/Migration20260910_checkout_privacy.ts
      provides: "Migração DDL PostgreSQL com constraints, trigger de imutabilidade, índices e down() guard"
      exports: [Migration20260910_checkout_privacy]
    - path: apps/backend/integration-tests/modules/checkout-privacy.postgres.spec.ts
      provides: "Testes PostgreSQL descartáveis provando constraints, imutabilidade de recibos, integridade e rollback"
    - path: apps/backend/scripts/run-disposable-postgres-modules.mjs
      provides: "Manifesto exato do runner descartável de módulos atualizado de 17 para 18 specs"
  key_links:
    - from: apps/backend/src/modules/checkout-privacy/models/protected-checkout-data.ts
      to: apps/backend/src/modules/checkout-privacy/models/protected-order-snapshot.ts
      via: "Campos de rastreabilidade de autoridade de origem (source_protected_data_id, source_data_revision)"
      pattern: "source_protected_data_id|source_data_revision"
    - from: apps/backend/src/modules/checkout-privacy/models/legal-receipt.ts
      to: apps/backend/src/modules/checkout-privacy/models/protected-order-snapshot.ts
      via: "Vinculação imutável de IDs e digests de recibos no snapshot do pedido"
      pattern: "receipt_ids|receipt_bindings"
---

<objective>
Materializar a camada de persistência e integridade relacional do domínio `Checkout Privacy` em PostgreSQL, definindo as 4 entidades centrais (`protected_checkout_data`, `legal_receipt`, `protected_order_snapshot`, `sanitized_purge_ledger`), índices parciais de unicidade, constraints de checagem do envelope atômico, trigger PostgreSQL de imutabilidade append-only para recibos, a migração DDL com proteção contra rollback destrutivo (`down()` guard cobrindo snapshots, recibos e carrinhos ativos não-expurgados), testes em PostgreSQL descartável e o Human DDL Checkpoint `P17-02-DDL-HR-01`.

Purpose: Atender CHK-06 e CHK-09, assegurando que o banco de dados PostgreSQL seja a autoridade irrevogável de integridade, unicidade e imutabilidade das evidências legais e dados sensíveis.
Output: 4 modelos Medusa v2, script DDL de migração com trigger append-only e down() guard abrangente, suite PostgreSQL de validação relacional e checkpoint humano DDL.
</objective>

<execution_context>
Harness: Antigravity IDE
Orchestrator: Gemini 3.8 Flash
Subagents: Gemini 3.8 Flash — REQUIRED FOR EXECUTION
Effort: orchestrator-selected per work unit (low | medium | high)
Parallelization: false (serial execution)
Max active implementation subagents: 1
Auto-chain: false
Auto-advance: false
Execution Authorization: NOT AUTHORIZED. Approval of this plan specification confers HUMAN APPROVED — PASS on the PLAN artifact set only and DOES NOT authorize execution, Wave 0, Plan 17-01, or runtime code implementation. Execution requires a separate, explicit human execution authorization.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-authenticated-br-checkout-privacy/17-CONTEXT.md
@.planning/phases/17-authenticated-br-checkout-privacy/17-DECISION-ADJUDICATION.md
@.planning/phases/17-authenticated-br-checkout-privacy/17-PATTERNS.md
@.planning/phases/17-authenticated-br-checkout-privacy/17-VALIDATION.md
@apps/backend/src/modules/cart-merge/models/customer-cart-authority.ts
@apps/backend/src/modules/store-resource-version/migrations/Migration20260809201808.ts
@apps/backend/integration-tests/helpers/checkout-privacy-postgres.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 17-02-01: Definição dos 4 Modelos Medusa v2 do domínio Checkout Privacy</name>
  <files>apps/backend/src/modules/checkout-privacy/models/protected-checkout-data.ts, apps/backend/src/modules/checkout-privacy/models/legal-receipt.ts, apps/backend/src/modules/checkout-privacy/models/protected-order-snapshot.ts, apps/backend/src/modules/checkout-privacy/models/sanitized-purge-ledger.ts</files>
  <read_first>
    - apps/backend/src/modules/cart-merge/models/customer-cart-authority.ts (para sintaxe model.define do Medusa v2)
    - apps/backend/src/modules/store-idempotency/models/store-idempotency-record.ts (para tipos e índices)
    - 17-DECISION-ADJUDICATION.md seções R17-HR-02, R17-HR-04, R17-HR-05 e R17-HR-09
    - 17-PATTERNS.md seção 3.2
  </read_first>
  <behavior>
    - "ProtectedCheckoutData modela o carrinho atual ativo com endereço estruturado BR (first_name, last_name, phone, postal_code, street, number, neighborhood, city, province, country_code='BR', complement), envelope de encriptação (encrypted_cpf, nonce, tag, wrapped_dek, key_version, aad_version), clocks de expurgo e controle durável de suspensão financeira (deferred_financial_authority boolean NOT NULL DEFAULT false)."
    - "A constraint de envelope atômico CK_protected_checkout_data_envelope_atomicidade suporta rascunho sem CPF: em 'draft', todas as 4 colunas de envelope (encrypted_cpf, cpf_nonce, cpf_tag, cpf_wrapped_dek) DEVEM ser NULL (draft inicial sem CPF) OU todas as 4 DEVEM ser NOT NULL (draft com CPF); em 'ready' e 'snapshot_prepared', todas as 4 colunas DEVEM ser NOT NULL; em 'purged', todas as 4 colunas DEVEM ser NULL."
    - "A constraint CK_protected_checkout_data_deferred_fin_auth assegura coerência estrita entre purge_state e deferred_financial_authority: freeze_suspended exige true; active, due_now e purged exigem false; manual_intervention_required permite true ou false."
    - "LegalReceipt modela recibos imutáveis com purpose, legal_act_type ('contract_acceptance', 'policy_acknowledgement', 'privacy_notice_acknowledgement', 'consent'), document_version, document_digest SHA-256 de 64 caracteres, accepted_at e correlation_id, sem user agent ou IP."
    - "ProtectedOrderSnapshot define os 23 campos lógicos mandatórios (com logical snapshot_id mapeado para a coluna física 'id' com prefixo 'posnap_...'), materializando exatamente 26 colunas físicas no PostgreSQL ao incluir os metadados de framework Medusa (created_at, updated_at, deleted_at), com envelope de CPF recriptografado com DEK própria, payment_attempt_id único, ccl_id único, order_id vinculável uma única vez e suporte a inserção direta no estado 'bound' para recuperação Option B-R."
    - "SanitizedPurgeLedger modela a trilha de expurgo com target_type, target_id, action, reason, state, timestamps e safe_error_class, sem dados PII ou ciphertext."
  </behavior>
  <action>
    Criar os 4 modelos Medusa v2 em `apps/backend/src/modules/checkout-privacy/models/` usando `model.define`:
    1. `protected-checkout-data.ts`: prefixo id `pcdata_`, colunas `cart_id`, `customer_id`, `data_revision` (number), `lifecycle_state` (`draft`, `ready`, `snapshot_prepared`, `purged`), `purge_state` (`active`, `freeze_suspended`, `due_now`, `purged`, `manual_intervention_required`), `deferred_financial_authority` (boolean, default false), campos de endereço, envelope de CPF (todas as 4 colunas nullable no tipo para acomodar rascunho parcial sem CPF e estado purged, com constraint relacional assegurando atomicidade) e clocks (`pii_last_meaningful_activity_at`, `cpf_purge_due_at`, `purged_at`, `purge_reason`).
    2. `legal-receipt.ts`: prefixo id `lgrcp_`, colunas `customer_id`, `cart_id`, `purpose`, `legal_act_type`, `document_version`, `document_digest`, `policy_version`, `accepted_at`, `correlation_id`, `superseded_by_receipt_id`, `revoked_at`.
    3. `protected-order-snapshot.ts`: prefixo id `posnap_`, com os 23 campos lógicos mandatados (coluna física PK `id` mapeada como logical `snapshot_id`, gerando 26 colunas físicas com metadados de framework Medusa): `id` (PK, `posnap_...`), `schema_version` (integer default 1), `envelope_version` (text default 'v1'), `aad_version` (text default 'v1'), `key_version` (text), `lifecycle_state` (`prepared`, `bound`, `purged`, `reconciliation_required`), `encrypted_cpf` (text nullable), `cpf_nonce` (text nullable), `cpf_tag` (text nullable), `cpf_wrapped_dek` (text nullable), `source_protected_data_id` (text), `source_data_revision` (integer), `customer_id` (text), `cart_id` (text), `payment_attempt_id` (text), `checkout_completion_log_id` (text), `order_id` (text nullable), `receipt_ids` (jsonb), `receipt_bindings` (jsonb), `retention_policy_version` (text default 'v1'), `prepared_at` (timestamptz), `bound_at` (timestamptz nullable), `purged_at` (timestamptz nullable), mais `created_at`, `updated_at`, `deleted_at`. Suporta inserção direta com `lifecycle_state = 'bound'` para Option B-R.
    4. `sanitized-purge-ledger.ts`: prefixo id `prgled_`, colunas `target_type`, `target_id`, `action`, `reason`, `state`, `attempts`, `due_at`, `claimed_at`, `completed_at`, `deferred_at`, `safe_error_class`.
  </action>
  <verify>
    <automated>cd apps/backend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Os 4 arquivos de modelo existem e compilam perfeitamente sem erros de tipagem no Medusa v2.
    - Zero campos de CPF cru ou PII desprotegida nos modelos.
    - `ProtectedCheckoutData` inclui `deferred_financial_authority` booleano persistível e colunas de envelope nullable.
    - `LegalReceipt` não possui colunas de user agent, IP ou endereço.
    - `ProtectedOrderSnapshot` define os 23 campos lógicos mandatórios (com id mapeado como PK 'posnap_...'), materializando exatamente 26 colunas físicas no PostgreSQL ao incluir as 3 colunas de framework Medusa (created_at, updated_at, deleted_at).
  </acceptance_criteria>
  <done>Definições dos modelos Medusa v2 completas e integradas ao TypeScript do projeto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 17-02-02: Migração DDL PostgreSQL, Trigger trg_legal_receipt_immutable, Constraints e down() Rollback Guard</name>
  <files>apps/backend/src/modules/checkout-privacy/migrations/Migration20260910_checkout_privacy.ts, apps/backend/integration-tests/modules/checkout-privacy.postgres.spec.ts, apps/backend/scripts/run-disposable-postgres-modules.mjs</files>
  <read_first>
    - apps/backend/src/modules/store-resource-version/migrations/Migration20260809201808.ts (para estrutura de migração MikroORM / Medusa v2)
    - apps/backend/scripts/run-disposable-postgres-modules.mjs (para atualizar EXPECTED_MODULE_SPECS de 17 para 18)
    - 17-PATTERNS.md seção 3.2 (para os comandos SQL exatos de constraints, trigger de imutabilidade com SQLSTATE 55000 e índices parciais)
    - 17-VALIDATION.md (para a suíte de testes de constraints em PostgreSQL)
  </read_first>
  <behavior>
    - "Cria as 4 tabelas no PostgreSQL com tipos exatos e constraints."
    - "Aplica índices únicos: UQ_protected_order_snapshot_ccl_id, UQ_protected_order_snapshot_pa_id e índices únicos parciais: UQ_protected_checkout_data_active_cart, UQ_legal_receipt_cart_purpose_version, UQ_protected_order_snapshot_order_id (WHERE order_id IS NOT NULL)."
    - "Aplica CHECK constraints: country_code = 'BR', data_revision >= 1, revocabilidade apenas para consent, CK_protected_checkout_data_purge_coherence e CK_protected_checkout_data_deferred_fin_auth."
    - "Aplica constraint de envelope atômico CK_protected_checkout_data_envelope_atomicidade com suporte a rascunho sem CPF: ((lifecycle_state = 'draft' AND ((encrypted_cpf IS NULL AND cpf_nonce IS NULL AND cpf_tag IS NULL AND cpf_wrapped_dek IS NULL) OR (encrypted_cpf IS NOT NULL AND cpf_nonce IS NOT NULL AND cpf_tag IS NOT NULL AND cpf_wrapped_dek IS NOT NULL))) OR (lifecycle_state IN ('ready', 'snapshot_prepared') AND encrypted_cpf IS NOT NULL AND cpf_nonce IS NOT NULL AND cpf_tag IS NOT NULL AND cpf_wrapped_dek IS NOT NULL) OR (lifecycle_state = 'purged' AND encrypted_cpf IS NULL AND cpf_nonce IS NULL AND cpf_tag IS NULL AND cpf_wrapped_dek IS NULL))."
    - "Aplica constraint CK_protected_checkout_data_deferred_fin_auth: ((purge_state = 'freeze_suspended' AND deferred_financial_authority = true) OR (purge_state = 'active' AND deferred_financial_authority = false) OR (purge_state = 'due_now' AND deferred_financial_authority = false) OR (purge_state = 'purged' AND deferred_financial_authority = false) OR (purge_state = 'manual_intervention_required'))."
    - "Aplica trigger PostgreSQL trg_legal_receipt_immutable e constraint CK_legal_receipt_deleted_at_null na tabela legal_receipt: bloqueia DELETE físico incondicionalmente com SQLSTATE 55000; bloqueia mutação de deleted_at (soft delete) incondicionalmente com SQLSTATE 55000; bloqueia UPDATE para legal_act_type != 'consent' com SQLSTATE 55000; para legal_act_type = 'consent', permite estritamente transitar revoked_at de NULL para NOT NULL uma única vez, exigindo que TODOS os demais campos históricos satisfaçam NEW.col IS NOT DISTINCT FROM OLD.col (incluindo expressamente superseded_by_receipt_id); violações lançam SQLSTATE 55000, reservando 23505 exclusivamente para unicidade relacional."
    - "Aplica migração store-idempotency Migration20260910120000_store_idempotency_keyrings.ts para suportar rotação de chaves sem lock em pepper_version=1."
    - "O método down() verifica se existe QUALQUER registro em protected_order_snapshot com lifecycle_state != 'purged' (incluindo 'reconciliation_required'), se existirem recibos legais, OU se protected_checkout_data tem registros ativos (lifecycle_state != 'purged' AND deleted_at IS NULL); em caso positivo, aborta com SQLSTATE 55000."
    - "Atualiza apps/backend/scripts/run-disposable-postgres-modules.mjs adicionando checkout-privacy.postgres.spec.ts a EXPECTED_MODULE_SPECS (expandindo de 17 para 18)."
    - "A suíte PostgreSQL descartável prova que tentativas de violar as constraints ou o trigger falham com erro PostgreSQL correspondente (distinguindo 23505 de 55000 e checagens de integridade)."
  </behavior>
  <action>
    1. Criar o arquivo de migração `apps/backend/src/modules/checkout-privacy/migrations/Migration20260910_checkout_privacy.ts`:
       - Em `up()`:
         - `CREATE TABLE "protected_checkout_data" (...)` com coluna `"deferred_financial_authority" boolean NOT NULL DEFAULT false`, constraint `CK_protected_checkout_data_envelope_atomicidade`, `CK_protected_checkout_data_purge_coherence` e `CK_protected_checkout_data_deferred_fin_auth`:
           ```sql
           CONSTRAINT "CK_protected_checkout_data_envelope_atomicidade" CHECK (
             (
               lifecycle_state = 'draft' AND (
                 (encrypted_cpf IS NULL AND cpf_nonce IS NULL AND cpf_tag IS NULL AND cpf_wrapped_dek IS NULL)
                 OR
                 (encrypted_cpf IS NOT NULL AND cpf_nonce IS NOT NULL AND cpf_tag IS NOT NULL AND cpf_wrapped_dek IS NOT NULL)
               )
             )
             OR
             (
               lifecycle_state IN ('ready', 'snapshot_prepared') AND
               encrypted_cpf IS NOT NULL AND cpf_nonce IS NOT NULL AND cpf_tag IS NOT NULL AND cpf_wrapped_dek IS NOT NULL
             )
             OR
             (
               lifecycle_state = 'purged' AND
               encrypted_cpf IS NULL AND cpf_nonce IS NULL AND cpf_tag IS NULL AND cpf_wrapped_dek IS NULL
             )
           )
           ```
           ```sql
           CONSTRAINT "CK_protected_checkout_data_deferred_fin_auth" CHECK (
             (purge_state = 'freeze_suspended' AND deferred_financial_authority = true) OR
             (purge_state = 'active' AND deferred_financial_authority = false) OR
             (purge_state = 'due_now' AND deferred_financial_authority = false) OR
             (purge_state = 'purged' AND deferred_financial_authority = false) OR
             (purge_state = 'manual_intervention_required')
           )
           ```
         - `CREATE TABLE "legal_receipt" (...)` com constraint `CONSTRAINT "CK_legal_receipt_deleted_at_null" CHECK ("deleted_at" IS NULL)`
         - `CREATE TABLE "protected_order_snapshot" (...)` com chave primária física `"id"` (`posnap_...`) e as 26 colunas físicas (23 campos lógicos + `created_at`, `updated_at`, `deleted_at`)
         - `CREATE TABLE "sanitized_purge_ledger" (...)`
         - Criar função e trigger de imutabilidade com SQLSTATE 55000, proteção a soft delete e `IS NOT DISTINCT FROM`:
           ```sql
           CREATE OR REPLACE FUNCTION fn_legal_receipt_immutable()
           RETURNS TRIGGER AS $$
           BEGIN
             IF TG_OP = 'DELETE' THEN
               RAISE EXCEPTION 'LEGAL_RECEIPT_DELETE_PROHIBITED' USING ERRCODE = '55000';
             ELSIF TG_OP = 'UPDATE' THEN
               IF NEW.deleted_at IS DISTINCT FROM OLD.deleted_at OR NEW.deleted_at IS NOT NULL THEN
                 RAISE EXCEPTION 'LEGAL_RECEIPT_SOFT_DELETE_PROHIBITED' USING ERRCODE = '55000';
               END IF;
               IF OLD.legal_act_type != 'consent' THEN
                 RAISE EXCEPTION 'LEGAL_RECEIPT_NON_CONSENT_UPDATE_PROHIBITED' USING ERRCODE = '55000';
               END IF;
               IF OLD.revoked_at IS NOT NULL THEN
                 RAISE EXCEPTION 'LEGAL_RECEIPT_CONSENT_ALREADY_REVOKED' USING ERRCODE = '55000';
               END IF;
               IF NEW.revoked_at IS NULL THEN
                 RAISE EXCEPTION 'LEGAL_RECEIPT_REVOKED_AT_CANNOT_BE_NULL' USING ERRCODE = '55000';
               END IF;
               IF NEW.id IS DISTINCT FROM OLD.id OR NEW.cart_id IS DISTINCT FROM OLD.cart_id OR
                  NEW.customer_id IS DISTINCT FROM OLD.customer_id OR NEW.purpose IS DISTINCT FROM OLD.purpose OR
                  NEW.legal_act_type IS DISTINCT FROM OLD.legal_act_type OR
                  NEW.document_version IS DISTINCT FROM OLD.document_version OR
                  NEW.document_digest IS DISTINCT FROM OLD.document_digest OR
                  NEW.policy_version IS DISTINCT FROM OLD.policy_version OR
                  NEW.accepted_at IS DISTINCT FROM OLD.accepted_at OR
                  NEW.correlation_id IS DISTINCT FROM OLD.correlation_id OR
                  NEW.superseded_by_receipt_id IS DISTINCT FROM OLD.superseded_by_receipt_id OR
                  NEW.created_at IS DISTINCT FROM OLD.created_at THEN
                 RAISE EXCEPTION 'LEGAL_RECEIPT_HISTORICAL_DATA_MUTATION_PROHIBITED' USING ERRCODE = '55000';
               END IF;
               RETURN NEW;
             END IF;
             RETURN NEW;
           END;
           $$ LANGUAGE plpgsql;

           CREATE TRIGGER trg_legal_receipt_immutable
           BEFORE UPDATE OR DELETE ON "legal_receipt"
           FOR EACH ROW EXECUTE FUNCTION fn_legal_receipt_immutable();
           ```
         - Adicionar índices:
           `CREATE UNIQUE INDEX "UQ_protected_order_snapshot_ccl_id" ON "protected_order_snapshot" ("checkout_completion_log_id") WHERE "deleted_at" IS NULL;`
           `CREATE UNIQUE INDEX "UQ_protected_order_snapshot_pa_id" ON "protected_order_snapshot" ("payment_attempt_id") WHERE "deleted_at" IS NULL;`
           `CREATE UNIQUE INDEX "UQ_protected_order_snapshot_order_id" ON "protected_order_snapshot" ("order_id") WHERE "order_id" IS NOT NULL AND "deleted_at" IS NULL;`
           `CREATE UNIQUE INDEX "UQ_protected_checkout_data_active_cart" ON "protected_checkout_data" ("cart_id") WHERE "lifecycle_state" != 'purged' AND "deleted_at" IS NULL;`
       - Em `down()`:
         - Adicionar bloco PL/pgSQL checando se `protected_order_snapshot` tem registros com `lifecycle_state != 'purged'` (incluindo 'reconciliation_required'), se `legal_receipt` tem registros, OU se `protected_checkout_data` tem registros ativos (`lifecycle_state != 'purged' AND deleted_at IS NULL`), abortando com `RAISE EXCEPTION 'CHECKOUT_PRIVACY_DATA_IN_USE' USING ERRCODE = '55000'`.
         - Dropar triggers, functions e tabelas caso autorizado.
    2. Criar a migração `apps/backend/src/modules/store-idempotency/migrations/Migration20260910120000_store_idempotency_keyrings.ts`:
       - Preserva colunas existentes: `idempotency_key_hash` (Locator HMAC), `hash_version` (Locator hash scheme 'hmac-sha256-v1'), `pepper_version` (Locator key version), `request_fingerprint` (Sensitive fingerprint HMAC).
       - Em `up()`:
         - Relaxar constraint de `pepper_version` de `= 1` para `CHECK ("pepper_version" >= 1)`.
         - Adicionar coluna `"fingerprint_scheme" text not null default 'rfc8785-hmac-sha256-v1'`.
         - Adicionar coluna `"fingerprint_key_version" integer not null default 1` com constraint `CHECK ("fingerprint_key_version" >= 1)`.
       - Em `down()`:
         - Reverter novas colunas e restabelecer `CHECK ("pepper_version" = 1)`.
    3. Atualizar `apps/backend/scripts/run-disposable-postgres-modules.mjs` adicionando `checkout-privacy.postgres.spec.ts` a `EXPECTED_MODULE_SPECS` (expandindo de 17 para 18).
    4. Criar a suite de testes `apps/backend/integration-tests/modules/checkout-privacy.postgres.spec.ts` usando o helper descartável:
       - Testar inserção válida e constraints de formato (country_code = 'BR', revision >= 1).
       - Testar integridade de `CK_protected_checkout_data_deferred_fin_auth`:
         - `active + false` = válido
         - `freeze_suspended + true` = válido
         - `freeze_suspended + false` = inválido (violação CHECK)
         - `due_now + true` = inválido (violação CHECK)
         - `due_now + false` = válido
         - `purged + true` = inválido (violação CHECK)
         - `purged + false` = válido
         - `manual_intervention_required + true` = válido (autoridade pendente)
         - `manual_intervention_required + false` = válido
         - liberação de congelamento transiciona para `false + due_now` com sucesso.
       - Testar atomicidade do envelope e rascunho sem CPF (21 casos):
         - `draft + all NULL = PASS` (rascunho parcial sem CPF)
         - `draft + all NOT NULL = PASS` (rascunho com CPF)
         - `draft + partial envelope (1 a 3 preenchidos) = FAIL (SQLSTATE 23514)`
         - `ready + all NULL = FAIL (SQLSTATE 23514)`
         - `ready + all NOT NULL = PASS`
         - `ready + partial envelope = FAIL (SQLSTATE 23514)`
         - `snapshot_prepared + all NULL = FAIL (SQLSTATE 23514)`
         - `snapshot_prepared + all NOT NULL = PASS`
         - `purged + all NULL = PASS`
         - `purged + any envelope field NOT NULL = FAIL (SQLSTATE 23514)`
       - Testar imutabilidade e soft delete de `legal_receipt` (11 casos):
         - DELETE físico falha com SQLSTATE 55000 (`LEGAL_RECEIPT_DELETE_PROHIBITED`).
         - Soft delete (`UPDATE SET deleted_at = NOW()`) falha com SQLSTATE 55000 (`LEGAL_RECEIPT_SOFT_DELETE_PROHIBITED`).
         - `revoked_at` + `deleted_at` simultâneos falha com SQLSTATE 55000 (`LEGAL_RECEIPT_SOFT_DELETE_PROHIBITED`).
         - UPDATE em `contract_acceptance` falha com SQLSTATE 55000 (`LEGAL_RECEIPT_NON_CONSENT_UPDATE_PROHIBITED`).
         - UPDATE em `consent` transicionando `revoked_at` de NULL para data válida tem sucesso.
         - Segundo UPDATE em `consent` revogado falha com SQLSTATE 55000 (`LEGAL_RECEIPT_CONSENT_ALREADY_REVOKED`).
         - UPDATE alterando `document_digest` em `consent` falha com SQLSTATE 55000.
         - UPDATE alterando `superseded_by_receipt_id` falha com SQLSTATE 55000.
         - Inserção com `deleted_at` não-nulo falha com SQLSTATE 23514 (`CK_legal_receipt_deleted_at_null`).
       - Testar unicidade de snapshot e carrinho ativo: 2 prepares para mesmo CCL disparam 23505; 2 prepares para mesmo PA disparam 23505; conflito CCL <-> PA dispara 23505; bind de order_id divergente para reconciliação; bind de mesmo order_id em 2 snapshots dispara 23505.
       - Testar bloqueio do `down()` com 1 snapshot em 'reconciliation_required' (lança 55000 sem rollback).
  </action>
  <verify>
    <automated>cd apps/backend && node scripts/run-disposable-postgres-tests.mjs -- npm run test:integration:modules -- --runTestsByPath integration-tests/modules/checkout-privacy.postgres.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - A suíte `checkout-privacy.postgres.spec.ts` passa 100% no PostgreSQL descartável comprovando todos os 21 casos de envelope e 11 casos de imutabilidade/soft-delete de recibos.
    - As constraints de integridade relacional (23505), a constraint `CK_protected_checkout_data_deferred_fin_auth`, o trigger append-only `trg_legal_receipt_immutable` com `IS NOT DISTINCT FROM` (55000) e o rollback guard abrangente (55000) bloqueiam violações conforme esperado.
    - `EXPECTED_MODULE_SPECS` em `run-disposable-postgres-modules.mjs` contém exatamente 18 specs.
  </acceptance_criteria>
  <done>Migração DDL PostgreSQL pronta com trigger de imutabilidade (SQLSTATE 55000), guard abrangente contra destruição de PII ativa, constraint de coerência de autoridade financeira e runner exato atualizado para 18 specs.</done>
</task>

<task type="checkpoint:decision">
  <name>Task 17-02-03: CHECKPOINT P17-02-DDL-HR-01 (Human DDL Checkpoint)</name>
  <files>apps/backend/src/modules/checkout-privacy/migrations/Migration20260910_checkout_privacy.ts</files>
  <read_first>
    - apps/backend/src/modules/checkout-privacy/migrations/Migration20260910_checkout_privacy.ts
    - 17-DECISION-ADJUDICATION.md R17-HR-02
  </read_first>
  <behavior>
    - "Revisão humana obrigatória da estrutura de tabelas, trigger de imutabilidade, índices e constraints DDL antes de qualquer aplicação irreversível em bancos de desenvolvimento ou staging."
  </behavior>
  <decision>Autorizar ou devolver para correção o DDL de Checkout Privacy (Migration20260910_checkout_privacy.ts) antes de qualquer aplicação em bancos persistentes.</decision>
  <options>
    <option id="approve-ddl">
      <name>Aprovar DDL</name>
      <pros>Permite prosseguir para os testes descartáveis e waves subsequentes.</pros>
      <cons>Estrutura de tabelas e constraints é congelada e alterações posteriores exigem migração coordenada.</cons>
    </option>
    <option id="revise-ddl">
      <name>Revisar DDL</name>
      <pros>Permite corrigir schema, triggers ou constraints antes do congelamento.</pros>
      <cons>Bloqueia o avanço da Wave 1 até apresentação de novo diff DDL corrigido.</cons>
    </option>
  </options>
  <action>
    Apresentar ao operador humano o diff do DDL das migrações `Migration20260910_checkout_privacy.ts` e `Migration20260910120000_store_idempotency_keyrings.ts`, detalhando:
    1. Estrutura das 4 tabelas (`protected_checkout_data` incluindo `deferred_financial_authority boolean NOT NULL DEFAULT false`, `legal_receipt` com `CK_legal_receipt_deleted_at_null`, `protected_order_snapshot` de 26 colunas físicas / 23 campos lógicos com PK `id`, `sanitized_purge_ledger`).
    2. Índices parciais de unicidade e constraints de checagem, incluindo atomicidade do envelope com suporte a rascunho sem CPF (`CK_protected_checkout_data_envelope_atomicidade`), coerência de purge_state e `CK_protected_checkout_data_deferred_fin_auth`.
    3. Trigger `trg_legal_receipt_immutable` bloqueando DELETE físico e mutação de `deleted_at` (soft delete) com SQLSTATE 55000, garantindo append-only via `IS NOT DISTINCT FROM`.
    4. Migração store-idempotency relaxando `pepper_version` para `>= 1` e adicionando colunas de fingerprint semântico.
    5. Proteção no `down()` com erro 55000 cobrindo snapshots não-expurgados (incluindo 'reconciliation_required'), recibos e carrinhos ativos.
    Solicitar confirmação explícita para prosseguir.
  </action>
  <verify>
    <automated>git diff --check apps/backend/src/modules/checkout-privacy/migrations/Migration20260910_checkout_privacy.ts</automated>
  </verify>
  <acceptance_criteria>
    - Operador humano aprova formalmente o checkpoint DDL P17-02-DDL-HR-01.
    - Zero aplicação direta em banco de dados fora do runner descartável antes da autorização.
  </acceptance_criteria>
  <done>Human DDL Checkpoint P17-02-DDL-HR-01 documentado para aprovação formal.</done>
  <resume-signal>Select: approve-ddl or revise-ddl.</resume-signal>
</task>

</tasks>

## Artifacts this phase produces
- Model: `ProtectedCheckoutData`
- Model: `LegalReceipt`
- Model: `ProtectedOrderSnapshot`
- Model: `SanitizedPurgeLedger`
- Migration: `apps/backend/src/modules/checkout-privacy/migrations/Migration20260910_checkout_privacy.ts`
- Test: `apps/backend/integration-tests/modules/checkout-privacy.postgres.spec.ts`
- Script: `apps/backend/scripts/run-disposable-postgres-modules.mjs`

## Phase 17 Execution Orchestration Policy
- **Harness:** Antigravity IDE
- **Orchestrator model:** Gemini 3.8 Flash
- **Subagent model:** Gemini 3.8 Flash (REQUIRED FOR EXECUTION)
- **Effort policy:** Dynamic `orchestrator-selected per work unit (low | medium | high)`; `recommended_default_effort: high`
- **Parallelization:** false
- **Max active implementation subagents:** 1
- **Auto-chain:** false
- **Auto-advance:** false
- **Plan Approval Gate Separation:** Human approval of Phase 17 PLAN at P17-PLAN-HR-01 does NOT authorize execution of Wave 0 or Plan 17-01.
- **Recommended subagent for Plan 17-02:** Perspective B (PostgreSQL & Data Model Specialist)
