---
phase: 17-authenticated-br-checkout-privacy
plan: 07
type: execute
wave: 6
depends_on: ["17-06-PLAN.md"]
files_modified:
  - apps/backend/src/api/store/carts/[id]/checkout-details/route.ts
  - apps/backend/src/api/store/carts/[id]/checkout-details/validate/route.ts
  - apps/backend/src/api/store/carts/checkout-details-validators.ts
  - apps/backend/src/api/store/carts/checkout-details-serializers.ts
  - apps/backend/src/modules/checkout/checkout-data.ts
  - apps/backend/src/modules/checkout-privacy/types.ts
  - apps/backend/src/modules/checkout-privacy/service.ts
  - apps/backend/src/modules/payment-attempt/eligibility.ts
  - apps/backend/src/api/store/carts/__tests__/checkout-details-validators.unit.spec.ts
  - apps/backend/integration-tests/http/checkout-details.spec.ts
autonomous: false
requirements: [CHK-01, CHK-02, CHK-03, CHK-04, CHK-08, CHK-09, CHK-10]
orchestrator_selected_effort: "low | medium | high"
recommended_default_effort: "high"
estimate:
  tokens: 58000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "GET /store/carts/{id}/checkout-details é somente leitura, devolve Cache-Control: no-store, CPF mascarado (***.***.***-NN), email derivado do Customer, status de recibos e NUNCA reseta o relógio de abandono."
    - "PATCH /store/carts/{id}/checkout-details atualiza campos válidos no protected_checkout_data, projeta o endereço de entrega para cart.shipping_address do Medusa core (address_1 = street + number, address_2 = neighborhood + complement) com metadata estritamente livre de federal_tax_id, valida CPF server-side, exige If-Match e Idempotency-Key, e reseta o relógio de abandono sob mutação válida."
    - "POST /store/carts/{id}/checkout-details/validate é estritamente atômico; falhas em qualquer pré-requisito (endereço incompleto, CPF ausente, recibos pendentes, itens vazios, total final zero) retornam 422 com todos os fieldErrors e zero mutação persistida."
    - "O total final zero é terminantemente rejeitado com 422 ZERO_TOTAL_NOT_SUPPORTED; apenas frete grátis com itens > 0 é permitido."
    - "Recibos de consentimento são aceitos e vinculados com purpose, document_version e document_digest (SHA-256), sem user agent ou IP."
    - "A presença de billing_address é estritamente rejeitada em PATCH e POST com 400 VALIDATION_ERROR através de omissão no schema com .strict() (regra ABSENT: ausente passa; {} ou null ou objeto falha com 400 sem ecoar input)."
    - "A migração de evaluatePaymentStartEligibility desacopla a validação física de endereço de federal_tax_id ({ requireFederalTaxId: false }) e consome a projeção saneada ProtectedCheckoutEligibilityProjection produzida explicitamente por CheckoutPrivacyModuleService.getPaymentEligibilityProjection() sob o lock canônico de autoridade (lockCartOrderAuthority): valida customer ownership, linha ativa, lifecycle_state === 'ready' e has_valid_cpf === true (derivado de envelope completo persistido após validação server-side de checksum; não-persistido, sem decifração de CPF em runtime); com zero raw CPF, ciphertext ou chaves em metadata, PaymentAttempt, Stripe ou logs."
    - "A suíte HTTP integration-tests/http/checkout-details.spec.ts prova o ciclo completo de resume, draft, projeção segura para o Medusa core, validação atômica, elegibilidade de início de pagamento sem CPF em metadados, concorrência ETag e isolamento de erros, com zero pedidos criados no banco."
  artifacts:
    - path: apps/backend/src/api/store/carts/[id]/checkout-details/route.ts
      provides: "Handlers GET (resume) e PATCH (draft com projeção segura de endereço) para /store/carts/{id}/checkout-details"
      exports: [GET, PATCH]
    - path: apps/backend/src/api/store/carts/[id]/checkout-details/validate/route.ts
      provides: "Handler POST (validação final atômica) para /store/carts/{id}/checkout-details/validate"
      exports: [POST]
    - path: apps/backend/src/api/store/carts/checkout-details-validators.ts
      provides: "Schemas Zod estritos, validação de CPF e construtores de fieldErrors"
      exports: [PatchCheckoutDetailsBodySchema, ValidateCheckoutDetailsBodySchema]
    - path: apps/backend/src/api/store/carts/checkout-details-serializers.ts
      provides: "Serializador de resposta de checkout-details com mascaramento de CPF"
      exports: [serializeCheckoutDetailsResponse, serializeCheckoutValidateResponse]
    - path: apps/backend/src/modules/checkout-privacy/types.ts
      provides: "Tipo público ProtectedCheckoutEligibilityProjection exportado para fronteira intermódulos"
      exports: [ProtectedCheckoutEligibilityProjection]
    - path: apps/backend/src/modules/checkout-privacy/service.ts
      provides: "Método produtor getPaymentEligibilityProjection() em CheckoutPrivacyModuleService"
      exports: [CheckoutPrivacyModuleService]
    - path: apps/backend/integration-tests/http/checkout-details.spec.ts
      provides: "Suíte HTTP cobrindo autenticação, rascunho, projeção de endereço, validação atômica, total zero e ETag"
  key_links:
    - from: apps/backend/src/api/store/carts/[id]/checkout-details/route.ts
      to: apps/backend/src/api/store/carts/checkout-details-precedence.ts
      via: "Execução do pipeline de precedência de 12 estágios"
      pattern: "runCheckoutPrecedencePipeline"
    - from: apps/backend/src/api/store/carts/[id]/checkout-details/route.ts
      to: apps/backend/src/modules/checkout-privacy/service.ts
      via: "Chamada a métodos de persistência de dados protegidos e recibos"
      pattern: "scope.resolve.*CHECKOUT_PRIVACY_MODULE"
    - from: apps/backend/src/modules/payment-attempt/eligibility.ts
      to: apps/backend/src/modules/checkout-privacy/service.ts
      via: "Chamada ao produtor getPaymentEligibilityProjection() sob lock canônico de autoridade"
      pattern: "checkoutPrivacyService.getPaymentEligibilityProjection"
---

<objective>
Implementar os handlers HTTP oficiais da Phase 17 (`GET /store/carts/{id}/checkout-details`, `PATCH /store/carts/{id}/checkout-details`, `POST /store/carts/{id}/checkout-details/validate`), os validadores Zod estritos (`.strict()`), a validação de CPF de 11 dígitos, a projeção saneada do endereço de entrega para a entidade `cart.shipping_address` do Medusa core (necessária para que o `completeCartWorkflow` funcione no nascimento do pedido sem reter CPF em metadados), a verificação de recibos de consentimento, a rejeição de total zero, a derivação de `checkout_data_complete` e a suíte completa de testes de integração HTTP.

Purpose: Satisfazer CHK-01, CHK-02, CHK-03, CHK-04, CHK-08, CHK-09 e CHK-10, garantindo contratos HTTP estáveis e rigorosos para a futura storefront consumidora.
Output: Endpoints funcionais, validadores, serializadores e suíte HTTP de integração com cobertura exata dos casos positivos e negativos.
</objective>

<execution_context>
Harness: Antigravity IDE
Orchestrator: Gemini 3.8 Flash
Subagents: Gemini 3.8 Flash — REQUIRED FOR EXECUTION
Effort: orchestrator-selected per work unit (low | medium | high)
Parallelization: false (serial execution)
Max active implementation subagents: 1
Auto-chain: false
Auto-advance: false
Execution Authorization: NOT AUTHORIZED. Approval of this plan specification confers HUMAN APPROVED — PASS on the PLAN artifact set only and DOES NOT authorize execution, Wave 0, Plan 17-01, or runtime code implementation. Execution requires a separate, explicit human execution authorization.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-authenticated-br-checkout-privacy/17-CONTEXT.md
@.planning/phases/17-authenticated-br-checkout-privacy/17-DECISION-ADJUDICATION.md
@.planning/phases/17-authenticated-br-checkout-privacy/17-PATTERNS.md
@.planning/phases/17-authenticated-br-checkout-privacy/17-VALIDATION.md
@apps/backend/src/api/store/carts/checkout-details-precedence.ts
@apps/backend/src/api/store/carts/checkout-details-validators.ts
@apps/backend/src/modules/checkout-privacy/service.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 17-07-01: Validadores Zod com Omissão Estrita de billing_address, Validação Server-Side de CPF e Serializadores com Mascaramento</name>
  <files>apps/backend/src/api/store/carts/checkout-details-validators.ts, apps/backend/src/api/store/carts/checkout-details-serializers.ts, apps/backend/src/api/store/carts/__tests__/checkout-details-validators.unit.spec.ts</files>
  <read_first>
    - 17-DECISION-ADJUDICATION.md seções D17-06, D17-07, D17-09, D17-10, R17-HR-06 e R17-HR-07
    - 17-PATTERNS.md seções 3.8 e 5.1-5.3
    - apps/backend/src/modules/checkout/checkout-data.ts (para algoritmo de checksum de CPF)
  </read_first>
  <behavior>
    - "PatchCheckoutDetailsBodySchema aceita campos opcionais de endereço individual BR, CPF (formatado ou 11 dígitos), e recibos; omite billing_address do schema com .strict()."
    - "ValidateCheckoutDetailsBodySchema valida corpo estrito com recibos opcionais e omite billing_address com .strict()."
    - "Se billing_address estiver ausente no body: passa com sucesso na validação Zod."
    - "Se billing_address estiver presente no body (seja {}, null, string ou objeto): o Zod .strict() rejeita como chave não reconhecida e o serializador gera 400 VALIDATION_ERROR com fieldErrors: { billing_address: 'Invalid value' } sem ecoar valores do cliente."
    - "A validação server-side de CPF rejeita sequências repetidas, tamanho diferente de 11 dígitos ou dígitos verificadores inválidos."
    - "O serializador projeta o CPF estritamente mascarado (***.***.***-NN) e nunca expõe o valor cru ou ciphertext."
    - "Calcula checkout_data_complete com base na presença e validade de todos os campos obrigatórios."
  </behavior>
  <action>
    Criar `apps/backend/src/api/store/carts/checkout-details-validators.ts`:
    1. Definir `BrazilShippingAddressDraftSchema` (.strict()).
    2. Definir `CheckoutReceiptInputSchema` (.strict()).
    3. Definir `PatchCheckoutDetailsBodySchema` e `ValidateCheckoutDetailsBodySchema` com `.strict()`, omitindo intencionalmente a chave `billing_address` da definição do objeto (SEM usar `z.never()`), de modo que campos ausentes passem e qualquer envio de `billing_address` seja barrado pelo `.strict()`.
    4. Implementar `isValidCpf(value: string): boolean` com validação de módulo 11 e rejeição de sequências homogêneas.
    Criar `apps/backend/src/api/store/carts/checkout-details-serializers.ts`:
    1. Implementar `maskCpf(rawCpf: string): string` retornando `***.***.***-${digits.slice(-2)}`.
    2. Implementar `serializeCheckoutDetailsResponse(cart, protectedData, receipts, version)` com allowlist fechada e `incomplete_fields`.
    3. Implementar `serializeCheckoutValidateResponse(cartId, version)`.
    Criar testes unitários em `apps/backend/src/api/store/carts/__tests__/checkout-details-validators.unit.spec.ts`:
    - Testar que corpo sem `billing_address` passa com sucesso.
    - Testar que corpo com `billing_address: {}`, `billing_address: null` ou `billing_address: { street: "x" }` falha com 400 `VALIDATION_ERROR`.
    - Testar algoritmo de CPF e mascaramento seguro.
  </action>
  <verify>
    <automated>cd apps/backend && npm run test:unit -- --runTestsByPath src/api/store/carts/__tests__/checkout-details-validators.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Testes unitários passam 100%.
    - `billing_address` ausente é aceito; `billing_address` presente em qualquer formato é rejeitado com 400.
    - O mascaramento de CPF nunca expõe mais que 2 dígitos.
  </acceptance_criteria>
  <done>Validadores e serializadores de checkout implementados e testados com rigor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 17-07-02: Route Handlers e Migração de Payment-Start Eligibility para protected_checkout_data</name>
  <files>apps/backend/src/api/store/carts/[id]/checkout-details/route.ts, apps/backend/src/api/store/carts/[id]/checkout-details/validate/route.ts, apps/backend/src/modules/checkout/checkout-data.ts, apps/backend/src/modules/checkout-privacy/types.ts, apps/backend/src/modules/checkout-privacy/service.ts, apps/backend/src/modules/payment-attempt/eligibility.ts</files>
  <read_first>
    - apps/backend/src/api/store/carts/checkout-details-precedence.ts (para execução de pipeline)
    - apps/backend/src/api/store/carts/checkout-details-validators.ts
    - apps/backend/src/api/store/carts/checkout-details-serializers.ts
    - apps/backend/src/modules/checkout/checkout-data.ts (validateBrazilShippingAddress e calculateCheckoutDataComplete)
    - apps/backend/src/modules/payment-attempt/eligibility.ts (evaluatePaymentStartEligibility)
    - 17-PATTERNS.md seção 3.8
    - 17-DECISION-ADJUDICATION.md R17-HR-02 (projeção permitida de shipping_address sem CPF em metadata)
  </read_first>
  <behavior>
    - "GET /store/carts/:id/checkout-details executa estágios 1 a 5 da precedência, busca dados protegidos, responde 200 com Cache-Control: no-store e ETag; não altera banco."
    - "PATCH /store/carts/:id/checkout-details executa os 12 estágios da precedência, atualiza campos válidos no banco protegidos, cifra CPF se presente, projeta o endereço de entrega para cart.shipping_address no Medusa core (address_1 = street, number; address_2 = neighborhood / complement; phone, postal_code, city, province, country_code = 'BR'; metadata: {} sem federal_tax_id), avança a versão do recurso (CAS), reseta o relógio se houve alteração e devolve 200 com ETag atualizado."
    - "POST /store/carts/:id/checkout-details/validate executa os 12 estágios da precedência, avalia completude atômica: se endereço incompleto, CPF ausente, recibos pendentes, itens vazios ou total zero, falha com 422 e todos os fieldErrors; se completo, reseta relógio e responde 200 { valid: true, checkout_data_complete: true }."
    - "validateBrazilShippingAddress aceita opção { requireFederalTaxId: false }, permitindo validar o endereço físico de entrega independentemente do CPF."
    - "evaluatePaymentStartEligibility sob lock transacional canônico de autoridade (lockCartOrderAuthority) consome a projeção saneada ProtectedCheckoutEligibilityProjection produzida por CheckoutPrivacyModuleService.getPaymentEligibilityProjection(): valida customer ownership, linha ativa, data_revision corrente, lifecycle_state === 'ready', has_valid_cpf === true (derivado sem decifração a partir de envelope completo persistido após validação server-side de checksum) e endereço físico válido; zero raw CPF, ciphertext ou chaves em metadata, PaymentAttempt, Stripe, logs ou responses."
  </behavior>
  <action>
    1. Atualizar `apps/backend/src/modules/checkout/checkout-data.ts`:
       - Em `validateBrazilShippingAddress(address, options)`: adicionar suporte a `options?.requireFederalTaxId?: boolean` (default: true). Se false, não exige `federal_tax_id` no input de endereço físico.
    2. Atualizar `apps/backend/src/modules/checkout-privacy/types.ts` e `apps/backend/src/modules/checkout-privacy/service.ts`:
       - Em `types.ts`: definir o tipo público da projeção:
         ```typescript
         export type ProtectedCheckoutEligibilityProjection = {
           protected_data_id: string
           cart_id: string
           customer_id: string
           lifecycle_state: "draft" | "ready" | "snapshot_prepared" | "purged"
           data_revision: number
           has_valid_cpf: boolean
         }
         ```
       - Em `service.ts`: implementar o método produtor explícito:
         ```typescript
         async getPaymentEligibilityProjection(
           cartId: string,
           sharedContext?: SharedTransactionContext
         ): Promise<ProtectedCheckoutEligibilityProjection | null>
         ```
         - Executa sob a transação/lock fornecido pelo chamador (`sharedContext`).
         - Busca a linha ativa em `protected_checkout_data` para o `cartId` (`deleted_at IS NULL`). Se inexistente, retorna `null`.
         - Deriva `has_valid_cpf` sem decifrar CPF:
           `has_valid_cpf = (row.lifecycle_state === "ready" && row.encrypted_cpf != null && row.cpf_nonce != null && row.cpf_tag != null && row.cpf_wrapped_dek != null)`.
           Como a persistência do envelope só ocorre após normalização e validação de checksum de 11 dígitos no servidor, um envelope completo no estado 'ready' garante CPF válido.
         - Retorna `ProtectedCheckoutEligibilityProjection` contendo zero CPF cru, zero ciphertext, zero nonce, zero tag e zero DEK.
    3. Atualizar `apps/backend/src/modules/payment-attempt/eligibility.ts`:
       - Migrar `evaluatePaymentStartEligibility` para consumir a projeção de elegibilidade:
         - Executa sob o lock transacional canônico de autoridade do carrinho (`lockCartOrderAuthority(transaction, cart.id)`).
         - Valida endereço físico com `validateBrazilShippingAddress(cart.shipping_address, { requireFederalTaxId: false })`.
         - Invoca `checkoutPrivacyService.getPaymentEligibilityProjection(cart.id, tx)`.
         - Valida ownership: `projection.customer_id === actor.customerId`.
         - Valida elegibilidade M1: `projection.lifecycle_state === "ready"` e `projection.has_valid_cpf === true`.
         - Se draft sem CPF, draft incompleto ou purged -> rejeita com `CHECKOUT_DATA_INCOMPLETE`.
         - Se ready com envelope incompleto -> fail closed.
         - Retorna `eligible: true` quando os requisitos são cumpridos, sem buscar nem exigir `federal_tax_id` em `cart.shipping_address.metadata`.
    4. Criar `apps/backend/src/api/store/carts/[id]/checkout-details/route.ts`:
       - Handler `GET`: executar estágios 1..5 da precedência, obter `protected_checkout_data`, `legal_receipt` e versão, responder com `serializeCheckoutDetailsResponse()`, `Cache-Control: no-store` e `ETag`.
       - Handler `PATCH`: executar pipeline de 12 estágios, validar body, lock row, atualizar `protected_checkout_data`, salvar recibos, projetar para `cart.shipping_address` (com `metadata` estritamente sem `federal_tax_id`), CAS, reset de abandono, responder com novo `ETag`.
    5. Criar `apps/backend/src/api/store/carts/[id]/checkout-details/validate/route.ts`:
       - Handler `POST`: executar 12 estágios, validar completude atômica (endereço, CPF, recibos, itens, total > 0). Se total zero -> 422 ZERO_TOTAL_NOT_SUPPORTED; se inválido -> 422 com fieldErrors; se válido -> avançar relógio e responder 200 com `checkout_data_complete: true`.
  </action>
  <verify>
    <automated>cd apps/backend && npm run test:unit -- --runTestsByPath src/api/store/carts/__tests__/checkout-details-validators.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Arquivos de rota compilam e exportam handlers nomeados GET, PATCH e POST.
    - Projeção para `cart.shipping_address` ocorre sem gravar CPF nos metadados da tabela Medusa.
    - `CheckoutPrivacyModuleService.getPaymentEligibilityProjection` retorna projeção saneada com has_valid_cpf sem decifrar CPF e sem expor envelope criptográfico.
    - `evaluatePaymentStartEligibility` valida com sucesso carrinhos que possuem CPF protegido em `protected_checkout_data` mesmo com `shipping_address.metadata` livre de CPF.
    - Total zero é rejeitado terminantemente com 422.
    - Nenhum pedido é criado em nenhuma das rotas (Orders = 0).
  </acceptance_criteria>
  <done>Handlers de rota, produtor de projeção de elegibilidade e migração de payment-start eligibility implementados com isolamento total de CPF.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 17-07-03: Suíte de Integração HTTP e Regressão de Payment-Start (integration-tests/http/checkout-details.spec.ts)</name>
  <files>apps/backend/integration-tests/http/checkout-details.spec.ts</files>
  <read_first>
    - apps/backend/integration-tests/http/cart-merge-review.spec.ts (para setup de testes HTTP com Customer autenticado e BFF guard)
    - 17-VALIDATION.md (seção Per-Task Verification Map CHK-01, CHK-02, CHK-03, CHK-04, CHK-08, CHK-09, CHK-10)
    - apps/backend/src/modules/payment-attempt/eligibility.ts
  </read_first>
  <behavior>
    - "Testa GET sem token -> 401; GET com carrinho de outro cliente -> 404; GET válido -> 200 com CPF mascarado e no-store."
    - "Testa PATCH sem billing_address -> 200 sucesso."
    - "Testa PATCH com billing_address presente ({}, null, objeto) -> 400 VALIDATION_ERROR sem ecoar input."
    - "Testa PATCH com CPF inválido -> 422 INVALID_CPF."
    - "Testa PATCH com If-Match desatualizado -> 412 CART_VERSION_MISMATCH com snapshot seguro."
    - "Testa PATCH sob congelamento financeiro -> 409 CHECKOUT_LOCKED."
    - "Testa POST validate com carrinho de total zero -> 422 ZERO_TOTAL_NOT_SUPPORTED."
    - "Testa POST validate com dados completos usando canário de CPF sintético válido por checksum módulo 11 -> 200 valid: true, checkout_data_complete: true."
    - "Testa que cart.shipping_address.metadata não contém federal_tax_id após o PATCH."
    - "Testa regressão de Payment Start cobrindo a matriz completa de elegibilidade:
       1) valid protected checkout + shipping_address.metadata sem federal_tax_id + lifecycle ready + complete valid CPF envelope -> payment start eligible;
       2) draft sem CPF -> payment start ineligible (CHECKOUT_DATA_INCOMPLETE);
       3) draft com CPF mas não final-ready -> payment start ineligible;
       4) purged protected data -> payment start ineligible;
       5) ready row com envelope impossível/ausente -> DB constraint ou fail closed;
       6) projection contém zero plaintext/ciphertext CPF;
       7) payment eligibility nunca lê shipping_address.metadata.federal_tax_id."
  </behavior>
  <action>
    Criar `apps/backend/integration-tests/http/checkout-details.spec.ts`:
    1. Configurar servidor HTTP de teste Medusa com client autenticado.
    2. Cobrir todos os cenários positivos e negativos: autenticação, BFF, propriedade, validação de CPF (rejeição de inválidos e aceitação de sintético válido via módulo 11), omissão vs presença de billing_address, endereço brasileiro, projeção para cart.shipping_address sem metadata de CPF, concorrência ETag e total zero.
    3. Testar regressão completa de elegibilidade de pagamento:
       - Provar que `evaluatePaymentStartEligibility` é aprovado para o carrinho após o PATCH/validate sem nenhum CPF em `shipping_address.metadata`.
       - Provar ineligibilidade para draft sem CPF, draft incompleto e checkout purged.
       - Provar que `ProtectedCheckoutEligibilityProjection` não contém nenhum dado criptográfico ou sensível de CPF.
       - Provar que `shipping_address.metadata.federal_tax_id` nunca é consultado ou exigido.
    4. Afirmar explicitamente que a contagem de pedidos no banco antes e depois das requisições permanece zero (`expect(orderCount).toBe(0)`).
  </action>
  <verify>
    <automated>cd apps/backend && npm run test:integration:http -- --runTestsByPath integration-tests/http/checkout-details.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - A suíte `checkout-details.spec.ts` passa 100%.
    - Todos os requisitos CHK-01, CHK-02, CHK-03, CHK-04, CHK-08, CHK-09 e CHK-10 são comprovados em nível HTTP.
    - Prova inequívoca de que evaluatePaymentStartEligibility opera a partir da projeção saneada de CheckoutPrivacyModuleService.getPaymentEligibilityProjection.
    - Matriz de testes de elegibilidade (eligible, draft sem CPF, draft incompleto, purged, envelope ausente) validada.
    - Zero pedidos criados durante toda a execução do teste.
  </acceptance_criteria>
  <done>Suíte de integração HTTP completa e regressão de payment start validadas com sucesso.</done>
</task>

</tasks>

## Artifacts this phase produces
- Handlers: `GET`, `PATCH` em `apps/backend/src/api/store/carts/[id]/checkout-details/route.ts`
- Handler: `POST` em `apps/backend/src/api/store/carts/[id]/checkout-details/validate/route.ts`
- Schema: `PatchCheckoutDetailsBodySchema`, `ValidateCheckoutDetailsBodySchema`
- Serializer: `serializeCheckoutDetailsResponse`, `serializeCheckoutValidateResponse`
- Test: `apps/backend/integration-tests/http/checkout-details.spec.ts`

## Phase 17 Execution Orchestration Policy
- **Harness:** Antigravity IDE
- **Orchestrator model:** Gemini 3.8 Flash
- **Subagent model:** Gemini 3.8 Flash (REQUIRED FOR EXECUTION)
- **Effort policy:** Dynamic `orchestrator-selected per work unit (low | medium | high)`; `recommended_default_effort: high`
- **Parallelization:** false
- **Max active implementation subagents:** 1
- **Auto-chain:** false
- **Auto-advance:** false
- **Plan Approval Gate Separation:** Human approval of Phase 17 PLAN at P17-PLAN-HR-01 does NOT authorize execution of Wave 0 or Plan 17-01.
- **Recommended subagent for Plan 17-07:** Perspective D (Checkout HTTP Surface Specialist)
