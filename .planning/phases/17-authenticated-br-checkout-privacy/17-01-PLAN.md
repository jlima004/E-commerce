---
phase: 17-authenticated-br-checkout-privacy
plan: 01
type: tracer
wave: 0
depends_on: []
files_modified:
  - package-lock.json
  - apps/backend/package.json
  - apps/backend/src/modules/checkout-privacy/index.ts
  - apps/backend/src/modules/checkout-privacy/types.ts
  - apps/backend/src/modules/checkout-privacy/service.ts
  - apps/backend/src/modules/checkout-privacy/crypto/kms-provider.ts
  - apps/backend/src/modules/checkout-privacy/crypto/aws-kms-provider.ts
  - apps/backend/src/modules/checkout-privacy/crypto/fake-kms-provider.ts
  - apps/backend/src/modules/checkout-privacy/crypto/envelope-service.ts
  - apps/backend/medusa-config.ts
  - apps/backend/src/infrastructure/__tests__/medusa-config.unit.spec.ts
  - apps/backend/src/modules/checkout-privacy/__tests__/envelope-service.unit.spec.ts
  - apps/backend/src/modules/checkout-privacy/__tests__/fake-kms-provider.unit.spec.ts
  - apps/backend/integration-tests/helpers/checkout-privacy-postgres.ts
autonomous: false
requirements: [CHK-05]
orchestrator_selected_effort: "low | medium | high"
recommended_default_effort: "high"
estimate:
  tokens: 45000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "O pacote @aws-sdk/client-kms é adicionado ao package.json para suportar o adapter KMS de produção."
    - "O módulo checkout_privacy é registrado oficialmente em medusa-config.ts e provado por teste unitário de configuração."
    - "A abstração KmsProvider define generateDataKey, decrypt e reEncrypt, todos exigindo KmsEncryptionContext com validação estrita; AwsKmsProvider implementa chamadas ao SDK da AWS; FakeKmsProvider executa localmente de forma determinística com injeção de falhas e zero chamadas AWS."
    - "O KmsEncryptionContext admite exclusivamente chaves semânticas canônicas: application, runtime_environment (derivado dinamicamente de config/ambiente, NUNCA hardcoded 'production'), domain, authority_kind ('cart' | 'order'), envelope_version, aad_version, logical_key_version; qualquer dado PII, ID de entidade, correlation_id ou identificador de provedor é estritamente proibido."
    - "Plaintext DEKs e nonces são gerados via CSPRNG (crypto.randomBytes), com suporte a RNG injetável para testes determinísticos de vetores conhecidos (KAT); zero uso de nonces determinísticos em código de produção."
    - "A encriptação local usa AES-256-GCM com nonce de 12 bytes CSPRNG, tag de 16 bytes e AAD canônica versionada vinculando authority ID estável, sem campos de revisão mutável."
    - "Plaintext DEK buffers são explicitamente zerados em memória (buffer.fill(0)) em blocos finally imediatamente após uso; zero cache de DEK entre requests."
    - "O helper de teste em checkout-privacy-postgres.ts roda sob PostgreSQL descartável/loopback com isolamento e rollback limpo."
    - "O tracer cria exatamente 0 pedidos (Orders=0) e não acessa Stripe, Gelato ou infraestrutura remota."
  artifacts:
    - path: package-lock.json
      provides: "Lockfile raiz atualizado pela adição de dependência no workspace"
    - path: apps/backend/package.json
      provides: "Dependência de produção @aws-sdk/client-kms"
    - path: apps/backend/src/modules/checkout-privacy/index.ts
      provides: "Token CHECKOUT_PRIVACY_MODULE e export default Module() do Medusa v2"
      exports: [CHECKOUT_PRIVACY_MODULE, default]
    - path: apps/backend/src/modules/checkout-privacy/service.ts
      provides: "CheckoutPrivacyModuleService com métodos de envelope de encriptação"
      exports: [CheckoutPrivacyModuleService]
    - path: apps/backend/src/modules/checkout-privacy/types.ts
      provides: "Tipos de dados DTO, envelope criptográfico e contexto KMS (sem declaração de interface KmsProvider)"
      exports: [EncryptedEnvelope, KmsDataKeyResult, KmsDecryptResult, KmsReEncryptResult, KmsEncryptionContext, EnvelopeAadContext]
    - path: apps/backend/src/modules/checkout-privacy/crypto/kms-provider.ts
      provides: "Interface canônica KmsProvider consumindo tipos de types.ts (única fonte da verdade)"
      exports: [KmsProvider]
    - path: apps/backend/src/modules/checkout-privacy/crypto/aws-kms-provider.ts
      provides: "Adapter de produção para AWS KMS chamando GenerateDataKey, Decrypt e ReEncrypt com EncryptionContext"
      exports: [AwsKmsProvider]
    - path: apps/backend/src/modules/checkout-privacy/crypto/fake-kms-provider.ts
      provides: "Implementação offline determinística para testes locais com injeção de falhas e validação de context"
      exports: [FakeKmsProvider]
    - path: apps/backend/src/modules/checkout-privacy/crypto/envelope-service.ts
      provides: "Serviço local AES-256-GCM com AAD versionada estável e zeroização de memória"
      exports: [EnvelopeEncryptionService]
    - path: apps/backend/src/modules/checkout-privacy/__tests__/fake-kms-provider.unit.spec.ts
      provides: "Testes unitários do FakeKmsProvider, validação de EncryptionContext e hooks de injeção de falhas"
    - path: apps/backend/integration-tests/helpers/checkout-privacy-postgres.ts
      provides: "Helper de fixtures e teardown para testes PostgreSQL descartáveis"
  key_links:
    - from: apps/backend/medusa-config.ts
      to: apps/backend/src/modules/checkout-privacy/index.ts
      via: "Registro do módulo local sob chave checkout_privacy"
      pattern: 'key: "checkout_privacy"|resolve: "./src/modules/checkout-privacy"'
    - from: apps/backend/src/modules/checkout-privacy/service.ts
      to: apps/backend/src/modules/checkout-privacy/crypto/envelope-service.ts
      via: "Injeção de EnvelopeEncryptionService no serviço principal do módulo"
      pattern: "EnvelopeEncryptionService|encryptCpf|decryptCpf"
---

<objective>
Construir a fundação arquitetural e o primeiro tracer de produção da Phase 17: o módulo `checkout_privacy` registrado no Medusa v2, a interface e provedor offline de KMS (`KmsProvider`, `FakeKmsProvider`), o motor local de envelope encryption AES-256-GCM com AAD versionada e zeroização de memória, e o harness de testes descartáveis em PostgreSQL.

Purpose: Detectar imediatamente incompatibilidades de container Medusa, lifecycles de buffers criptográficos e isolamento transacional, provando CHK-05 e CHK-06 na camada fundamental sem criar pedidos ou chamar provedores remotos.
Output: Módulo Medusa registrado, serviço criptográfico com testes unitários, FakeKmsProvider offline determinístico e helper PostgreSQL de Wave 0.
</objective>

<execution_context>
Harness: Antigravity IDE
Orchestrator: Gemini 3.8 Flash
Subagents: Gemini 3.8 Flash — REQUIRED FOR EXECUTION
Effort: orchestrator-selected per work unit (low | medium | high)
Parallelization: false (serial execution)
Max active implementation subagents: 1
Auto-chain: false
Auto-advance: false
Execution Authorization: NOT AUTHORIZED. Approval of this plan specification confers HUMAN APPROVED — PASS on the PLAN artifact set only and DOES NOT authorize execution, Wave 0, Plan 17-01, or runtime code implementation. Execution requires a separate, explicit human execution authorization.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-authenticated-br-checkout-privacy/17-CONTEXT.md
@.planning/phases/17-authenticated-br-checkout-privacy/17-DECISION-ADJUDICATION.md
@.planning/phases/17-authenticated-br-checkout-privacy/17-PATTERNS.md
@.planning/phases/17-authenticated-br-checkout-privacy/17-VALIDATION.md
@apps/backend/src/modules/cart-merge/index.ts
@apps/backend/src/modules/cart-merge/service.ts
@apps/backend/medusa-config.ts
@apps/backend/src/infrastructure/__tests__/medusa-config.unit.spec.ts
@apps/backend/integration-tests/helpers/cart-merge-postgres.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 17-01-01: Dependência AWS KMS, Módulo checkout_privacy e motor de Envelope Encryption AES-256-GCM</name>
  <files>package-lock.json, apps/backend/package.json, apps/backend/src/modules/checkout-privacy/index.ts, apps/backend/src/modules/checkout-privacy/types.ts, apps/backend/src/modules/checkout-privacy/service.ts, apps/backend/src/modules/checkout-privacy/crypto/kms-provider.ts, apps/backend/src/modules/checkout-privacy/crypto/aws-kms-provider.ts, apps/backend/src/modules/checkout-privacy/crypto/fake-kms-provider.ts, apps/backend/src/modules/checkout-privacy/crypto/envelope-service.ts, apps/backend/medusa-config.ts, apps/backend/src/infrastructure/__tests__/medusa-config.unit.spec.ts, apps/backend/src/modules/checkout-privacy/__tests__/envelope-service.unit.spec.ts, apps/backend/src/modules/checkout-privacy/__tests__/fake-kms-provider.unit.spec.ts</files>
  <read_first>
    - apps/backend/package.json (para adicionar @aws-sdk/client-kms)
    - apps/backend/src/modules/cart-merge/index.ts (para padrão de exportação de módulo Medusa v2)
    - apps/backend/src/modules/cart-merge/service.ts (para estrutura de classe de serviço Medusa v2)
    - apps/backend/medusa-config.ts e apps/backend/src/infrastructure/__tests__/medusa-config.unit.spec.ts (para registro único em expectedLocalModules)
    - 17-DECISION-ADJUDICATION.md seções R17-HR-02 e R17-HR-03 (para requisitos de envelope encryption, KEK, DEK, AAD, nonces de 12 bytes, tag de 16 bytes e zeroização de memória)
    - 17-PATTERNS.md seções 3.1 e 3.3
  </read_first>
  <behavior>
    - "Adiciona dependência @aws-sdk/client-kms (^3.758.0) ao workspace via npm install, mantendo sincronizado o package-lock.json raiz sem quebrar a compilação."
    - "O container Medusa v2 resolve CHECKOUT_PRIVACY_MODULE com sucesso."
    - "KmsProvider abstrai generateDataKey, decrypt e reEncrypt; AwsKmsProvider implementa essas chamadas sobre o cliente AWS KMS; FakeKmsProvider executa localmente de forma determinística com hooks de failure injection (outage, key-corrupted, key-revoked)."
    - "Plaintext DEKs e nonces são gerados via CSPRNG (crypto.randomBytes), com suporte a RNG injetável exclusivamente para testes KAT determinísticos; código produtivo nunca usa nonces fixos."
    - "A encriptação de CPF produz ciphertext, nonce de 12 bytes (96 bits) e tag de 16 bytes (128 bits) em base64url, além de wrapped DEK via KMS."
    - "AAD versionada vincula authority ID estável e envelope version, sem revision mutável: indicio:checkout-privacy:aad:v1|<env>|<authority_kind>|<authority_id>|cpf|<envelope_version>|<key_version>; qualquer adulteração na AAD ou no ciphertext resulta em erro de autenticação GCM fail-closed."
    - "Plaintext DEK buffers são zerados na memória (buffer.fill(0)) no bloco finally imediatamente após o uso; nenhum cache de DEK cru existe entre requests."
    - "medusa-config.unit.spec.ts prova que checkout_privacy está registrado no Medusa."
  </behavior>
  <action>
    1. Instalar dependência no workspace via `npm install --workspace=apps/backend @aws-sdk/client-kms@^3.758.0` atualizando `apps/backend/package.json` e o `package-lock.json` raiz de forma canônica.
    2. Criar `apps/backend/src/modules/checkout-privacy/types.ts` com tipos de dados de valor e DTOs para `EncryptedEnvelope`, `KmsDataKeyResult`, `KmsDecryptResult`, `KmsReEncryptResult`, `KmsEncryptionContext` e `EnvelopeAadContext` (estritamente sem declarar a interface `KmsProvider`).
    3. Criar `apps/backend/src/modules/checkout-privacy/crypto/kms-provider.ts` como a autoridade canônica e exclusiva exportando a interface abstrata `KmsProvider` (importando os tipos de `../types.ts`), com os métodos:
       - `generateDataKey(input: { keyId: string, keySpec?: string, encryptionContext: KmsEncryptionContext }): Promise<KmsDataKeyResult>`
       - `decrypt(input: { ciphertextBlob: Buffer | Uint8Array, encryptionContext: KmsEncryptionContext, keyId?: string }): Promise<KmsDecryptResult>`
       - `reEncrypt(input: { ciphertextBlob: Buffer | Uint8Array, sourceKeyId?: string, destinationKeyId: string, sourceEncryptionContext: KmsEncryptionContext, destinationEncryptionContext: KmsEncryptionContext }): Promise<KmsReEncryptResult>`
       Validando rigorosamente as chaves permitidas em `KmsEncryptionContext`: `application`, `runtime_environment` (dinâmico, nunca hardcoded 'production'), `domain`, `authority_kind` ('cart' | 'order'), `envelope_version`, `aad_version`, `logical_key_version`. Proibindo qualquer PII, IDs de cliente/carrinho/pedido/tentativa/CCL ou dados de provedor.
    4. Criar `apps/backend/src/modules/checkout-privacy/crypto/aws-kms-provider.ts` implementando `KmsProvider` via `KMSClient` da AWS (`GenerateDataKeyCommand`, `DecryptCommand`, `ReEncryptCommand`) repassando o `EncryptionContext`.
    5. Criar `apps/backend/src/modules/checkout-privacy/crypto/fake-kms-provider.ts` com implementação in-memory offline determinística para testes locais, com injeção de falhas (`failNext`, `simulateUnavailable`), validação fechada de `KmsEncryptionContext` (contexto incorreto ou ausente falha fechado), geração de DEKs por CSPRNG (`crypto.randomBytes(32)`) e suporte a gerador injetável para testes KAT.
    6. Criar `apps/backend/src/modules/checkout-privacy/crypto/envelope-service.ts` implementando encriptação/decriptação local usando `crypto.createCipheriv("aes-256-gcm", dek, nonce)` e `crypto.createDecipheriv("aes-256-gcm", dek, nonce)`. Formatar AAD canônica estrita: `indicio:checkout-privacy:aad:v1|<env>|<authority_kind>|<authority_id>|cpf|<envelope_version>|<key_version>`. Em `finally`, executar `dekBuffer.fill(0)` incondicionalmente.
    7. Criar `apps/backend/src/modules/checkout-privacy/service.ts` como `CheckoutPrivacyModuleService` integrando o serviço criptográfico.
    8. Criar `apps/backend/src/modules/checkout-privacy/index.ts` exportando `CHECKOUT_PRIVACY_MODULE = "checkout_privacy"` e `default Module(CHECKOUT_PRIVACY_MODULE, { service: CheckoutPrivacyModuleService })`.
    9. Registrar `key: "checkout_privacy", resolve: "./src/modules/checkout-privacy"` em `apps/backend/medusa-config.ts` e adicionar `"checkout_privacy"` em `expectedLocalModules` em `apps/backend/src/infrastructure/__tests__/medusa-config.unit.spec.ts`.
    10. Criar a suíte de testes unitários `apps/backend/src/modules/checkout-privacy/__tests__/envelope-service.unit.spec.ts` validando cifragem, decifragem, AAD mismatch, tag corrompida, injeção de falhas e zeroização.
    11. Criar a suíte de testes unitários `apps/backend/src/modules/checkout-privacy/__tests__/fake-kms-provider.unit.spec.ts` validando:
        - contexto correto tem sucesso;
        - contexto ausente falha fechado;
        - contexto adulterado falha fechado;
        - authority_kind divergente ('cart' vs 'order') falha fechado;
        - rewrap utiliza o source context correto e gera o destination context correto;
        - rejeição de qualquer dado PII ou ID em chaves de context;
        - geração de DEK com CSPRNG, hooks de injeção de falhas (outage, key-revoked, corrupted) e zero chamadas remotas.
  </action>
  <verify>
    <automated>cd apps/backend && npm run test:unit -- --runTestsByPath src/infrastructure/__tests__/medusa-config.unit.spec.ts src/modules/checkout-privacy/__tests__/envelope-service.unit.spec.ts src/modules/checkout-privacy/__tests__/fake-kms-provider.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `medusa-config.unit.spec.ts` passa com `checkout_privacy` na lista de módulos locais.
    - `envelope-service.unit.spec.ts` e `fake-kms-provider.unit.spec.ts` passam 100%, comprovando AES-256-GCM com nonce de 12 bytes, tag de 16 bytes, AAD canônica sem campos de revisão mutável, validação fechada de EncryptionContext, fail-closed em caso de adulteração e destruição do buffer de DEK.
    - `AwsKmsProvider` compila sem erros TypeScript contra `@aws-sdk/client-kms`.
    - Nenhum dado de CPF ou chave de teste é emitido em logs.
    - Zero chamadas para AWS ou serviços de rede externa durante execução de testes.
  </acceptance_criteria>
  <done>Módulo base, dependência AWS KMS, adapter de produção e motor de envelope encryption funcional e testado unitariamente com provedor KMS determinístico.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 17-01-02: Test Harness PostgreSQL Descartável de Wave 0</name>
  <files>apps/backend/integration-tests/helpers/checkout-privacy-postgres.ts</files>
  <read_first>
    - apps/backend/integration-tests/helpers/cart-merge-postgres.ts (para o padrão de runner PostgreSQL descartável com rollback e cleanup)
    - 17-VALIDATION.md (seção Wave 0 Requirements)
  </read_first>
  <behavior>
    - "O helper conecta exclusivamente ao PostgreSQL descartável loopback sem usar Redis ou mocks de persistência."
    - "Fornece factories para criar carrinhos de teste, clientes e registros descartáveis com limpeza atômica garantida ao final de cada suite."
    - "Permite injeção de falhas transacionais antes do commit para testar rollbacks estritos."
  </behavior>
  <action>
    Criar `apps/backend/integration-tests/helpers/checkout-privacy-postgres.ts` seguindo o design de `cart-merge-postgres.ts`:
    1. Obter DSN do PostgreSQL via variáveis de ambiente do runner descartável (`TEST_DATABASE_URL`).
    2. Implementar `createTestCustomer()`, `createTestCart()`, e fixtures sanitizadas para testes de privacidade.
    3. Implementar `cleanupCheckoutPrivacyFixtures()` que limpa exatamente as entidades criadas no teste.
    4. Garantir que nenhuma chave real ou dado PII real seja aceito como fixture.
  </action>
  <verify>
    <automated>cd apps/backend && npm run test:unit -- --runTestsByPath src/modules/checkout-privacy/__tests__/envelope-service.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo `apps/backend/integration-tests/helpers/checkout-privacy-postgres.ts` existe, compila sem erros TypeScript e exporta as funções de setup/teardown de teste.
    - Nenhuma dependência com serviços externos ou nuvem.
  </acceptance_criteria>
  <done>Harness PostgreSQL descartável pronto para dar suporte às suítes de banco de dados das próximas waves.</done>
</task>

</tasks>

## Artifacts this phase produces
- Class: `CheckoutPrivacyModuleService`
- Interface: `KmsProvider`
- Class: `AwsKmsProvider`
- Class: `FakeKmsProvider`
- Class: `EnvelopeEncryptionService`
- Constant: `CHECKOUT_PRIVACY_MODULE`
- Types: `EncryptedEnvelope`, `KmsDataKeyResult`, `KmsDecryptResult`, `KmsReEncryptResult`, `KmsEncryptionContext`, `EnvelopeAadContext`
- File: `apps/backend/integration-tests/helpers/checkout-privacy-postgres.ts`

## Phase 17 Execution Orchestration Policy
- **Harness:** Antigravity IDE
- **Orchestrator model:** Gemini 3.8 Flash
- **Subagent model:** Gemini 3.8 Flash (REQUIRED FOR EXECUTION)
- **Effort policy:** Dynamic `orchestrator-selected per work unit (low | medium | high)`; `recommended_default_effort: high`
- **Parallelization:** false
- **Max active implementation subagents:** 1
- **Auto-chain:** false
- **Auto-advance:** false
- **Plan Approval Gate Separation:** Human approval of Phase 17 PLAN at P17-PLAN-HR-01 does NOT authorize execution of Wave 0 or Plan 17-01.
- **Recommended subagent for Plan 17-01:** Perspective B & C (Data Model & Cryptography Specialist)
