---
phase: 17-authenticated-br-checkout-privacy
plan: 03
type: execute
wave: 2
depends_on: ["17-02-PLAN.md"]
files_modified:
  - apps/backend/src/modules/store-idempotency/models/store-idempotency-record.ts
  - apps/backend/src/modules/store-idempotency/service.ts
  - apps/backend/src/modules/store-idempotency/operations.ts
  - apps/backend/src/modules/checkout-privacy/crypto/keyrings.ts
  - apps/backend/src/modules/checkout-privacy/crypto/fingerprint-service.ts
  - apps/backend/src/modules/checkout-privacy/crypto/reencryption-service.ts
  - apps/backend/src/modules/checkout-privacy/crypto/kek-lifecycle.ts
  - apps/backend/src/jobs/rewrap-protected-data.ts
  - apps/backend/src/modules/checkout-privacy/service.ts
  - apps/backend/src/modules/checkout-privacy/__tests__/keyrings.unit.spec.ts
  - apps/backend/src/modules/checkout-privacy/__tests__/reencryption.unit.spec.ts
  - apps/backend/src/modules/checkout-privacy/__tests__/kek-lifecycle.unit.spec.ts
  - apps/backend/integration-tests/modules/checkout-privacy.postgres.spec.ts
autonomous: false
requirements: [CHK-05, CHK-06]
orchestrator_selected_effort: "low | medium | high"
recommended_default_effort: "high"
estimate:
  tokens: 54000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "A autoridade de persistência do double keyring reutiliza e estende a entidade StoreIdempotencyRecord (store_idempotency_record), sem criar um segundo engine concorrente no checkout-privacy."
    - "O sistema de keyrings duplos separa estritamente o Keyring 1 (Locator HMAC) do Keyring 2 (Fingerprint Semântico Sensível). No Keyring 1, para cada versão retida { pepper_version, secret }, gera o candidato idempotency_key_hash sob o esquema estável hash_version ('hmac-sha256-v1') e consulta via WHERE idempotency_key_hash IN ($candidates) FOR UPDATE."
    - "A avaliação do Keyring 1 segue a matriz determinística: 0 matches + keyring completo = primeira reivindicação (persiste idempotency_key_hash, hash_version 'hmac-sha256-v1', active pepper_version, request_fingerprint, fingerprint_scheme 'rfc8785-hmac-sha256-v1', active fingerprint_key_version); 1 match = recomputa fingerprint com o fingerprint_scheme e a chave retida do Keyring 2 correspondente a fingerprint_key_version do registro persistido, comparando via crypto.timingSafeEqual (mesmo fingerprint = replay 200/409; fingerprint divergente = 409 IDEMPOTENCY_KEY_REUSE_CONFLICT; versão/esquema desconhecido = fail closed 500); >1 matches = fail closed (500 IDEMPOTENCY_KEYRING_COLLISION + alerta); keyring incompleto ou corrompido = fail closed (503 PRIVACY_KEYRING_UNAVAILABLE) antes de 0 significar novo."
    - "O Keyring 2 gera canonical JSON RFC 8785 domain-separated e computa HMAC-SHA-256 efêmero comparado via crypto.timingSafeEqual; o payload canônico e dados crus são descartados imediatamente da memória."
    - "Invariantes absolutos: a chave de idempotência crua (raw idempotency key) e o CPF cru NUNCA são persistidos na autoridade de idempotência."
    - "A reencriptação Cart -> Order gera nova DEK via KMS, novo nonce de 12 bytes e AAD canônica específica do snapshot (sem revision mutável), zerando buffers em memória; o ciphertext do Cart nunca é copiado byte a byte."
    - "O ciclo de vida de KEKs gerencia versões lógicas no KekVersionRegistry, aplicando política estrita de decrypt/rewrap-only para versões antigas, job de rewrap atômico para DEKs encapsuladas sem decifrar payload PII, e inventory gates bloqueando aposentadoria ou deleção de chaves com dependências ativas."
    - "Nenhum valor cru de CPF ou chave de criptografia é retido em cache de processo ou exposto em logs."
  artifacts:
    - path: apps/backend/src/modules/checkout-privacy/crypto/keyrings.ts
      provides: "Gerenciamento de versões de chaves HMAC, verificação de integridade e rotação de keyrings"
      exports: [LocatorKeyring, FingerprintKeyring, KeyringEvaluationResult]
    - path: apps/backend/src/modules/checkout-privacy/crypto/fingerprint-service.ts
      provides: "Geração canônica RFC 8785 e cálculo de fingerprint semântico"
      exports: [CheckoutFingerprintService]
    - path: apps/backend/src/modules/checkout-privacy/crypto/reencryption-service.ts
      provides: "Serviço de reencriptação transacional de CPF para snapshot de pedido"
      exports: [SnapshotReencryptionService]
    - path: apps/backend/src/modules/checkout-privacy/crypto/kek-lifecycle.ts
      provides: "Registro lógico de KEKs, política de rotação anual/emergencial e gates de inventário de deleção"
      exports: [KekVersionRegistry, KekLifecycleManager]
    - path: apps/backend/src/jobs/rewrap-protected-data.ts
      provides: "Job agendado para rewrap de DEKs com reEncrypt no KMS sem tocar nos dados cifrados"
      exports: [rewrapProtectedDataJob]
    - path: apps/backend/src/modules/checkout-privacy/__tests__/keyrings.unit.spec.ts
      provides: "Testes unitários da matriz de decisão de keyrings e rotação"
    - path: apps/backend/src/modules/checkout-privacy/__tests__/reencryption.unit.spec.ts
      provides: "Testes unitários de reencriptação de envelope com nova DEK e AAD canônica"
    - path: apps/backend/src/modules/checkout-privacy/__tests__/kek-lifecycle.unit.spec.ts
      provides: "Testes unitários do ciclo de vida de KEKs, rewrap de DEKs e inventory gates"
  key_links:
    - from: apps/backend/src/modules/checkout-privacy/service.ts
      to: apps/backend/src/modules/checkout-privacy/crypto/keyrings.ts
      via: "Avaliação de locator de idempotência e fingerprint no serviço do módulo"
      pattern: "evaluateLocatorKeyring|computeSemanticFingerprint"
    - from: apps/backend/src/modules/checkout-privacy/service.ts
      to: apps/backend/src/modules/checkout-privacy/crypto/reencryption-service.ts
      via: "Chamada de reencryptCartCpfForOrderSnapshot durante preparação de snapshot"
      pattern: "reencryptCartCpfForOrderSnapshot"
    - from: apps/backend/src/jobs/rewrap-protected-data.ts
      to: apps/backend/src/modules/checkout-privacy/crypto/kek-lifecycle.ts
      via: "Consulta a versões ativas e elegibilidade de rewrap"
      pattern: "KekVersionRegistry|reEncrypt"
---

<objective>
Implementar a infraestrutura de chaves e criptografia avançada da Phase 17: o sistema de double keyrings (Keyring 1: locator HMAC e Keyring 2: fingerprint semântico RFC 8785 com matriz de decisão R17-HR-10), o serviço de reencriptação com geração de nova DEK/nonce/AAD canônica para o snapshot pré-Order, e o ciclo de vida completo de KEKs (registro lógico de versões, política decrypt/rewrap-only para KEKs antigas, job de rewrap de DEKs e inventory gates contra deleção prematura).

Purpose: Atender CHK-05 e CHK-06, garantindo conformidade com D17-10, R17-HR-03 e R17-HR-10, evitando qualquer reutilização de nonces ou DEKs entre carrinho e pedido e prevenindo duplicação de requisições sob concorrência ou rotação de chaves.
Output: Módulos de keyrings, fingerprint canônico, reencriptação, ciclo de vida de KEKs, job de rewrap e testes unitários exaustivos.
</objective>

<execution_context>
Harness: Antigravity IDE
Orchestrator: Gemini 3.8 Flash
Subagents: Gemini 3.8 Flash — REQUIRED FOR EXECUTION
Effort: orchestrator-selected per work unit (low | medium | high)
Parallelization: false (serial execution)
Max active implementation subagents: 1
Auto-chain: false
Auto-advance: false
Execution Authorization: NOT AUTHORIZED. Approval of this plan specification confers HUMAN APPROVED — PASS on the PLAN artifact set only and DOES NOT authorize execution, Wave 0, Plan 17-01, or runtime code implementation. Execution requires a separate, explicit human execution authorization.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-authenticated-br-checkout-privacy/17-CONTEXT.md
@.planning/phases/17-authenticated-br-checkout-privacy/17-DECISION-ADJUDICATION.md
@.planning/phases/17-authenticated-br-checkout-privacy/17-PATTERNS.md
@.planning/phases/17-authenticated-br-checkout-privacy/17-VALIDATION.md
@apps/backend/src/modules/checkout-privacy/crypto/envelope-service.ts
@apps/backend/src/modules/store-idempotency/operations.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 17-03-01: Sistema de Double Keyrings (Locator HMAC & Sensitive Semantic Fingerprint Persistidos via StoreIdempotencyRecord)</name>
  <files>apps/backend/src/modules/store-idempotency/models/store-idempotency-record.ts, apps/backend/src/modules/store-idempotency/service.ts, apps/backend/src/modules/store-idempotency/operations.ts, apps/backend/src/modules/checkout-privacy/crypto/keyrings.ts, apps/backend/src/modules/checkout-privacy/crypto/fingerprint-service.ts, apps/backend/src/modules/checkout-privacy/__tests__/keyrings.unit.spec.ts, apps/backend/integration-tests/modules/checkout-privacy.postgres.spec.ts</files>
  <read_first>
    - 17-DECISION-ADJUDICATION.md seção R17-HR-10 (regras estritas de double keyring, avaliação em lote, matriz de decisão e fail-closed)
    - 17-PATTERNS.md seção 3.4 (Double Keyrings, Matriz de Decisão e Persistência de Idempotência)
    - apps/backend/src/modules/store-idempotency/models/store-idempotency-record.ts
    - apps/backend/src/modules/store-idempotency/service.ts
    - apps/backend/src/modules/store-idempotency/operations.ts (padrões de canonicalização de operações)
    - apps/backend/integration-tests/modules/checkout-privacy.postgres.spec.ts
  </read_first>
  <behavior>
    - "A autoridade de persistência do double keyring reutiliza e estende a entidade StoreIdempotencyRecord (store_idempotency_record), sem criar um segundo engine concorrente."
    - "Keyring 1 gera hashes HMAC-SHA-256 para locator com prefixo indicio:checkout-privacy:locator:v1|<operation>|<customer_id>|<cart_id>|<idempotency_key> sob o esquema estável hash_version ('hmac-sha256-v1') para todas as versões de chaves retidas (active + retained old pepper_version)."
    - "A consulta de autoridade utiliza WHERE operation = $op AND actor_scope_hash = $actor AND resource_scope_hash = $cart AND idempotency_key_hash IN ($candidate_hashes) FOR UPDATE."
    - "Se 0 matches com keyring completo: primeira reivindicação ({ outcome: 'first_claim', activePepperVersion, activeFingerprintKeyVersion }). Cria registro com idempotency_key_hash da chave ativa (pepper_version), hash_version = 'hmac-sha256-v1', request_fingerprint calculado com o esquema ativo (fingerprint_scheme = 'rfc8785-hmac-sha256-v1') e a versão ativa do Keyring 2 (fingerprint_key_version)."
    - "Se 1 match: lê fingerprint_scheme e fingerprint_key_version do registro persistido, recomputa request_fingerprint utilizando a chave retida do Keyring 2 correspondente e compara via crypto.timingSafeEqual. Mesmo fingerprint -> replay; fingerprint divergente -> 409 IDEMPOTENCY_KEY_REUSE_CONFLICT; versão de chave ou esquema desconhecido -> fail closed 500."
    - "Se >1 matches: lança erro 500 IDEMPOTENCY_KEYRING_COLLISION e emite alerta operacional."
    - "Se keyring incompleto, chave ausente ou corrompida: lança erro 503 PRIVACY_KEYRING_UNAVAILABLE antes de 0 poder significar novo."
    - "Keyring 2 gera representação JSON canônica RFC 8785 com chaves ordenadas lexicalmente e calcula HMAC-SHA-256 efêmero sob fingerprint_scheme ('rfc8785-hmac-sha256-v1'); payload canônico e dados crus são descartados imediatamente da memória."
    - "Invariantes absolutos: a chave de idempotência crua (raw idempotency key) e o CPF cru NUNCA são persistidos na autoridade de idempotência."
  </behavior>
  <action>
    1. Atualizar `StoreIdempotencyRecord` e migração correspondente (em 17-02-PLAN.md) para suportar a autoridade canônica do double keyring (preservando o schema existente sem introduzir fingerprint_version e sem reinterpretar hash_version):
       - `idempotency_key_hash`: Locator HMAC persistido (text)
       - `hash_version`: Locator hash scheme estável (text 'hmac-sha256-v1', não é key version)
       - `pepper_version`: Locator HMAC key version (integer >= 1, versão numérica da chave usada no locator)
       - `request_fingerprint`: Sensitive semantic fingerprint HMAC persistido (text)
       - `fingerprint_scheme`: Fingerprint algorithm + canonicalization scheme (text 'rfc8785-hmac-sha256-v1')
       - `fingerprint_key_version`: Sensitive fingerprint key version (integer >= 1, versão numérica da chave usada no fingerprint)
       - `actor_scope_hash` e `resource_scope_hash` (text)
    2. Criar `apps/backend/src/modules/checkout-privacy/crypto/keyrings.ts`:
       - Implementar `LocatorKeyringManager` gerenciando chaves ativas e retidas de Locator HMAC (`{ pepper_version: number, secret: string }`).
       - Implementar `generateCandidateLocators(operation, actorScope, resourceScope, rawKey, keyring)` gerando `idempotency_key_hash` para cada chave retida sob o esquema estável `hash_version` ('hmac-sha256-v1') e retornando lista de `{ pepper_version, idempotency_key_hash }`.
       - Implementar `FingerprintKeyringManager` gerenciando chaves ativas e retidas de fingerprint semântico sensível (`{ fingerprint_key_version: number, secret: string }`).
       - Implementar a matriz de decisão exata de R17-HR-10:
         - 0 matches: `first_claim` (requer validação de keyring completo prévia)
         - 1 match: recomputar e comparar fingerprint usando `fingerprint_scheme` e a chave correspondente a `fingerprint_key_version`
         - >1 matches: erro 500 `IDEMPOTENCY_KEYRING_COLLISION` + alerta
         - keyring incompleto/inválido: fail-closed 503 `PRIVACY_KEYRING_UNAVAILABLE` antes de qualquer query SQL
    3. Criar `apps/backend/src/modules/checkout-privacy/crypto/fingerprint-service.ts`:
       - Implementar `buildCanonicalCheckoutFingerprintDto()` formatando o payload com `schema: "indicio/store/checkout-fingerprint/v1/<operation>"`, ordenando arrays (`fields_present`, `receipt_digests`), e normalizando CPF apenas em memória.
       - Implementar `computeSemanticFingerprint(dto, secretKey, scheme)` retornando hex string (padrão 'rfc8785-hmac-sha256-v1').
       - Implementar `compareFingerprints(computed, stored)` usando `crypto.timingSafeEqual`.
    4. Atualizar `StoreIdempotencyService` em `apps/backend/src/modules/store-idempotency/service.ts`:
       - Adicionar suporte a consulta multi-candidato `IN ($candidates)` com lock transacional (`FOR UPDATE`).
       - Integrar avaliação do double keyring nos fluxos de checkout PATCH e POST validate.
    5. Criar testes unitários em `apps/backend/src/modules/checkout-privacy/__tests__/keyrings.unit.spec.ts`:
       - Testar geração de candidatos para múltiplas versões de chaves retidas (`pepper_version`).
       - Testar 4 ramos da matriz de decisão (0 matches, 1 match replay, 1 match conflito 409, >1 matches colisão 500).
       - Testar fail-closed 503 para keyring incompleto antes de consulta.
       - Testar canonicalização RFC 8785 e comparação constante no tempo (`timingSafeEqual`).
       - Testar que versão de chave de locator retida desconhecida ou versão de fingerprint desconhecida falha de forma fechada (fail closed 500).
    6. Estender a suite de testes PostgreSQL em `apps/backend/integration-tests/modules/checkout-privacy.postgres.spec.ts` com os 15 casos obrigatórios de persistência, consulta, concorrência e rotação de double-keyring (HR-24):
       1. First claim persiste active pepper_version
       2. Old locator criado com pepper_version=N continua casando após rotação para N+1
       3. New locator após rotação persiste pepper_version=N+1
       4. hash_version permanece exatamente o esquema estável do locator ('hmac-sha256-v1') e nunca é interpretado como versão de chave
       5. Old fingerprint_key_version permanece reproduzível (replayable) enquanto retido
       6. New fingerprint persiste current fingerprint_key_version
       7. fingerprint_scheme permanece o esquema de algoritmo/canonicalização estável ('rfc8785-hmac-sha256-v1')
       8. Versão de chave de locator retida desconhecida -> fail closed (500)
       9. fingerprint_key_version desconhecida -> fail closed (500)
       10. >1 matches de candidatos de locator -> IDEMPOTENCY_KEYRING_COLLISION (500)
       11. Keyring de locator incompleto/corrompido -> fail closed (503 PRIVACY_KEYRING_UNAVAILABLE) antes de 0 matches poder virar primeira reivindicação
       12. Concorrência de lookup/claim utiliza FOR UPDATE e impede primeira reivindicação duplicada/competitiva
       13. Chave de idempotência crua (raw Idempotency-Key) NUNCA é persistida
       14. CPF cru NUNCA é persistido
       15. Payload canônico de fingerprint NUNCA é persistido
  </action>
  <verify>
    <automated>cd apps/backend && npm run test:unit -- --runTestsByPath src/modules/checkout-privacy/__tests__/keyrings.unit.spec.ts && node scripts/run-disposable-postgres-tests.mjs -- npm run test:integration:modules -- --runTestsByPath integration-tests/modules/checkout-privacy.postgres.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Suíte unitária `keyrings.unit.spec.ts` passa 100% (testes de HMAC, canonicalização RFC 8785 e matriz de decisão em memória).
    - Suíte PostgreSQL `checkout-privacy.postgres.spec.ts` passa 100% no runner descartável, cobrindo os 15 casos obrigatórios de double-keyring persistence, consulta multi-candidato, rotação, locking FOR UPDATE e não-persistência de dados crus.
    - Zero persistência de raw idempotency key e zero persistência de CPF cru na autoridade de idempotência.
    - Teste de concorrência relacional confirma proteção contra double-submit sob FOR UPDATE sem criação de claims concorrentes.
  </acceptance_criteria>
  <done>Sistema de double keyrings com autoridade de persistência integrada em StoreIdempotencyRecord implementado e testado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 17-03-02: Serviço de Reencriptação Cart -> Order Snapshot com AAD Canônica</name>
  <files>apps/backend/src/modules/checkout-privacy/crypto/reencryption-service.ts, apps/backend/src/modules/checkout-privacy/service.ts, apps/backend/src/modules/checkout-privacy/__tests__/reencryption.unit.spec.ts</files>
  <read_first>
    - 17-DECISION-ADJUDICATION.md seções R17-HR-02 e R17-HR-03 (para requisitos de snapshot pré-Order e reencriptação com nova DEK/nonce/AAD)
    - 17-PATTERNS.md seção 3.3 e 3.10 (para AAD canônica sem revision mutável)
    - apps/backend/src/modules/checkout-privacy/crypto/envelope-service.ts
  </read_first>
  <behavior>
    - "A partir de um registro de carrinho ativo, decifra o CPF em buffer isolado via kmsProvider.decrypt({ ciphertextBlob, encryptionContext: cartEncryptionContext })."
    - "Gera nova DEK de 32 bytes via KMS especificando KmsEncryptionContext com authority_kind: 'order'."
    - "Cifra o CPF com AES-256-GCM usando nonce novo de 12 bytes e AAD canônica estrita do snapshot: indicio:checkout-privacy:aad:v1|<env>|order|<snapshot_id>|cpf|v1|<key_version> (sem data_revision mutável)."
    - "Zera todos os buffers intermediários (plaintext CPF, cart DEK, snapshot DEK) via .fill(0)."
    - "Retorna o novo envelope completo do snapshot sem copiar bytes do envelope anterior."
  </behavior>
  <action>
    Criar `apps/backend/src/modules/checkout-privacy/crypto/reencryption-service.ts`:
    1. Implementar `reencryptCartCpfForOrderSnapshot({ cartEnvelope, cartAadContext, snapshotId, kmsProvider })`.
    2. Decifrar DEK do carrinho com `kmsProvider.decrypt({ ciphertextBlob: cartEnvelope.cpf_wrapped_dek, encryptionContext: cartEncryptionContext })` e decifrar CPF com `EnvelopeEncryptionService.decrypt()`.
    3. Chamar `kmsProvider.generateDataKey()` com `encryptionContext` contendo:
       - `application: "indicio-ecommerce"`
       - `runtime_environment: getEnvironmentName()` (dinâmico, nunca hardcoded)
       - `domain: "checkout-privacy"`
       - `authority_kind: "order"`
       - `envelope_version: "v1"`
       - `aad_version: "v1"`
       - `logical_key_version: activeKeyVersion`
    4. Cifrar com nova DEK e AAD canônica do snapshot (`buildCanonicalAad({ authorityKind: 'order', authorityId: snapshotId, envelopeVersion: 'v1', keyVersion: activeKeyVersion })`).
    5. Limpar buffers com `buffer.fill(0)`.
    Integrar métodos no `CheckoutPrivacyModuleService` em `apps/backend/src/modules/checkout-privacy/service.ts`.
    Criar testes unitários em `apps/backend/src/modules/checkout-privacy/__tests__/reencryption.unit.spec.ts` validando que ciphertexts, nonces, tags e contextos KMS são completamente distintos entre o Cart e o Snapshot para o mesmo CPF.
  </action>
  <verify>
    <automated>cd apps/backend && npm run test:unit -- --runTestsByPath src/modules/checkout-privacy/__tests__/reencryption.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Suíte `reencryption.unit.spec.ts` passa 100%.
    - Prova inequívoca de que o snapshot usa novo nonce, nova DEK, nova AAD canônica e novo KmsEncryptionContext.
    - Decifragem do snapshot com a AAD ou EncryptionContext do carrinho falha, comprovando isolamento estrito de contexto.
  </acceptance_criteria>
  <done>Serviço de reencriptação para snapshots de pedido implementado e testado com rigor criptográfico.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 17-03-03: Ciclo de Vida de KEKs (Logical Registry, Decrypt/Rewrap Policy, Rewrap Job & Deletion Gates)</name>
  <files>apps/backend/src/modules/checkout-privacy/crypto/kek-lifecycle.ts, apps/backend/src/jobs/rewrap-protected-data.ts, apps/backend/src/modules/checkout-privacy/__tests__/kek-lifecycle.unit.spec.ts</files>
  <read_first>
    - 17-PATTERNS.md seção 3.3.4 (Ciclo de Vida de KEKs, Rotação e Rewrap de DEKs)
    - 17-PATTERNS.md seção 3.3.5 (Inventory e Destruction Gates de KEKs)
    - 17-DECISION-ADJUDICATION.md seção R17-HR-03
  </read_first>
  <behavior>
    - "KekVersionRegistry mapeia identificadores lógicos de KEK (ex: 'v1', 'v2') para ARNs ou aliases do KMS."
    - "A versão de KEK ativa é a única utilizada para gerar novas DEKs (generateDataKey); versões antigas registradas são estritamente restritas a decrypt e reEncrypt/rewrap."
    - "Versões de KEK não registradas ou desconhecidas falham imediatamente de forma fechada (fail closed)."
    - "O job rewrap-protected-data itera sobre registros com KEK antiga chamando kmsProvider.reEncrypt() especificando sourceEncryptionContext e destinationEncryptionContext para atualizar cpf_wrapped_dek e key_version sem decifrar o CPF ou expor a DEK em memória."
    - "KekLifecycleManager implementa canary de inventário que consulta o banco: se houver qualquer linha em protected_checkout_data (não expurgado) ou protected_order_snapshot referenciando a KEK antiga, a tentativa de transicionar para retired ou scheduleDeletion é bloqueada com KEK_ACTIVE_DEPENDENCY_DETECTED."
  </behavior>
  <action>
    1. Criar `apps/backend/src/modules/checkout-privacy/crypto/kek-lifecycle.ts`:
       - Definir `KekVersionMetadata` (`version`, `status: 'active' | 'decrypt_rewrap_only' | 'retired' | 'pending_deletion'`, `keyArn`, `createdAt`, `rotatedAt`).
       - Implementar `KekVersionRegistry`:
         - `getActiveKeyVersion()`: retorna versão com status 'active'.
         - `getKeyMetadata(version)`: retorna metadados ou lança `UNKNOWN_KEK_VERSION`.
         - `assertCanEncrypt(version)`: garante que status é 'active'.
         - `assertCanDecrypt(version)`: garante que status é 'active' ou 'decrypt_rewrap_only'.
         - `rotateActiveKey(newVersion, newKeyArn)`: transiciona a chave atual para 'decrypt_rewrap_only' e define a nova como 'active'.
         - `assertZeroDependencies(version, dependencyCounter)`: executa contagem de linhas ativas em banco; se > 0, lança exceção impeditiva.
    2. Criar `apps/backend/src/jobs/rewrap-protected-data.ts`:
       - Job agendado que busca envelopes onde `key_version != activeKeyVersion` e `lifecycle_state != 'purged'`.
       - Para cada registro, invoca `kmsProvider.reEncrypt({ ciphertextBlob: wrappedDek, sourceKeyId, destinationKeyId, sourceEncryptionContext, destinationEncryptionContext })` repassando o logical_key_version de origem e destino nos contextos.
       - Atualiza atomicamente `cpf_wrapped_dek` e `key_version` no banco sob lock de linha.
    3. Criar `apps/backend/src/modules/checkout-privacy/__tests__/kek-lifecycle.unit.spec.ts`:
       - Testar autorização de encriptação apenas para a chave ativa.
       - Testar decifragem permitida para versão ativa e versão em decrypt_rewrap_only.
       - Testar bloqueio de encriptação em versão em decrypt_rewrap_only.
       - Testar falha imediata para versão de KEK inexistente ou não registrada.
       - Testar rewrap de DEKs com source e destination EncryptionContext sem alteração de ciphertext de dados.
       - Testar bloqueio do inventory gate quando ainda existem linhas com a KEK antiga.
       - Testar liberação de aposentadoria após todas as DEKs serem migradas.
  </action>
  <verify>
    <automated>cd apps/backend && npm run test:unit -- --runTestsByPath src/modules/checkout-privacy/__tests__/kek-lifecycle.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Suíte `kek-lifecycle.unit.spec.ts` passa 100%.
    - Política de decrypt/rewrap-only é rigorosamente aplicada.
    - Tentativa de deletar KEK com dependências ativas é bloqueada com erro explícito.
  </acceptance_criteria>
  <done>Ciclo de vida de KEKs, rewrap de DEKs e inventory gates implementados e testados com sucesso.</done>
</task>

</tasks>

## Artifacts this phase produces
- Class: `LocatorKeyringManager`
- Class: `CheckoutFingerprintService`
- Class: `SnapshotReencryptionService`
- Class: `KekVersionRegistry`
- Class: `KekLifecycleManager`
- Job: `apps/backend/src/jobs/rewrap-protected-data.ts`
- Types: `LocatorKeyringEntry`, `KeyringEvaluationResult`, `CanonicalCheckoutFingerprintDto`, `KekVersionMetadata`
- Test: `apps/backend/src/modules/checkout-privacy/__tests__/keyrings.unit.spec.ts`
- Test: `apps/backend/src/modules/checkout-privacy/__tests__/reencryption.unit.spec.ts`
- Test: `apps/backend/src/modules/checkout-privacy/__tests__/kek-lifecycle.unit.spec.ts`
- Test: `apps/backend/integration-tests/modules/checkout-privacy.postgres.spec.ts` (casos de double-keyring persistence/rotation/concurrency)

## Phase 17 Execution Orchestration Policy
- **Harness:** Antigravity IDE
- **Orchestrator model:** Gemini 3.8 Flash
- **Subagent model:** Gemini 3.8 Flash (REQUIRED FOR EXECUTION)
- **Effort policy:** Dynamic `orchestrator-selected per work unit (low | medium | high)`; `recommended_default_effort: high`
- **Parallelization:** false
- **Max active implementation subagents:** 1
- **Auto-chain:** false
- **Auto-advance:** false
- **Plan Approval Gate Separation:** Human approval of Phase 17 PLAN at P17-PLAN-HR-01 does NOT authorize execution of Wave 0 or Plan 17-01.
- **Recommended subagent for Plan 17-03:** Perspective C (Cryptography Specialist)
