---
phase: 13-storefront-contract-foundation-surface-lockdown
plan: 03
type: execute
wave: 3
depends_on: [13-02]
files_modified:
  - apps/backend/src/api/store-surface/errors.ts
  - apps/backend/src/api/middlewares.ts
  - apps/backend/src/api/store-surface/__tests__/errors.unit.spec.ts
  - apps/backend/integration-tests/http/store-error-contract.spec.ts
autonomous: false
requirements: [FND-03]
must_haves:
  truths:
    - "Todo erro Store usa code/message/retryable, correlationId sanitizado e somente fieldErrors/snapshot allowlisted."
    - "message não é discriminador de lógica; auth/ownership não enumeram e unknown/provider não vazam detalhes."
    - "Admin e Webhooks mantêm seus contratos e Sentry recebe somente contexto scrubbed."
  artifacts:
    - path: apps/backend/src/api/store-surface/errors.ts
      provides: Catálogo e normalizador StoreErrorResponse
    - path: apps/backend/integration-tests/http/store-error-contract.spec.ts
      provides: Prova HTTP de status, correlação e isolamento
  key_links:
    - from: apps/backend/src/api/middlewares.ts
      to: apps/backend/src/api/store-surface/errors.ts
      via: branch Store no error handler, preservando delegação externa
      pattern: "StoreErrorResponse|normalizeStore"
---

<objective>
Fechar B13-06 com um contrato público de erro mínimo, estável, correlacionável e isolado à Store.

Purpose: implementar D13-08..D13-12 sem mudar Admin/Webhooks.
Output: catálogo/normalizador, integração Sentry/correlation e testes unit/HTTP.
</objective>

<execution_context>@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/13-storefront-contract-foundation-surface-lockdown/13-CONTEXT.md
@.planning/phases/13-storefront-contract-foundation-surface-lockdown/13-RESEARCH.md
@.planning/phases/13-storefront-contract-foundation-surface-lockdown/13-02-SUMMARY.md
@apps/backend/src/api/middlewares.ts
@apps/backend/src/observability/sentry-scrub.ts
</context>

## Dependencies

Lockdown 13-02 aprovado para que erros precoces de classificação/auth passem pelo envelope Store.

## Files expected to change

Somente os quatro paths do frontmatter.

## Files forbidden

Admin/Webhooks handlers, Sentry SDK/config, operações Phase 14–21, OpenAPI gerado, package/lockfile, DB/migrations.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Criar catálogo fechado e normalizador StoreErrorResponse</name>
  <files>apps/backend/src/api/store-surface/errors.ts, apps/backend/src/api/store-surface/__tests__/errors.unit.spec.ts</files>
  <read_first>apps/backend/src/api/middlewares.ts; apps/backend/src/api-docs/components/errors.ts; apps/backend/src/observability/sentry-scrub.ts</read_first>
  <action>Definir `StoreErrorResponse` com code, message, optional correlationId, retryable, optional fieldErrors e optional cart previsto apenas como snapshot seguro. Fixar code como lógica pública e message apenas como apresentação per D13-08; manter envelope allowlisted per D13-09. Mapear validation, auth, ownership, custom domain, Medusa native, provider/internal e unknown para status/códigos estáveis; impedir detalhes internos per D13-10 e enumeração per D13-12. Sanitizar correlation com o mesmo valor do middleware per D13-11. Não ecoar payload, marcar retryable sem certeza nem integrar snapshot Cart concreto da Phase 15.</action>
  <verify><automated>cd apps/backend &amp;&amp; npm run test:unit -- --runTestsByPath src/api/store-surface/__tests__/errors.unit.spec.ts --runInBand</automated></verify>
  <acceptance_criteria>Catálogo cobre status 400/401/403-or-404/409/412/422/429/500/503 e unknown/provider vira erro genérico sem dados internos.</acceptance_criteria>
  <done>Primitive de erro estável implementado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrar erro Store, correlação e Sentry sem afetar outras superfícies</name>
  <files>apps/backend/src/api/middlewares.ts, apps/backend/integration-tests/http/store-error-contract.spec.ts</files>
  <read_first>normalizador criado; createSentryErrorHandler; correlation middleware; testes HTTP Admin/Webhooks existentes</read_first>
  <action>No error handler existente, normalizar somente requests Store após construir o contexto Sentry sanitizado; manter delegação Medusa atual para Admin/Webhooks. Garantir header/body correlation iguais, substituir inputs fora de `[A-Za-z0-9._-]{1,128}`, e testar erros nativos, auth, validation, domain, provider-shaped e unknown. Provar que stack, raw internal error, identifiers sensíveis e ownership state não saem em HTTP/log/Sentry; usar canaries sintéticas.</action>
  <verify><automated>cd apps/backend &amp;&amp; npm run test:integration:http -- --runTestsByPath integration-tests/http/store-error-contract.spec.ts integration-tests/http/api-docs-runtime-webhooks-header-override.spec.ts --runInBand</automated></verify>
  <acceptance_criteria>Store sempre usa envelope fechado; Admin/Webhooks permanecem compatíveis; correlação é única e sanitizada.</acceptance_criteria>
  <done>FND-03 e B13-06 possuem prova behavior-level.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Revisar catálogo público e isolamento</name>
  <files>None (review-only checkpoint)</files>
  <read_first>13-03-SUMMARY.md e matriz status/code</read_first>
  <action>Revisar codes/status/retryable, não enumeração, campo cart apenas opcional e ausência de mudança em Admin/Webhooks.</action>
  <verify><automated>git diff --check</automated></verify>
  <acceptance_criteria>Aprovação humana explícita.</acceptance_criteria>
  <done>Manual gate D13-32 concluído.</done>
</task>
</tasks>

## Tests / evidence

Unit do catálogo e HTTP de todas as famílias, incluindo Admin/Webhooks regressão.

## Security negatives

Canaries: raw Idempotency-Key, JWT, capability, confirmation token, CPF, client_secret, Pix payload, provider ID/payload, stack e raw error ausentes de response/log/Sentry.

## Migration implications

Nenhuma.

## Stop conditions

BLOCKED por envelope nativo escapando, correlation divergente/inválida, enumeração, retryable incerto, vazamento ou regressão Admin/Webhooks.

## Rollback / recovery

Reverter branch Store do handler e o catálogo juntos; preservar capture/scrub existente.

## Manual gate

Parar após SUMMARY; 13-04 exige aprovação.

## Artifacts this phase produces

- StoreErrorResponse runtime e catálogo status/code.
- Evidência de correlation/redaction/isolation.

<threat_model>
| Threat | Disposition | Mitigation |
|---|---|---|
| Information disclosure | mitigate | allowlist + generic unknown/provider |
| Enumeration | mitigate | respostas ownership indistinguíveis |
| Correlation injection | mitigate | regex/tamanho + UUID substituto |
| Supply chain | accept | nenhum install |
</threat_model>
<verification>Suites unit/HTTP passam e canaries não aparecem nos sinks.</verification>
<success_criteria>B13-06 eliminado sem alterar contratos Admin/Webhooks.</success_criteria>
<output>Create `13-03-SUMMARY.md` e pare.</output>
