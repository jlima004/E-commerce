---
phase: 13-storefront-contract-foundation-surface-lockdown
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - apps/backend/src/api/store-surface/guard.ts
  - apps/backend/src/api/middlewares.ts
  - apps/backend/src/api/store/carts/[id]/complete/route.ts
  - apps/backend/src/api/store-surface/__tests__/guard.unit.spec.ts
  - apps/backend/integration-tests/http/store-surface-lockdown.spec.ts
  - "<legacy-test-exact-set: no máximo 4 arquivos existentes, inventariados e registrados no 13-02-SUMMARY.md antes da primeira edição>"
autonomous: false
requirements: [FND-01, FND-02, FND-07]
must_haves:
  truths:
    - "UNKNOWN, BLOCKED/DENY e toda entrada com runtime_policy DENY são negadas antes do handler."
    - "PRESERVE_LEGACY alcança somente o comportamento legado explicitamente preservado, sem enablement M1 nem publicação como executable M1 OpenAPI."
    - "EXTENDED e OUTSIDE_FRONTEND_M1 continuam M1 disabled independentemente de sua runtime_policy; M1_ENABLED permanece 0 nesta phase."
    - "POST /store/carts/{id}/complete e GET /store/custom não chegam ao handler nem criam Order."
    - "HEAD, OPTIONS, aliases, paths codificados e métodos alternativos não criam bypass; preflight é deliberado."
    - "Todo teste legado afetado é inventariado antes da mudança como OBSOLETE_CONTRACT_EXPECTATION, STILL_VALID_INTERNAL_INVARIANT ou MUST_REMAIN_GREEN, sem perda de cobertura de domínio."
  artifacts:
    - path: apps/backend/src/api/store-surface/guard.ts
      provides: Guard global manifest-driven fail-closed
    - path: apps/backend/integration-tests/http/store-surface-lockdown.spec.ts
      provides: Matriz HTTP de negação
  key_links:
    - from: apps/backend/src/api/middlewares.ts
      to: apps/backend/src/api/store-surface/manifest.ts
      via: matcher global Store anterior aos matchers específicos
      pattern: "storeSurface"
---

<objective>
Aplicar o lockdown Store fail-closed e eliminar B13-01, B13-02 e B13-03 no boundary HTTP.

Purpose: fazer a classificação virar enforcement antes de qualquer phase proprietária, per D13-01..D13-07.
Output: guard global, defesa pontual do native complete e matriz HTTP negativa.
</objective>

<execution_context>@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/13-storefront-contract-foundation-surface-lockdown/13-CONTEXT.md
@.planning/phases/13-storefront-contract-foundation-surface-lockdown/13-RESEARCH.md
@.planning/phases/13-storefront-contract-foundation-surface-lockdown/13-01-SUMMARY.md
@apps/backend/src/api/middlewares.ts
</context>

## Dependencies

13-01 PASS humano e manifest exact-set. Nenhuma operação EXTENDED é habilitada neste plano.

## Files expected to change

Os cinco paths fixos do frontmatter e, somente após a Task 1, o exact-set fechado
de no máximo 4 arquivos de teste existentes comprovadamente afetados pelo
lockdown. O limite conta paths únicos; um mesmo arquivo pode possuir vários
casos inventariados em linhas distintas. O executor registra no
`13-02-SUMMARY.md`, **antes da primeira edição legada**, cada path,
case/describe/test, family, classificação A/B/C, motivo e replacement de
coverage quando aplicável. O marcador do frontmatter não é glob nem autorização
para descoberta aberta durante a edição.

## Files forbidden

Handlers finais das Phases 14–21, core Cart, CheckoutCompletionLog, webhook Stripe, migrations, OpenAPI gerado, package/lockfile.

<tasks>
<task type="auto">
  <name>Task 1: Inventariar impacto legado e fechar a transição contratual</name>
  <files>13-02-SUMMARY.md (seção de inventário pré-mudança; nenhum teste legado é editado nesta task)</files>
  <read_first>manifest 58/58 aprovado; suites Store/checkout/payment/API Docs/invariants existentes que referenciem operações agora BLOCKED, OUTSIDE_FRONTEND_M1 ou EXTENDED disabled</read_first>
  <action>Antes de tocar qualquer teste legado, criar em `13-02-SUMMARY.md` a seção `## Legacy test impact inventory (pre-change)` com `exact_set_status: LOCKED_BEFORE_EDIT` e tabela `Path | Case/describe/test | Family | Class | Reason | Coverage replacement`. Produzir duas identidades: exact-set de arquivos e inventário de casos. Permitir várias linhas para o mesmo path, exigindo case identifier não vazio e único dentro daquele arquivo. Classificar cada caso afetado como `A — OBSOLETE_CONTRACT_EXPECTATION` quando sua única expectativa for sucesso público de rota agora negada, `B — STILL_VALID_INTERNAL_INVARIANT` quando a rota pública mudar mas a lógica de domínio continuar obrigatória, ou `C — MUST_REMAIN_GREEN` quando não contradizer o contrato novo. `Family` é exatamente `unit`, `http` ou `modules` e determina o runner; todas as linhas do mesmo path devem usar a mesma family. Para A, planejar a substituição pela expectativa fail-closed. Para B, exigir replacement não vazio e registrar a boundary interna/unit/module equivalente que preserva integralmente cart creation, money derivation, serializer safety, Order invariant ou outro comportamento factual. Para C, proibir alteração por conveniência. Registrar razão e replacement de coverage antes da edição. Contar `unique(paths)` e exigir 1..4; se exceder 4 paths únicos, incluir path fora das suites Store/checkout/payment/API Docs/invariants diretamente afetadas, repetir case identifier dentro do path, divergir family no mesmo path ou não houver replacement fechado para algum B, parar `13-02: BLOCKED` e retornar à revisão humana.</action>
  <verify><automated>node -e "const fs=require('fs'),cp=require('child_process');const p='.planning/phases/13-storefront-contract-foundation-surface-lockdown/13-02-SUMMARY.md';const s=fs.readFileSync(p,'utf8');if(!s.includes('## Legacy test impact inventory (pre-change)')||!s.includes('exact_set_status: LOCKED_BEFORE_EDIT'))process.exit(1);const rows=[...s.matchAll(/^\| `(apps\/backend\/[^`]+\.spec\.ts)` \| `([^`]+)` \| `(unit|http|modules)` \| `(A|B|C) — [^`]+` \| ([^|]+) \| ([^|]+) \|$/gm)].map(m=&gt;({path:m[1],caseId:m[2],family:m[3],klass:m[4],replacement:m[6].trim()}));const paths=[...new Set(rows.map(r=&gt;r.path))];if(!rows.length||paths.length&lt;1||paths.length&gt;4)process.exit(1);if(new Set(rows.map(r=&gt;r.path+'\u0000'+r.caseId)).size!==rows.length)process.exit(1);for(const path of paths){if(new Set(rows.filter(r=&gt;r.path===path).map(r=&gt;r.family)).size!==1)process.exit(1)}if(rows.some(r=&gt;r.klass==='B'&amp;&amp;(!r.replacement||r.replacement==='-')))process.exit(1);const changed=new Set(cp.execFileSync('git',['diff','--name-only'],{encoding:'utf8'}).trim().split(/\n/).filter(Boolean));if(paths.some(path=&gt;changed.has(path)))process.exit(1)"</automated></verify>
  <acceptance_criteria>Cada teste afetado possui path/caso/classificação/motivo/replacement fechado; nenhum glob, `.skip`, `.only`, deleção ou relaxamento de assertion é autorizado.</acceptance_criteria>
  <done>Escopo legado fechado e transição controlada autorizável sem reduzir cobertura.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar guard global method/path-aware, defesa de native complete e transição legada aprovada</name>
  <files>apps/backend/src/api/store-surface/guard.ts, apps/backend/src/api/middlewares.ts, apps/backend/src/api/store/carts/[id]/complete/route.ts, apps/backend/src/api/store-surface/__tests__/guard.unit.spec.ts, exact-set legado registrado na Task 1</files>
  <read_first>apps/backend/src/api/store-surface/manifest.ts; apps/backend/src/api/middlewares.ts; Medusa 2.16.0 router/routes-sorter; native carts/[id]/complete/route.js</read_first>
  <action>Registrar matcher global `/store*` antes dos matchers específicos. Resolver method + template sem inferir HEAD a partir de GET; normalizar apenas a forma canônica, negar double/trailing slash ambígua, encoded separator, alias, unknown method/path e static-vs-parameter mismatch. OPTIONS só passa como preflight sintético estritamente validado e nunca chama handler de negócio. O fluxo é: lookup ausente/UNKNOWN → DENY; classification BLOCKED → DENY; runtime_policy DENY → DENY; runtime_policy PRESERVE_LEGACY → somente o handler/comportamento v1.0 explicitamente aceito, sem novo guard de owner phase, auth/capability ampliada, M1 enablement ou publicação executable OpenAPI; runtime_policy M1_ENABLED → guards M1 específicos, mas esse ramo permanece sem entradas nesta Phase 13. `classification` EXTENDED ou OUTSIDE_FRONTEND_M1 nunca habilita M1 por si. Criar override/disable local pontual de POST complete que nega mesmo se a ordem global regredir. Preservar contexto nativo CORS/publishable/auth, mas nunca tratá-los como autorização BFF, per D13-01 e D13-02. Aplicar fail-closed/classificação per D13-03 e D13-04, tratar bypass como blocker per D13-05, impedir Order per D13-06 e exigir invariant parity para qualquer reuso per D13-07. Editar somente o exact-set legado aprovado: A passa a provar fail-closed; B mantém ou move a prova para boundary interna equivalente; C permanece intocado. É proibido deletar suites, usar `.skip`/`.only`, relaxar assertion sem justificativa ou aprovar snapshot para esconder falha. No mesmo task, exercitar as 58 entradas e canaries de method mismatch, HEAD, OPTIONS, trailing/double slash, encoded path, unknown/alias e static-vs-parameter. Provar separadamente: BLOCKED/DENY e UNKNOWN param antes do handler; PRESERVE_LEGACY continua com o comportamento legado esperado mas não ganha enablement M1 nem aparece no executable M1 OpenAPI; M1_ENABLED só será testado quando uma phase proprietária o materializar. Provar handler/workflow/Order zero para complete/custom/payment/shipping/order-transfer e toda entrada DENY. Rodar todas as suites do exact-set uma única vez por arquivo/family e a seleção fechada de invariants: `invariants-inv01-02-order-birth.spec.ts`, `money-units.unit.spec.ts` e toda replacement boundary declarada para B. A evidência mostra A atualizado, B preservado e C verde sem alteração.</action>
  <verify>
    <automated>cd apps/backend &amp;&amp; npm run test:unit -- --runTestsByPath src/api/store-surface/__tests__/guard.unit.spec.ts src/utils/__tests__/money-units.unit.spec.ts --runInBand</automated>
    <automated>cd apps/backend &amp;&amp; npm run test:integration:http -- --runTestsByPath integration-tests/http/store-surface-lockdown.spec.ts integration-tests/http/invariants-inv01-02-order-birth.spec.ts --runInBand</automated>
    <automated>node -e "const fs=require('fs'),{spawnSync}=require('child_process');const s=fs.readFileSync('.planning/phases/13-storefront-contract-foundation-surface-lockdown/13-02-SUMMARY.md','utf8');const rows=[...s.matchAll(/^\| `(apps\/backend\/[^`]+\.spec\.ts)` \| `([^`]+)` \| `(unit|http|modules)` \| `(A|B|C) — [^`]+` \| ([^|]+) \| ([^|]+) \|$/gm)].map(m=&gt;({path:m[1].replace(/^apps\/backend\//,''),family:m[3]}));const unique=[...new Map(rows.map(r=&gt;[r.family+'\u0000'+r.path,r])).values()];const uniquePaths=new Set(unique.map(r=&gt;r.path));if(!rows.length||uniquePaths.size&lt;1||uniquePaths.size&gt;4)process.exit(1);for(const [family,script] of Object.entries({unit:'test:unit',http:'test:integration:http',modules:'test:integration:modules'})){const paths=unique.filter(r=&gt;r.family===family).map(r=&gt;r.path);if(!paths.length)continue;const x=spawnSync('npm',['run',script,'--','--runTestsByPath',...paths,'--runInBand'],{cwd:'apps/backend',stdio:'inherit'});if(x.status!==0)process.exit(x.status??1)}"</automated>
  </verify>
  <acceptance_criteria>Matriz cobre 58/58 e variantes; toda entrada DENY para antes do handler; PRESERVE_LEGACY continua funcional apenas no limite herdado e permanece fora do M1/OpenAPI executable; M1_ENABLED=0; `/complete` e `/store/custom` não executam; nenhuma Store cria Order; inventário por caso suporta múltiplas linhas por arquivo, roda cada path uma vez e preserva A/B/C e invariants fechados.</acceptance_criteria>
  <done>Lockdown, transição legada e FND-02 possuem prova behavior-level sem perda de coverage.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Revisar lockdown, transição legada e ausência de abertura EXTENDED</name>
  <files>None (review-only checkpoint)</files>
  <read_first>13-02-SUMMARY.md, diff e relatório da matriz</read_first>
  <action>Confirmar 0 AUTHORIZED, 10 EXTENDED disabled, 17 BLOCKED, 31 OUTSIDE, zero invocação do completion path e inventário A/B/C completo sem deleção ou perda de invariant.</action>
  <verify><automated>git diff --check</automated></verify>
  <acceptance_criteria>Aprovação humana explícita; qualquer route drift ou handler atingido mantém BLOCKED.</acceptance_criteria>
  <done>Manual gate D13-32 concluído.</done>
</task>
</tasks>

## Tests / evidence

Unit de decisão, HTTP integration completa, todas as suites legadas do exact-set
e seleção fechada de invariants não relacionados, com spy de handler/workflow e
Order count zero.

## Security negatives

Browser direto não é autorizado por CORS; respostas não enumeram recurso; método/path inválido não cai em handler; sem JWT/capability/provider ID em evidência.

## Migration implications

Nenhuma.

## Stop conditions

BLOCKED por drift, rota desconhecida, complete/custom executável, entrada DENY
passando, PRESERVE_LEGACY ganhando M1 enablement/guard/contrato ou perdendo o
comportamento herdado aprovado, OPTIONS alcançando negócio, qualquer
Order/complete invocation por Store, exact-set legado acima de 4 paths únicos
ou fora dos domínios fechados, case identifier duplicado dentro do path, item B
sem replacement equivalente, invocação Jest duplicada pelo mesmo arquivo,
deleção/skip/only ou regressão C.

## Rollback / recovery

Reverter guard/override e matcher como unidade; nunca remover o manifest aprovado. Se o override factual conflitar com loader 2.16.0, manter o guard e marcar BLOCKED para nova decisão, sem monkey patch.

## Manual gate

Parar após SUMMARY; 13-03 exige aprovação.

## Artifacts this phase produces

- Guard global Store fail-closed.
- Defesa em profundidade do native complete.
- Matriz HTTP negativa versionada.

<threat_model>
| Threat | Disposition | Mitigation |
|---|---|---|
| Native/alias bypass | mitigate | guard method+template + override complete |
| OPTIONS/HEAD bypass | mitigate | tratamento explícito sem inferência |
| Direct browser | mitigate | BFF-only contract; CORS não concede autorização |
| Supply chain | accept | nenhuma instalação |
</threat_model>
<verification>Unit e HTTP passam com zero handler/workflow/Order nas negativas.</verification>
<success_criteria>B13-01, B13-02 e B13-03 deixam de ser executáveis no runtime.</success_criteria>
<output>Create `13-02-SUMMARY.md` e pare.</output>
