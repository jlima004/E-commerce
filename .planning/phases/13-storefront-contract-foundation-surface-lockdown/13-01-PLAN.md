---
phase: 13-storefront-contract-foundation-surface-lockdown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/api/store-surface/manifest.ts
  - apps/backend/scripts/store-surface/scan-installed.ts
  - apps/backend/src/api/store-surface/__tests__/manifest.unit.spec.ts
  - apps/backend/src/infrastructure/store-foundation-transaction-compatibility.ts
  - apps/backend/src/modules/checkout/__tests__/store-foundation-transaction-compatibility.spec.ts
autonomous: false
requirements: [FND-01, FND-06]
must_haves:
  truths:
    - "A superfície Medusa 2.16.0 possui exatamente 58 operações classificadas, sem duplicatas nem desconhecidas, na distribuição 0/10/17/31."
    - "Cada uma das 58 entradas separa classification de runtime_policy e possui decisão factual individual com rationale; nenhuma combinação é inferida apenas pela classe."
    - "EXTENDED e OUTSIDE_FRONTEND_M1 permanecem sem exposição M1, mas cada rota declara DENY ou PRESERVE_LEGACY; BLOCKED declara sempre DENY e UNKNOWN continua ausente/negado."
    - "A Phase 13 possui 0 M1_ENABLED e não promove nenhuma EXTENDED; PRESERVE_LEGACY mantém somente o comportamento herdado explicitamente aceito, sem autorização M1."
    - "Uma mutação Medusa controlada e a atualização de versão compartilham um único transaction manager PostgreSQL e commit/rollback atômico."
    - "Dois CAS concorrentes com a mesma versão esperada produzem exatamente um vencedor, mesmo sem Redis disponível."
  artifacts:
    - path: apps/backend/src/api/store-surface/manifest.ts
      provides: Manifest único e versionado das 58 operações Store com classification, runtime_policy e enablement M1 independentes
    - path: apps/backend/src/modules/checkout/__tests__/store-foundation-transaction-compatibility.spec.ts
      provides: Wave 0 binária de transação compartilhada e CAS
  key_links:
    - from: apps/backend/scripts/store-surface/scan-installed.ts
      to: apps/backend/src/api/store-surface/manifest.ts
      via: comparação exact-set método + template + Medusa 2.16.0
      pattern: "58|2\\.16\\.0"
---

<objective>
Materializar o contrato único da superfície Store e executar, como Wave 0 lógica, a prova binária de atomicidade que condiciona toda persistência concorrente posterior.

Purpose: eliminar a incerteza mais destrutiva cedo, per D13-03, D13-04, D13-24, D13-30 e D13-31.
Output: manifest/scanner 58/58 e prova PostgreSQL de transaction manager compartilhado, rollback e CAS.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-storefront-contract-foundation-surface-lockdown/13-CONTEXT.md
@.planning/phases/13-storefront-contract-foundation-surface-lockdown/13-RESEARCH.md
@.planning/phases/13-storefront-contract-foundation-surface-lockdown/13-VALIDATION.md
@apps/backend/src/api-docs/coverage/native-routes.ts
@apps/backend/integration-tests/postgres/disposable-postgres-harness.ts
</context>

## Dependencies

- Primeira wave GSD e “Wave 0” lógica; nenhum plano dependente pode iniciar antes do PASS humano deste plano.
- Reutiliza Medusa/Jest/PostgreSQL descartável existentes; nenhum pacote, lockfile, DB remoto, Redis real ou provider.

## Files expected to change

Somente os cinco paths do frontmatter.

## Files forbidden

`package.json`, `package-lock.json`, `apps/backend/medusa-config.ts`, migrations de domínio, `CheckoutCompletionLog`, core Cart, JSON OpenAPI e qualquer arquivo de Phase 14+.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fixar manifest único e scanner exact-set da superfície Store</name>
  <files>apps/backend/src/api/store-surface/manifest.ts, apps/backend/scripts/store-surface/scan-installed.ts, apps/backend/src/api/store-surface/__tests__/manifest.unit.spec.ts</files>
  <read_first>13-RESEARCH.md seção 5; apps/backend/src/api-docs/coverage/native-routes.ts; apps/backend/src/api-docs/coverage/discover-routes.ts; node_modules/@medusajs/medusa/dist/api/store</read_first>
  <action>Criar um manifest TypeScript fechado com method, path template, source/origin, Medusa version, classification, runtime_policy, reason/rationale, owner phase/domain, OpenAPI M1 expectation e explicit M1 enablement state para cada uma das 58 operações, per D13-03 e D13-04. `classification` admite AUTHORIZED, EXTENDED, BLOCKED e OUTSIDE_FRONTEND_M1; `runtime_policy` admite DENY, PRESERVE_LEGACY e M1_ENABLED. Codificar exatamente AUTHORIZED=0, EXTENDED=10, BLOCKED=17 e OUTSIDE_FRONTEND_M1=31, com M1 enabled=0. BLOCKED combina obrigatoriamente com DENY. UNKNOWN não é entrada válida e qualquer lookup ausente nega/falha drift. Cada EXTENDED disabled e cada OUTSIDE_FRONTEND_M1 recebe individualmente DENY ou PRESERVE_LEGACY com rationale factual; é proibido inferir uma política universal a partir da classe. PRESERVE_LEGACY significa apenas manter o comportamento v1.0 explicitamente aceito, sem ampliar auth/capability, sem aplicar genericamente guards específicos das Phases 14–21, sem M1 enablement e sem operação executable no OpenAPI M1. Se uma rota não puder ser preservada nem negada com segurança sem decisão de produto, parar `13-01: BLOCKED` para revisão humana; não decidir ad hoc em 13-02. O scanner read-only extrai as 51 operações nativas instaladas e as 7 locais não sobrepostas, compara exact-set, rejeita duplicata, mudança de versão, rota desconhecida, classificação/política/rationale ausente ou combinação inválida. Manifest, runtime guard futuro, runtime compatibility policy, M1 exposure, OpenAPI M1 coverage, drift e matriz negativa devem consumir a mesma exportação; não criar lista paralela. Tratar fatos as-built apenas como evidência per D13-30 e falhar closed em incerteza per D13-31.</action>
  <verify>
    <automated>cd apps/backend &amp;&amp; npm run test:unit -- --runTestsByPath src/api/store-surface/__tests__/manifest.unit.spec.ts --runInBand</automated>
    <automated>cd apps/backend &amp;&amp; ./node_modules/.bin/ts-node --swc scripts/store-surface/scan-installed.ts --check</automated>
  </verify>
  <acceptance_criteria>Scanner e teste provam 58/58, 0 desconhecidas, 0 duplicatas, 0/10/17/31, M1_ENABLED=0 e 58 decisões classification+runtime_policy+rationale; contagens DENY+PRESERVE_LEGACY somam 58, BLOCKED é sempre DENY e EXTENDED/OUTSIDE permanecem M1 disabled mesmo quando PRESERVE_LEGACY.</acceptance_criteria>
  <done>B13-02 possui fonte única materializada e FND-01 tem prova exact-set versionada.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Executar Wave 0 binária de transação compartilhada e CAS</name>
  <files>apps/backend/src/infrastructure/store-foundation-transaction-compatibility.ts, apps/backend/src/modules/checkout/__tests__/store-foundation-transaction-compatibility.spec.ts</files>
  <read_first>apps/backend/integration-tests/postgres/disposable-postgres-harness.ts; apps/backend/src/modules/checkout-completion/__tests__/checkout-completion-log.postgres.spec.ts; tipos sharedContext/transaction manager Medusa 2.16.0</read_first>
  <action>Construir um adapter estreito e uma prova PostgreSQL descartável que criem apenas uma tabela de probe no banco temporário, sem migration de produto. Dentro do mesmo manager, executar uma mutação Medusa controlada e um CAS de versão; provar commit conjunto, injetar erro após a primeira parte e provar rollback de ambas, e disputar dois writers com expected version igual para obter um commit e um conflito. Repetir com Locking Redis ausente/falhando e provar que PostgreSQL continua decidindo unicidade/versão, per D13-18 e D13-24. A prova deve observar identidade real do transaction manager; duas transações correlacionadas ou dois commits são FAIL.</action>
  <verify>
    <automated>cd apps/backend &amp;&amp; node scripts/run-disposable-postgres-tests.mjs -- npm run test:integration:modules -- --runTestsByPath src/modules/checkout/__tests__/store-foundation-transaction-compatibility.spec.ts --runInBand</automated>
  </verify>
  <acceptance_criteria>PASS somente se mutação + versão commitam/rollbackam juntas e o CAS concorrente tem exatamente um vencedor sem depender de Redis.</acceptance_criteria>
  <done>B13-05 possui feasibility proof; FAIL bloqueia 13-04, 13-05 e dependentes, sem workaround de dois commits.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Revisar o resultado binário da Wave 0</name>
  <files>None (review-only checkpoint)</files>
  <read_first>13-01-SUMMARY.md e evidência sanitizada dos dois tasks</read_first>
  <action>Apresentar contagens do manifest, diff permitido, identidade do transaction manager, cenários commit/rollback/CAS e ausência de dependência Redis. Não avançar sob evidência parcial.</action>
  <verify><automated>git diff --check</automated></verify>
  <acceptance_criteria>Humano registra PASS ou BLOCKED; somente PASS libera 13-02.</acceptance_criteria>
  <done>Manual gate D13-32 concluído.</done>
</task>

</tasks>

## Tests / evidence

Exact-set do scanner, teste unitário do manifest e suite PostgreSQL descartável com commit, rollback, dois writers e Redis failure.

## Security negatives

Nenhum secret/DSN/provider payload em logs/fixtures; nenhum acesso externo; scanner não executa handlers; manifest não contém credenciais.

## Migration implications

Nenhuma migration de produto neste plano. A tabela de probe existe apenas no banco descartável e é removida pelo harness.

## Stop conditions

BLOCKED por divergência 58/58, distribuição diferente, rota desconhecida, entrada sem runtime_policy/rationale, inferência `OUTSIDE_FRONTEND_M1 => DENY`, combinação inválida, rota sem decisão factual segura, qualquer M1_ENABLED nesta phase, manager não compartilhado, estado parcial, dois CAS vencedores ou dependência de Redis. Não adotar atomicidade eventual/two-commit.

## Rollback / recovery

Remover somente os cinco artefatos novos; cleanup do banco/container pelo harness exato, nunca por glob. Nenhum dado persistente requer rollback.

## Manual gate

Parar após `13-01-SUMMARY.md`; revisão humana obrigatória antes de 13-02.

## Artifacts this phase produces

- Manifest Store 2.16.0 com 58 entradas, semântica dual classification/runtime_policy e scanner de drift.
- Prova Wave 0 binária reutilizável para StoreResourceVersion.

<threat_model>
| Threat | Disposition | Mitigation |
|---|---|---|
| UNKNOWN route / method alias | mitigate | exact-set + classificação obrigatória |
| Lost update / partial commit | mitigate | transaction identity + rollback + CAS real |
| Redis treated as truth | mitigate | suíte passa com Redis indisponível |
| Supply chain | accept | nenhum install/package change |
</threat_model>

<verification>Os dois gates automatizados passam e a revisão humana aceita explicitamente o resultado binário.</verification>
<success_criteria>FND-01 está implementável e a premissa transacional de FND-06 está PROVEN, não inferida.</success_criteria>
<output>Create `13-01-SUMMARY.md` e pare para revisão humana.</output>
