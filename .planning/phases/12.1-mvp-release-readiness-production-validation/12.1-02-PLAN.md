---
phase: 12.1-mvp-release-readiness-production-validation
plan: "02"
type: execute
wave: 2
depends_on: [12.1-01]
files_modified:
  - .planning/phases/12.1-mvp-release-readiness-production-validation/12.1-02-SUMMARY.md
autonomous: false
requirements: [SETUP-01, SETUP-02, SETUP-04, SETUP-05, OBS-03]
must_haves:
  truths:
    - "O preflight consulta produção somente após autorização humana específica."
    - "A identidade Gelato aplicada é decidida factualmente antes de qualquer deploy."
    - "Config é verificada somente por nome/presença e a migration URL não usa 6543."
    - "O contrato estruturado P12_RELEASE_CONTRACT_V1 identifica a release atual pré-deploy como rollback target, a release anterior elegível como fallback histórico e a compatibilidade de schema do rollback target até o candidate SHA."
    - "O resultado é exatamente GO ou BLOCKED."
  artifacts:
    - path: ".planning/phases/12.1-mvp-release-readiness-production-validation/12.1-02-SUMMARY.md"
      provides: "Preflight saneado, risco Gelato e decisão GO/BLOCKED"
  key_links:
    - from: "public.mikro_orm_migrations"
      to: "apps/backend/src/modules/gelato-fulfillment/migrations/Migration20260703000000.ts"
      via: "comparação de identity names"
      pattern: "Migration(TBDGelatoFulfillment|20260703000000)"
---

<objective>
Produzir o go/no-go operacional antes de qualquer deploy.

Purpose: resolver de forma read-only o risco de migrations, configuração nominal e rollback eligibility.
Output: `12.1-02-SUMMARY.md` com evidência `production read-only`, `Heroku release` e `inferred only`.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-CONTEXT.md
@.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-RESEARCH.md
@.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-VALIDATION.md
@.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-01-SUMMARY.md
@Procfile
@apps/backend/scripts/run-migrations.mjs
@apps/backend/src/infrastructure/release-migration-mode.ts
</context>

## Goal, dependencies and manual gates

- Depende de 12.1-01 PASS; Wave 2.
- Começa com autorização humana read-only e termina com decisão humana sobre o preflight.
- Não autoriza 12.1-03, deploy ou reconciliação.

## Exact allowlist and evidence

- Único arquivo gravável: `12.1-02-SUMMARY.md`.
- Runtime surfaces: Heroku release/config metadata, one-off DB read-only e migration source.
- Evidence classes: `production read-only`, `Heroku release`, `inferred only`.

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Autorizar consultas read-only de migration e control plane</name>
  <files>.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-02-SUMMARY.md</files>
  <action>Solicitar autorização específica para consultar nomes de migrations, presença booleana de config contracts e metadados de releases/processos no app `espacoliminar`. Per D12.1-04, D12.1-10, D12.1-14 e D12.1-15, a autorização não inclui valores, mudança de config, DDL/DML, deploy, provider ou business mutation.</action>
  <verify>
    <automated>rtk git diff --check</automated>
    <human-check>Responder `autorizar preflight read-only` ou `não autorizar`.</human-check>
  </verify>
  <done>Escopo externo foi autorizado explicitamente ou o plano termina BLOCKED sem consulta.</done>
</task>

<task type="auto">
  <name>Task 2: Executar preflight sanitizado e resolver o hard stop Gelato</name>
  <files>.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-02-SUMMARY.md</files>
  <action>Após autorização, usar três consultas read-only separadas e fish-compatible, cada uma com saída allowlist-first e sem tempfile. (1) Iniciar one-off Heroku DB-only cujo processo Node lê `DATABASE_MIGRATION_URL` apenas em memória, valida protocolo PostgreSQL e emite somente `migration_url_present=true|false` e `migration_port_is_6543=true|false`; bloquear se ausente, inválida ou 6543. O mesmo one-off conecta com `pg` e executa literalmente `SELECT name FROM public.mikro_orm_migrations WHERE name = ANY($1::text[]) ORDER BY name`, passando as doze identities allowlisted (as 11 locais mais `MigrationTBDGelatoFulfillment`) e imprimindo somente `migration_name=&lt;nome-allowlisted&gt;`; fechar o cliente em `finally`. Comparar localmente o conjunto retornado com as 11 identities comprovadas no 12.1-01 e calcular pending sem nova consulta. Se `MigrationTBDGelatoFulfillment` estiver aplicada, `Migration20260703000000` estiver no source e aparecer pending, declarar `BLOCKED`, exigir decisão humana de reconciliação e proibir release. Não editar histórico, não executar migration, `down()`, drop ou rename. (2) Não executar `heroku config` nem baixar o mapa de config. O one-off Node deve manter duas constantes distintas. `ENV_INPUT_ALLOWLIST` contém exatamente: `DATABASE_URL`, `DATABASE_MIGRATION_URL`, `REDIS_URL`, `CACHE_REDIS_URL`, `EVENTS_REDIS_URL`, `WE_REDIS_URL`, `REDIS_CACHE_PROVIDER_DISABLED`, `HEROKU_BUILD_COMMIT`, `HEROKU_SLUG_COMMIT`, `APP_VERSION`, `API_PUBLIC_URL`, `STORE_CORS`, `ADMIN_CORS`, `AUTH_CORS`, `JWT_SECRET`, `COOKIE_SECRET`, `S3_ENDPOINT`, `S3_REGION`, `S3_BUCKET`, `S3_ACCESS_KEY_ID`, `S3_SECRET_ACCESS_KEY`, `S3_FILE_URL`, `SENTRY_DSN`, `STRIPE_REAL_INITIATION_ENABLED`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_INGESTION_ENABLED`, `STRIPE_WEBHOOK_SECRET`, `RESEND_ORDER_CONFIRMATION_ENABLED`, `RESEND_API_KEY`, `RESEND_FROM_EMAIL`, `GELATO_DISPATCH_ENABLED`, `GELATO_API_KEY`, `GELATO_SHIPMENT_METHOD_UID`, `GELATO_WEBHOOK_SECRET` e `TRACKING_TOKEN_PEPPER`. `OUTPUT_BOOLEAN_ALLOWLIST` contém exatamente: `database_url_present`, `database_migration_url_present`, `migration_port_is_6543`, `redis_url_present`, `cache_redis_url_present`, `events_redis_url_present`, `workflow_redis_url_present`, `redis_cache_provider_disabled_contract_ok`, `version_metadata_present`, `api_public_url_present`, `store_cors_present`, `admin_cors_present`, `auth_cors_present`, `jwt_secret_present`, `cookie_secret_present`, `s3_contract_complete`, `sentry_contract_complete`, `stripe_contract_complete`, `resend_contract_complete`, `gelato_contract_complete`, `tracking_token_pepper_present`. Construir do zero um objeto somente com essas 21 chaves e valores booleanos; `redis_cache_provider_disabled_contract_ok` é verdadeiro apenas quando `REDIS_CACHE_PROVIDER_DISABLED` está ausente/vazio ou, após trim/lowercase, é literalmente `false`, sem emitir o valor bruto. Flags e dependências de provider são agregadas apenas nos três booleans de contrato; metadata de versão é agregada em `version_metadata_present`. Canalizar a única linha JSON diretamente a `jq -ce`, que exige `keys | sort` igual à allowlist fechada, `all(.[]; type == "boolean")`, `migration_port_is_6543 == false` e `redis_cache_provider_disabled_contract_ok == true`, e projeta somente as mesmas 21 chaves; nenhuma saída anterior ao filtro pode ser redirecionada, copiada ou incluída no SUMMARY. (3) Extrair `candidate_sha` do campo estruturado único `execution_base_sha: &lt;40-hex&gt;` em `12.1-01-SUMMARY.md`, validando o formato de 40 hex antes de qualquer chamada externa. Buscar no máximo 20 releases: `rtk heroku releases --json --app espacoliminar | jq -ce '[.[:20][] | {id,version,status,created_at}]'`. A primeira entrada é a `predeploy_current_release` — a release ativa antes deste deploy candidato, e é ela que se torna o rollback target, nunca a anterior. Consultar essa primeira entrada com `rtk heroku releases:info &lt;version&gt; --json --app espacoliminar`, projetando diretamente somente campos allowlisted, incluindo o SHA completo de 40 hex do commit associado, por exemplo `{id,version,status,created_at,slug_eligible:(.slug.id | type == "string" and length &gt; 0),sha:.slug.commit}` — usar o campo de commit factualmente disponível no schema retornado (`slug.commit` ou equivalente) e documentar no SUMMARY exatamente qual campo allowlisted foi usado para o SHA. Se nenhum campo allowlisted expuser um SHA completo de 40 hex sem outros dados sensíveis, declarar `preflight_decision: BLOCKED` sem aproximar ou inferir o valor. Percorrer, na ordem, somente as 19 entradas anteriores com a mesma projeção allowlisted, sem persistir respostas brutas, para localizar `predeploy_previous_eligible_release` — a primeira com status de sucesso e `slug_eligible=true`; esta permanece apenas fallback histórico e nunca o alvo primário de rollback. Copiar `rollback_target_release_id`, `rollback_target_release_version` e `rollback_target_sha` exatamente dos valores de `predeploy_current_release` (nunca da anterior elegível) e exigir `rollback_target_slug_eligible=true` — ou seja, a própria release atual precisa ter slug elegível; se não tiver, declarar `preflight_decision: BLOCKED` mesmo que exista uma anterior elegível disponível. Analisar somente diffs locais de migration/schema no intervalo `rollback_target_sha...candidate_sha` para classificar `rollback_schema_compatibility` como `compatible`, `incompatible` ou `unknown`; `GO` exige estritamente `compatible`. Registrar no SUMMARY exatamente um bloco delimitado pelas linhas literais `P12_RELEASE_CONTRACT_V1_BEGIN` e `P12_RELEASE_CONTRACT_V1_END`, contendo entre elas, uma por linha, nesta ordem exata e sem duplicação: `release_contract_version: 1`, `candidate_sha: &lt;40-hex&gt;`, `predeploy_current_release_id: &lt;non-empty&gt;`, `predeploy_current_release_version: &lt;vN&gt;`, `predeploy_current_release_sha: &lt;40-hex&gt;`, `predeploy_current_slug_eligible: true|false`, `predeploy_previous_eligible_release_id: &lt;non-empty&gt;`, `predeploy_previous_eligible_release_version: &lt;vN&gt;`, `rollback_target_release_id: &lt;igual a predeploy_current_release_id&gt;`, `rollback_target_release_version: &lt;igual a predeploy_current_release_version&gt;`, `rollback_target_sha: &lt;igual a predeploy_current_release_sha&gt;`, `rollback_target_slug_eligible: true`, `rollback_schema_compatibility: compatible|incompatible|unknown` e `preflight_decision: GO|BLOCKED`. Nenhum valor sensível (URL, credencial, header, token) pode aparecer dentro do bloco. O hard stop Gelato do item (1) permanece independente: mesmo com `preflight_decision: GO` no contrato de release, identidade Gelato ambígua bloqueia por si só. Per D12.1-05, D12.1-08, D12.1-09, D12.1-12 e D12.1-15, qualquer chave extra, valor sensível, consulta ampla, pending inesperado, ausência de `predeploy_previous_eligible_release` dentro das 20 releases, bloco `P12_RELEASE_CONTRACT_V1` ausente/duplicado/incompleto, SHA fora do formato 40 hex, `rollback_target_slug_eligible=false` ou `rollback_schema_compatibility` diferente de `compatible` bloqueia o `GO`; a release anterior elegível permanece apenas fallback histórico e jamais substitui o rollback target.</action>
  <verify>
    <automated>rtk git diff --check; and rtk rg -n 'Migration20260703000000|MigrationTBDTrackingAccessToken|MigrationTBDRefundRequest|MigrationTBDExchangeRequest' apps/backend/src/modules/*/migrations/*.ts; and not rtk rg -n 'heroku config( |$)|config --json' .planning/phases/12.1-mvp-release-readiness-production-validation/12.1-02-SUMMARY.md; and set -l p12_summary .planning/phases/12.1-mvp-release-readiness-production-validation/12.1-02-SUMMARY.md; and set -l p12_begin (rtk rg -n '^P12_RELEASE_CONTRACT_V1_BEGIN$' "$p12_summary"); and set -l p12_end (rtk rg -n '^P12_RELEASE_CONTRACT_V1_END$' "$p12_summary"); and test (count $p12_begin) -eq 1; and test (count $p12_end) -eq 1; and for p12_f in release_contract_version candidate_sha predeploy_current_release_id predeploy_current_release_version predeploy_current_release_sha predeploy_current_slug_eligible predeploy_previous_eligible_release_id predeploy_previous_eligible_release_version rollback_target_release_id rollback_target_release_version rollback_target_sha rollback_target_slug_eligible rollback_schema_compatibility preflight_decision; test (count (rtk rg --no-filename "^$p12_f: " "$p12_summary")) -eq 1; or exit 1; end; and set -l p12_cand (string replace 'candidate_sha: ' '' -- (rtk rg --no-filename '^candidate_sha: ' "$p12_summary")); and set -l p12_cur_sha (string replace 'predeploy_current_release_sha: ' '' -- (rtk rg --no-filename '^predeploy_current_release_sha: ' "$p12_summary")); and set -l p12_rb_sha (string replace 'rollback_target_sha: ' '' -- (rtk rg --no-filename '^rollback_target_sha: ' "$p12_summary")); and string match -qr '^[0-9a-f]{40}$' -- "$p12_cand"; and string match -qr '^[0-9a-f]{40}$' -- "$p12_cur_sha"; and test "$p12_rb_sha" = "$p12_cur_sha"; and set -l p12_cur_id (string replace 'predeploy_current_release_id: ' '' -- (rtk rg --no-filename '^predeploy_current_release_id: ' "$p12_summary")); and set -l p12_rb_id (string replace 'rollback_target_release_id: ' '' -- (rtk rg --no-filename '^rollback_target_release_id: ' "$p12_summary")); and test "$p12_rb_id" = "$p12_cur_id"; and set -l p12_cur_ver (string replace 'predeploy_current_release_version: ' '' -- (rtk rg --no-filename '^predeploy_current_release_version: ' "$p12_summary")); and set -l p12_rb_ver (string replace 'rollback_target_release_version: ' '' -- (rtk rg --no-filename '^rollback_target_release_version: ' "$p12_summary")); and test "$p12_rb_ver" = "$p12_cur_ver"; and test (string replace 'rollback_target_slug_eligible: ' '' -- (rtk rg --no-filename '^rollback_target_slug_eligible: ' "$p12_summary")) = true; and set -l p12_decision (string replace 'preflight_decision: ' '' -- (rtk rg --no-filename '^preflight_decision: ' "$p12_summary")); and set -l p12_compat (string replace 'rollback_schema_compatibility: ' '' -- (rtk rg --no-filename '^rollback_schema_compatibility: ' "$p12_summary")); and if test "$p12_decision" = GO; test "$p12_compat" = compatible; end; and not rtk rg -n 'postgres://|Bearer |sk_live_|sk_test_|DATABASE_URL=' "$p12_summary"</automated>
  </verify>
  <done>Histórico/pending, identidade Gelato, configuração nominal e o contrato P12_RELEASE_CONTRACT_V1 (releases e rollback compatibility) têm fatos saneados.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Decidir GO ou BLOCKED antes do deploy</name>
  <files>.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-02-SUMMARY.md</files>
  <action>Apresentar somente a síntese allowlisted. GO exige 12.1-01 PASS, 11/11 migrations coerentes, nenhuma identidade Gelato ambígua, migration connection adequada, contratos nominais presentes e um único bloco `P12_RELEASE_CONTRACT_V1` coerente, onde o rollback target é exatamente a release atual pré-deploy (`predeploy_current_release_*`), com `rollback_target_slug_eligible: true` e `rollback_schema_compatibility: compatible`. A release anterior elegível (`predeploy_previous_eligible_release_*`) serve somente como evidência de fallback histórico e nunca substitui o rollback target no GO. Qualquer ausência ou incoerência produz BLOCKED. Mesmo GO não autoriza deploy.</action>
  <verify>
    <automated>rtk git diff --check; and rtk git status --short --untracked-files=all</automated>
    <human-check>Confirmar `GO de preflight` ou `BLOCKED`; uma futura autorização de deploy continuará separada.</human-check>
  </verify>
  <done>O SUMMARY contém decisão binária e o plano para sem iniciar 12.1-03.</done>
</task>

</tasks>

## Acceptance criteria, negative proofs and stop conditions

- Closure criterion 4 tem preflight factual; criteria 5 e 17 têm metadados preparatórios.
- Hard stop Gelato ocorre antes de deploy.
- Não aparece qualquer config-var value, URL credenciada ou segredo.
- Falta de autorização, identity mismatch, pending inesperado, 6543, contrato nominal ausente, bloco `P12_RELEASE_CONTRACT_V1` ausente/duplicado/incompleto, rollback target diferente da release atual pré-deploy, `rollback_target_slug_eligible: false` ou `rollback_schema_compatibility` diferente de `compatible` resulta em BLOCKED.

## Rollback, evidence, summary and commit contracts

- Nenhuma mutation ocorreu; não há rollback técnico.
- SUMMARY registra somente nomes/booleanos/SHA/release IDs necessários, classes, o bloco único `P12_RELEASE_CONTRACT_V1` e GO/BLOCKED.
- Commit somente do SUMMARY e apenas com autorização explícita.
- Prohibited: migration, mudança de histórico/config, deploy, provider, PaymentIntent, Order, refund, exchange, Gelato dispatch, e-mail ou analytics.
- Next permitted gate: revisão humana; se GO, autorização de deploy específica para 12.1-03.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Heroku CLI → evidence | Control plane pode retornar valores sensíveis. |
| one-off → Supabase | Consulta cruza para banco de produção, estritamente read-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-12.1-04 | Information Disclosure | config/history output | mitigate | jq booleano e SELECT de nomes somente. |
| T-12.1-05 | Tampering | migration history | mitigate | SELECT-only e hard stop; zero reconciliação. |
| T-12.1-06 | Spoofing | release identity | mitigate | Contrato P12_RELEASE_CONTRACT_V1 fixa o rollback target na release atual pré-deploy, com SHA completo de 40 hex e ancestry, nunca uma release aproximada. |
| T-12.1-SC | Tampering | packages | accept | zero install. |
</threat_model>

<verification>
Produzir apenas `12.1-02-SUMMARY.md` e parar no gate humano.
</verification>

<success_criteria>
GO ou BLOCKED factual, com Gelato resolvido antes de deploy e segredo protegido.
</success_criteria>

<output>
Create `.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-02-SUMMARY.md` when done.
</output>
