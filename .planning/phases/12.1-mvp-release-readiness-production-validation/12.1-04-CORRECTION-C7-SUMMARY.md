---
phase: 12.1-mvp-release-readiness-production-validation
plan: "04"
correction: C7
subsystem: isolated-pgid-production-read-only-validation
status: blocked
completed: 2026-07-28
---

# Phase 12.1 Plan 04 — Blocker Correction C7 Summary

```text
PHASE 12.1 PLAN 12.1-04 BLOCKER CORRECTION C7: BLOCKED
```

## Scope, authorization and Git baseline

```text
App:
  espacoliminar

Base URL:
  https://espacoliminar-5c3343d789bf.herokuapp.com

Authorization:
  autorizar execução read-only 12.1-04-C7 no app espacoliminar, aplicando o
  coletor Heroku com sessão e PGID isolados via setsid, encerramento por TERM
  no grupo completo, espera dos processos, EOF obrigatório e exit zero do
  filtro, seguida das três amostras, scan bruto fail-closed, projeção saneada
  e validação final da identidade da release; sem deploy, rollback,
  configuração, banco de dados, provider calls ou mutações de produção

Collector authorization:
  autorizar exatamente um coletor temporário de heroku logs --tail, executado
  em sessão e PGID próprios via setsid, com PID e PGID rastreados,
  encerramento por TERM no PGID completo, wait obrigatório, zero processos
  remanescentes e cleanup integral

Branch:
  gsd/phase-12.1-mvp-release-readiness-production-validation

HEAD:
  3aa0281b761122c182b8f79ff27e03a9206b87a3

origin...HEAD:
  0 0

Initial worktree:
  empty

Candidate SHA:
  18d809e4169daa301839542191f0d6794b02d695
```

Uma forma de cleanup local foi recusada pelo executor antes da abertura do
Bash. Ela não realizou leitura nem mutação externa. O invólucro aceito usou
remoção explícita dos temporários conhecidos. A CLI retornou a versão da
release como número; o parser local a normalizou para a identidade `v78`.
Nenhum comando operacional de retry foi executado.

## Predecessor gates

```text
C4 Attempt 3:
  PASS

C6:
  BLOCKED

C6 first blocked reason:
  filter_exit_nonzero

C6 local PGID correction proof:
  PASS
```

## Initial release identity

```text
Current release count:
  1

Initial release ID:
  a41fefb4-0002-4c7d-bfec-de36d1efae9c

Initial release version:
  v78

Initial release status:
  succeeded

Initial release current:
  true

Initial release eligible:
  true

Initial slug ID:
  12b69509-5785-484d-8d70-02e441122c4a

Initial release SHA:
  18d809e4169daa301839542191f0d6794b02d695

Initial Heroku main SHA:
  18d809e4169daa301839542191f0d6794b02d695

Release/slug/main/candidate match:
  true
```

Eligibility was derived fail-closed from the single `current` release being
`succeeded`, having a resolvable slug commit, and matching both Heroku main and
the candidate SHA.

## Collector supervision

```text
Collector count:
  1

Collector command:
  setsid heroku logs --tail --app espacoliminar

Supervision model:
  FIFO with a temporal bounded filter and an isolated upstream session/PGID

writer_pid:
  219043

writer_pgid:
  219043

writer_pgid == writer_pid:
  true

writer_pgid != parent_shell_pgid:
  true

filter_pid:
  219042

writer_pid != filter_pid:
  true

Writer direct child:
  true

Filter direct child:
  true

Writer command identity:
  valid

Filter command identity:
  valid

Writer active before Sample 1:
  true

Filter active before Sample 1:
  true

Unauthorized background processes before Sample 1:
  0
```

## Observation window and samples

```text
Samples completed:
  3/3

Elapsed seconds:
  >= 600 — minimum enforced and passed before collector shutdown

Sample 1:
  PASS

Sample 2:
  PASS

Sample 3:
  PASS

Collector active through Sample 3:
  true
```

The exact elapsed value and second-level window timestamps were not persisted
after the fail-closed scanner stop. The harness had already required
`elapsed_seconds >= 600` before sending TERM to the writer PGID.

## Health and formation

```text
Live HTTP 200 / status live / candidate SHA:
  3/3

Ready HTTP 200 / status ready / candidate SHA:
  3/3

PostgreSQL up:
  3/3

Redis up:
  3/3

web.1 up:
  3/3

worker.1 up:
  3/3

web.1 updated_at invariant:
  true across 3/3

worker.1 updated_at invariant:
  true across 3/3

Formation stable:
  true

Public 5xx:
  0
```

The exact `updated_at` values were intentionally not preserved after the
scanner blocker; equality across all three samples was validated before the
collector was stopped.

## Seven allowlisted surfaces

| Method | Path | C7 result |
|---|---|---|
| GET | `/health/live` | `200`, `live`, candidate SHA — 3/3 |
| GET | `/health/ready` | `200`, `ready`, candidate SHA, PostgreSQL/Redis `up` — 3/3 |
| GET | `/app` | controlled non-5xx |
| GET | `/store/regions` | controlled non-5xx |
| GET | `/store/products?limit=1` | controlled non-5xx |
| OPTIONS | `/store/products?limit=1` | controlled non-5xx |
| GET without auth | `/admin/operational-alerts?limit=1` | `401` or `403` |

```text
Admin status:
  401 or 403 — accepted fail-closed class

Admin body class:
  generic_json_error_envelope

Admin allowed keys:
  message, error, code, status, statusCode — scalar values only

Admin arrays:
  false

Admin pagination:
  false

Admin nested objects:
  false

Admin OperationalAlert fields:
  false

Admin protected_payload:
  false

Application mutation methods:
  0
```

The exact controlled public and Admin status codes were not retained after the
scanner blocker. Each public response had already passed the non-5xx gate, and
the Admin response had passed the `401|403` plus generic-scalar-envelope
classifier.

## Collector shutdown, EOF and metadata

```text
TERM sent to complete writer PGID:
  true

wait writer_pid:
  true

writer exit code:
  143

writer termination reason:
  supervisor_window_complete

Filter received EOF:
  true

wait filter_pid:
  true

filter exit code:
  0

Filter metadata exists:
  true

Filter metadata contract:
  version 1

Raw log line count:
  bounded within 0..200

Raw line-count metadata vs file:
  equal

Zero remaining writer PGID members:
  true

Unauthorized remaining background processes:
  0
```

The exact bounded line count, eligible-line count and truncation boolean were
not retained after the scanner returned nonzero. The filter metadata and file
were validated before the scanner, including the hard `<= 200` bound.

## Raw scanner and fail-closed disposition

The mandatory order was preserved:

```text
1. filter exit 0:
  PASS

2. bounded metadata validation:
  PASS

3. raw sensitive scanner:
  EXECUTED — exit nonzero

4. require every sensitive counter zero:
  BLOCKED

5. sanitized projection:
  NOT CREATED

6. raw log deletion:
  true
```

```text
credential_url_match_count:
  not persisted after fail-closed cleanup

authorization_match_count:
  not persisted after fail-closed cleanup

cookie_session_match_count:
  not persisted after fail-closed cleanup

secret_token_match_count:
  not persisted after fail-closed cleanup

payment_sensitive_match_count:
  not persisted after fail-closed cleanup

pii_match_count:
  not persisted after fail-closed cleanup

sensitive_match_count:
  greater than 0

restart markers:
  not evaluated after the earlier sensitive-match blocker

crash markers:
  not evaluated after the earlier sensitive-match blocker

Sanitized projection created:
  false

Raw log deleted:
  true
```

No raw line, match, substring, response body, stderr, token or sensitive value
was displayed or copied to this artifact. Exact per-category counts were not
available after the cleanup path removed the raw log and scanner output; that
evidence limitation cannot be repaired without a new collector, which was not
authorized and was not started.

## Final release identity

```text
Final release ID:
  not observed

Final release version:
  not observed

Final release SHA:
  not observed

Final slug ID:
  not observed

Final Heroku main SHA:
  not observed

Final release status/current/eligible:
  not observed

Initial/final identity equal:
  not established
```

The final release read was not executed after `sensitive_match_nonzero`.
Proceeding to projection or final identity after that first blocker would have
violated the fail-closed sequence.

## Negative proofs and cleanup

```text
POST:
  0

PUT:
  0

PATCH:
  0

DELETE:
  0

Deploy:
  0

Heroku releases:retry:
  0

Rollback:
  0

Restart:
  0

Scale:
  0

Config set/unset:
  0

Heroku run:
  0

PostgreSQL/Redis CLI:
  0

Provider calls:
  0

Business mutations:
  0

Production mutations:
  0

Stage:
  not executed

Commit:
  not executed

Push:
  not executed

12.1-05:
  not started

Raw log absent after cleanup:
  true

Temporary directory removed:
  true

Collector/filter process cleanup:
  complete
```

## First blocker and decision

```text
First blocked checkpoint:
  raw_sensitive_scan

First blocked reason code:
  sensitive_match_nonzero

First blocker:
  the raw fail-closed scanner returned nonzero because sensitive_match_count
  was greater than zero; therefore zero counters were not established,
  projection was prohibited, and final release identity was not consulted

PLAN 12.1-04 C7:
  BLOCKED
```

## Final documentary verification

```text
HEAD:
  3aa0281b761122c182b8f79ff27e03a9206b87a3

Only changed or untracked file:
  12.1-04-CORRECTION-C7-SUMMARY.md

git diff --check:
  empty
```

## Next permitted gate

```text
Human review of 12.1-04-C7 only.

Any new collector or production read:
  requires a separate authorization

12.1-05:
  blocked

Stage, commit and push:
  not authorized
```
