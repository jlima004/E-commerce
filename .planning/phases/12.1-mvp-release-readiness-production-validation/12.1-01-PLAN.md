---
phase: 12.1-mvp-release-readiness-production-validation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12.1-mvp-release-readiness-production-validation/12.1-01-SUMMARY.md
autonomous: false
requirements: [SETUP-01, SETUP-02, SETUP-04, SETUP-05, OBS-02, OBS-03, TEST-01]
must_haves:
  truths:
    - "O release candidate descende do merge mínimo aceito e não contém runtime posterior não autorizado, per D12.1-01, D12.1-02 e D12.1-03."
    - "Unit, Modules, HTTP, lint e build passam no mesmo SHA candidato."
    - "As 11 migrations customizadas são descobertas em PostgreSQL local descartável, com tabelas, constraints e índices esperados."
    - "Nenhum PostgreSQL remoto/persistente, Redis real ou provider externo é usado."
    - "Lockfile, manifests, config, runtime, testes e migrations permanecem byte-identical."
  artifacts:
    - path: ".planning/phases/12.1-mvp-release-readiness-production-validation/12.1-01-SUMMARY.md"
      provides: "Evidence ledger local, SHA-base, resultados e gate manual"
  key_links:
    - from: "apps/backend/scripts/run-disposable-postgres-tests.mjs"
      to: "apps/backend/src/modules/*/migrations/*.ts"
      via: "PostgreSQL vazio e descartável com discovery Medusa"
      pattern: "P12_DISPOSABLE_POSTGRES_CLEAN"
---

<objective>
Qualificar localmente o release candidate sem tocar produção.

Purpose: produzir evidência contemporânea dos closure criteria 1–3 e bloquear qualquer release sem regressão completa e migration discovery descartável.
Output: `12.1-01-SUMMARY.md`, classificado somente como `local process` e `local PostgreSQL disposable`.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-CONTEXT.md
@.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-RESEARCH.md
@.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-VALIDATION.md
@Procfile
@package.json
@package-lock.json
@apps/backend/package.json
@apps/backend/jest.config.js
@apps/backend/scripts/run-disposable-postgres-tests.mjs
</context>

## Goal

Provar que o SHA local aprovado é reproduzível, testado e migrável em infraestrutura estritamente descartável.

## Non-goals

- Nenhuma produção, Heroku app, Supabase remoto, provider, deploy ou migration persistente.
- Nenhuma correção de runtime, teste, migration, manifest, lockfile ou config.
- Nenhum fechamento de requisito; os IDs do frontmatter são somente rastreabilidade de revalidação.

## Dependencies and manual gate

- Wave 1; sem predecessor.
- A aprovação do PLAN não autoriza execução.
- O plano termina em checkpoint humano e não autoriza 12.1-02 automaticamente.

## Baseline and allowlist

- Baseline factual do PLAN: `fccbf6c538ff5db0e2489dd0b4344b19773593d9`.
- Antes de qualquer diff, definir no fish `set -gx PHASE12_1_PLAN_BASE_SHA fccbf6c538ff5db0e2489dd0b4344b19773593d9`; capturar `set -gx PHASE12_1_EXECUTION_BASE_SHA (git rev-parse HEAD)` no início e reutilizar ambos sem depender de variável herdada.
- Único arquivo gravável: `12.1-01-SUMMARY.md`.
- Runtime surfaces consulted read-only: Git, Node/npm, manifests, lockfile, Jest config, harness descartável, migration sources e catálogo do PostgreSQL descartável.
- Evidence classes exatas: `local process`, `local PostgreSQL disposable`.

<tasks>

<task type="auto">
  <name>Task 1: Fixar o execution base e qualificar processos locais</name>
  <files>.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-01-SUMMARY.md</files>
  <action>Per D12.1-01, D12.1-02, D12.1-03, D12.1-14 e D12.1-15, no fish executar `set -gx PHASE12_1_PLAN_BASE_SHA fccbf6c538ff5db0e2489dd0b4344b19773593d9` e `set -gx PHASE12_1_EXECUTION_BASE_SHA (git rev-parse HEAD)` antes dos diffs. Registrar branch, ambos os SHAs completos, incluindo a linha estruturada única `execution_base_sha: &lt;40-hex&gt;`, ancestry de `b4c1ee954c5d8337bff80a945eadec57ad2a0e0a`, worktree inicial e versões factuais de Node/npm. Comparar `$PHASE12_1_PLAN_BASE_SHA...HEAD` e bloquear se houver arquivo runtime posterior ao PLAN fora do artefato documental esperado. Decidir `npm ci --include=dev` somente por fatos: executá-lo se `node_modules` estiver ausente ou `npm ls --depth=0` falhar; caso contrário registrar que a instalação existente foi aceita. Se executado, exigir lockfile byte-identical. Rodar Unit completa, lint e build fresco com comandos workspace-scoped e ambientes temporários já aceitos. Qualquer falha gera `BLOCKED`; não alterar código para fazê-la passar.</action>
  <verify>
    <automated>set -gx PHASE12_1_PLAN_BASE_SHA fccbf6c538ff5db0e2489dd0b4344b19773593d9; and set -gx PHASE12_1_EXECUTION_BASE_SHA (git rev-parse HEAD); and test -n "$PHASE12_1_EXECUTION_BASE_SHA"; and rtk git merge-base --is-ancestor b4c1ee954c5d8337bff80a945eadec57ad2a0e0a HEAD; and rtk git diff --check; and env TMPDIR=/tmp rtk npm run test:unit -w @dtc/backend; and env HOME=/tmp XDG_CONFIG_HOME=/tmp TMPDIR=/tmp ADMIN_DISABLED=true rtk npm run lint -w @dtc/backend; and env HOME=/tmp XDG_CONFIG_HOME=/tmp TMPDIR=/tmp ADMIN_DISABLED=true rtk npm run build -w @dtc/backend</automated>
  </verify>
  <done>SHA-base, ancestry, versões, decisão factual de instalação, Unit, lint e build estão registrados sem mudanças fora da allowlist.</done>
</task>

<task type="auto">
  <name>Task 2: Provar integrações e as 11 migrations em PostgreSQL descartável</name>
  <files>.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-01-SUMMARY.md</files>
  <action>Antes de executar, provar Docker daemon funcional ou fornecer simultaneamente `P12_DISPOSABLE_DATABASE_URL` e `P12_DISPOSABLE_DB_NAME` para um PostgreSQL genuinamente local e descartável; o host deve ser loopback, a maintenance database deve ser literalmente `postgres`, o alvo deve casar `^p12_disposable_[a-z0-9_]+$`, e nenhum valor de URL pode ser persistido. Sem uma dessas opções, declarar `BLOCKED`. Executar Modules completa e HTTP completa em invocações separadas do harness, sempre com `rtk` somente no comando externo antes de `node`; depois de `--`, os filhos executam diretamente as ferramentas normais (`npm` ou `node`). Para a terceira invocação, criar em `/tmp` um child audit Node/`pg`, modo `0600`, sem gravar no repositório. O child recebe `DATABASE_URL`/`DB_TEMP_NAME` do harness, valida novamente protocolo PostgreSQL, host loopback, maintenance `postgres`, nome do alvo e que alvo difere da maintenance; deriva a maintenance URL trocando somente o pathname por `/postgres`; conecta um `pg.Client` à maintenance; confirma que o alvo não existe; executa `CREATE DATABASE "&lt;nome-já-validado&gt;"` (interpolação de identificador permitida somente após o regex fechado); monta a target URL em memória; e chama via `spawn` diretamente `npm run db:migrate:safe -w @dtc/backend` com `DATABASE_URL` e `DATABASE_MIGRATION_URL` apontando ao alvo. Após exit zero, outro `pg.Client` consulta somente: (a) `public.mikro_orm_migrations.name` limitado pelo manifest de 11 identities; (b) `pg_catalog.pg_tables.tablename` com `schemaname='public'` e `tablename = ANY($1)` para as 11 tabelas; (c) `pg_catalog.pg_constraint.conname` unido a `pg_class` e filtrado pelas mesmas 11 tabelas; (d) `pg_catalog.pg_indexes.indexname` com `schemaname='public'` e `tablename = ANY($1)`. Usar literalmente os quatro manifests explícitos de `12.1-VALIDATION.md`; normalizar/sortear/retirar duplicatas e falhar em missing ou extra em cada conjunto. Persistir somente contagens e nomes allowlisted; nunca listar o schema inteiro nem aceitar nome fora dos manifests. Em `finally`, encerrar o client do alvo, terminar sessões residuais do alvo pela maintenance, executar `DROP DATABASE IF EXISTS "&lt;nome-já-validado&gt;"`, consultar `pg_database` para provar ausência e somente então emitir `P12_CHILD_DATABASE_CLEAN`; qualquer falha de cleanup prevalece sobre o resultado anterior. O finally externo do harness ainda deve emitir `P12_DISPOSABLE_POSTGRES_CLEAN` e remover o container/recurso mesmo se create, migrate, consulta ou comparação falhar. Exigir ambos os markers CLEAN e ausência residual após cada invocação. Per D12.1-08, D12.1-09, D12.1-14 e D12.1-15, nenhum Redis/provider real ou URL credenciada pode aparecer.</action>
  <verify>
    <automated>set -l p12_audit_script /tmp/p12-01-migration-audit.mjs; and function p12_audit_cleanup --on-event fish_exit; command rm -f -- "$p12_audit_script"; end; and env TMPDIR=/tmp rtk node apps/backend/scripts/run-disposable-postgres-tests.mjs -- npm run test:integration:modules -w @dtc/backend; and env TMPDIR=/tmp rtk node apps/backend/scripts/run-disposable-postgres-tests.mjs -- npm run test:integration:http -w @dtc/backend; and test -f "$p12_audit_script"; and test (stat -c '%a' "$p12_audit_script") = 600; and env TMPDIR=/tmp rtk node apps/backend/scripts/run-disposable-postgres-tests.mjs -- node "$p12_audit_script"; and p12_audit_cleanup</automated>
  </verify>
  <done>Modules, HTTP, migration discovery, catálogo de schema e cleanup passam em PostgreSQL local descartável, com 11/11 identities comprovadas.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Revisar o gate local antes de qualquer consulta externa</name>
  <files>.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-01-SUMMARY.md</files>
  <action>Apresentar resultados, classes de evidência, diffs negativos e qualquer blocker sem promover evidência histórica. O SUMMARY deve conter um campo estruturado único `execution_base_sha: &lt;40-hex&gt;`, capturado pela Task 1. Não iniciar 12.1-02.</action>
  <verify>
    <automated>set -l p12_summary .planning/phases/12.1-mvp-release-readiness-production-validation/12.1-01-SUMMARY.md; and set -l p12_base_lines (rtk rg --no-filename '^execution_base_sha: [0-9a-f]{40}$' "$p12_summary"); and test (count $p12_base_lines) -eq 1; and set -l PHASE12_1_EXECUTION_BASE_SHA (string replace 'execution_base_sha: ' '' -- "$p12_base_lines[1]"); and string match -qr '^[0-9a-f]{40}$' -- "$PHASE12_1_EXECUTION_BASE_SHA"; and rtk git cat-file -e "$PHASE12_1_EXECUTION_BASE_SHA^{commit}"; and rtk git diff --check; and rtk git diff --exit-code "$PHASE12_1_EXECUTION_BASE_SHA"...HEAD -- package.json package-lock.json apps/backend/package.json apps/backend/jest.config.js apps/backend/medusa-config.ts apps/backend/src apps/backend/integration-tests apps/backend/scripts</automated>
    <human-check>Confirmar PASS/BLOCKED do release candidate local e autorizar separadamente apenas o início do preflight read-only 12.1-02.</human-check>
  </verify>
  <done>Gate humano registra a decisão; nenhum acesso externo foi iniciado.</done>
</task>

</tasks>

## Acceptance criteria and negative proofs

- Closure criteria 1, 2 e 3 têm evidência atual.
- `git diff` desde o execution base mostra apenas o SUMMARY deste plano.
- Nenhuma URL, credencial, PII, payload ou token está no evidence package.
- Ausência de Docker e de PostgreSQL local descartável é `BLOCKED`, nunca skip.

## Stop conditions

Ancestry inválida, runtime drift, instalação que altera lockfile, qualquer suite/lint/build falho, migration diferente de 11/11, schema incompleto, Redis/provider real detectado ou cleanup incompleto interrompe o plano.

## Rollback for local changes

Não há mudança runtime a reverter. Remover apenas recursos descartáveis criados por este plano; preservar arquivos do usuário e não usar reset/stash.

## Evidence, summary and commit contracts

- SUMMARY: SHA-base, comandos, exits/contagens, migration/schema inventory, cleanup, classes, limitações e decisão binária.
- Commit: no máximo um commit do SUMMARY, somente se o executor receber autorização explícita; nenhum commit técnico.
- Prohibited actions: produção, Heroku, Supabase remoto, providers, deploy, migration persistente, mudança runtime/test/migration/package/config.
- Next permitted gate: revisão humana e autorização específica para 12.1-02.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| workspace → subprocessos | Dependências e comandos podem modificar arquivos ou expor ambiente. |
| harness → PostgreSQL | O alvo deve ser local, descartável e distinto da manutenção. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-12.1-01 | Tampering | execution baseline | mitigate | SHA-base, ancestry e diff base...HEAD. |
| T-12.1-02 | Information Disclosure | saída do harness | mitigate | redaction e inventário allowlisted somente. |
| T-12.1-03 | Denial of Service | cleanup | mitigate | finally + confirmação de ausência residual. |
| T-12.1-SC | Tampering | installs | mitigate | zero pacote novo; lockfile byte-identical. |
</threat_model>

<verification>
Executar somente os comandos deste plano após autorização, produzir `12.1-01-SUMMARY.md` e parar no checkpoint.
</verification>

<success_criteria>
Release candidate local PASS ou blocker factual registrado; produção permanece intocada.
</success_criteria>

<output>
Create `.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-01-SUMMARY.md` when done.
</output>
