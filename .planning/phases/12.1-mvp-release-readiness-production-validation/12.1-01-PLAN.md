---
phase: 12.1-mvp-release-readiness-production-validation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12.1-mvp-release-readiness-production-validation/12.1-01-SUMMARY.md
autonomous: false
requirements: [SETUP-01, SETUP-02, SETUP-04, SETUP-05, OBS-02, OBS-03, TEST-01]
must_haves:
  truths:
    - "O release candidate descende do merge mínimo aceito e não contém runtime posterior não autorizado, per D12.1-01, D12.1-02 e D12.1-03."
    - "Unit, lint, build, Modules normal, cinco specs PostgreSQL isoladas (5/5), HTTP e exact-set audit passam no mesmo SHA candidato."
    - "As 11 migrations customizadas são descobertas em PostgreSQL local descartável próprio, com tabelas, constraints e índices esperados."
    - "Nenhum PostgreSQL remoto/persistente, Redis real ou provider externo é usado."
    - "Lockfile, manifests, config, runtime, testes e migrations permanecem byte-identical."
    - "Empilhar todas as specs PostgreSQL Medusa num único processo Jest descartável é proibido; o erro Map.prototype.set é evitado por isolamento de processo."
  artifacts:
    - path: ".planning/phases/12.1-mvp-release-readiness-production-validation/12.1-01-SUMMARY.md"
      provides: "Evidence ledger local, SHA-base, resultados e gate manual"
  key_links:
    - from: "apps/backend/scripts/run-disposable-postgres-tests.mjs"
      to: "apps/backend/src/modules/*/__tests__/*.postgres.spec.ts"
      via: "Um processo Jest + um lifecycle descartável por spec PostgreSQL (serial)"
      pattern: "P12_DISPOSABLE_POSTGRES_CLEAN"
    - from: "apps/backend/scripts/run-disposable-postgres-tests.mjs"
      to: "apps/backend/src/modules/*/migrations/*.ts"
      via: "PostgreSQL vazio e descartável com discovery Medusa (audit lifecycle separado)"
      pattern: "P12_DISPOSABLE_POSTGRES_CLEAN"
---

<objective>
Qualificar localmente o release candidate sem tocar produção.

Purpose: produzir evidência contemporânea dos closure criteria 1–3 e bloquear qualquer release sem regressão completa e migration discovery descartável.
Output: `12.1-01-SUMMARY.md`, classificado somente como `local process` e `local PostgreSQL disposable`.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-CONTEXT.md
@.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-RESEARCH.md
@.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-VALIDATION.md
@Procfile
@package.json
@package-lock.json
@apps/backend/package.json
@apps/backend/jest.config.js
@apps/backend/scripts/run-disposable-postgres-tests.mjs
</context>

## Goal

Provar que o SHA local aprovado é reproduzível, testado e migrável em infraestrutura estritamente descartável.

## Non-goals

- Nenhuma produção, Heroku app, Supabase remoto, provider, deploy ou migration persistente.
- Nenhuma correção de runtime, teste, migration, manifest, lockfile, config, Jest, test-utils, runner, harness ou spec.
- Nenhum fechamento de requisito; os IDs do frontmatter são somente rastreabilidade de revalidação.

## Human classification of attempt 1 (binding)

```text
First execution result:
  correctly BLOCKED under the current PLAN

Product/runtime regression:
  not demonstrated

PostgreSQL constraint failure:
  not demonstrated

Disposable infrastructure failure:
  no

Known stacked Jest/Medusa loader limitation:
  strongly indicated

Current execution contract:
  unsatisfied

Required correction:
  documentary only
```

Attempt 1 remains historically `BLOCKED` (committed). It is never reclassified as PASS.

## Dependencies and manual gate

- Wave 1; sem predecessor.
- A aprovação do PLAN não autoriza execução.
- A correção documental R1 não autoriza retry.
- Retry exige autorização literal separada:
  `autorizar nova execução local do plano 12.1-01 após correção R1`
- O plano termina em checkpoint humano e não autoriza 12.1-02 automaticamente.

## Baseline and allowlist

- Baseline factual do PLAN: `fccbf6c538ff5db0e2489dd0b4344b19773593d9`.
- Antes de qualquer diff, definir no fish `set -gx PHASE12_1_PLAN_BASE_SHA fccbf6c538ff5db0e2489dd0b4344b19773593d9`; capturar `set -gx PHASE12_1_EXECUTION_BASE_SHA (git rev-parse HEAD)` no início e reutilizar ambos sem depender de variável herdada.
- Único arquivo gravável na execução: `12.1-01-SUMMARY.md` (atualizar o path tracked; não criar segundo SUMMARY).
- Runtime surfaces consulted read-only: Git, Node/npm, manifests, lockfile, Jest config, harness descartável, migration sources e catálogo do PostgreSQL descartável.
- Evidence classes exatas: `local process`, `local PostgreSQL disposable`.

## Technical change prohibition (binding)

```text
runtime changes:
  prohibited

test changes:
  prohibited

Jest changes:
  prohibited

test-utils changes:
  prohibited

runner changes:
  prohibited

spec changes:
  prohibited

harness changes:
  prohibited

package/lockfile/config changes:
  prohibited
```

A estratégia corrige a orquestração documental do gate, não o produto nem a suíte.

## Composite Modules/C02/C03 gate (P12-12-06-R1 lineage)

Contrato removido (não obrigatório; reproduziu `Map.prototype.set`):

```text
run-disposable-postgres-tests.mjs
  -> full test:integration:modules
  -> one Jest process containing all PostgreSQL Medusa runners
```

Gate composto obrigatório — quatro provas separadas, nesta ordem após Unit/lint/build:

```text
A. Modules normal regression
B. PostgreSQL specs isolated serially (exactly five)
C. HTTP full regression
D. Migration/schema exact-set audit
```

Ordem completa obrigatória:

```text
Unit
-> lint
-> build
-> Modules normal
-> five isolated PostgreSQL specs
-> HTTP
-> migration exact-set audit
```

Qualquer falha interrompe as etapas seguintes.

PASS composto exige simultaneamente:

```text
Unit: PASS
Lint: PASS
Build: PASS
Modules normal: PASS
PostgreSQL isolated specs: 5/5 PASS
HTTP: PASS
Migration identities: 11/11 exact
Tables: 11/11 exact
Constraints: 33/33 exact
Indexes: 76/76 exact
All cleanup: PASS
```

O erro histórico `Map.prototype.set` não é ignorado; é evitado estruturalmente por isolamento de processo. Se ocorrer em qualquer spec isolada → `BLOCKED`. Se Modules normal falhar → `BLOCKED`. Não existe PASS com 4/5.

<tasks>

<task type="auto">
  <name>Task 1: Fixar o execution base e qualificar processos locais</name>
  <files>.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-01-SUMMARY.md</files>
  <action>Per D12.1-01, D12.1-02, D12.1-03, D12.1-14 e D12.1-15, no fish executar `set -gx PHASE12_1_PLAN_BASE_SHA fccbf6c538ff5db0e2489dd0b4344b19773593d9` e `set -gx PHASE12_1_EXECUTION_BASE_SHA (git rev-parse HEAD)` antes dos diffs. Registrar branch, ambos os SHAs completos, incluindo a linha estruturada única `execution_base_sha: &lt;40-hex&gt;`, ancestry de `b4c1ee954c5d8337bff80a945eadec57ad2a0e0a`, worktree inicial e versões factuais de Node/npm. Comparar `$PHASE12_1_PLAN_BASE_SHA...HEAD` e bloquear se houver arquivo runtime posterior ao PLAN fora do artefato documental esperado. Decidir `npm ci --include=dev` somente por fatos: executá-lo se `node_modules` estiver ausente ou `npm ls --depth=0` falhar; caso contrário registrar que a instalação existente foi aceita. Se executado, exigir lockfile byte-identical. Rodar Unit completa, lint e build fresco com comandos workspace-scoped e ambientes temporários já aceitos. Qualquer falha gera `BLOCKED`; não alterar código para fazê-la passar. Em retry (attempt ≥ 2), o SUMMARY tracked é atualizado (não duplicado); preservar seção histórica com `previous_attempt_result: BLOCKED`, `previous_attempt_commit` do commit histórico do attempt 1, e `previous_execution_base_sha: fff96d5fa5d1942e195d37025526bff701744551`; o `execution_base_sha` corrente é o HEAD factual no início do retry (após correção R1 e paridade remota). O candidate do attempt 1 não é reutilizado.</action>
  <verify>
    <automated>set -gx PHASE12_1_PLAN_BASE_SHA fccbf6c538ff5db0e2489dd0b4344b19773593d9; and set -gx PHASE12_1_EXECUTION_BASE_SHA (git rev-parse HEAD); and test -n "$PHASE12_1_EXECUTION_BASE_SHA"; and rtk git merge-base --is-ancestor b4c1ee954c5d8337bff80a945eadec57ad2a0e0a HEAD; and rtk git diff --check; and env TMPDIR=/tmp rtk npm run test:unit -w @dtc/backend; and env HOME=/tmp XDG_CONFIG_HOME=/tmp TMPDIR=/tmp ADMIN_DISABLED=true rtk npm run lint -w @dtc/backend; and env HOME=/tmp XDG_CONFIG_HOME=/tmp TMPDIR=/tmp ADMIN_DISABLED=true rtk npm run build -w @dtc/backend</automated>
  </verify>
  <done>SHA-base, ancestry, versões, decisão factual de instalação, Unit, lint e build estão registrados sem mudanças fora da allowlist.</done>
</task>

<task type="auto">
  <name>Task 2: Gate composto Modules normal + PostgreSQL isolado + HTTP + exact-set audit</name>
  <files>.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-01-SUMMARY.md</files>
  <action>Antes de executar, provar Docker daemon funcional ou fornecer simultaneamente `P12_DISPOSABLE_DATABASE_URL` e `P12_DISPOSABLE_DB_NAME` para um PostgreSQL genuinamente local e descartável; o host deve ser loopback, a maintenance database deve ser literalmente `postgres`, o alvo deve casar `^p12_disposable_[a-z0-9_]+$`, e nenhum valor de URL pode ser persistido. Sem uma dessas opções, declarar `BLOCKED`.

**Prova A — Modules normal** (comando literal comprovado em Phase 12 / P12-12-06-R1; sem empilhar todas as specs PostgreSQL num único processo descartável):

```fish
env TMPDIR=/tmp rtk npm run test:integration:modules -w @dtc/backend
```

PASS exige exit zero; todas as suites/tests esperadas da regressão normal executadas; nenhum teste omitido silenciosamente. Se alguma das cinco specs PostgreSQL listadas na Prova B for reportada como skipped/omitted sob Modules normal por ausência de disposable env, registrar explicitamente no SUMMARY e exigir cobertura 5/5 na Prova B — nunca reduzir cobertura normal sem essa prova. Contagens devem ser registradas; baseline igual ou superior à vigente.

**Prova B — cinco specs PostgreSQL isoladas serialmente** (estrutura operacional comprovada em Phase 12 / P12-12-06-R1; paths adaptados ao contrato 12.1-01; `runTestsByPath`; um processo por spec; um lifecycle descartável por processo; cleanup após cada). Paths exatos, nesta ordem serial:

1. `src/modules/operational-alert/__tests__/operational-alert.postgres.spec.ts`
2. `src/modules/checkout-completion/__tests__/checkout-completion-log.postgres.spec.ts`
3. `src/modules/gelato-fulfillment/__tests__/gelato-fulfillment.postgres.spec.ts`
4. `src/modules/webhooks/__tests__/webhook-event-log.postgres.spec.ts`
5. `src/modules/webhooks/__tests__/disposable-postgres-harness.spec.ts`

Para cada path, exatamente uma invocação (proibido empilhar dois paths no mesmo Jest):

```fish
env TMPDIR=/tmp rtk node apps/backend/scripts/run-disposable-postgres-tests.mjs -- npm run test:integration:modules -w @dtc/backend -- --runTestsByPath &lt;one-spec&gt; --runInBand
```

Cada execução exige: exit zero; exatamente uma spec; child process isolado; marker `P12_DISPOSABLE_POSTGRES_CLEAN`; residual database none; residual container none (`docker ps -a --filter name=p12-pg-` vazio). Agregado: `PostgreSQL isolated specs: 5/5 PASS`. Se qualquer spec falhar com `Map.prototype.set` → `BLOCKED`.

**Prova C — HTTP** (somente após A e B PASS; invocação já aprovada no PLAN):

```fish
env TMPDIR=/tmp rtk node apps/backend/scripts/run-disposable-postgres-tests.mjs -- npm run test:integration:http -w @dtc/backend
```

Exigir HTTP full suite PASS, cleanup PASS, runtime changes none. Sempre com `rtk` somente no comando externo antes de `node`; depois de `--`, os filhos executam diretamente (`npm` ou `node`).

**Prova D — migration exact-set audit** (somente após A, B e C PASS; lifecycle descartável próprio, separado das cinco specs). Criar em `/tmp` um child audit Node/`pg`, modo `0600`, sem gravar no repositório. O child recebe `DATABASE_URL`/`DB_TEMP_NAME` do harness, valida novamente protocolo PostgreSQL, host loopback, maintenance `postgres`, nome do alvo e que alvo difere da maintenance; deriva a maintenance URL trocando somente o pathname por `/postgres`; conecta um `pg.Client` à maintenance; confirma que o alvo não existe; executa `CREATE DATABASE "&lt;nome-já-validado&gt;"` (interpolação de identificador permitida somente após o regex fechado); monta a target URL em memória; e chama via `spawn` diretamente `npm run db:migrate:safe -w @dtc/backend` com `DATABASE_URL` e `DATABASE_MIGRATION_URL` apontando ao alvo. Após exit zero, outro `pg.Client` consulta somente: (a) `public.mikro_orm_migrations.name` limitado pelo manifest de 11 identities; (b) `pg_catalog.pg_tables.tablename` com `schemaname='public'` e `tablename = ANY($1)` para as 11 tabelas; (c) `pg_catalog.pg_constraint.conname` unido a `pg_class` e filtrado pelas mesmas 11 tabelas; (d) `pg_catalog.pg_indexes.indexname` com `schemaname='public'` e `tablename = ANY($1)`. Usar literalmente os quatro manifests explícitos de `12.1-VALIDATION.md`; normalizar/sortear/retirar duplicatas e falhar em missing ou extra em cada conjunto (11/11 migrations, 11/11 tables, 33/33 constraints, 76/76 indexes). Persistir somente contagens e nomes allowlisted; nunca listar o schema inteiro nem aceitar nome fora dos manifests. Em `finally`, encerrar o client do alvo, terminar sessões residuais do alvo pela maintenance, executar `DROP DATABASE IF EXISTS "&lt;nome-já-validado&gt;"`, consultar `pg_database` para provar ausência e somente então emitir `P12_CHILD_DATABASE_CLEAN`; qualquer falha de cleanup prevalece sobre o resultado anterior. O finally externo do harness ainda deve emitir `P12_DISPOSABLE_POSTGRES_CLEAN` e remover o container/recurso mesmo se create, migrate, consulta ou comparação falhar. Exigir ambos os markers CLEAN e ausência residual após cada invocação descartável (B, C e D). Per D12.1-08, D12.1-09, D12.1-14 e D12.1-15, nenhum Redis/provider real ou URL credenciada pode aparecer.

Não alterar: jest.config.js, test-utils, medusaIntegrationTestRunner, as cinco specs, runtime, harness.</action>
  <verify>
    <automated>set -l p12_audit_script /tmp/p12-01-migration-audit.mjs; and function p12_audit_cleanup --on-event fish_exit; command rm -f -- "$p12_audit_script"; end; and env TMPDIR=/tmp rtk npm run test:integration:modules -w @dtc/backend; and env TMPDIR=/tmp rtk node apps/backend/scripts/run-disposable-postgres-tests.mjs -- npm run test:integration:modules -w @dtc/backend -- --runTestsByPath src/modules/operational-alert/__tests__/operational-alert.postgres.spec.ts --runInBand; and env TMPDIR=/tmp rtk node apps/backend/scripts/run-disposable-postgres-tests.mjs -- npm run test:integration:modules -w @dtc/backend -- --runTestsByPath src/modules/checkout-completion/__tests__/checkout-completion-log.postgres.spec.ts --runInBand; and env TMPDIR=/tmp rtk node apps/backend/scripts/run-disposable-postgres-tests.mjs -- npm run test:integration:modules -w @dtc/backend -- --runTestsByPath src/modules/gelato-fulfillment/__tests__/gelato-fulfillment.postgres.spec.ts --runInBand; and env TMPDIR=/tmp rtk node apps/backend/scripts/run-disposable-postgres-tests.mjs -- npm run test:integration:modules -w @dtc/backend -- --runTestsByPath src/modules/webhooks/__tests__/webhook-event-log.postgres.spec.ts --runInBand; and env TMPDIR=/tmp rtk node apps/backend/scripts/run-disposable-postgres-tests.mjs -- npm run test:integration:modules -w @dtc/backend -- --runTestsByPath src/modules/webhooks/__tests__/disposable-postgres-harness.spec.ts --runInBand; and env TMPDIR=/tmp rtk node apps/backend/scripts/run-disposable-postgres-tests.mjs -- npm run test:integration:http -w @dtc/backend; and test -f "$p12_audit_script"; and test (stat -c '%a' "$p12_audit_script") = 600; and env TMPDIR=/tmp rtk node apps/backend/scripts/run-disposable-postgres-tests.mjs -- node "$p12_audit_script"; and p12_audit_cleanup</automated>
  </verify>
  <done>Modules normal, PostgreSQL isolado 5/5, HTTP, migration discovery exact-set e cleanup passam; 11/11 identities comprovadas.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Revisar o gate local antes de qualquer consulta externa</name>
  <files>.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-01-SUMMARY.md</files>
  <action>Apresentar resultados, classes de evidência, diffs negativos e qualquer blocker sem promover evidência histórica. O SUMMARY deve conter um campo estruturado único corrente `execution_base_sha: &lt;40-hex&gt;` (capturado pela Task 1 desta tentativa). Em attempt ≥ 2, incluir seção histórica separada com previous_attempt_* sem reutilizar o candidate antigo. Não iniciar 12.1-02.</action>
  <verify>
    <automated>set -l p12_summary .planning/phases/12.1-mvp-release-readiness-production-validation/12.1-01-SUMMARY.md; and set -l p12_base_lines (rtk rg --no-filename '^execution_base_sha: [0-9a-f]{40}$' "$p12_summary"); and test (count $p12_base_lines) -eq 1; and set -l PHASE12_1_EXECUTION_BASE_SHA (string replace 'execution_base_sha: ' '' -- "$p12_base_lines[1]"); and string match -qr '^[0-9a-f]{40}$' -- "$PHASE12_1_EXECUTION_BASE_SHA"; and rtk git cat-file -e "$PHASE12_1_EXECUTION_BASE_SHA^{commit}"; and rtk git diff --check; and rtk git diff --exit-code "$PHASE12_1_EXECUTION_BASE_SHA"...HEAD -- package.json package-lock.json apps/backend/package.json apps/backend/jest.config.js apps/backend/medusa-config.ts apps/backend/src apps/backend/integration-tests apps/backend/scripts</automated>
    <human-check>Confirmar PASS/BLOCKED do release candidate local e autorizar separadamente apenas o início do preflight read-only 12.1-02.</human-check>
  </verify>
  <done>Gate humano registra a decisão; nenhum acesso externo foi iniciado.</done>
</task>

</tasks>

## Acceptance criteria and negative proofs

- Closure criteria 1, 2 e 3 têm evidência atual sob o gate composto.
- `git diff` desde o execution base mostra apenas o SUMMARY deste plano.
- Nenhuma URL, credencial, PII, payload ou token está no evidence package.
- Ausência de Docker e de PostgreSQL local descartável é `BLOCKED`, nunca skip.
- Empilhar as cinco specs PostgreSQL no mesmo processo Jest é `BLOCKED`.

## Stop conditions

Ancestry inválida, runtime drift, instalação que altera lockfile, qualquer suite/lint/build falho, Modules normal falho, qualquer isolated PostgreSQL spec falho, menos de 5/5, processo contendo mais de uma spec PostgreSQL, cleanup ausente, migration diferente de 11/11, schema incompleto, Redis/provider real detectado, mudança técnica (Jest/test-utils/runner/spec/harness/runtime) usada para contornar o loader, ou cleanup incompleto interrompe o plano.

## Rollback for local changes

Não há mudança runtime a reverter. Remover apenas recursos descartáveis criados por este plano; preservar arquivos do usuário e não usar reset/stash.

## Evidence, summary and commit contracts

- SUMMARY: SHA-base, comandos, exits/contagens, Modules normal, 5/5 isoladas, HTTP, migration/schema inventory, cleanup, classes, limitações e decisão binária.
- Attempt 1 histórico: preservado no Git; path SUMMARY único atualizado em retries.
- Commit: no máximo um commit do SUMMARY por execução autorizada; nenhum commit técnico.
- Prohibited actions: produção, Heroku, Supabase remoto, providers, deploy, migration persistente, mudança runtime/test/migration/package/config/Jest/runner/harness/spec.
- Next permitted gate: revisão humana e autorização específica para 12.1-02 (após PASS); retry do 12.1-01 exige autorização literal separada após correção R1.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| workspace → subprocessos | Dependências e comandos podem modificar arquivos ou expor ambiente. |
| harness → PostgreSQL | O alvo deve ser local, descartável e distinto da manutenção. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-12.1-01 | Tampering | execution baseline | mitigate | SHA-base, ancestry e diff base...HEAD. |
| T-12.1-02 | Information Disclosure | saída do harness | mitigate | redaction e inventário allowlisted somente. |
| T-12.1-03 | Denial of Service | cleanup | mitigate | finally + confirmação de ausência residual. |
| T-12.1-SC | Tampering | installs | mitigate | zero pacote novo; lockfile byte-identical. |
</threat_model>

<verification>
Executar somente os comandos deste plano após autorização, produzir/atualizar `12.1-01-SUMMARY.md` e parar no checkpoint.
</verification>

<success_criteria>
Release candidate local PASS ou blocker factual registrado sob o gate composto; produção permanece intocada.
</success_criteria>

<output>
Create or update `.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-01-SUMMARY.md` when done.
</output>
