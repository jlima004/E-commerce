---
phase: 12.1-mvp-release-readiness-production-validation
plan: "05"
type: execute
wave: 5
depends_on: [12.1-04]
files_modified:
  - .planning/phases/12.1-mvp-release-readiness-production-validation/12.1-05-SUMMARY.md
autonomous: false
requirements: [SETUP-05, OBS-01, OBS-02, OBS-03]
must_haves:
  truths:
    - "Health, formation, Heroku logs e SHA são a evidência obrigatória não-provider."
    - "Sentry, Stripe e Resend só são consultados após autorização humana específica."
    - "Gelato, PostHog e Correios reais não são requisitos para PASS."
    - "Provider não autorizado é registrado como `not exercised` e `declared limitation`."
    - "Nenhum payload, recipient, header, cookie, secret ou DSN é persistido."
  artifacts:
    - path: ".planning/phases/12.1-mvp-release-readiness-production-validation/12.1-05-SUMMARY.md"
      provides: "Observability ledger e limitações explícitas de providers"
  key_links:
    - from: ".planning/phases/12.1-mvp-release-readiness-production-validation/12.1-04-SUMMARY.md"
      to: ".planning/phases/12.1-mvp-release-readiness-production-validation/12.1-05-SUMMARY.md"
      via: "mandatory non-provider evidence"
      pattern: "health|web.1|worker.1|SHA"
---

<objective>
Registrar a observabilidade permitida e declarar explicitamente providers não exercitados.

Purpose: impedir que evidência opcional de provider vire requisito implícito ou que inferência seja promovida.
Output: `12.1-05-SUMMARY.md` com classes `Heroku release`, `real external provider` e `inferred only`.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-CONTEXT.md
@.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-RESEARCH.md
@.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-04-SUMMARY.md
@apps/backend/src/observability/sentry.ts
@apps/backend/src/observability/sentry-scrub.ts
</context>

## Boundaries

- Wave 5; depende de 12.1-04 PASS.
- Único arquivo gravável: `12.1-05-SUMMARY.md`.
- Sem provider mutation em qualquer opção.
- Evidência obrigatória: health, dynos, logs Heroku, release/SHA e limitações.

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Selecionar evidência opcional de provider</name>
  <files>.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-05-SUMMARY.md</files>
  <action>Per D12.1-10, D12.1-11, D12.1-14 e D12.1-15, oferecer opções mutuamente explícitas: (A) nenhum provider; (B) Sentry release/issues read-only para organização/projeto nominalmente informados; (C) Stripe webhook endpoint/delivery metadata read-only para a conta nominalmente informada; (D) Resend delivery metadata read-only para o domínio/projeto nominalmente informado; ou combinação nominal autorizada. Para cada opção B–D, a resposta humana deve autorizar provider, alvo exato, janela temporal máxima de 10 minutos após o release e limite máximo de 20 registros. Sem alvo exato ou sem superfície MCP conectada que seja read-only e permita projeção allowlisted antes de persistência, registrar `not exercised` / `declared limitation`; não improvisar CLI, curl, token ou dashboard. A autorização nunca inclui event body, replay, create/capture/refund, send, dispatch ou dados de destinatário. Gelato, PostHog e Correios permanecem `not exercised` por padrão.</action>
  <verify>
    <automated>rtk git diff --check</automated>
    <human-check>Selecionar `nenhum provider` ou nomear exatamente Sentry, Stripe e/ou Resend em modo read-only.</human-check>
  </verify>
  <done>O conjunto opcional ficou fechado por autorização explícita.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidar observabilidade obrigatória e providers autorizados</name>
  <files>.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-05-SUMMARY.md</files>
  <action>Reusar a evidência atual do 12.1-04 para registrar health, dynos, logs, release e SHA como obrigatória `Heroku release`; não repetir smokes nem promover material histórico. Se nenhum provider foi autorizado, registrar Sentry/Stripe/Resend/Gelato/PostHog/Correios como `not exercised`, `declared limitation`, classe `inferred only` para qualquer conclusão baseada apenas em configuração/código, e permitir continuidade se todo critério não-provider passou. Para cada provider autorizado, descobrir e ler o schema da superfície MCP conectada somente depois do checkpoint e usar exclusivamente operação declaradamente read-only; se houver qualquer dúvida de side effect ou impossibilidade de limitar campos/janela/quantidade, não chamar e registrar limitação. Ramo Sentry: consultar o alvo autorizado filtrado pelo SHA completo da release e pela janela autorizada, limite 20; persistir somente `release_sha`, contagem limitada e agregados por `level`, `status` e código curto saneado, sem título, mensagem, stack, breadcrumb, usuário, request ou evento. Ramo Stripe: consultar somente metadata de webhook endpoints e deliveries existentes na conta autorizada, janela autorizada, limite 20; persistir somente contagem, `enabled/disabled`, `delivery_status`, código HTTP agregado e timestamp truncado, sem URL do endpoint, event ID, event type correlacionável, payload/data/object, customer, PaymentIntent, client secret, request ou response body. Ramo Resend: consultar somente metadata de deliveries existentes no alvo autorizado, janela autorizada, limite 20; persistir somente contagem, status agregado, código agregado e timestamp truncado, sem email ID, destinatário/remetente, domínio, assunto, headers, tags, body ou conteúdo. A resposta bruta de MCP deve permanecer apenas na memória transitória da chamada e nunca ser copiada ao SUMMARY; o executor cria o ledger do zero com os campos allowlisted. Classificar cada consulta concluída como `real external provider`. Não consultar Gelato/PostHog/Correios e não disparar qualquer operação.</action>
  <verify>
    <automated>rtk git diff --check; and rtk rg -n "not exercised|declared limitation|real external provider|inferred only" .planning/phases/12.1-mvp-release-readiness-production-validation/12.1-05-SUMMARY.md</automated>
  </verify>
  <done>Evidence ledger distingue obrigatório, provider exercitado e limitações sem transformar ausência opcional em blocker.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Revisar observabilidade e limitações</name>
  <files>.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-05-SUMMARY.md</files>
  <action>Apresentar matriz provider por provider com `exercised/not exercised`, classe, autorização e sanitização; listar evidência obrigatória separadamente. Não iniciar rollback ou final gate.</action>
  <verify>
    <automated>rtk git diff --check; and rtk git status --short --untracked-files=all</automated>
    <human-check>Confirmar que limitações estão completas e que nenhuma ausência opcional foi tratada como requisito de PASS.</human-check>
  </verify>
  <done>Gate humano concluído; 12.1-06 permanece separadamente bloqueado.</done>
</task>

</tasks>

## Acceptance, negatives and stop conditions

- Criterion 18 tem limitações explícitas; criterion 15 permanece protegido.
- Provider opcional jamais mascara falha de health/dyno/log/SHA.
- Parar em autorização ambígua, payload sensível, tentativa de mutation ou falta de sanitização.

## Rollback and contracts

- Nenhuma mutation deste plano; não há rollback.
- SUMMARY contém matriz de autorização, classe, campos persistidos e limitações.
- Commit somente do SUMMARY sob autorização explícita.
- Prohibited: replay/create/refund/capture/send/dispatch e qualquer payload/body/header/secret.
- Next permitted gate: revisão humana e autorização para 12.1-06.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| providers externos → evidence | Metadados podem carregar PII/secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-12.1-13 | Information Disclosure | provider metadata | mitigate | campos allowlisted e bounded counts. |
| T-12.1-14 | Tampering | provider operation | mitigate | read-only gate; mutation proibida. |
| T-12.1-15 | Repudiation | evidence provenance | mitigate | registrar autorização e classe por item. |
| T-12.1-SC | Tampering | packages | accept | zero install. |
</threat_model>

<verification>
Produzir somente o ledger de observabilidade/limitações e parar.
</verification>

<success_criteria>
Evidência obrigatória preservada, providers opcionais corretamente classificados e nenhum dado sensível persistido.
</success_criteria>

<output>
Create `.planning/phases/12.1-mvp-release-readiness-production-validation/12.1-05-SUMMARY.md` when done.
</output>
