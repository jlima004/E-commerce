---
phase: 12.1-mvp-release-readiness-production-validation
plan: "04"
correction: C6
subsystem: collector-supervision-diagnostic
status: blocked
completed: 2026-07-28
---

# Phase 12.1 Plan 04 — Blocker Correction C6 Summary

```text
PHASE 12.1 PLAN 12.1-04 BLOCKER CORRECTION C6: BLOCKED
```

## Scope and documentary baseline

```text
App:
  espacoliminar

Authorization source:
  current human request to execute Plan 12.1-04 through a separate C6
  correction, initially limited to collector supervision diagnostics,
  sanitized exit codes for each pipeline process, individual PIDs, classified
  stderr without content, liveness proof, and release of the complete
  read-only execution only after that precondition

Branch:
  gsd/phase-12.1-mvp-release-readiness-production-validation

Documentary HEAD:
  6fa81ea19c05b6b93813de9d2af8fe9e34b54865

Remote parity:
  origin/gsd/phase-12.1-mvp-release-readiness-production-validation...HEAD = 0 0

Initial worktree:
  empty

git diff --check:
  empty

GSD routing:
  gsd-execute-phase initialized the phase, identified 12.1-05 and 12.1-06 as
  the ordinary incomplete plans, and did not execute either one; C6 was
  handled inline as the explicitly authorized manual correction of 12.1-04
```

## Predecessor and candidate

```text
Predecessors:
  12.1-04-CORRECTION-C4-ATTEMPT-3-SUMMARY.md /
  P12_CURRENT_RELEASE_IDENTITY_V1;
  12.1-04-CORRECTION-C5-SUMMARY.md

C4 Attempt 3 decision:
  PASS

C5 decision:
  BLOCKED — collector_not_active before Sample 1

Candidate SHA:
  18d809e4169daa301839542191f0d6794b02d695
```

## Corrected C6 supervision model

```text
Shell:
  Bash

Execution environment:
  Codex inside WSL

Supervision model:
  FIFO with two individually tracked direct child processes

heroku logs --tail PID:
  203491

Filter PID:
  203490

PIDs distinct:
  true

heroku logs direct child:
  true

Filter direct child:
  true

heroku logs command identity matched tracked PID:
  true

Filter command identity matched tracked PID:
  true

Detached:
  false

nohup:
  false

Timeout process used:
  false

Log session POST:
  false

Production mutation:
  false
```

## Collector precondition and window

```text
Collector precondition checks:
  3/3

Precondition observation span:
  6 seconds after the initial process-start delay

Collector active at every precondition check:
  true

Individual PID/parent/command identity proven before full execution:
  true

Complete execution released only after precondition:
  true

Capture gate enabled only after precondition and initial release identity:
  true

Window started:
  2026-07-28T22:43:13Z

Window completed:
  2026-07-28T22:53:29Z

Elapsed seconds:
  616

Collector liveness checks during the window:
  119

Collector active through Sample 3:
  true

Samples completed:
  3/3
```

## Initial release identity

```text
Initial release ID:
  a41fefb4-0002-4c7d-bfec-de36d1efae9c

Initial release version:
  v78

Initial release status:
  succeeded

Initial release current:
  true

Initial slug ID:
  12b69509-5785-484d-8d70-02e441122c4a

Initial release SHA:
  18d809e4169daa301839542191f0d6794b02d695

Initial Heroku main SHA:
  18d809e4169daa301839542191f0d6794b02d695

Slug/release/main/candidate match:
  true
```

## Samples 1–3

```text
Sample 1:
  live HTTP 200 / status live / candidate SHA match
  ready HTTP 200 / status ready / candidate SHA match
  postgres up
  redis up
  web.1 up
  worker.1 up

Sample 2:
  live HTTP 200 / status live / candidate SHA match
  ready HTTP 200 / status ready / candidate SHA match
  postgres up
  redis up
  web.1 up
  worker.1 up

Sample 3:
  live HTTP 200 / status live / candidate SHA match
  ready HTTP 200 / status ready / candidate SHA match
  postgres up
  redis up
  web.1 up
  worker.1 up

Live valid samples:
  3/3

Ready valid samples:
  3/3

Postgres up samples:
  3/3

Redis up samples:
  3/3

Health SHA match:
  3/3
```

## Formation stability

```text
web.1:
  up in 3/3

web.1 updated_at:
  2026-07-28T17:42:01Z in 3/3

worker.1:
  up in 3/3

worker.1 updated_at:
  2026-07-28T17:41:55Z in 3/3

Formation stable:
  true

Unexpected updated_at change:
  false

Crash-loop evidence from formation:
  false
```

## Seven allowlisted HTTP surfaces

| Method | Path | Result |
|---|---|---:|
| GET | `/health/live` | `200` in 3/3 |
| GET | `/health/ready` | `200` in 3/3 |
| GET | `/app` | `200` |
| GET | `/store/regions` | `400` controlled |
| GET | `/store/products?limit=1` | `400` controlled |
| OPTIONS | `/store/products?limit=1` | `400` controlled |
| GET without auth | `/admin/operational-alerts?limit=1` | `401` |

```text
Distinct HTTP surfaces:
  7

Application HTTP requests:
  11

Public 5xx count:
  0

Admin body class:
  generic_json_error_envelope

Admin protected payload present:
  false

Admin fail-closed:
  true
```

## Sanitized process exit diagnostics

```text
heroku logs --tail exit code:
  143

heroku logs termination reason:
  supervisor_window_complete

heroku logs stderr byte count:
  0

heroku logs stderr line count:
  0

heroku logs stderr class:
  empty

heroku logs stderr sensitive shape detected:
  false

Filter exit code:
  143

Filter termination reason:
  supervisor_kill_after_grace_period

Filter stderr byte count:
  0

Filter stderr line count:
  0

Filter stderr class:
  empty

Filter stderr sensitive shape detected:
  false

Filter metadata produced:
  false
```

No stderr line, substring, match, or raw content was displayed or persisted in
this artifact.

## Collector diagnosis

```text
Immediate heroku logs --tail exit during the window:
  disproved for C6 — tracked PID remained active through Sample 3

Python filter failure during the window:
  not observed — tracked PID remained active and stderr was empty

Premature pipeline closure during the window:
  disproved for C6 — both tracked PIDs remained active through Sample 3

PID propagation failure:
  disproved for C6 — distinct direct-child PIDs and command identities were proven

Timeout termination:
  disproved for C6 — no timeout process was used

Parent shell termination:
  disproved for C6 — parent completed the window and final diagnostic

Observed failure:
  after the supervisor intentionally sent TERM to the tracked heroku logs PID,
  that PID exited 143, but the filter did not observe EOF during the ten-second
  grace period; the supervisor then terminated the filter, also with exit 143

Root-cause class:
  upstream writer graph / inherited FIFO writer descriptor was not proven
  closed by terminating only the tracked leader PID

Exact descendant or descriptor owner:
  not captured; naming a specific child would be inference
```

This is enough to reject the C5/C6 leader-PID-only shutdown design. It is not
enough to claim which exact descendant retained the FIFO writer descriptor.

## Local correction proof

After production reads stopped, a synthetic local harness exercised the
structural correction without contacting Heroku:

```text
Correction:
  launch the upstream writer in its own session/process group with setsid;
  require writer_pgid == writer_pid;
  require writer_pgid != parent_shell_pgid;
  terminate the entire upstream process group;
  wait for the leader;
  require the filter to observe EOF and exit zero;
  require zero remaining process-group members

Local writer exit code:
  143

Local filter exit code:
  0

Local filter EOF observed:
  true

Local remaining process-group members:
  0

Local writer stderr bytes:
  0

Local filter stderr bytes:
  0

Local correction proof:
  PASS
```

The corrected PGID design was not applied to a second production collection in
this gate because that would be a new read-only execution requiring separate
human authorization.

## Logs and C15

```text
Logs bounded:
  false — filter finalization did not complete

Raw log line count:
  not_observed — filter metadata was not produced

Raw scan performed:
  false

credential_url_match_count:
  not_observed

authorization_match_count:
  not_observed

cookie_session_match_count:
  not_observed

secret_token_match_count:
  not_observed

payment_sensitive_match_count:
  not_observed

pii_match_count:
  not_observed

sensitive_match_count:
  not_observed

Sanitized projection created:
  false

Raw log file deleted:
  true

C15:
  BLOCKED
```

The fail-closed order remained preserved: no projection was created because
the raw scanner could not run after the collector finalization blocker.

## Final release identity

```text
Final release ID:
  not_observed

Final release version:
  not_observed

Final release SHA:
  not_observed

Final Heroku main SHA:
  not_observed

Release unchanged:
  not_observed — final identity was not consulted after the collector exit blocker
```

## Negative proofs

```text
Production GET/control-plane reads:
  17

POST:
  0

PUT:
  0

PATCH:
  0

DELETE:
  0

Auth headers on application HTTP:
  0

Cookies:
  0

Sessions:
  0

Provider calls:
  0

Business mutations:
  0

Config mutations:
  0

Database operations:
  0

Deploy:
  0

Rollback:
  0

Restart:
  0

Scale:
  0

Production mutations:
  0
```

## Cleanup and documentary result

```text
C6 collector temporary directory removed:
  true

Raw log absent after cleanup:
  true

Local correction harness removed:
  true

Sensitive values displayed:
  0

Documentary files changed:
  .planning/phases/12.1-mvp-release-readiness-production-validation/12.1-04-CORRECTION-C6-SUMMARY.md

Files outside allowlist:
  none
```

## First blocker and decision

```text
First blocked checkpoint:
  collector_exit_diagnostic

First blocked reason code:
  filter_exit_nonzero

Active blocker count:
  1

Active blocker:
  tracked leader-PID termination did not close the complete upstream writer
  graph, so the filter did not receive EOF, did not emit bounded raw metadata,
  and the mandatory raw scan/projection/final release revalidation remained
  prohibited

PLAN 12.1-04:
  BLOCKED
```

## C6 sanitized contract

```text
P12_COLLECTOR_SUPERVISION_C6_V1_BEGIN
collector_supervision_contract_version: 1
app: espacoliminar
candidate_sha: 18d809e4169daa301839542191f0d6794b02d695
supervision_model: fifo_with_individual_child_pids
timeout_process_used: false
detached: false
heroku_logs_pid: 203491
filter_pid: 203490
pids_distinct: true
direct_children_proven: true
command_identities_proven: true
preflight_checks: 3
window_liveness_checks: 119
complete_execution_released_after_precondition: true
samples_completed: 3
elapsed_seconds: 616
health_samples_valid: true
formation_samples_valid: true
formation_stable: true
route_matrix_complete: true
public_5xx_count: 0
admin_http_status: 401
admin_body_class: generic_json_error_envelope
admin_protected_payload: false
heroku_logs_exit_code: 143
heroku_logs_termination_reason: supervisor_window_complete
heroku_logs_stderr_class: empty
filter_exit_code: 143
filter_termination_reason: supervisor_kill_after_grace_period
filter_stderr_class: empty
filter_metadata_produced: false
raw_scan_performed: false
sensitive_match_count: not_observed
sanitized_projection_created: false
raw_log_file_deleted: true
final_release_identity_observed: false
local_pgid_correction_proof: PASS
production_mutations: 0
first_blocked_checkpoint: collector_exit_diagnostic
first_blocked_reason: filter_exit_nonzero
collector_supervision_decision: BLOCKED
P12_COLLECTOR_SUPERVISION_C6_V1_END
```

## Final documentary verification

```text
Result-ledger contract:
  PASS

Repository allowlist:
  PASS — exactly this C6 SUMMARY is untracked

git diff --no-index --check for the new SUMMARY:
  PASS

Sensitive-shape scan of the SUMMARY:
  PASS

Temporary cleanup:
  PASS

Final HEAD:
  6fa81ea19c05b6b93813de9d2af8fe9e34b54865 — unchanged

Final remote parity:
  origin/gsd/phase-12.1-mvp-release-readiness-production-validation...HEAD = 0 0

Commit:
  not executed

Push:
  not executed
```

## Next permitted gate

```text
Human review of 12.1-04-C6 only.

Commit and push:
  not authorized

New production execution using the PGID correction:
  not authorized

12.1-05:
  blocked

Rollback:
  not authorized

Production mutation:
  not authorized
```

A future separately authorized correction, for example `12.1-04-C7`, must use
the locally proven isolated-PGID shutdown contract and must still stop
fail-closed before projection if the filter does not exit zero with bounded
metadata.
