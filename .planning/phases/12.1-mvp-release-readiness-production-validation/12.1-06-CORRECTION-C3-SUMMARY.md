# 12.1-06-CORRECTION-C3 — Direct Platform API Verification

## Escopo e decisão

Gate documental read-only executado exclusivamente para verificar as releases
Heroku `v78` e `v77` e seus slugs pela Platform API, com evidência projetada e
sanitizada.

```text
12.1-06-CORRECTION-C3: PASS
```

Esta decisão não é a decisão final da Fase 12.1 e não substitui a reexecução,
sob autorização separada, do gate `12.1-06`.

## Baseline Git

```text
repository:
  /home/jlima/Projetos/ecommerce/Backend
branch:
  gsd/phase-12.1-mvp-release-readiness-production-validation
HEAD:
  7bbf4904d36fddfbd1f89732e724438d54482de3
origin...HEAD:
  0 0
worktree before execution:
  empty
```

O preflight foi executado apenas contra as refs locais; nenhum `fetch` foi
executado.

## Autorizações aplicadas

- Verificação read-only das releases `v78` e `v77` e de seus slugs pela Heroku
  Platform API, seguida somente desta evidência documental sanitizada.
- Rede restrita a quatro requisições `GET` HTTPS para `api.heroku.com`, sem
  retry.
- Autenticação restrita a `curl -n/--netrc` com as credenciais Heroku já
  existentes, sem leitura, impressão, cópia ou persistência do material de
  credencial.
- Header `Accept: application/vnd.heroku+json; version=3`.
- Processamento local em memória para projeção imediata dos campos allowlisted.
- Quatro subagents sequenciais e read-only; somente o coordenador criou este
  arquivo.

## Subagents sequenciais

| Ordem | Responsabilidade | Rede | Escrita | Resultado |
|---|---|---:|---:|---|
| 1 | Preflight Git, leitura integral das três fontes e contrato reconciliado | 0 | 0 | PASS |
| 2 | Quatro `GET` allowlisted e projeção imediata | 4 GET | 0 no repositório | PASS |
| 3 | Validação de identidade, estado, elegibilidade, slugs, SHAs e cruzamentos | 0 | 0 | PASS |
| 4 | Revisão final de sanitização, escopo, Git e não-ações | 0 | 0 | Executado após a criação deste SUMMARY |

## Fontes locais e contrato reconciliado

Foram lidos integralmente:

- `12.1-06-CORRECTION-C1-SUMMARY.md`
- `12.1-06-CORRECTION-C2-SUMMARY.md`
- `12.1-06-SUMMARY.md`

Foi localizado exatamente um bloco `P12_RELEASE_CONTRACT_RECONCILED_V1`. As
fontes confirmaram o seguinte contrato, sem mera cópia não validada:

| Papel | Release | Release ID | Slug ID | SHA |
|---|---|---|---|---|
| deployed | `v78` | `a41fefb4-0002-4c7d-bfec-de36d1efae9c` | `12b69509-5785-484d-8d70-02e441122c4a` | `18d809e4169daa301839542191f0d6794b02d695` |
| rollback target | `v77` | `57cafc0a-4f22-46ed-9024-b237110a6b73` | `36a68502-65b5-442a-8603-03e13abd3fec` | `9373b04e524512150bc10c1f759051e794a20000` |

## Requisições GET executadas

Foram executadas exatamente quatro requisições, todas com sucesso HTTP, sem
retry e sem endpoint alternativo:

1. `GET https://api.heroku.com/apps/espacoliminar/releases/v78`
2. `GET https://api.heroku.com/apps/espacoliminar/releases/v77`
3. `GET https://api.heroku.com/apps/espacoliminar/slugs/12b69509-5785-484d-8d70-02e441122c4a`
4. `GET https://api.heroku.com/apps/espacoliminar/slugs/36a68502-65b5-442a-8603-03e13abd3fec`

```text
requests:
  4
HTTP success:
  true (4/4)
retries:
  0
```

## Campos projetados

Cada resposta foi canalizada diretamente para o projetor local. Somente estes
campos foram retidos na projeção em memória:

```text
release:
  id
  version
  status
  current
  eligible_for_rollback
  slug.id

slug:
  id
  commit
```

A Platform API retornou `version` numericamente (`78` e `77`). Para comparar
com a forma canônica do contrato, a validação aplicou somente a normalização
determinística `78 -> v78` e `77 -> v77`; os valores projetados permaneceram
identificados como numéricos.

## Identidade e elegibilidade de v78

| Invariante | Valor projetado/derivado | Confirmado |
|---|---|---:|
| Release ID | `a41fefb4-0002-4c7d-bfec-de36d1efae9c` | true |
| Version canônica | `v78` | true |
| Status | `succeeded` | true |
| Current | `true` | true |
| Eligible for rollback | `true` | true |
| Slug ID da release | `12b69509-5785-484d-8d70-02e441122c4a` | true |
| Slug ID consultado | `12b69509-5785-484d-8d70-02e441122c4a` | true |
| Slug commit | `18d809e4169daa301839542191f0d6794b02d695` | true |
| Commit igual ao deployed SHA reconciliado | `true` | true |

## Identidade e elegibilidade de v77

| Invariante | Valor projetado/derivado | Confirmado |
|---|---|---:|
| Release ID | `57cafc0a-4f22-46ed-9024-b237110a6b73` | true |
| Version canônica | `v77` | true |
| Status | `succeeded` | true |
| Current | `false` | true |
| Eligible for rollback | `true` | true |
| Slug ID da release | `36a68502-65b5-442a-8603-03e13abd3fec` | true |
| Slug ID consultado | `36a68502-65b5-442a-8603-03e13abd3fec` | true |
| Slug commit | `9373b04e524512150bc10c1f759051e794a20000` | true |
| Commit igual ao rollback SHA reconciliado | `true` | true |

## Invariantes cruzados

```text
v78 != v77:
  true
v78 commit == reconciled deployed SHA:
  true
v77 commit == reconciled rollback SHA:
  true
rollback executed by this gate:
  false
current state consistent with no rollback:
  true
missing required fields:
  0
confirmed invariant checks:
  19
```

O valor `rollback executed by this gate: false` deriva do ledger operacional:
o gate executou somente os quatro `GET` allowlisted e nenhuma operação de
mutação. Ele também é consistente com `v78.current=true` e
`v77.current=false`.

## Sanitização

```text
raw JSON printed:
  false
raw evidence persisted:
  false
response headers persisted:
  false
credential material exposed:
  false
non-allowlisted payload persisted:
  false
temporary files retained:
  false
```

Nenhum JSON bruto, header de resposta, token, `.netrc`, usuário, e-mail,
descrição, `output_stream_url`, metadata de app, artifacts, config,
environment, cookies, PII ou payload não allowlisted foi impresso ou
persistido.

## Ledger de não-ações

Não foram executados:

- `POST`, `PATCH`, `PUT` ou `DELETE`;
- rollback, deploy, criação ou retry de release;
- restart, scale ou leitura/mutação de config;
- `heroku auth:token` ou leitura direta de credenciais;
- logs, dynos, health/smoke, banco, migrations ou providers;
- endpoint adicional ou retry;
- edição de SUMMARY histórico;
- stage, commit ou push;
- `REVIEW`, `CLOSURE` ou trabalho da Phase 13.

```text
rollback:
  not executed
production mutations:
  0
stage/commit/push:
  not executed
```

## Estado final e próximo gate permitido

```text
HEAD:
  7bbf4904d36fddfbd1f89732e724438d54482de3
only changed or untracked file:
  12.1-06-CORRECTION-C3-SUMMARY.md
git diff --check:
  empty
HTTP requests:
  4 GET
raw evidence persisted:
  false
credential material exposed:
  false
temporary files:
  removed
rollback:
  not executed
production mutations:
  0
stage/commit/push:
  not executed
```

Próximo gate permitido: reexecução do `12.1-06`, somente mediante autorização
separada. Parar aqui para revisão humana.
