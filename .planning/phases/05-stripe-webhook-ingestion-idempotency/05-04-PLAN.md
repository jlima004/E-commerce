---
phase: "05-stripe-webhook-ingestion-idempotency"
plan: 04
type: validation
wave: 4
depends_on:
  - 05-03
files_modified:
  - apps/backend/integration-tests/http/stripe-webhook-store.spec.ts
  - apps/backend/src/modules/webhooks/__tests__/webhook-event-log.unit.spec.ts
  - apps/backend/src/modules/payment-attempt/__tests__/payment-attempt-webhook.unit.spec.ts
  - .planning/phases/05-stripe-webhook-ingestion-idempotency/05-04-SUMMARY.md
autonomous: false
requirements:
  - WHK-01
  - WHK-02
requirements_addressed:
  - WHK-01
  - WHK-02
must_haves:
  truths:
    - "Phase 05 tem matriz completa de assinatura, dedup, eventos suportados, divergencias e replay."
    - "Provas negativas confirmam ausencia de Order, CheckoutCompletionLog, purchase_completed, Gelato, email e analytics."
    - "Stripe CLI smoke fica documentado, mas nao executado automaticamente."
  prohibitions:
    - "Nao executar Phase 06."
    - "Nao rodar smoke Stripe real sem aprovacao humana e env local preparado."
    - "Nao registrar segredos ou payloads sensiveis em summaries."
---

<objective>
Fechar a Phase 05 com validacao automatizada e negativa, deixando o proximo manual gate pronto para Phase 06.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-stripe-webhook-ingestion-idempotency/05-VALIDATION.md
@.planning/phases/05-stripe-webhook-ingestion-idempotency/05-01-SUMMARY.md
@.planning/phases/05-stripe-webhook-ingestion-idempotency/05-02-SUMMARY.md
@.planning/phases/05-stripe-webhook-ingestion-idempotency/05-03-SUMMARY.md
</context>

## Artifacts this phase produces

- Final Phase 05 negative proof.
- Stripe CLI smoke instructions, not executed.
- `05-04-SUMMARY.md` with acceptance evidence and explicit manual gate before Phase 06.

<tasks>
<task id="05-04-01" type="test">
  <name>Task 05-04-01: Completar matriz unitária e HTTP</name>
  <files>apps/backend/integration-tests/http/stripe-webhook-store.spec.ts, apps/backend/src/modules/webhooks/__tests__/webhook-event-log.unit.spec.ts, apps/backend/src/modules/payment-attempt/__tests__/payment-attempt-webhook.unit.spec.ts</files>
  <read_first>
    @.planning/phases/05-stripe-webhook-ingestion-idempotency/05-VALIDATION.md
    @apps/backend/package.json
  </read_first>
  <action>Garantir cobertura para assinatura valida, assinatura ausente, assinatura invalida, evento duplicado, replay concorrente, `payment_intent.succeeded`, `payment_intent.payment_failed`, `payment_intent.canceled`, evento ignorado, tentativa inexistente, amount/currency divergente, tentativa terminal, ausencia de Order, ausencia de `purchase_completed`, ausencia de Gelato e ausencia de segredo persistido/logado.</action>
  <verify>
    <automated>cd apps/backend && TMPDIR=/tmp npm run test:unit -- --runTestsByPath src/modules/webhooks/__tests__/webhook-event-log.unit.spec.ts src/modules/payment-attempt/__tests__/payment-attempt-webhook.unit.spec.ts && TMPDIR=/tmp npm run test:integration:http -- --runTestsByPath integration-tests/http/stripe-webhook-store.spec.ts</automated>
  </verify>
  <acceptance_criteria>Todos os cenarios listados aparecem por nome de teste ou matcher no output Jest; suites passam sem watch mode.</acceptance_criteria>
</task>

<task id="05-04-02" type="validation">
  <name>Task 05-04-02: Executar build e provas negativas</name>
  <files>.planning/phases/05-stripe-webhook-ingestion-idempotency/05-04-SUMMARY.md</files>
  <read_first>
    @.planning/phases/05-stripe-webhook-ingestion-idempotency/05-VALIDATION.md
    @.planning/phases/04A-stripe-real-layer-activation/04A-SUMMARY.md
  </read_first>
  <action>Rodar unit, integration HTTP, build com `ADMIN_DISABLED=true` e greps negativos. Registrar no summary apenas comandos e resultados saneados; nao colar secrets, assinatura Stripe, client_secret, Pix QR/copia-e-cola ou payload bruto.</action>
  <verify>
    <automated>cd apps/backend && HOME=/tmp XDG_CONFIG_HOME=/tmp TMPDIR=/tmp ADMIN_DISABLED=true npm run build</automated>
    <automated>cd apps/backend && ! rg -n "completeCartWorkflow|createOrderWorkflow|CheckoutCompletionLog|purchase_completed|order\\.gelatoapis\\.com|gelato_order_id|EmailDeliveryLog|AnalyticsEventLog" src/api/hooks src/modules/webhooks src/modules/payment-attempt</automated>
    <automated>cd apps/backend && ! rg -n "client_secret|pi_[A-Za-z0-9_]+_secret_|pix_display_qr_code|copy_paste|hosted_instructions_url|Authorization|cookies" src/modules/webhooks src/api/hooks/stripe</automated>
  </verify>
  <acceptance_criteria>`05-04-SUMMARY.md` registra build verde, negative grep verde, nenhum Order/completion/purchase/Gelato/e-mail/analytics/refund e `PaymentAttempt.order_id = null`.</acceptance_criteria>
</task>

<task id="05-04-03" type="manual">
  <name>Task 05-04-03: Documentar smoke Stripe CLI futuro e manual gate</name>
  <files>.planning/phases/05-stripe-webhook-ingestion-idempotency/05-04-SUMMARY.md</files>
  <read_first>
    @.planning/phases/05-stripe-webhook-ingestion-idempotency/05-RESEARCH.md
    @.planning/phases/04A-stripe-real-layer-activation/04A-SUMMARY.md
  </read_first>
  <action>Documentar smoke conceitual com `stripe listen --forward-to localhost:9001/hooks/stripe` e evento correspondente a um `PaymentAttempt` real, sem executar e sem registrar segredo. Encerrar com manual gate: Phase 06 pode ser planejada/executada somente após revisao humana de `05-04-SUMMARY.md` e deve criar Order apenas a partir de `payment_confirmed_by_webhook`.</action>
  <verify>
    <automated>rg -n "stripe listen --forward-to localhost:9001/hooks/stripe|payment_confirmed_by_webhook|Phase 06|manual gate" .planning/phases/05-stripe-webhook-ingestion-idempotency/05-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>Summary contem proximo manual gate e nao contem `whsec_`, `sk_test_`, `client_secret`, `pi_.*_secret`, QR ou copia-e-cola reais.</acceptance_criteria>
</task>
</tasks>

<verification>
- `cd apps/backend && TMPDIR=/tmp npm run test:unit -- --runTestsByPath src/modules/webhooks/__tests__/webhook-event-log.unit.spec.ts src/modules/payment-attempt/__tests__/payment-attempt-webhook.unit.spec.ts`
- `cd apps/backend && TMPDIR=/tmp npm run test:integration:http -- --runTestsByPath integration-tests/http/stripe-webhook-store.spec.ts`
- `cd apps/backend && HOME=/tmp XDG_CONFIG_HOME=/tmp TMPDIR=/tmp ADMIN_DISABLED=true npm run build`
- `cd apps/backend && ! rg -n "completeCartWorkflow|createOrderWorkflow|CheckoutCompletionLog|purchase_completed|order\\.gelatoapis\\.com|gelato_order_id|EmailDeliveryLog|AnalyticsEventLog" src/api/hooks src/modules/webhooks src/modules/payment-attempt`
- `cd apps/backend && ! rg -n "client_secret|pi_[A-Za-z0-9_]+_secret_|pix_display_qr_code|copy_paste|hosted_instructions_url|Authorization|cookies" src/modules/webhooks src/api/hooks/stripe`
</verification>

<success_criteria>
- `WHK-01` e `WHK-02` completos para Phase 05.
- Assinatura Stripe, raw body e dedup idempotente provados.
- `PaymentAttempt` confirmado/falhado/cancelado por webhook sem Order.
- Phase 06 fica como proximo passo natural para Order creation.
- Execucao para em manual gate.
</success_criteria>

<manual_review_gate>
PARAR AQUI. A proxima acao e revisao humana de `05-04-SUMMARY.md`; Phase 06 nao deve iniciar automaticamente.
</manual_review_gate>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-I | Information Disclosure | summaries/tests | mitigate | Evidencia saneada e grep de segredo/payload. |
| T-05-04-R | Repudiation | acceptance evidence | mitigate | Summary registra comandos, resultados e manual gate. |
| T-05-04-E | Elevation of Privilege | phase boundary | mitigate | Grep/test bloqueia Phase 06+ e efeitos downstream. |
</threat_model>
