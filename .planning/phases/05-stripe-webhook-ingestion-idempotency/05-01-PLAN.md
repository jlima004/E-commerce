---
phase: "05-stripe-webhook-ingestion-idempotency"
plan: 01
type: implementation
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/webhooks/index.ts
  - apps/backend/src/modules/webhooks/models/webhook-event-log.ts
  - apps/backend/src/modules/webhooks/service.ts
  - apps/backend/src/modules/webhooks/types.ts
  - apps/backend/src/modules/webhooks/migrations/Migration20260701000000.ts
  - apps/backend/src/modules/webhooks/__tests__/webhook-event-log.unit.spec.ts
  - apps/backend/src/config/env.ts
  - apps/backend/src/config/__tests__/env.unit.spec.ts
  - .planning/phases/05-stripe-webhook-ingestion-idempotency/05-01-SUMMARY.md
autonomous: false
requirements:
  - WHK-01
  - WHK-02
requirements_addressed:
  - WHK-02
must_haves:
  truths:
    - "`WebhookEventLog` existe como modulo custom isolado para eventos Stripe/Gelato."
    - "`WebhookEventLog` tem `deduplication_key` obrigatoria e unique canonica `provider + deduplication_key`."
    - "Stripe deriva `deduplication_key` de `event.id` quando presente; fallback por hash entra na propria `deduplication_key`."
    - "Env planejado inclui `STRIPE_WEBHOOK_SECRET` e `STRIPE_WEBHOOK_INGESTION_ENABLED` com fail-closed em producao."
  prohibitions:
    - "Nao criar endpoint `/hooks/stripe` neste plano."
    - "Nao criar Order."
    - "Nao criar CheckoutCompletionLog."
    - "Nao emitir purchase_completed."
    - "Nao chamar Gelato, email, analytics ou refund flow."
    - "Nao aplicar migration automaticamente."
---

<objective>
Criar a base de schema/modulo/config da Phase 05 para registrar eventos externos de forma idempotente, sem ainda expor a rota Stripe.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-stripe-webhook-ingestion-idempotency/05-CONTEXT.md
@.planning/phases/05-stripe-webhook-ingestion-idempotency/05-RESEARCH.md
@docs/DB_MODEL_v1.21.md
@apps/backend/src/config/env.ts
@apps/backend/src/modules/payment-attempt/migrations/Migration20260629000000.ts
</context>

## Artifacts this phase produces

- `WebhookEventLog` model, service, types and migration draft.
- Helpers for `payload_hash`, `deduplication_key` and status vocabulary.
- Env parsing additions for `STRIPE_WEBHOOK_SECRET` and `STRIPE_WEBHOOK_INGESTION_ENABLED`.
- Unit tests for dedup key, status and config fail-closed behavior.

<tasks>
<task id="05-01-01" type="code">
  <name>Task 05-01-01: Modelar WebhookEventLog e migration draft</name>
  <files>apps/backend/src/modules/webhooks/models/webhook-event-log.ts, apps/backend/src/modules/webhooks/migrations/Migration20260701000000.ts</files>
  <read_first>
    @docs/DB_MODEL_v1.21.md
    @apps/backend/src/modules/payment-attempt/models/payment-attempt.ts
    @apps/backend/src/modules/payment-attempt/migrations/Migration20260629000000.ts
  </read_first>
  <action>Criar modelo `webhook_event_log` com campos: `id`, `provider`, `external_event_id`, `event_type`, `entity_type`, `entity_id`, `payload_hash`, `deduplication_key`, `status`, `processing_attempts`, `error_code`, `error_message`, `metadata`, `received_at`, `processed_at`, `ignored_at`, `failed_at`, timestamps Medusa e `deleted_at`. A migration draft deve criar unique parcial `provider, external_event_id` quando `external_event_id` nao nulo, unique canonico `provider, deduplication_key`, indice `provider, payload_hash`, indice `event_type`, indice `status, received_at`, indice `entity_type, entity_id`, e check constraints para provider/status/entity_type. Nao executar `medusa db:migrate`.</action>
  <verify>
    <automated>cd apps/backend && TMPDIR=/tmp npm run test:unit -- --runTestsByPath src/modules/webhooks/__tests__/webhook-event-log.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>Migration contem `UNIQUE` ou `CREATE UNIQUE INDEX` para `provider, deduplication_key` e nao contem campos para payload bruto. Modelo nao inclui `raw_body`, `client_secret`, `authorization`, `cookies`, `qr_code` ou `copy_paste`.</acceptance_criteria>
</task>

<task id="05-01-02" type="code">
  <name>Task 05-01-02: Criar service/helpers idempotentes e vocabulary de status</name>
  <files>apps/backend/src/modules/webhooks/service.ts, apps/backend/src/modules/webhooks/types.ts, apps/backend/src/modules/webhooks/__tests__/webhook-event-log.unit.spec.ts</files>
  <read_first>
    @docs/DB_MODEL_v1.21.md
    @apps/backend/src/modules/payment-attempt/state-machine.ts
    @apps/backend/src/observability/sanitize.ts
  </read_first>
  <action>Criar helpers puros para `buildWebhookPayloadHash`, `buildStripeDeduplicationKey`, status `received | processing | processed | ignored | failed`, metadata allowlist e erro saneado. Para Stripe, `buildStripeDeduplicationKey({ external_event_id, payload_hash })` retorna `external_event_id` quando presente; caso contrario retorna `payload_hash:${payload_hash}`. Rejeitar metadata com secrets conhecidos, raw body, headers, cookies, Pix QR/copia-e-cola integral ou `client_secret`.</action>
  <verify>
    <automated>cd apps/backend && TMPDIR=/tmp npm run test:unit -- --runTestsByPath src/modules/webhooks/__tests__/webhook-event-log.unit.spec.ts -t "dedup|metadata|hash"</automated>
  </verify>
  <acceptance_criteria>Testes provam dedup por `event.id`, fallback por `payload_hash:` e rejeicao de metadata sensivel.</acceptance_criteria>
</task>

<task id="05-01-03" type="code">
  <name>Task 05-01-03: Planejar env fail-closed para webhook Stripe</name>
  <files>apps/backend/src/config/env.ts, apps/backend/src/config/__tests__/env.unit.spec.ts</files>
  <read_first>
    @apps/backend/src/config/env.ts
    @apps/backend/package.json
    @docs/PRD_Backend_v1.1.md
  </read_first>
  <action>Adicionar `STRIPE_WEBHOOK_SECRET` e `STRIPE_WEBHOOK_INGESTION_ENABLED` ao parser de env. A flag deve usar o padrao booleano existente. Em producao, se ingestao estiver habilitada, `STRIPE_WEBHOOK_SECRET` deve ser obrigatorio, nao placeholder e com formato `whsec_`. Em ambiente nao-producao, permitir ausencia somente quando ingestao estiver desabilitada. Nao documentar segredo real.</action>
  <verify>
    <automated>cd apps/backend && TMPDIR=/tmp npm run test:unit -- --runTestsByPath src/config/__tests__/env.unit.spec.ts -t "STRIPE_WEBHOOK"</automated>
  </verify>
  <acceptance_criteria>`parseEnv` expoe `STRIPE_WEBHOOK_SECRET` e `STRIPE_WEBHOOK_INGESTION_ENABLED`; testes cobrem secret ausente, formato invalido, producao fail-closed e modo local desabilitado.</acceptance_criteria>
</task>
</tasks>

<verification>
- `cd apps/backend && TMPDIR=/tmp npm run test:unit -- --runTestsByPath src/modules/webhooks/__tests__/webhook-event-log.unit.spec.ts src/config/__tests__/env.unit.spec.ts`
- `cd apps/backend && ! rg -n "raw_body|client_secret|authorization|cookies|copy_paste|qr_code" src/modules/webhooks/models src/modules/webhooks/migrations`
</verification>

<success_criteria>
- `WebhookEventLog` e migration draft existem sem serem aplicados automaticamente.
- Dedup canonico esta no banco por `provider + deduplication_key`.
- Config de webhook Stripe falha fechado em producao.
- Nenhuma rota `/hooks/stripe` existe ainda neste plano.
- Nenhum Order, `CheckoutCompletionLog`, `purchase_completed` ou Gelato e criado.
</success_criteria>

<manual_review_gate>
Parar após summary `05-01-SUMMARY.md` para revisar schema e config antes de expor `/hooks/stripe`.
</manual_review_gate>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-R | Repudiation | WebhookEventLog | mitigate | Unique `provider + deduplication_key` e status auditavel. |
| T-05-01-I | Information Disclosure | metadata/log | mitigate | Allowlist e bloqueio de secrets/payload bruto. |
| T-05-01-T | Tampering | env/config | mitigate | Produção exige `whsec_` quando ingestao habilitada. |
</threat_model>
