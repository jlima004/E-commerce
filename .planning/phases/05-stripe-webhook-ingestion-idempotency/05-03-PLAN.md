---
phase: "05-stripe-webhook-ingestion-idempotency"
plan: 03
type: implementation
wave: 3
depends_on:
  - 05-02
files_modified:
  - apps/backend/src/modules/payment-attempt/types.ts
  - apps/backend/src/modules/payment-attempt/state-machine.ts
  - apps/backend/src/modules/payment-attempt/models/payment-attempt.ts
  - apps/backend/src/modules/payment-attempt/migrations/Migration20260629000000.ts
  - apps/backend/src/modules/payment-attempt/service.ts
  - apps/backend/src/modules/payment-attempt/__tests__/payment-attempt-webhook.unit.spec.ts
  - apps/backend/src/api/hooks/stripe/route.ts
  - apps/backend/integration-tests/http/stripe-webhook-store.spec.ts
  - .planning/phases/05-stripe-webhook-ingestion-idempotency/05-03-SUMMARY.md
autonomous: false
requirements:
  - WHK-01
  - WHK-02
requirements_addressed:
  - WHK-02
must_haves:
  truths:
    - "`payment_intent.succeeded` atualiza PaymentAttempt para `payment_confirmed_by_webhook`."
    - "`payment_intent.payment_failed` atualiza PaymentAttempt para `payment_failed`."
    - "`payment_intent.canceled` atualiza PaymentAttempt para `payment_canceled`."
    - "`PaymentAttempt.order_id` permanece null em todos os caminhos da Phase 05."
  prohibitions:
    - "Nao chamar completeCartWorkflow ou createOrderWorkflow."
    - "Nao criar CheckoutCompletionLog."
    - "Nao emitir purchase_completed."
    - "Nao chamar Gelato, email, analytics ou refund flow."
---

<objective>
Processar os eventos Stripe suportados para atualizar somente `PaymentAttempt`, deixando a tentativa confirmada localmente para a Phase 06 criar Order.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-stripe-webhook-ingestion-idempotency/05-CONTEXT.md
@apps/backend/src/modules/payment-attempt/types.ts
@apps/backend/src/modules/payment-attempt/state-machine.ts
@apps/backend/src/modules/payment-attempt/service.ts
@apps/backend/src/api/hooks/stripe/route.ts
</context>

## Artifacts this phase produces

- Novo status `payment_confirmed_by_webhook`.
- Helpers de aplicacao de eventos Stripe em `PaymentAttempt`.
- Handler para `payment_intent.succeeded`, `payment_intent.payment_failed`, `payment_intent.canceled`.
- Testes de correlacao, divergencia e eventos fora de ordem.

<tasks>
<task id="05-03-01" type="code">
  <name>Task 05-03-01: Adicionar estado confirmado por webhook</name>
  <files>apps/backend/src/modules/payment-attempt/types.ts, apps/backend/src/modules/payment-attempt/state-machine.ts, apps/backend/src/modules/payment-attempt/models/payment-attempt.ts, apps/backend/src/modules/payment-attempt/migrations/Migration20260629000000.ts</files>
  <read_first>
    @apps/backend/src/modules/payment-attempt/types.ts
    @apps/backend/src/modules/payment-attempt/state-machine.ts
    @apps/backend/src/modules/payment-attempt/migrations/Migration20260629000000.ts
  </read_first>
  <action>Adicionar `PAYMENT_CONFIRMED_BY_WEBHOOK: "payment_confirmed_by_webhook"` aos tipos, enum do model e migration preparada. Permitir transicoes de `card_client_secret_created`, `payment_client_confirmed`, `awaiting_pix_payment` e `awaiting_webhook_confirmation` para `payment_confirmed_by_webhook`. Manter `order_id = null`. Nao adicionar labels proibidos `paid`, `succeeded`, `captured` ou `confirmed_payment`. Tratar o estado como ativo para impedir nova tentativa antes da Phase 06.</action>
  <verify>
    <automated>cd apps/backend && TMPDIR=/tmp npm run test:unit -- --runTestsByPath src/modules/payment-attempt/__tests__/payment-attempt-webhook.unit.spec.ts -t "payment_confirmed_by_webhook"</automated>
  </verify>
  <acceptance_criteria>`PAYMENT_ATTEMPT_STATUSES` contem `payment_confirmed_by_webhook`; `PROHIBITED_PAYMENT_ATTEMPT_STATUSES` continua bloqueando labels financeiros proibidos; teste prova `order_id` null.</acceptance_criteria>
</task>

<task id="05-03-02" type="code">
  <name>Task 05-03-02: Validar PaymentIntent contra PaymentAttempt</name>
  <files>apps/backend/src/modules/payment-attempt/service.ts, apps/backend/src/modules/payment-attempt/__tests__/payment-attempt-webhook.unit.spec.ts</files>
  <read_first>
    @docs/DB_MODEL_v1.21.md
    @apps/backend/src/modules/payment-attempt/service.ts
    @apps/backend/src/modules/payment-attempt/types.ts
  </read_first>
  <action>Criar helper puro para localizar e validar tentativa por `provider_payment_intent_id`. Validar `amount_received` ou `amount`, `currency = brl`, `metadata.cart_id` quando presente, `payment_method_type`, e status nao-terminal. Retornar erro saneado para tentativa inexistente, status terminal, amount/currency divergente ou metodo divergente. Nao incluir payload bruto em erros.</action>
  <verify>
    <automated>cd apps/backend && TMPDIR=/tmp npm run test:unit -- --runTestsByPath src/modules/payment-attempt/__tests__/payment-attempt-webhook.unit.spec.ts -t "amount|currency|terminal|missing"</automated>
  </verify>
  <acceptance_criteria>Testes cobrem tentativa inexistente, terminal, amount divergente, currency divergente e metodo divergente sem atualizar status.</acceptance_criteria>
</task>

<task id="05-03-03" type="code">
  <name>Task 05-03-03: Processar eventos PaymentIntent suportados</name>
  <files>apps/backend/src/api/hooks/stripe/route.ts, apps/backend/integration-tests/http/stripe-webhook-store.spec.ts</files>
  <read_first>
    @apps/backend/src/api/hooks/stripe/route.ts
    @apps/backend/src/modules/webhooks/service.ts
    @apps/backend/src/modules/payment-attempt/service.ts
  </read_first>
  <action>Integrar o processamento de eventos suportados depois da deduplicacao. `payment_intent.succeeded` chama helper de confirmacao e marca log `processed`. `payment_intent.payment_failed` marca tentativa `payment_failed`. `payment_intent.canceled` marca tentativa `payment_canceled`. Tentativa ausente ou divergente marca log `failed` saneado e retorna resposta Stripe segura conforme decisao do plano; nao cria Order. Duplicata retorna 200 no-op.</action>
  <verify>
    <automated>cd apps/backend && TMPDIR=/tmp npm run test:integration:http -- --runTestsByPath integration-tests/http/stripe-webhook-store.spec.ts -t "payment_intent.succeeded|payment_failed|canceled"</automated>
  </verify>
  <acceptance_criteria>Eventos assinados atualizam uma tentativa correlata exatamente uma vez; `order_id` continua null; replay nao altera novamente; log termina `processed`, `ignored` ou `failed`.</acceptance_criteria>
</task>
</tasks>

<verification>
- `cd apps/backend && TMPDIR=/tmp npm run test:unit -- --runTestsByPath src/modules/payment-attempt/__tests__/payment-attempt-webhook.unit.spec.ts`
- `cd apps/backend && TMPDIR=/tmp npm run test:integration:http -- --runTestsByPath integration-tests/http/stripe-webhook-store.spec.ts -t "payment_intent"`
- `cd apps/backend && ! rg -n "completeCartWorkflow|createOrderWorkflow|CheckoutCompletionLog|purchase_completed|order\\.gelatoapis\\.com|gelato_order_id" src/api/hooks src/modules/payment-attempt src/modules/webhooks`
</verification>

<success_criteria>
- Sucesso Stripe confirma apenas `PaymentAttempt`.
- Falha/cancelamento Stripe atualiza apenas `PaymentAttempt`.
- Tentativas inexistentes/stale/divergentes nao criam Order nem crasham o handler.
- Duplicatas sao idempotentes.
</success_criteria>

<manual_review_gate>
Parar após summary `05-03-SUMMARY.md` para revisar a fronteira entre PaymentAttempt confirmado e Phase 06.
</manual_review_gate>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-T | Tampering | PaymentIntent correlation | mitigate | Validar PI id, amount, currency, cart e metodo antes da mutacao. |
| T-05-03-E | Elevation of Privilege | Order boundary | mitigate | Negative grep/test bloqueia workflows de Order e efeitos downstream. |
| T-05-03-R | Repudiation | replay/out-of-order | mitigate | `WebhookEventLog` registra resultado e dedup impede nova mutacao. |
</threat_model>
