---
phase: "05-stripe-webhook-ingestion-idempotency"
plan: 02
type: implementation
wave: 2
depends_on:
  - 05-01
files_modified:
  - apps/backend/src/api/middlewares.ts
  - apps/backend/src/api/hooks/stripe/route.ts
  - apps/backend/src/api/hooks/stripe/__tests__/stripe-webhook-route.unit.spec.ts
  - apps/backend/integration-tests/http/stripe-webhook-store.spec.ts
  - .planning/phases/05-stripe-webhook-ingestion-idempotency/05-02-SUMMARY.md
autonomous: false
requirements:
  - WHK-01
  - WHK-02
requirements_addressed:
  - WHK-01
  - WHK-02
must_haves:
  truths:
    - "`POST /hooks/stripe` usa raw body preservado e `stripe.webhooks.constructEvent`."
    - "Assinatura ausente ou invalida retorna 400 antes de DB."
    - "Evento assinado valido registra/localiza `WebhookEventLog` idempotente."
    - "Eventos nao suportados sao `ignored` e nao mutam negocio."
  prohibitions:
    - "Nao usar `req.body` parseado para assinatura."
    - "Nao aceitar fallback sem assinatura valida."
    - "Nao criar Order, CheckoutCompletionLog, purchase_completed ou Gelato."
---

<objective>
Implementar a rota Stripe webhook com raw body e assinatura, registrando eventos validos de forma idempotente e ignorando eventos fora do escopo sem efeitos de negocio.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-stripe-webhook-ingestion-idempotency/05-CONTEXT.md
@.planning/phases/05-stripe-webhook-ingestion-idempotency/05-RESEARCH.md
@.planning/phases/05-stripe-webhook-ingestion-idempotency/05-01-SUMMARY.md
@apps/backend/src/api/middlewares.ts
@apps/backend/src/modules/webhooks/service.ts
</context>

## Artifacts this phase produces

- Raw-body middleware config for `/hooks/stripe`.
- `apps/backend/src/api/hooks/stripe/route.ts`.
- Tests for signature valid/missing/invalid, disabled ingestion and ignored events.

<tasks>
<task id="05-02-01" type="code">
  <name>Task 05-02-01: Configurar raw body no middleware Medusa</name>
  <files>apps/backend/src/api/middlewares.ts</files>
  <read_first>
    @apps/backend/src/api/middlewares.ts
    @.planning/phases/05-stripe-webhook-ingestion-idempotency/05-RESEARCH.md
  </read_first>
  <action>Adicionar rota de middleware para `method: ["POST"]`, `matcher: "/hooks/stripe"`, `bodyParser: { preserveRawBody: true }`. Manter `correlationAndAccessLogMiddleware` global. Nao adicionar auth Store/Admin ou CORS especifico ao webhook.</action>
  <verify>
    <automated>cd apps/backend && rg -n "matcher: \"/hooks/stripe\"|preserveRawBody" src/api/middlewares.ts</automated>
  </verify>
  <acceptance_criteria>`src/api/middlewares.ts` contem `bodyParser: { preserveRawBody: true }` para `/hooks/stripe`.</acceptance_criteria>
</task>

<task id="05-02-02" type="code">
  <name>Task 05-02-02: Implementar verificacao de assinatura e ingestao minima</name>
  <files>apps/backend/src/api/hooks/stripe/route.ts, apps/backend/src/api/hooks/stripe/__tests__/stripe-webhook-route.unit.spec.ts</files>
  <read_first>
    @apps/backend/src/config/env.ts
    @apps/backend/src/modules/webhooks/service.ts
    @apps/backend/src/observability/sanitize.ts
  </read_first>
  <action>Criar `POST` que verifica `STRIPE_WEBHOOK_INGESTION_ENABLED`, exige `req.rawBody`, exige header `stripe-signature`, valida com `stripe.webhooks.constructEvent`, calcula hash/dedup, e registra/localiza `WebhookEventLog`. Assinatura ausente/invalida/raw body ausente retorna 400 sem chamada ao service. Ingestao desabilitada retorna 503 saneado sem processar. Nao logar payload bruto ou secrets.</action>
  <verify>
    <automated>cd apps/backend && TMPDIR=/tmp npm run test:unit -- --runTestsByPath src/api/hooks/stripe/__tests__/stripe-webhook-route.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>Teste prova que `constructEvent` recebe `rawBody`; assinatura ausente/invalida nao chama `WebhookEventLog`; evento valido chama o service com metadata allowlist.</acceptance_criteria>
</task>

<task id="05-02-03" type="code">
  <name>Task 05-02-03: Persistir eventos ignorados idempotentemente</name>
  <files>apps/backend/src/api/hooks/stripe/route.ts, apps/backend/integration-tests/http/stripe-webhook-store.spec.ts</files>
  <read_first>
    @apps/backend/src/modules/webhooks/types.ts
    @docs/DB_MODEL_v1.21.md
  </read_first>
  <action>Para eventos fora de `payment_intent.succeeded`, `payment_intent.payment_failed` e `payment_intent.canceled`, registrar `WebhookEventLog.status = ignored`, `event_type` original, `entity_type = unknown` ou `payment_attempt` quando houver PaymentIntent seguro, e retornar 200. Redelivery do mesmo evento deve retornar 200 sem nova mutacao.</action>
  <verify>
    <automated>cd apps/backend && TMPDIR=/tmp npm run test:integration:http -- --runTestsByPath integration-tests/http/stripe-webhook-store.spec.ts -t "ignored|duplicate"</automated>
  </verify>
  <acceptance_criteria>Evento assinado nao suportado gera exatamente um log `ignored`; replay retorna 200 e nao altera contagem.</acceptance_criteria>
</task>
</tasks>

<verification>
- `cd apps/backend && TMPDIR=/tmp npm run test:unit -- --runTestsByPath src/api/hooks/stripe/__tests__/stripe-webhook-route.unit.spec.ts`
- `cd apps/backend && TMPDIR=/tmp npm run test:integration:http -- --runTestsByPath integration-tests/http/stripe-webhook-store.spec.ts -t "signature|ignored|duplicate"`
- `cd apps/backend && ! rg -n "completeCartWorkflow|createOrderWorkflow|CheckoutCompletionLog|purchase_completed|order\\.gelatoapis\\.com|gelato_order_id" src/api/hooks src/modules/webhooks`
</verification>

<success_criteria>
- `/hooks/stripe` rejeita assinatura ausente/invalida com 400 antes de DB.
- `/hooks/stripe` aceita evento assinado valido e persiste log idempotente.
- Eventos ignorados sao idempotentes e nao mutam negocio.
- Nenhum Order ou efeito downstream e criado.
</success_criteria>

<manual_review_gate>
Parar após summary `05-02-SUMMARY.md` para revisar a superficie HTTP antes de atualizar `PaymentAttempt`.
</manual_review_gate>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-S | Spoofing | `/hooks/stripe` | mitigate | `constructEvent` com raw body e `stripe-signature`; 400 antes de DB. |
| T-05-02-I | Information Disclosure | route logs | mitigate | Metadata/log allowlist, sem raw payload. |
| T-05-02-D | Denial of Service | webhook route | defer | Phase 05 mantem handler minimo; rate/backpressure avancado fica ops futura. |
</threat_model>
