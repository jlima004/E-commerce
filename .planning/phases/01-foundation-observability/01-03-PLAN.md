---
phase: 01-foundation-observability
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - apps/backend/package.json
  - package-lock.json
  - apps/backend/medusa-config.ts
  - apps/backend/src/config/env.ts
  - apps/backend/src/infrastructure/redis-config.ts
  - apps/backend/src/infrastructure/__tests__/redis-config.unit.spec.ts
autonomous: false
requirements:
  - SETUP-02
must_haves:
  truths:
    - "D-11: os quatro contratos REDIS_URL, CACHE_REDIS_URL, EVENTS_REDIS_URL e WE_REDIS_URL existem mesmo quando apontam à mesma instância."
    - "D-12: production não inicia com cache, event bus ou workflow engine in-memory."
    - "D-13: a ausência de qualquer URL Redis obrigatória falha no startup antes da montagem dos módulos."
    - "Cache, event bus e workflow engine usam os providers Redis atuais do Medusa 2.16.0."
  artifacts:
    - path: "apps/backend/src/infrastructure/redis-config.ts"
      provides: "Configuração testável dos três módulos e endpoints únicos de probe"
      exports: ["buildRedisModules", "uniqueRedisUrls"]
    - path: "apps/backend/medusa-config.ts"
      provides: "Registro Redis no Medusa"
      contains: "@medusajs/medusa/workflow-engine-redis"
    - path: "apps/backend/src/infrastructure/__tests__/redis-config.unit.spec.ts"
      provides: "Prova de contratos separados e proibição de fallback"
  key_links:
    - from: "apps/backend/src/config/env.ts"
      to: "apps/backend/src/infrastructure/redis-config.ts"
      via: "objeto env validado"
      pattern: "(CACHE|EVENTS|WE)_REDIS_URL"
    - from: "apps/backend/src/infrastructure/redis-config.ts"
      to: "apps/backend/medusa-config.ts"
      via: "buildRedisModules(env)"
      pattern: "buildRedisModules"
---

<objective>
Substituir toda infraestrutura in-memory de produção por Redis com contratos separados.

Purpose: tornar cache, eventos e workflows duráveis e preparar os probes reais de readiness.
Output: providers Redis Medusa, schema fail-fast e utilitários testados para wiring/probes.
</objective>

## Phase Goal

**Como operador**, **quero iniciar e observar o backend Medusa com Admin, banco e Redis reais**, **para que as próximas capacidades de comércio sejam construídas sobre uma fundação executável, segura e verificável**.

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-observability/01-CONTEXT.md
@.planning/phases/01-foundation-observability/01-RESEARCH.md
@.planning/phases/01-foundation-observability/01-VALIDATION.md
@.planning/phases/01-foundation-observability/01-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING] Aprovar legitimidade do provider Redis e ioredis</name>
  <read_first>
    - .planning/phases/01-foundation-observability/01-RESEARCH.md — Package Legitimacy Audit e Standard Stack
    - apps/backend/package.json — versões Medusa já instaladas
  </read_first>
  <files>Sem alterações; gate anterior a npm install.</files>
  <action>Interromper antes da instalação. Conferir no npmjs.com e nos repositórios oficiais que `@medusajs/caching-redis@2.16.0` pertence ao source Medusa e que `ioredis@5.11.1` pertence ao repositório oficial Redis/ioredis. Confirmar que `@medusajs/caching-redis` está no mesmo minor dos pacotes Medusa existentes. Não trocar para o módulo legado nem alterar as versões pesquisadas.</action>
  <verify>
    <automated>npm view @medusajs/caching-redis@2.16.0 version repository.url && npm view ioredis@5.11.1 version repository.url</automated>
    <human-check>Confirmar visualmente os registros oficiais e responder "approved" antes da instalação.</human-check>
  </verify>
  <acceptance_criteria>
    - As versões retornadas são exatamente 2.16.0 e 5.11.1.
    - Os source repositories correspondem às organizações Medusa e Redis.
    - A instalação só começa depois da aprovação humana.
  </acceptance_criteria>
  <done>Pacotes Redis SUS aprovados sem alteração de versão.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Definir contratos Redis e builder testável</name>
  <read_first>
    - apps/backend/src/infrastructure/__tests__/redis-config.unit.spec.ts — contrato Wave 0
    - apps/backend/src/config/env.ts — schema existente
    - .planning/phases/01-foundation-observability/01-CONTEXT.md — D-11, D-12 e D-13
    - .planning/phases/01-foundation-observability/01-RESEARCH.md — wiring Redis atual e Pitfall 1
  </read_first>
  <files>apps/backend/src/config/env.ts, apps/backend/src/infrastructure/redis-config.ts, apps/backend/src/infrastructure/__tests__/redis-config.unit.spec.ts</files>
  <behavior>
    - Production sem qualquer um dos quatro contratos Redis falha.
    - Uma única URL pode preencher explicitamente os quatro contratos, sem apagar seus nomes.
    - `buildRedisModules` registra caching provider, event bus e workflow engine Redis com as opções atuais.
    - `uniqueRedisUrls` deduplica endpoints iguais para futuros probes sem perder a validação individual.
  </behavior>
  <action>Converter os `test.todo` Redis em testes RED. Estender `parseEnv` para exigir os quatro nomes em production. Criar `redis-config.ts` com `buildRedisModules(env)` retornando descritores do Caching Module `@medusajs/medusa/caching` + provider `@medusajs/caching-redis`, event bus `@medusajs/medusa/event-bus-redis` e workflow engine `@medusajs/medusa/workflow-engine-redis` usando `redis.redisUrl`. Exportar `uniqueRedisUrls(env)` para deduplicar endpoints destinados ao readiness. Manter `REDIS_URL` como contrato geral e os três contratos específicos como fontes explícitas dos módulos, per D-11.</action>
  <verify>
    <automated>cd apps/backend && npm run test:unit -- --runTestsByPath src/infrastructure/__tests__/redis-config.unit.spec.ts src/config/__tests__/env.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Cada URL Redis ausente em fixture production causa falha citando somente o nome da variável.
    - A configuração produz exatamente um provider default de caching, um event bus Redis e um workflow engine Redis.
    - O workflow engine usa a opção `redisUrl`.
    - Nenhuma configuração production seleciona implementação in-memory.
  </acceptance_criteria>
  <done>Contratos e descritores Redis estão testados antes do wiring runtime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Instalar providers aprovados e conectar o Medusa</name>
  <read_first>
    - apps/backend/src/infrastructure/redis-config.ts — builder aprovado
    - apps/backend/src/infrastructure/__tests__/redis-config.unit.spec.ts — assertions de wiring
    - apps/backend/medusa-config.ts — composição atual
    - apps/backend/package.json — dependências atuais
  </read_first>
  <files>apps/backend/package.json, package-lock.json, apps/backend/medusa-config.ts</files>
  <behavior>
    - Medusa production recebe os três módulos Redis do builder.
    - Local usa Redis quando os contratos são fornecidos.
    - Build resolve provider 2.16.0 e cliente 5.11.1.
  </behavior>
  <action>Instalar, após o gate humano, `@medusajs/caching-redis@2.16.0` e `ioredis@5.11.1`. Em `medusa-config.ts`, usar `buildRedisModules(env)` em production e também em local quando URLs forem fornecidas; não permitir seleção de fallback in-memory em production. Não adicionar configuração Redis alternativa fora do builder.</action>
  <verify>
    <automated>cd apps/backend && npm run test:unit -- --runTestsByPath src/infrastructure/__tests__/redis-config.unit.spec.ts src/config/__tests__/env.unit.spec.ts && npm run build && npm ls @medusajs/caching-redis ioredis</automated>
  </verify>
  <acceptance_criteria>
    - `medusa-config.ts` consome `buildRedisModules`.
    - Production monta exatamente caching, event bus e workflow engine Redis.
    - `npm ls` resolve 2.16.0 e 5.11.1.
    - Build retorna exit code 0.
  </acceptance_criteria>
  <done>Infraestrutura Redis durável está conectada ao runtime Medusa.</done>
</task>

</tasks>

## Artifacts this phase produces

- `apps/backend/src/infrastructure/redis-config.ts` — `buildRedisModules` e `uniqueRedisUrls`.
- Registro do Caching Module Redis, event bus Redis e workflow engine Redis.
- Contratos env dos quatro endpoints Redis.
- Testes unitários de wiring e fail-fast.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| configuração → Redis | URLs controlam conexões de cache, eventos e workflows. |
| npm registry → backend | Provider e cliente Redis recentes entram no runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-06 | Tampering | módulos Redis | mitigate | Builder testado e versões alinhadas ao Medusa 2.16.0. |
| T-01-07 | Denial of Service | fallback in-memory/restart | mitigate | Falha de startup em production sem os quatro contratos. |
| T-01-08 | Information Disclosure | erros de conexão | mitigate | Schema e testes nunca imprimem URLs. |
| T-01-SC | Tampering | npm installs | mitigate | Checkpoint humano bloqueante para ambos os pacotes SUS. |
</threat_model>

<verification>
- `cd apps/backend && npm run test:unit -- --runTestsByPath src/infrastructure/__tests__/redis-config.unit.spec.ts src/config/__tests__/env.unit.spec.ts`
- `cd apps/backend && npm run build`
- `npm ls --prefix apps/backend @medusajs/caching-redis ioredis`
</verification>

<success_criteria>
- Production exige os quatro contratos Redis.
- Os três subsistemas usam Redis e nenhum fallback in-memory.
- O build e os testes direcionados passam.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-observability/01-03-SUMMARY.md` when done.
</output>
