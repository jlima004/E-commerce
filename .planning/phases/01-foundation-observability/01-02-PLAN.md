---
phase: 01-foundation-observability
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - apps/backend/.env.template
  - apps/backend/medusa-config.ts
  - apps/backend/package.json
  - apps/backend/src/config/env.ts
  - apps/backend/src/config/__tests__/env.unit.spec.ts
  - apps/backend/scripts/run-migrations.mjs
autonomous: false
requirements:
  - SETUP-01
must_haves:
  truths:
    - "D-08: o schema distingue local e production; staging aparece apenas como convenção documentada."
    - "D-09: server e worker usam DATABASE_URL no runtime, enquanto migrations usam DATABASE_MIGRATION_URL direct/session apenas no subprocesso."
    - "D-10: migrations recusam URL ausente ou transaction pooler identificado por porta 6543."
    - "D-13: produção falha antes de conectar quando configuração obrigatória ou secrets seguros estão ausentes."
    - "Uma migration real pode ser executada no Supabase sem alterar permanentemente DATABASE_URL do shell ou do processo pai."
  artifacts:
    - path: "apps/backend/src/config/env.ts"
      provides: "Schema tipado e sensível ao ambiente"
      exports: ["env", "parseEnv"]
    - path: "apps/backend/scripts/run-migrations.mjs"
      provides: "Guard e subprocesso seguro de migrations"
      exports: ["assertMigrationUrl", "runMigrations"]
    - path: "apps/backend/.env.template"
      provides: "Contrato sem secrets para local e production"
    - path: "apps/backend/src/config/__tests__/env.unit.spec.ts"
      provides: "Prova de fail-fast e rejeição do transaction pooler"
  key_links:
    - from: "apps/backend/medusa-config.ts"
      to: "apps/backend/src/config/env.ts"
      via: "primeiro import de configuração antes de defineConfig"
      pattern: "from.*src/config/env"
    - from: "apps/backend/scripts/run-migrations.mjs"
      to: "apps/backend/package.json"
      via: "script npm db:migrate:safe"
      pattern: "db:migrate:safe"
---

<objective>
Implementar a estratégia Supabase/Postgres e o caminho de migrations fail-closed.

Purpose: garantir que o runtime use pooling adequado e que migrations nunca caiam no transaction pooler, per D-09 e D-10.
Output: schema de ambiente, template de variáveis, guard testado e execução externa bloqueante de migration.
</objective>

## Phase Goal

**Como operador**, **quero iniciar e observar o backend Medusa com Admin, banco e Redis reais**, **para que as próximas capacidades de comércio sejam construídas sobre uma fundação executável, segura e verificável**.

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-observability/01-CONTEXT.md
@.planning/phases/01-foundation-observability/01-RESEARCH.md
@.planning/phases/01-foundation-observability/01-VALIDATION.md
@.planning/phases/01-foundation-observability/01-01-SUMMARY.md
@docs/SRS_v1.5.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Definir schema de ambiente tipado e fail-fast</name>
  <read_first>
    - apps/backend/src/config/__tests__/env.unit.spec.ts — contrato Wave 0 a transformar em RED
    - apps/backend/medusa-config.ts — consumidor principal
    - .planning/phases/01-foundation-observability/01-CONTEXT.md — D-08, D-09, D-10 e D-13
    - .planning/phases/01-foundation-observability/01-RESEARCH.md — Pattern 1 e Pitfalls 2/3
  </read_first>
  <files>apps/backend/src/config/env.ts, apps/backend/src/config/__tests__/env.unit.spec.ts, apps/backend/.env.template, apps/backend/medusa-config.ts</files>
  <behavior>
    - Local aceita `WORKER_MODE=shared` e valores sintéticos de desenvolvimento.
    - Production exige `DATABASE_URL`, `DATABASE_MIGRATION_URL`, URLs públicas parametrizadas, `JWT_SECRET` e `COOKIE_SECRET` não padrão.
    - Erros citam somente nomes de variáveis e regras, nunca os valores recebidos.
    - Medusa consome o objeto validado antes de construir `defineConfig`.
  </behavior>
  <action>Substituir os `test.todo` de ambiente por testes RED e somente então criar `src/config/env.ts` usando o Zod já fixado pelo scaffold. Exportar `parseEnv(input)` para testes e `env` para runtime. Mensagens de erro devem citar apenas nomes de variáveis e regras violadas, nunca valores. Em production exigir URLs e secrets com comprimento mínimo de 32 caracteres, rejeitando valores de exemplo conhecidos; em local permitir defaults exclusivamente não sensíveis. Importar `env` antes de `defineConfig` e mapear `projectConfig.databaseUrl`, `workerMode`, CORS, JWT e cookie, além de `admin.path: "/app"`, `admin.backendUrl: API_PUBLIC_URL` e `admin.disable: ADMIN_DISABLED`. Validar `WORKER_MODE` como `shared|server|worker` e `ADMIN_DISABLED` como booleano explícito. Per D-08, documentar `local` e `production`; mencionar staging apenas como convenção sem criar ambiente operacional.</action>
  <verify>
    <automated>cd apps/backend && npm run test:unit -- --runTestsByPath src/config/__tests__/env.unit.spec.ts -t "environment" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Fixtures production sem `DATABASE_MIGRATION_URL` lançam erro que contém o nome da variável e não contém a URL de runtime.
    - `medusa-config.ts` importa configuração validada antes de construir `defineConfig`.
    - `WORKER_MODE` e `ADMIN_DISABLED` rejeitam valores fora dos contratos.
    - O template contém nomes e marcadores parametrizados, sem credenciais reais.
  </acceptance_criteria>
  <done>Contrato de ambiente local/production e fail-fast estão codificados e verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar guard e subprocesso de migrations direct/session</name>
  <read_first>
    - apps/backend/src/config/__tests__/env.unit.spec.ts — suite de ambiente existente
    - apps/backend/src/config/env.ts — contrato DATABASE_MIGRATION_URL
    - apps/backend/package.json — scripts atuais
    - .planning/phases/01-foundation-observability/01-CONTEXT.md — D-09 e D-10
    - .planning/phases/01-foundation-observability/01-RESEARCH.md — Pitfall transaction pooler
  </read_first>
  <files>apps/backend/scripts/run-migrations.mjs, apps/backend/package.json, apps/backend/src/config/__tests__/env.unit.spec.ts</files>
  <behavior>
    - `assertMigrationUrl` rejeita string vazia e qualquer URL cuja porta efetiva seja 6543.
    - URL direct/session com porta 5432 passa.
    - `runMigrations` cria subprocesso com `DATABASE_URL` igual à migration URL sem mutar `process.env.DATABASE_URL`.
  </behavior>
  <action>Adicionar testes RED de migration à suite existente. Criar `scripts/run-migrations.mjs` com `assertMigrationUrl(url)` e `runMigrations()`: validar `DATABASE_MIGRATION_URL`, rejeitar `:6543`, copiar o ambiente, sobrescrever `DATABASE_URL` somente na cópia do subprocesso e executar `npx medusa db:migrate` com cwd do backend. Implementar `--check-only` sem conexão externa e expor `npm run db:migrate:safe`, per D-09 e D-10.</action>
  <verify>
    <automated>cd apps/backend && npm run test:unit -- --runTestsByPath src/config/__tests__/env.unit.spec.ts -t "migration" && node scripts/run-migrations.mjs --check-only</automated>
  </verify>
  <acceptance_criteria>
    - Fixtures com porta `6543` são rejeitadas.
    - Fixtures direct/session com porta `5432` passam no modo `--check-only`.
    - Após testar `runMigrations`, `process.env.DATABASE_URL` mantém o valor original.
    - O subprocesso recebe a migration URL somente na sua cópia de ambiente.
  </acceptance_criteria>
  <done>Guard automatizado impede migration ausente ou via transaction pooler.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Executar migrations reais pelo DATABASE_MIGRATION_URL</name>
  <read_first>
    - apps/backend/scripts/run-migrations.mjs — guard aprovado na tarefa anterior
    - apps/backend/.env.template — nomes exatos das variáveis
    - .planning/phases/01-foundation-observability/01-VALIDATION.md — verificação manual de migration real
  </read_first>
  <files>Sem alteração obrigatória; a evidência externa será registrada em 01-02-SUMMARY.md.</files>
  <action>Antes da verificação final, executar `cd apps/backend && DATABASE_MIGRATION_URL="$DATABASE_MIGRATION_URL" npm run db:migrate:safe` com uma URL Supabase direct/session fornecida fora do Git. O script deve sobrescrever `DATABASE_URL` apenas no subprocesso Medusa. Se as credenciais reais não estiverem disponíveis ao executor, pausar e solicitar ao operador que exporte a variável no shell e execute exatamente o comando; não registrar a URL, não copiar a saída com credenciais e não substituir a prova por mock.</action>
  <verify>
    <automated>cd apps/backend && npm run test:unit -- --runTestsByPath src/config/__tests__/env.unit.spec.ts && node scripts/run-migrations.mjs --check-only</automated>
    <human-check>Confirmar que o comando real encerra com exit code 0, sem erro de prepared statement, e que a URL utilizada é direct/session, não porta 6543.</human-check>
  </verify>
  <acceptance_criteria>
    - O guard automatizado permanece verde.
    - Uma migration real foi executada antes da conclusão do plano.
    - A evidência registra apenas data, comando saneado, exit code e resultado; nenhuma credencial ou host privado é gravado.
    - Se a credencial não estava disponível, o plano permaneceu bloqueado até a confirmação manual.
  </acceptance_criteria>
  <done>Migrations reais comprovadas pelo caminho direct/session exigido.</done>
</task>

</tasks>

## Artifacts this phase produces

- `apps/backend/src/config/env.ts` — `parseEnv` e `env`.
- `apps/backend/scripts/run-migrations.mjs` — `assertMigrationUrl` e `runMigrations`.
- `apps/backend/.env.template` — contrato local/production sem valores reais.
- Script npm `db:migrate:safe`.
- Testes unitários de configuração e migration guard.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| ambiente → processo Medusa | URLs e secrets não confiáveis entram no startup. |
| processo de deploy → Supabase | O comando de migration cruza rede e pode atingir o pool incorreto. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-03 | Tampering | configuração production | mitigate | Zod fail-fast e mensagens sem valores. |
| T-01-04 | Denial of Service | migration via transaction pooler | mitigate | Rejeitar ausência e porta 6543 antes do subprocesso. |
| T-01-05 | Information Disclosure | erros de startup/deploy | mitigate | Nunca interpolar connection strings; evidência saneada. |
| T-01-SC | Tampering | package-manager installs | accept | Este plano não instala pacotes; usa dependências já aprovadas no 01-01. |
</threat_model>

<verification>
- `cd apps/backend && npm run test:unit -- --runTestsByPath src/config/__tests__/env.unit.spec.ts`
- `cd apps/backend && npm run build`
- `cd apps/backend && node scripts/run-migrations.mjs --check-only`
- Migration Supabase real confirmada no checkpoint bloqueante.
</verification>

<success_criteria>
- Runtime e migrations usam variáveis distintas.
- URL ausente ou porta 6543 impede deploy.
- Configuração production inválida falha antes de conexões.
- Migration real direct/session termina sem erro de prepared statement.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-observability/01-02-SUMMARY.md` when done.
</output>
