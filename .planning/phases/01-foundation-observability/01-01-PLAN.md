---
phase: 01-foundation-observability
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - apps/backend/package.json
  - apps/backend/medusa-config.ts
  - apps/backend/jest.config.js
  - apps/backend/integration-tests/setup.js
  - apps/backend/integration-tests/http/bootstrap.spec.ts
autonomous: false
requirements:
  - SETUP-01
must_haves:
  truths:
    - "D-08: o operador consegue usar o backend em ambiente local, enquanto production fica representado por contratos explícitos sem provisionar staging."
    - "O repositório contém Medusa v2.16.0 em apps/backend, sem aplicação storefront, e o build termina com exit code 0."
    - "O scaffold compila e o contrato test-first próprio do bootstrap prova que o processo Medusa e o Admin em /app pertencem ao mesmo backend headless."
    - "Wave 0 contém somente o runner/setup do Jest e os stubs mínimos do teste de bootstrap; cada slice posterior cria seu próprio teste antes da implementação."
  artifacts:
    - path: "apps/backend/package.json"
      provides: "Scripts e dependências alinhadas do backend Medusa"
      contains: "@medusajs/medusa"
    - path: "apps/backend/medusa-config.ts"
      provides: "Configuração inicial do processo Medusa"
    - path: "apps/backend/integration-tests/http/bootstrap.spec.ts"
      provides: "Contrato end-to-end do Walking Skeleton local"
    - path: "apps/backend/jest.config.js"
      provides: "Infraestrutura Jest exigida pelo contrato Nyquist"
  key_links:
    - from: "package.json"
      to: "apps/backend/package.json"
      via: "workspace npm sem pacote de storefront"
      pattern: "apps/backend"
    - from: "apps/backend/integration-tests/http/bootstrap.spec.ts"
      to: "apps/backend/medusa-config.ts"
      via: "boot HTTP do backend e disponibilidade de /app"
      pattern: "/app"
---

<objective>
Criar o scaffold revisável do Medusa e a infraestrutura mínima do runner de testes.

Purpose: estabelecer o primeiro caminho executável do Walking Skeleton sem apagar `.planning`, `docs` ou `AGENTS.md`, e sem introduzir storefront ou capacidades de negócio.
Output: monorepo Medusa em `apps/backend`, lockfiles fixos, build local, Jest configurado e teste próprio do bootstrap.
</objective>

## Phase Goal

**Como operador**, **quero iniciar e observar o backend Medusa com Admin, banco e Redis reais**, **para que as próximas capacidades de comércio sejam construídas sobre uma fundação executável, segura e verificável**.

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-observability/01-CONTEXT.md
@.planning/phases/01-foundation-observability/01-RESEARCH.md
@.planning/phases/01-foundation-observability/01-VALIDATION.md
@.planning/phases/01-foundation-observability/01-SKELETON.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING] Aprovar legitimidade do scaffold e pacotes Medusa 2.16.0</name>
  <read_first>
    - .planning/phases/01-foundation-observability/01-RESEARCH.md — seção Package Legitimacy Audit
    - .planning/research/STACK.md — compatibilidade Medusa/Node
  </read_first>
  <files>Sem alterações; gate anterior a qualquer download ou instalação.</files>
  <action>Interromper antes de executar npx ou npm install. Conferir no npmjs.com e no repositório github.com/medusajs/medusa que `create-medusa-app@2.16.0`, `@medusajs/medusa@2.16.0`, `@medusajs/framework@2.16.0`, `@medusajs/cli@2.16.0` e `@medusajs/test-utils@2.16.0` são pacotes oficiais, que a versão existe e que o source repository aponta para a organização Medusa. Registrar a aprovação humana no log do executor; não substituir versões por `latest`.</action>
  <verify>
    <automated>npm view create-medusa-app@2.16.0 version repository.url && npm view @medusajs/medusa@2.16.0 version repository.url && npm view @medusajs/framework@2.16.0 version repository.url && npm view @medusajs/cli@2.16.0 version repository.url && npm view @medusajs/test-utils@2.16.0 version repository.url</automated>
    <human-check>Confirmar visualmente os cinco registros oficiais e responder "approved" antes da instalação.</human-check>
  </verify>
  <acceptance_criteria>
    - A saída automatizada mostra `2.16.0` para todos os pacotes.
    - O humano confirma que os registros apontam para `github.com/medusajs/medusa`.
    - Nenhum comando de instalação foi executado antes da aprovação.
  </acceptance_criteria>
  <done>Gate de supply chain aprovado para as versões pesquisadas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gerar scaffold isolado e completar o slice test-first do bootstrap</name>
  <read_first>
    - .planning/phases/01-foundation-observability/01-SKELETON.md — capacidade ponta a ponta
    - .planning/phases/01-foundation-observability/01-VALIDATION.md — responsabilidade Wave 0 mínima do plano 01-01
    - .planning/phases/01-foundation-observability/01-RESEARCH.md — Standard Stack e Recommended Project Structure
    - package.json — se inexistente, usar o workspace do DTC Starter gerado como fonte
    - apps/backend/package.json — se inexistente, usar `/tmp/pod-backend/apps/backend/package.json` gerado pelo scaffolder
  </read_first>
  <files>package.json, package-lock.json, apps/backend/package.json, apps/backend/medusa-config.ts, apps/backend/jest.config.js, apps/backend/integration-tests/setup.js, apps/backend/integration-tests/http/bootstrap.spec.ts</files>
  <behavior>
    - O teste `bootstrap.spec.ts` verifica o contrato do backend headless e do Admin em `/app` sem depender de serviços externos.
    - O workspace não contém pacote de storefront.
    - O runner Jest e o setup de integração existem; nenhum teste específico de configuração, Redis, logger, Sentry, health, PM2 ou Nginx é antecipado por este plano.
  </behavior>
  <action>Executar `npx create-medusa-app@2.16.0 pod-backend --directory-path /tmp --skip-db --version 2.16.0 --use-npm --no-browser`, confirmar que nenhum Next.js starter foi solicitado e importar seletivamente o workspace e `apps/backend` para o repositório. Preservar integralmente `.planning`, `docs`, `.codex`, `.git` e `AGENTS.md`. Remover qualquer pacote de storefront que o gerador tenha incluído, manter todos os `@medusajs/*` em `2.16.0`, fixar `engines.node` em `>=22 <23` e manter lockfile npm. Configurar somente `jest.config.js` e `integration-tests/setup.js` necessários ao runner. Criar `integration-tests/http/bootstrap.spec.ts` antes de ajustar o scaffold, registrar RED por uma expectativa estática/isolada sobre `admin.path: "/app"` e ausência de storefront, então ajustar `medusa-config.ts` e scripts `dev`, `build`, `test:unit` e `test:integration:http` até o teste e o build passarem. Não criar arquivos ou `test.todo` pertencentes aos planos 01-02..01-07; cada slice materializa seu teste específico test-first.</action>
  <verify>
    <automated>cd apps/backend && npm run test:integration:http -- --runTestsByPath integration-tests/http/bootstrap.spec.ts && test -f jest.config.js && test -f integration-tests/setup.js</automated>
  </verify>
  <acceptance_criteria>
    - O histórico da tarefa registra RED de `bootstrap.spec.ts` antes do ajuste do scaffold e GREEN depois.
    - `find . -maxdepth 3 -type d -iname '*storefront*'` não retorna aplicação de storefront.
    - `jest.config.js`, `integration-tests/setup.js` e `bootstrap.spec.ts` existem; testes dos demais slices ainda não existem.
    - `npm ls @medusajs/medusa @medusajs/framework @medusajs/cli @medusajs/test-utils` resolve somente a linha 2.16.0.
  </acceptance_criteria>
  <done>Scaffold backend-only importado, compilável e coberto pelo teste próprio do bootstrap, sem antecipar testes futuros.</done>
</task>

</tasks>

## Artifacts this phase produces

- `package.json` e lockfile do workspace backend-only.
- `apps/backend/` — scaffold oficial Medusa v2.16.0 sem storefront.
- `apps/backend/integration-tests/http/bootstrap.spec.ts` — símbolo/suite `Walking Skeleton bootstrap`.
- `apps/backend/jest.config.js` e `apps/backend/integration-tests/setup.js` — runner/setup mínimo do Wave 0.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| npm registry → workspace | Código de pacotes recentes entra no repositório e será executado localmente. |
| browser → Medusa Admin local | A superfície `/app` cruza HTTP, embora ainda não esteja publicada externamente. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-01 | Tampering | scaffold/import seletivo | mitigate | Fixar 2.16.0, revisar origem, preservar arquivos existentes e manter lockfile. |
| T-01-02 | Information Disclosure | fixtures de teste | mitigate | Usar somente valores sintéticos e impedir URLs/credenciais reais no Git. |
| T-01-SC | Tampering | npm installs | mitigate | Checkpoint humano bloqueante para todos os pacotes SUS antes do download. |
</threat_model>

<verification>
- `cd apps/backend && npm run build`
- `cd apps/backend && npm run test:integration:http -- --runTestsByPath integration-tests/http/bootstrap.spec.ts`
- `npm ls --workspaces --depth=0`
</verification>

<success_criteria>
- Medusa v2.16.0 compila em `apps/backend` e o Admin local responde em `/app`.
- Não existe aplicação storefront.
- O Wave 0 contém apenas runner/setup mínimo; testes específicos permanecem com seus slices.
- Instalações recentes foram aprovadas no gate de legitimidade.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-observability/01-01-SUMMARY.md` when done.
</output>
