---
phase: 01-foundation-observability
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - apps/backend/package.json
  - apps/backend/medusa-config.ts
  - apps/backend/jest.config.js
  - apps/backend/integration-tests/setup.js
  - apps/backend/integration-tests/http/bootstrap.spec.ts
  - apps/backend/src/config/__tests__/env.unit.spec.ts
  - apps/backend/src/infrastructure/__tests__/redis-config.unit.spec.ts
  - apps/backend/src/observability/__tests__/redaction.unit.spec.ts
  - apps/backend/src/observability/__tests__/logger.unit.spec.ts
  - apps/backend/integration-tests/http/sentry.spec.ts
  - apps/backend/integration-tests/http/health.spec.ts
  - ops/tests/nginx-routing-smoke.sh
  - ops/tests/pm2-config.test.mjs
autonomous: false
requirements:
  - SETUP-01
user_setup:
  - service: local-infrastructure
    why: "O smoke completo do Medusa requer PostgreSQL acessível; Redis será exigido a partir do plano 01-03."
    env_vars:
      - name: DATABASE_URL
        source: "URL local/gerenciada de desenvolvimento, fornecida fora do Git"
must_haves:
  truths:
    - "D-08: o operador consegue usar o backend em ambiente local, enquanto production fica representado por contratos explícitos sem provisionar staging."
    - "O repositório contém Medusa v2.16.0 em apps/backend, sem aplicação storefront, e o build termina com exit code 0."
    - "O primeiro caminho executável prova que o processo Medusa e o Admin em /app pertencem ao mesmo backend headless."
    - "A infraestrutura Wave 0 do 01-VALIDATION.md existe antes dos slices que a consomem e não mascara testes ainda não implementados como aprovados."
  artifacts:
    - path: "apps/backend/package.json"
      provides: "Scripts e dependências alinhadas do backend Medusa"
      contains: "@medusajs/medusa"
    - path: "apps/backend/medusa-config.ts"
      provides: "Configuração inicial do processo Medusa"
    - path: "apps/backend/integration-tests/http/bootstrap.spec.ts"
      provides: "Contrato end-to-end do Walking Skeleton local"
    - path: "apps/backend/jest.config.js"
      provides: "Infraestrutura Jest exigida pelo contrato Nyquist"
    - path: "ops/tests/pm2-config.test.mjs"
      provides: "Contrato estrutural antecipado do template PM2"
  key_links:
    - from: "package.json"
      to: "apps/backend/package.json"
      via: "workspace npm sem pacote de storefront"
      pattern: "apps/backend"
    - from: "apps/backend/integration-tests/http/bootstrap.spec.ts"
      to: "apps/backend/medusa-config.ts"
      via: "boot HTTP do backend e disponibilidade de /app"
      pattern: "/app"
---

<objective>
Criar o bootstrap local revisável do Medusa e toda a infraestrutura de testes Wave 0.

Purpose: estabelecer o primeiro caminho executável do Walking Skeleton sem apagar `.planning`, `docs` ou `AGENTS.md`, e sem introduzir storefront ou capacidades de negócio.
Output: monorepo Medusa em `apps/backend`, lockfiles fixos, build local e arquivos de teste exigidos por `01-VALIDATION.md`.
</objective>

## Phase Goal

**Como operador**, **quero iniciar e observar o backend Medusa com Admin, banco e Redis reais**, **para que as próximas capacidades de comércio sejam construídas sobre uma fundação executável, segura e verificável**.

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-observability/01-CONTEXT.md
@.planning/phases/01-foundation-observability/01-RESEARCH.md
@.planning/phases/01-foundation-observability/01-VALIDATION.md
@.planning/phases/01-foundation-observability/01-SKELETON.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING] Aprovar legitimidade do scaffold e pacotes Medusa 2.16.0</name>
  <read_first>
    - .planning/phases/01-foundation-observability/01-RESEARCH.md — seção Package Legitimacy Audit
    - .planning/research/STACK.md — compatibilidade Medusa/Node
  </read_first>
  <files>Sem alterações; gate anterior a qualquer download ou instalação.</files>
  <action>Interromper antes de executar npx ou npm install. Conferir no npmjs.com e no repositório github.com/medusajs/medusa que `create-medusa-app@2.16.0`, `@medusajs/medusa@2.16.0`, `@medusajs/framework@2.16.0`, `@medusajs/cli@2.16.0` e `@medusajs/test-utils@2.16.0` são pacotes oficiais, que a versão existe e que o source repository aponta para a organização Medusa. Registrar a aprovação humana no log do executor; não substituir versões por `latest`.</action>
  <verify>
    <automated>npm view create-medusa-app@2.16.0 version repository.url && npm view @medusajs/medusa@2.16.0 version repository.url && npm view @medusajs/framework@2.16.0 version repository.url && npm view @medusajs/cli@2.16.0 version repository.url && npm view @medusajs/test-utils@2.16.0 version repository.url</automated>
    <human-check>Confirmar visualmente os cinco registros oficiais e responder "approved" antes da instalação.</human-check>
  </verify>
  <acceptance_criteria>
    - A saída automatizada mostra `2.16.0` para todos os pacotes.
    - O humano confirma que os registros apontam para `github.com/medusajs/medusa`.
    - Nenhum comando de instalação foi executado antes da aprovação.
  </acceptance_criteria>
  <done>Gate de supply chain aprovado para as versões pesquisadas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gerar scaffold isolado e criar o RED do Walking Skeleton + Wave 0</name>
  <read_first>
    - .planning/phases/01-foundation-observability/01-SKELETON.md — capacidade ponta a ponta
    - .planning/phases/01-foundation-observability/01-VALIDATION.md — lista completa Wave 0
    - .planning/phases/01-foundation-observability/01-RESEARCH.md — Standard Stack e Recommended Project Structure
    - package.json — se inexistente, usar o workspace do DTC Starter gerado como fonte
    - apps/backend/package.json — se inexistente, usar `/tmp/pod-backend/apps/backend/package.json` gerado pelo scaffolder
  </read_first>
  <files>package.json, package-lock.json, apps/backend/**, ops/tests/nginx-routing-smoke.sh, ops/tests/pm2-config.test.mjs</files>
  <behavior>
    - O teste `bootstrap.spec.ts` inicia o backend com fixtures locais e exige resposta HTTP do Admin em `/app`.
    - O workspace não contém pacote de storefront.
    - Os arquivos Wave 0 existem; contratos de slices posteriores são declarados como `test.todo` específicos, nunca como testes verdes vazios.
  </behavior>
  <action>Executar `npx create-medusa-app@2.16.0 pod-backend --directory-path /tmp --skip-db --version 2.16.0 --use-npm --no-browser`, confirmar que nenhum Next.js starter foi solicitado e importar seletivamente o workspace e `apps/backend` para o repositório. Preservar integralmente `.planning`, `docs`, `.codex`, `.git` e `AGENTS.md`. Remover qualquer pacote de storefront que o gerador tenha incluído, manter todos os `@medusajs/*` em `2.16.0`, fixar `engines.node` em `>=22 <23` e manter lockfile npm. Criar primeiro `integration-tests/http/bootstrap.spec.ts` com a expectativa do Admin `/app` e executá-lo para registrar RED. Em seguida criar todos os arquivos Wave 0 listados em `01-VALIDATION.md`, com suites nomeadas e contratos `test.todo` correspondentes aos planos 01-02..01-07; os scripts de ops devem começar em modo de validação estrutural e retornar erro claro quando o artefato operacional ainda não existir.</action>
  <verify>
    <automated>test -f apps/backend/jest.config.js && test -f apps/backend/integration-tests/setup.js && test -f apps/backend/src/config/__tests__/env.unit.spec.ts && test -f apps/backend/src/infrastructure/__tests__/redis-config.unit.spec.ts && test -f apps/backend/src/observability/__tests__/redaction.unit.spec.ts && test -f apps/backend/src/observability/__tests__/logger.unit.spec.ts && test -f apps/backend/integration-tests/http/sentry.spec.ts && test -f apps/backend/integration-tests/http/health.spec.ts && test -f ops/tests/nginx-routing-smoke.sh && test -f ops/tests/pm2-config.test.mjs && ! (cd apps/backend && npm run test:integration:http -- --runTestsByPath integration-tests/http/bootstrap.spec.ts)</automated>
  </verify>
  <acceptance_criteria>
    - Antes da implementação local, `bootstrap.spec.ts` falha pela ausência da configuração de boot esperada, não por erro de sintaxe.
    - `find . -maxdepth 3 -type d -iname '*storefront*'` não retorna aplicação de storefront.
    - Todos os dez artefatos Wave 0 do `01-VALIDATION.md` existem.
    - `npm ls @medusajs/medusa @medusajs/framework @medusajs/cli @medusajs/test-utils` resolve somente a linha 2.16.0.
  </acceptance_criteria>
  <done>Scaffold importado com RED reproduzível e infraestrutura Nyquist completa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fazer o bootstrap local e o Admin /app passarem</name>
  <read_first>
    - apps/backend/integration-tests/http/bootstrap.spec.ts — contrato RED criado na tarefa anterior
    - apps/backend/medusa-config.ts — configuração gerada
    - apps/backend/package.json — scripts gerados
    - .planning/phases/01-foundation-observability/01-CONTEXT.md — D-08 e limite backend-only
  </read_first>
  <files>apps/backend/medusa-config.ts, apps/backend/package.json, package.json</files>
  <behavior>
    - `npm run build` compila o backend.
    - Com `DATABASE_URL` de desenvolvimento disponível, o processo local responde e serve o Admin em `/app`.
    - O ambiente local usa papel `shared`; a separação server/worker permanece para produção no plano 01-07.
  </behavior>
  <action>Ajustar os scripts do workspace para expor `dev`, `build`, `test:unit` e `test:integration:http` do backend. Em `medusa-config.ts`, manter `admin.path` igual a `/app`, usar somente `DATABASE_URL` para o boot local e selecionar `workerMode: "shared"` quando o ambiente for local, per D-08. Não adicionar catálogo, seed comercial, Stripe, Gelato ou storefront. Executar migrations locais apenas se necessárias ao smoke do scaffold; a estratégia Supabase e o gate de migrations de produção pertencem ao plano 01-02.</action>
  <verify>
    <automated>cd apps/backend && npm run build && npm run test:integration:http -- --runTestsByPath integration-tests/http/bootstrap.spec.ts</automated>
    <human-check>Com `DATABASE_URL` de desenvolvimento fornecida fora do Git, executar `npm run dev`, abrir `http://localhost:9000/app` e confirmar que o Admin Medusa carrega sem storefront.</human-check>
  </verify>
  <acceptance_criteria>
    - `cd apps/backend && npm run build` retorna exit code 0.
    - `bootstrap.spec.ts` passa.
    - GET `/app` responde com a superfície do Admin; nenhum caminho de storefront foi criado.
    - Nenhum arquivo versionado contém valor real de `DATABASE_URL`.
  </acceptance_criteria>
  <done>Backend Medusa local compilável e Admin `/app` disponíveis como primeiro slice do Walking Skeleton.</done>
</task>

</tasks>

## Artifacts this phase produces

- `package.json` e lockfile do workspace backend-only.
- `apps/backend/` — scaffold oficial Medusa v2.16.0 sem storefront.
- `apps/backend/integration-tests/http/bootstrap.spec.ts` — símbolo/suite `Walking Skeleton bootstrap`.
- Infraestrutura Wave 0 completa conforme `01-VALIDATION.md`.
- `ops/tests/nginx-routing-smoke.sh` e `ops/tests/pm2-config.test.mjs` — contratos operacionais antecipados.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| npm registry → workspace | Código de pacotes recentes entra no repositório e será executado localmente. |
| browser → Medusa Admin local | A superfície `/app` cruza HTTP, embora ainda não esteja publicada externamente. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-01 | Tampering | scaffold/import seletivo | mitigate | Fixar 2.16.0, revisar origem, preservar arquivos existentes e manter lockfile. |
| T-01-02 | Information Disclosure | fixtures de teste | mitigate | Usar somente valores sintéticos e impedir URLs/credenciais reais no Git. |
| T-01-SC | Tampering | npm installs | mitigate | Checkpoint humano bloqueante para todos os pacotes SUS antes do download. |
</threat_model>

<verification>
- `cd apps/backend && npm run build`
- `cd apps/backend && npm run test:integration:http -- --runTestsByPath integration-tests/http/bootstrap.spec.ts`
- `npm ls --workspaces --depth=0`
- Verificação manual de `/app` com PostgreSQL de desenvolvimento disponível.
</verification>

<success_criteria>
- Medusa v2.16.0 compila em `apps/backend` e o Admin local responde em `/app`.
- Não existe aplicação storefront.
- Todos os arquivos Wave 0 do contrato Nyquist existem.
- Instalações recentes foram aprovadas no gate de legitimidade.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-observability/01-01-SUMMARY.md` when done.
</output>
