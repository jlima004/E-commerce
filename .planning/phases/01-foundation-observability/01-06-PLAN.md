---
phase: 01-foundation-observability
plan: "06"
type: execute
wave: 5
depends_on:
  - "01-03"
  - "01-04"
files_modified:
  - apps/backend/src/infrastructure/health.ts
  - apps/backend/src/api/health/live/route.ts
  - apps/backend/src/api/health/ready/route.ts
  - apps/backend/integration-tests/http/health.spec.ts
autonomous: true
requirements:
  - OBS-03
must_haves:
  truths:
    - "D-23: GET /health/live consulta somente a vida do processo HTTP e retorna 200."
    - "D-24: GET /health/ready verifica Postgres e todos os endpoints Redis únicos, retorna 200 somente quando todos estão up e 503 com status not_ready em qualquer falha."
    - "D-25: Stripe, Gelato, Resend, PostHog e Sentry não participam do readiness."
    - "D-26: respostas públicas contêm somente status, service, timestamp, version e checks genéricos."
    - "D-27: probes Postgres/Redis executam em paralelo, com timeout independente de 1500 ms, limite global de 2000 ms e sem cache."
    - "D-28 e D-29: health é público, barato, fora de rate limit sensível, não gera log por sucesso e trata indisponibilidade esperada como warn saneado sem Sentry."
  artifacts:
    - path: "apps/backend/src/infrastructure/health.ts"
      provides: "Probes paralelos, timeouts e resposta mínima"
      exports: ["checkPostgres", "checkRedis", "checkReadiness", "withTimeout"]
    - path: "apps/backend/src/api/health/live/route.ts"
      provides: "Liveness HTTP"
      exports: ["GET"]
    - path: "apps/backend/src/api/health/ready/route.ts"
      provides: "Readiness HTTP"
      exports: ["GET"]
    - path: "apps/backend/integration-tests/http/health.spec.ts"
      provides: "Contratos 200/503, shape e paralelismo"
  key_links:
    - from: "apps/backend/src/api/health/ready/route.ts"
      to: "apps/backend/src/infrastructure/health.ts"
      via: "checkReadiness(req.scope)"
      pattern: "checkReadiness"
    - from: "apps/backend/src/infrastructure/health.ts"
      to: "apps/backend/src/infrastructure/redis-config.ts"
      via: "uniqueRedisUrls(env)"
      pattern: "uniqueRedisUrls"
---

<objective>
Expor liveness e readiness mínimos, baratos e seguros.

Purpose: permitir que operador, PM2 e Nginx distingam processo vivo de backend pronto sem criar dependências cascata.
Output: probes reais Postgres/Redis e endpoints públicos com contrato estável.
</objective>

## Phase Goal

**Como operador**, **quero iniciar e observar o backend Medusa com Admin, banco e Redis reais**, **para que as próximas capacidades de comércio sejam construídas sobre uma fundação executável, segura e verificável**.

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-observability/01-CONTEXT.md
@.planning/phases/01-foundation-observability/01-RESEARCH.md
@.planning/phases/01-foundation-observability/01-VALIDATION.md
@.planning/phases/01-foundation-observability/01-03-SUMMARY.md
@.planning/phases/01-foundation-observability/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar probes paralelos Postgres/Redis</name>
  <read_first>
    - apps/backend/jest.config.js — runner criado no plano 01-01
    - apps/backend/src/infrastructure/redis-config.ts — endpoints Redis únicos
    - apps/backend/src/observability/logger.ts — warn saneado
    - apps/backend/src/config/env.ts — service/version/timeouts
    - .planning/phases/01-foundation-observability/01-CONTEXT.md — D-24..D-29
    - .planning/phases/01-foundation-observability/01-RESEARCH.md — Pattern 5 e probe PG oficial
  </read_first>
  <files>apps/backend/src/infrastructure/health.ts, apps/backend/integration-tests/http/health.spec.ts</files>
  <behavior>
    - `checkPostgres` executa `SELECT 1` pela conexão registrada no container.
    - `checkRedis` usa clientes ioredis com offline queue desabilitada, retry curto e encerra conexões de probe.
    - `checkReadiness` inicia todos os checks no mesmo tick, aplica 1500 ms por check e 2000 ms global.
    - Falha esperada produz estado `down` e warn saneado; erro inesperado é relançado/categorizado para o middleware global.
  </behavior>
  <action>Criar `integration-tests/http/health.spec.ts` neste plano e escrever os casos RED de probe com fakes controláveis e relógio fake. Criar `withTimeout`, `checkPostgres`, `checkRedis` e `checkReadiness`. Resolver Postgres via `ContainerRegistrationKeys.PG_CONNECTION` e executar somente `SELECT 1`. Derivar Redis por `uniqueRedisUrls(env)` e criar clientes de probe com `enableOfflineQueue: false`, `maxRetriesPerRequest: 1`, `lazyConnect: true` e connect timeout de 1000 ms; desconectar em finally. Iniciar todas as promises antes de aguardar resultados, usar 1500 ms independente e 2000 ms global, sem retries internos além do limite do cliente e sem cache. Retornar apenas `up|down`; logs warn devem conter check, operação e correlation ID, nunca URL/host/exception crua. Não consultar as integrações excluídas por D-25.</action>
  <verify>
    <automated>cd apps/backend && npm run test:integration:http -- --runTestsByPath integration-tests/http/health.spec.ts -t "probes"</automated>
  </verify>
  <acceptance_criteria>
    - O teste prova que Postgres e Redis iniciam antes de qualquer resultado ser aguardado.
    - Cada timeout encerra em até 1500 ms e o conjunto em até 2000 ms sob relógio fake.
    - `SELECT 1` é a única query executada.
    - Endpoints Redis duplicados produzem um único PING.
    - Saída/log fake não contém URL, host, stack ou credencial.
  </acceptance_criteria>
  <done>Probes reais e não-cascateantes estão implementados.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expor contratos HTTP live e ready</name>
  <read_first>
    - apps/backend/src/infrastructure/health.ts — funções da tarefa anterior
    - apps/backend/integration-tests/http/health.spec.ts — casos HTTP criados na tarefa anterior
    - apps/backend/src/api/ — convenção de rotas do scaffold
    - .planning/phases/01-foundation-observability/01-CONTEXT.md — D-23, D-24, D-26, D-28 e D-29
  </read_first>
  <files>apps/backend/src/api/health/live/route.ts, apps/backend/src/api/health/ready/route.ts, apps/backend/integration-tests/http/health.spec.ts</files>
  <behavior>
    - Live retorna 200 sem chamar Postgres ou Redis.
    - Ready retorna 200/ready com todos up e 503/not_ready quando qualquer obrigatório está down.
    - A resposta contém apenas status, service, timestamp, version e checks.
    - Chamadas saudáveis não emitem log; dependência down emite no máximo um warn saneado por chamada.
  </behavior>
  <action>Adicionar testes HTTP RED com resposta exata. Criar `GET` em `src/api/health/live/route.ts` retornando status 200 e `{ status: "live", service: "medusa-backend", timestamp: ISO-8601, version: APP_VERSION }`, sem resolver dependências. Criar `GET` em `ready/route.ts`, chamar `checkReadiness(req.scope)`, responder 200 com `status: "ready"` ou 503 com `status: "not_ready"` e checks genéricos `postgres`/`redis` como `up|down`. Não incluir latência, host, URL, mensagem, stack ou nomes opcionais. Manter endpoints sem autenticação e sem middleware de rate limit; não emitir log saudável. Erros inesperados devem seguir o middleware global, permitindo Sentry após o merge do plano 01-05, per D-29.</action>
  <verify>
    <automated>cd apps/backend && npm run test:integration:http -- --runTestsByPath integration-tests/http/health.spec.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - GET `/health/live` retorna 200 e zero chamadas de probe.
    - GET `/health/ready` retorna 200 com `status: ready` quando Postgres/Redis estão up.
    - GET `/health/ready` retorna 503 com `status: not_ready` quando qualquer check está down.
    - `Object.keys(body).sort()` equivale a `checks,service,status,timestamp,version` em ready.
    - Nenhum teste observa chamada a Stripe, Gelato, Resend, PostHog ou Sentry para decidir readiness.
  </acceptance_criteria>
  <done>Endpoints live/ready públicos cumprem o contrato mínimo e seguro.</done>
</task>

</tasks>

## Artifacts this phase produces

- `health.ts` — `withTimeout`, `checkPostgres`, `checkRedis`, `checkReadiness`.
- `GET /health/live`.
- `GET /health/ready`.
- Suite integrada de probes, resposta, timeout e logging.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| internet → health endpoints | Chamadas públicas podem ser abusadas ou usadas para reconhecimento. |
| backend → Postgres/Redis | Probes cruzam dependências obrigatórias e podem amplificar outage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-15 | Information Disclosure | resposta health | mitigate | Shape mínima e estados genéricos sem detalhes de infraestrutura. |
| T-01-16 | Denial of Service | probes | mitigate | Paralelismo, timeouts curtos, sem retry/cache e query/PING baratos. |
| T-01-17 | Repudiation | mudanças de saúde | mitigate | Warn saneado em falha esperada e middleware para erro inesperado. |
| T-01-SC | Tampering | package-manager installs | accept | Nenhum pacote novo; usa ioredis aprovado no 01-03. |
</threat_model>

<verification>
- `cd apps/backend && npm run test:integration:http -- --runTestsByPath integration-tests/http/health.spec.ts`
- `cd apps/backend && npm run build`
- Com dependências reais, `curl -fsS http://127.0.0.1:9000/health/live` e `curl -fsS http://127.0.0.1:9000/health/ready`.
</verification>

<success_criteria>
- Live mede somente processo.
- Ready mede Postgres e Redis em paralelo.
- Contrato 200/503 e shape mínima passam.
- Endpoints são baratos, públicos e silenciosos no caminho saudável.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-observability/01-06-SUMMARY.md` when done.
</output>
