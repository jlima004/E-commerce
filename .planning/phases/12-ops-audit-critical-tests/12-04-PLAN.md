---
phase: 12-ops-audit-critical-tests
plan: 04
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - apps/backend/src/modules/admin-action-log/models/admin-action-log.ts
  - apps/backend/src/modules/admin-action-log/migrations/Migration20260720000200.ts
  - apps/backend/src/modules/admin-action-log/service.ts
  - apps/backend/src/modules/admin-action-log/index.ts
  - apps/backend/src/modules/admin-action-log/__tests__/admin-action-log.postgres.spec.ts
  - apps/backend/src/jobs/admin-action-log-reconciliation.ts
  - apps/backend/src/jobs/__tests__/admin-action-log-reconciliation.unit.spec.ts
  - apps/backend/src/api/admin/_shared/require-admin-actor.ts
  - apps/backend/src/api/admin/_shared/audit-admin-action.ts
  - apps/backend/src/api/admin/_shared/__tests__/audit-admin-action.unit.spec.ts
autonomous: false
requirements: [OPS-02]
must_haves:
  truths:
    - "Uma ação Admin identificada grava intent antes do domínio e outcome em nova linha correlacionada."
    - "Actor ausente ou não-user falha fechado antes de qualquer mutação de domínio."
    - "AdminActionLog rejeita update/delete em PostgreSQL e preserva retries como novos fatos."
    - "Constraints parciais PostgreSQL garantem um intent e um único fato terminal por action_attempt_id, inclusive sob concorrência entre outcome e reconciliation."
    - "Um job worker-only reconcilia intents com mais de 15 minutos apenas por fatos locais comprováveis, sem provider e sem completar ambiguidades."
  artifacts:
    - path: "apps/backend/src/modules/admin-action-log/models/admin-action-log.ts"
      provides: "Schema factual append-only"
    - path: "apps/backend/src/modules/admin-action-log/migrations/Migration20260720000200.ts"
      provides: "Tabela, índices e proteção PostgreSQL contra update/delete"
    - path: "apps/backend/src/api/admin/_shared/audit-admin-action.ts"
      provides: "Sequenciamento Strategy B e reconciliação factual"
    - path: "apps/backend/src/modules/admin-action-log/__tests__/admin-action-log.postgres.spec.ts"
      provides: "Wiring test-only explícito do módulo antes das migrations e prova PostgreSQL real"
    - path: "apps/backend/src/jobs/admin-action-log-reconciliation.ts"
      provides: "Executor runtime worker-only, paginado e factual para intents órfãos"
  key_links:
    - from: "apps/backend/src/api/admin/_shared/audit-admin-action.ts"
      to: "apps/backend/src/modules/admin-action-log/service.ts"
      via: "append intent/outcome por action_attempt_id"
      pattern: "action_attempt_id"
    - from: "apps/backend/src/api/admin/_shared/require-admin-actor.ts"
      to: "req.auth_context"
      via: "actor_type user e actor_id obrigatório"
      pattern: "actor_type"
    - from: "apps/backend/src/modules/admin-action-log/__tests__/admin-action-log.postgres.spec.ts"
      to: "apps/backend/src/modules/admin-action-log/index.ts"
      via: "hooks.beforeServerStart registra configModule.modules.admin_action_log antes de runModulesMigrations"
      pattern: "beforeServerStart"
    - from: "apps/backend/src/jobs/admin-action-log-reconciliation.ts"
      to: "apps/backend/src/modules/admin-action-log/service.ts"
      via: "listagem paginada de órfãos e append terminal idempotente"
      pattern: "ADMIN_ACTION_ORPHAN_AFTER_MS|reconciliation"
---

<objective>
Criar os primitives append-only e user-only do AdminActionLog conforme Strategy B aprovada em H12-04.

Purpose: auditar factual e correlacionadamente ações Admin sem afirmar atomicidade cross-module inexistente.
Output: módulo/migration append-only, actor guard, helper de intent/outcome e job runtime estreito de reconciliação.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-ops-audit-critical-tests/12-CONTEXT.md
@.planning/phases/12-ops-audit-critical-tests/12-RESEARCH.md
@.planning/phases/12-ops-audit-critical-tests/12-01-SUMMARY.md
@apps/backend/src/api/admin/refunds/request/route.ts
@apps/backend/src/api/admin/exchanges/route.ts
@apps/backend/src/api/admin/exchanges/[id]/route.ts
</context>

## Objective

Fornecer a infraestrutura OPS-02 antes da instrumentação das rotas, com schema e falhas explicitamente definidos.

## Requirement coverage

- OPS-02: persistência factual de ação Admin.
- D12-09: refund/exchange no escopo; nenhum reprocess route inventado.
- D12-10: actor obrigatório.
- D12-11/D12-12: append-only e resultados requested/succeeded/failed/blocked.
- H12-04: Strategy B intent/outcome correlacionados.
- H12-05: somente actor user; API key/missing actor falham fechado.

## Dependencies

- Depende do harness 12-01.
- Pode executar em paralelo com 12-02; nenhum arquivo compartilhado.
- 12-05 registrará o módulo e o job em runtime e consumirá os helpers sem modificá-los.

## Files to create

- Os cinco arquivos do módulo `apps/backend/src/modules/admin-action-log/` listados no frontmatter; os tipos públicos ficam junto ao modelo/service.
- `apps/backend/src/jobs/admin-action-log-reconciliation.ts` e seu unit spec.
- Helpers compartilhados Admin e respectivos testes.

## Files to modify

- Nenhum arquivo existente.

## Explicit non-goals

- Não instrumentar rotas ainda; isso pertence a 12-05.
- Não prometer transação única entre AdminActionLog e módulos de refund/exchange.
- Não usar actor client-supplied, API key como admin_id ou interceptor genérico.
- Não criar update/delete/purge do log.
- Não antecipar o registro runtime em `apps/backend/medusa-config.ts`; 12-04 registra `admin_action_log` somente no lifecycle suportado do test runner, e 12-05 continua responsável pelo wiring runtime.

## Tasks in execution order

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Definir schema factual e append-only AdminActionLog</name>
  <files>apps/backend/src/modules/admin-action-log/models/admin-action-log.ts, apps/backend/src/modules/admin-action-log/index.ts, apps/backend/src/modules/admin-action-log/migrations/Migration20260720000200.ts</files>
  <read_first>apps/backend/src/modules/refund-request/models/refund-request.ts, apps/backend/src/modules/exchange-request/models/exchange-request.ts, apps/backend/src/modules/webhooks/migrations/Migration20260701000000.ts</read_first>
  <behavior>
    - Cada linha possui action_attempt_id, correlation_id, stage, actor, ação, entidade, resultado e timestamps.
    - Result aceita somente requested/succeeded/failed/blocked; stage aceita intent/outcome/reconciliation.
    - A migration cria `UNIQUE` parcial em action_attempt_id onde audit_stage = 'intent' e outro `UNIQUE` parcial onde audit_stage IN ('outcome', 'reconciliation').
    - Um intent e um fato terminal do mesmo action_attempt_id podem coexistir; retries usam novos attempts e podem repetir idempotency_key, sem uniqueness global nessa chave.
    - PostgreSQL rejeita UPDATE e DELETE de linhas existentes.
  </behavior>
  <action>Criar modelo prefixado `admact` com `admin_id` obrigatório, `admin_email` opcional, action, entity_type/id, result, severity `info|warning|critical`, reason sanitizada, previous_state/new_state allowlisted, metadata allowlisted, `action_attempt_id` obrigatório, `correlation_id` obrigatório, `idempotency_key` opcional e `audit_stage` intent/outcome/reconciliation. Exportar os tipos públicos junto ao modelo/service, sem arquivo de tipos separado. Além dos índices de consulta por actor/created_at, entity, action_attempt_id/created_at, correlation_id e idempotency_key, a migration deve criar exatamente duas garantias concurrency-safe equivalentes a `CREATE UNIQUE INDEX ... ON admin_action_log (action_attempt_id) WHERE audit_stage = 'intent'` e `CREATE UNIQUE INDEX ... ON admin_action_log (action_attempt_id) WHERE audit_stage IN ('outcome', 'reconciliation')`. Não adicionar uniqueness global sobre `idempotency_key` nem uniqueness de correlation_id. Adicionar trigger PostgreSQL que rejeita UPDATE/DELETE, mantendo down migration exata. Isso materializa D12-11/H12-04 sem Strategy A.</action>
  <verify><automated>cd apps/backend &amp;&amp; rtk npm run build</automated></verify>
  <acceptance_criteria>DDL, índices e trigger passam em PostgreSQL real; intent/outcome/retry coexistem e mutação é rejeitada.</acceptance_criteria>
  <done>Schema factual append-only está fechado e migrável.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar append idempotente e executor factual de reconciliação</name>
  <files>apps/backend/src/modules/admin-action-log/service.ts, apps/backend/src/modules/admin-action-log/__tests__/admin-action-log.postgres.spec.ts, apps/backend/src/jobs/admin-action-log-reconciliation.ts, apps/backend/src/jobs/__tests__/admin-action-log-reconciliation.unit.spec.ts</files>
  <read_first>apps/backend/src/modules/admin-action-log/models/admin-action-log.ts, apps/backend/src/modules/refund-request/service.ts, apps/backend/src/modules/exchange-request/service.ts, apps/backend/medusa-config.ts, node_modules/@medusajs/test-utils/dist/medusa-test-runner.d.ts, node_modules/@medusajs/test-utils/dist/medusa-test-runner.js, node_modules/@medusajs/test-utils/dist/medusa-test-runner-utils/use-db.js</read_first>
  <behavior>
    - `appendIntent` e `appendOutcome` criam linhas sanitizadas; conflito terminal busca e devolve o fato canônico existente, sem segunda linha, overwrite ou erro bruto.
    - Falha ao inserir intent é propagada e bloqueia o chamador antes do domínio.
    - `listOrphanIntents` encontra intents sem outcome/reconciliation pelo action_attempt_id.
    - Os seis cenários PostgreSQL reais cobrem intents concorrentes, outcomes concorrentes, outcome versus reconciliation, dois workers, retry com novo attempt e mesma idempotency_key em attempts diferentes.
    - `ADMIN_ACTION_ORPHAN_AFTER_MS = 15 * 60_000` é janela operacional local, não SLA externo; o job roda apenas no worker, é no-op em release migration mode e pagina com limite.
    - O job seleciona somente intents mais antigos que a janela e sem fato terminal, nunca chama provider nem altera/remove linha; anexa somente fato comprovável e mantém ambiguidades órfãs.
  </behavior>
  <action>Implementar somente métodos de append/listagem, sem wrappers de update/delete. Sanitizar reason, estados e metadata na borda do service. Tratar `reused_idempotency` como fato metadata, não como motivo para sobrescrever. Em conflito da constraint terminal, buscar e devolver idempotentemente o fato terminal canônico já persistido; não anexar uma segunda linha, não sobrescrever a primeira e não propagar conflito esperado como erro bruto. Implementar consulta paginada/limitada de orphan intent e append de reconciliation. Criar `admin-action-log-reconciliation.ts` com `ADMIN_ACTION_ORPHAN_AFTER_MS = 15 * 60_000`, documentada como janela operacional local para não tocar requisição em andamento, não SLA. O job executa somente no worker, vira no-op em release migration mode, filtra intents anteriores ao cutoff, ignora attempts que já possuam outcome/reconciliation, nunca chama provider e nunca altera/remove linhas. Para RefundRequest, existência pelo ID pré-gerado permite `reconciliation/result=requested`; ausência só pode gerar failed quando o fluxo síncrono e a ausência do fato forem prova suficiente, caso contrário permanece órfão. Para Exchange create, existência pelo ID pré-gerado permite succeeded. Para update/reject/cancel, succeeded só quando o estado persistido corresponder inequivocamente ao `new_state` allowlisted; divergência ou atualização posterior torna o caso ambíguo e não cria terminal. Logs contêm somente códigos e IDs internos saneados. No spec PostgreSQL real, provar: dois appends concorrentes de intent; dois outcomes concorrentes; outcome direto concorrendo com reconciliation; dois workers no mesmo órfão; retry com novo action_attempt_id; mesma idempotency_key em attempts diferentes. A cardinalidade final é um intent e um terminal por attempt. O unit spec do job cobre janela, paginação, worker/release-mode, RefundRequest, Exchange create/update/reject/cancel, fato terminal já existente, ausência não comprovável, ambiguidade e ausência de provider/mutação. No spec PG, usar `medusaIntegrationTestRunner` com `dbName: process.env.DB_TEMP_NAME` e `hooks.beforeServerStart` para registrar `configModule.modules.admin_action_log = { resolve: "./src/modules/admin-action-log" }` antes de migrations, falhando em resolução divergente. Não modificar `apps/backend/medusa-config.ts`; 12-05 mantém o registro runtime.</action>
  <verify><automated>cd apps/backend &amp;&amp; rtk npm run test:unit -- --runTestsByPath src/jobs/__tests__/admin-action-log-reconciliation.unit.spec.ts --runInBand &amp;&amp; rtk node scripts/run-disposable-postgres-tests.mjs -- rtk npm run test:integration:modules -- --runTestsByPath src/modules/admin-action-log/__tests__/admin-action-log.postgres.spec.ts --runInBand</automated></verify>
  <acceptance_criteria>O hook registra `admin_action_log` antes de `runModulesMigrations`; constraints parciais, dedupe terminal, seis disputas/retries, trigger, service e reconciliação factual passam em PostgreSQL real; o job worker-only mantém casos ambíguos órfãos sem provider ou mutação.</acceptance_criteria>
  <done>Primitives de Strategy B e wiring test-only autocontido estão provados no PostgreSQL descartável.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implementar actor guard e sequenciador auditado</name>
  <files>apps/backend/src/api/admin/_shared/require-admin-actor.ts, apps/backend/src/api/admin/_shared/audit-admin-action.ts, apps/backend/src/api/admin/_shared/__tests__/audit-admin-action.unit.spec.ts</files>
  <read_first>apps/backend/src/api/admin/refunds/request/route.ts, apps/backend/src/api/admin/exchanges/route.ts, apps/backend/src/api/admin/exchanges/[id]/route.ts, node_modules/@medusajs/framework/dist/http/types.d.ts</read_first>
  <behavior>
    - Actor user com ID não vazio é aceito e se torna a única fonte de admin_id.
    - Actor ausente ou de outro tipo falha antes de append/domínio e gera apenas log de segurança sanitizado.
    - Sequência é require actor → append intent → executar domínio → append outcome.
    - Se append intent falha, o domínio não executa e a requisição falha de forma saneada.
    - Se o domínio falha após intent, o helper tenta append failed e retorna o erro original saneado do domínio; se esse append falhar, deixa o intent órfão e registra somente erro de auditoria estruturado/saneado.
    - Se o domínio persiste com sucesso e append outcome falha, preserva a resposta HTTP de sucesso do domínio, emite erro estruturado saneado e deixa o intent para reconciliação, sem induzir retry enganoso.
    - Uma falha de audit outcome nunca executa novamente o callback de domínio.
  </behavior>
  <action>Criar `requireAdminActor` usando exclusivamente `req.auth_context` e exigindo `actor_type === user` mais actor_id não vazio per D12-10/H12-05. Criar helper explícito Strategy B que recebe IDs já gerados, descritores allowlisted e callback de domínio. Fixar o contrato de falhas nos testes: se append intent falha, não chamar o domínio e falhar a request de forma saneada; se o domínio falha, tentar append failed e sempre retornar o erro original saneado do domínio, registrando falha do append apenas como log estruturado saneado e deixando o intent órfão; se o domínio já foi persistido e append outcome falha, preservar a resposta HTTP de sucesso, emitir erro estruturado saneado e deixar o intent para o reconciliador. Provar por contador/spy que o callback de domínio é chamado exatamente uma vez e não roda novamente após falha do audit outcome. Não criar middleware/interceptor genérico per H12-03.</action>
  <verify><automated>cd apps/backend &amp;&amp; rtk npm run test:unit -- --runTestsByPath src/api/admin/_shared/__tests__/audit-admin-action.unit.spec.ts --runInBand</automated></verify>
  <acceptance_criteria>Actor e ordem de chamadas são exatos; todos os modos de falha preservam fatos honestos.</acceptance_criteria>
  <done>Helpers estão prontos para instrumentação factual em 12-05.</done>
</task>

</tasks>

## Data/schema decisions

- `audit_stage`: intent/outcome/reconciliation; `result`: D12-12.
- `action_attempt_id` correlaciona o par; retries recebem novo attempt. `correlation_id` agrupa request; idempotency_key pode repetir.
- PostgreSQL garante um intent e um terminal (`outcome` ou `reconciliation`) por attempt com dois UNIQUE parciais; não existe uniqueness global de idempotency_key.
- A migration protege append-only no banco, além de não exportar APIs mutáveis.

## Security/redaction constraints

- Admin ID vem apenas de auth_context user.
- Reason, previous/new state e metadata usam allowlists específicas; nenhum body bruto.
- Falha sem actor não cria linha inválida; só log estruturado sanitizado sem credenciais.

## Test strategy

- Unit para actor e Strategy B nos modos intent/domain/outcome failure.
- PostgreSQL descartável para service, dois UNIQUE parciais, seis casos de concorrência/retry, dedupe terminal e trigger update/delete.
- O spec PostgreSQL registra `admin_action_log` no `beforeServerStart` suportado pelo test runner 2.16.0, antes da migração automática; nenhum config/helper adicional é criado e `medusa-config.ts` permanece para 12-05.
- Reconciliação testada com fato presente, ausente e ambíguo.

## Exact validation commands

1. Recarregar `PHASE12_EXECUTION_BASE_SHA` do valor exato registrado em `12-01-SUMMARY.md`.
2. `cd apps/backend && rtk npm run test:unit -- --runTestsByPath src/api/admin/_shared/__tests__/audit-admin-action.unit.spec.ts src/jobs/__tests__/admin-action-log-reconciliation.unit.spec.ts --runInBand`
3. `cd apps/backend && rtk node scripts/run-disposable-postgres-tests.mjs -- rtk npm run test:integration:modules -- --runTestsByPath src/modules/admin-action-log/__tests__/admin-action-log.postgres.spec.ts --runInBand`
4. `cd apps/backend && rtk npm run build`
5. `rtk git diff --check`
6. `rtk git status --short`
7. `rtk git diff --exit-code "$PHASE12_EXECUTION_BASE_SHA"...HEAD -- apps/backend/package.json package-lock.json apps/backend/jest.config.js`

## Negative proofs

- `rtk rg -n "updateAdminAction|deleteAdminAction|softDelete" apps/backend/src/modules/admin-action-log` deve retornar vazio fora de testes negativos.
- `rtk rg -n "requested_by_operator_id|created_by_operator_id" apps/backend/src/api/admin/_shared` deve retornar vazio.
- Prova PostgreSQL de que UPDATE/DELETE falham e duas linhas correlacionadas podem coexistir.
- Prova de catálogo dos dois UNIQUE parciais e cardinalidade final dos seis casos concorrentes/retry; nenhuma uniqueness global sobre idempotency_key.

## Manual review gate

Parar. O humano revisa o DDL append-only, a Strategy B e todos os modos de falha. Aprovação explícita libera 12-05; ausência de prova PostgreSQL mantém BLOCKED.

## Rollback/recovery considerations

- Down migration remove trigger/function/índices/tabela na ordem inversa.
- Remover helpers não altera domínios existentes porque nenhuma rota foi instrumentada neste plano.
- Intent órfão é evidência preservada; recovery anexa reconciliação, nunca edita a linha.

## Completion evidence required

- Catálogo PostgreSQL e erro esperado de update/delete.
- Sequência de chamadas em cada modo de falha.
- Casos de orphan/reconciliation factual, janela de 15 minutos, paginação, worker-only/release-mode no-op e ambiguidade preservada.
- Evidência de que `beforeServerStart` registrou `configModule.modules.admin_action_log` antes de `runModulesMigrations` e de que o service foi resolvido pelo container resultante.
- Prova user-only, ausência de package/lock/Jest changes no intervalo e nenhum config change fora da allowlist Phase 12.

## Artifacts this phase produces

- Módulo AdminActionLog append-only.
- Actor guard user-only.
- Sequenciador Strategy B e primitives de reconciliação factual.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin auth→actor guard | Identidade autenticada determina admin_id |
| route→audit/domain | Falhas entre dois módulos não compartilham transação |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-S | Spoofing | admin_id | mitigate | aceitar somente auth_context user, nunca body/API key |
| T-12-04-R | Repudiation | pares intent/outcome | mitigate | IDs de attempt/correlation e append-only PostgreSQL |
| T-12-04-T | Tampering | linhas de auditoria | mitigate | trigger contra update/delete e service sem APIs mutáveis |
| T-12-04-I | Information Disclosure | metadata | mitigate | allowlists e sanitizer antes do append |
| T-12-04-SC | Tampering | installs | accept | Nenhum pacote novo |
</threat_model>

<verification>
Unit e PostgreSQL passam; actor/user, append-only e modes de falha correspondem à Strategy B aprovada.
</verification>

<success_criteria>
OPS-02 possui primitives factuais, correlacionados e resistentes a repúdio sem afirmar atomicidade impossível.
</success_criteria>

<output>
Create `.planning/phases/12-ops-audit-critical-tests/12-04-SUMMARY.md` when done e aguarde revisão humana.
</output>
