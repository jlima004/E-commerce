---
phase: 12-ops-audit-critical-tests
plan: 06
type: execute
wave: 4
depends_on: [12-03, 12-05]
files_modified:
  - apps/backend/integration-tests/http/invariants-inv01-02-order-birth.spec.ts
  - apps/backend/integration-tests/http/invariants-inv03-04-webhook-idempotency.spec.ts
  - apps/backend/integration-tests/http/invariants-inv08-gelato-single-active.spec.ts
  - apps/backend/integration-tests/http/invariants-inv09-10-refund-decoupling.spec.ts
  - apps/backend/src/modules/webhooks/__tests__/webhook-event-log.postgres.spec.ts
  - apps/backend/src/modules/checkout-completion/__tests__/checkout-completion-log.postgres.spec.ts
  - apps/backend/src/modules/gelato-fulfillment/__tests__/gelato-fulfillment.postgres.spec.ts
autonomous: false
requirements: [TEST-01, OPS-01, OPS-02]
must_haves:
  truths:
    - "Os testes impedem regressão que crie Order sem pagamento confirmado pelo webhook canônico."
    - "Replay e concorrência não duplicam WebhookEventLog, CheckoutCompletionLog, Order ou Gelato fulfillment ativo."
    - "Refund só finaliza por evento Stripe refund factual e nunca cancela automaticamente o Order."
    - "A validação final distingue provas HTTP/double de constraints e concorrência comprovadas em PostgreSQL real."
    - "OPS-02 só fecha com intent, outcome, orphan detection, reconciliação runtime e dedupe terminal, incluindo outcome concorrendo com reconciliation."
    - "As negativas finais comparam todo o intervalo desde PHASE12_EXECUTION_BASE_SHA e revisam medusa-config.ts por allowlist."
  artifacts:
    - path: "apps/backend/integration-tests/http/invariants-inv01-02-order-birth.spec.ts"
      provides: "INV-1/2 nomeados"
    - path: "apps/backend/integration-tests/http/invariants-inv03-04-webhook-idempotency.spec.ts"
      provides: "INV-3/4 nomeados"
    - path: "apps/backend/integration-tests/http/invariants-inv08-gelato-single-active.spec.ts"
      provides: "INV-8 nomeado"
    - path: "apps/backend/integration-tests/http/invariants-inv09-10-refund-decoupling.spec.ts"
      provides: "INV-9/10 nomeados"
    - path: "apps/backend/src/modules/gelato-fulfillment/__tests__/gelato-fulfillment.postgres.spec.ts"
      provides: "Prova real single-active"
  key_links:
    - from: "apps/backend/integration-tests/http/invariants-inv03-04-webhook-idempotency.spec.ts"
      to: "apps/backend/src/modules/webhooks/__tests__/webhook-event-log.postgres.spec.ts"
      via: "contrato HTTP complementado por unique/concurrency PostgreSQL"
      pattern: "WebhookEventLog"
    - from: "apps/backend/integration-tests/http/invariants-inv08-gelato-single-active.spec.ts"
      to: "apps/backend/src/modules/gelato-fulfillment/__tests__/gelato-fulfillment.postgres.spec.ts"
      via: "duplo do provider complementado por constraint real"
      pattern: "single.active|single_active"
---

<objective>
Materializar a suíte híbrida nomeada de invariantes e executar a validação final da Phase 12.

Purpose: transformar INV-1/2, INV-3/4, INV-8 e INV-9/10 em contratos de regressão nas fronteiras corretas.
Output: quatro specs HTTP exatos, três specs PostgreSQL e evidence bundle final.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-ops-audit-critical-tests/12-CONTEXT.md
@.planning/phases/12-ops-audit-critical-tests/12-RESEARCH.md
@.planning/phases/12-ops-audit-critical-tests/12-01-SUMMARY.md
@.planning/phases/12-ops-audit-critical-tests/12-03-SUMMARY.md
@.planning/phases/12-ops-audit-critical-tests/12-05-SUMMARY.md
@.planning/phases/12-ops-audit-critical-tests/12-VALIDATION.md
@apps/backend/integration-tests/http/stripe-webhook-store.spec.ts
@apps/backend/integration-tests/http/stripe-webhook-order-creation.spec.ts
@apps/backend/integration-tests/http/stripe-refund-webhook.spec.ts
</context>

## Objective

Fechar TEST-01 sem substituir as provas reais por mocks e sem chamar providers externos.

## Requirement coverage

- TEST-01: cobre explicitamente INV-1/2, INV-3/4, INV-8, INV-9/10.
- D12-13: suítes nomeadas + reuse dos harnesses existentes.
- D12-14: cada invariant no nível HTTP/unit/PostgreSQL indicado.
- D12-15: PostgreSQL local descartável; Redis/providers dobrados.
- OPS-01/OPS-02: regressão final das novas superfícies e módulos junto às suites completas.
- P12-PLAN-R1: OPS-02 exige executor runtime, dedupe terminal e contrato de sucesso HTTP quando o audit outcome falha.

## Dependencies

- Depende de 12-03 e 12-05; Wave 4.
- Consome o harness 12-01 e as provas PG de 12-02/12-04 sem modificar esses arquivos.
- O `12-03-SUMMARY.md` deve citar e levar adiante o `PHASE12_EXECUTION_BASE_SHA` registrado em `12-01-SUMMARY.md`. A negativa worktree-only legada do 12-03 é apenas evidência focada intermediária e não autoriza PASS final.
- Não altera runtime, manifests, Jest config ou migrations.

## Files to create

- Os sete specs exatos listados no frontmatter.

## Files to modify

- Nenhum arquivo existente.

## Explicit non-goals

- Sem Stripe CLI/real, Gelato real, Resend/PostHog/Correios real, Supabase, Heroku ou Redis externo.
- Sem load/performance, Admin UI e2e, health redesign ou REL-02.
- Sem alterar comportamento runtime para acomodar teste.

## Tasks in execution order

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar suítes nomeadas INV-1/2 e INV-3/4</name>
  <files>apps/backend/integration-tests/http/invariants-inv01-02-order-birth.spec.ts, apps/backend/integration-tests/http/invariants-inv03-04-webhook-idempotency.spec.ts</files>
  <read_first>apps/backend/integration-tests/http/stripe-webhook-store.spec.ts, apps/backend/integration-tests/http/stripe-webhook-order-creation.spec.ts, apps/backend/src/api/hooks/stripe/route.ts, apps/backend/src/workflows/order/webhook-order-entrypoint.ts</read_first>
  <behavior>
    - INV-1: checkout/client confirmation e webhook não sucedido não criam Order; somente payment_intent.succeeded validado alcança o entrypoint.
    - INV-2: `pix_expired` e todos os estados Pix aguardando allowlisted deixam contagem de Order em zero e não chamam o entrypoint.
    - INV-3: raw body e assinatura válida são obrigatórios; raw body ausente e assinatura ausente/inválida falham fechado antes de WebhookEventLog, CheckoutCompletionLog, workflow ou Order.
    - INV-4: replay do mesmo Stripe event/payment_intent retorna resposta idempotente e não duplica logs, claim ou Order; falha intermediária permite recuperação sem duplicação.
  </behavior>
  <action>Criar os dois specs flat com nomes exatos exigidos. Reutilizar factory/signature helpers de `stripe-webhook-store.spec.ts` e `stripe-webhook-order-creation.spec.ts`, porém declarar `describe`/`it` com IDs INV visíveis. Em `invariants-inv01-02-order-birth.spec.ts`, provar INV-1 separado de INV-2: checkout/client confirmation e eventos não sucedidos deixam zero Order; `pix_expired`, `awaiting_pix_payment`, `awaiting_webhook_confirmation`, `payment_instructions_displayed`, `payment_client_confirmed` e `client_action_required` deixam zero Order e zero chamada ao entrypoint. Em `invariants-inv03-04-webhook-idempotency.spec.ts`, provar INV-3 antes de persistência/workflow para raw body ausente e assinatura ausente/inválida, e reservar replay/dedupe/falha recuperável nominalmente para INV-4. Providers permanecem doubles injetáveis; não afirmar constraint/concorrência apenas pelo mock, pois as provas reais vêm no Task 3 per D12-14.</action>
  <verify><automated>cd apps/backend &amp;&amp; rtk npm run test:integration:http -- --runTestsByPath integration-tests/http/invariants-inv01-02-order-birth.spec.ts integration-tests/http/invariants-inv03-04-webhook-idempotency.spec.ts --runInBand</automated></verify>
  <acceptance_criteria>INV-1/2/3/4 aparecem nominalmente: INV-2 cobre `pix_expired` e todos os estados Pix aguardando com zero Order; INV-3 cobre raw body/assinatura fail-closed antes de DB/workflow; INV-4 cobre replay/dedupe.</acceptance_criteria>
  <done>Order birth e webhook idempotency têm regressões nomeadas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar suítes nomeadas INV-8 e INV-9/10</name>
  <files>apps/backend/integration-tests/http/invariants-inv08-gelato-single-active.spec.ts, apps/backend/integration-tests/http/invariants-inv09-10-refund-decoupling.spec.ts</files>
  <read_first>apps/backend/integration-tests/http/gelato-webhook.spec.ts, apps/backend/integration-tests/http/stripe-refund-webhook.spec.ts, apps/backend/src/modules/gelato-fulfillment/service.ts, apps/backend/src/modules/refund-request/stripe-refund-webhook.ts</read_first>
  <behavior>
    - Repetição/concorrrência lógica de dispatch não cria dois fulfillments Gelato ativos para o mesmo Order.
    - Providers são doubles e nenhuma chamada externa ocorre.
    - refund.created não finaliza dinheiro; refund.updated/succeeded factual atualiza refund/payment truth uma vez.
    - charge.refunded permanece informacional/idempotente e refund nunca altera order_status para canceled.
  </behavior>
  <action>Criar os specs flat exatos e reutilizar harnesses existentes. INV-8 deve provar guard e idempotency key observáveis no boundary Gelato; deixar a constraint concorrente real para Task 3. INV-9/10 deve testar eventos refund object, replay, evento charge informacional, payment_status recomputado e order_status preservado. Não chamar Stripe/Gelato e não introduzir cancel/refund automático per D12-15.</action>
  <verify><automated>cd apps/backend &amp;&amp; rtk npm run test:integration:http -- --runTestsByPath integration-tests/http/invariants-inv08-gelato-single-active.spec.ts integration-tests/http/invariants-inv09-10-refund-decoupling.spec.ts --runInBand</automated></verify>
  <acceptance_criteria>INV-8/9/10 aparecem nominalmente e falham se houver duplicação, finalização por evento errado ou cancelamento de Order.</acceptance_criteria>
  <done>Fulfillment único e refund decoupling têm regressões nomeadas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Provar constraints/concorrência em PostgreSQL e executar gate final</name>
  <files>apps/backend/src/modules/webhooks/__tests__/webhook-event-log.postgres.spec.ts, apps/backend/src/modules/checkout-completion/__tests__/checkout-completion-log.postgres.spec.ts, apps/backend/src/modules/gelato-fulfillment/__tests__/gelato-fulfillment.postgres.spec.ts</files>
  <read_first>apps/backend/integration-tests/postgres/disposable-postgres-harness.ts, apps/backend/src/modules/webhooks/migrations/Migration20260701000000.ts, apps/backend/src/modules/checkout-completion/migrations/Migration20260702000000.ts, apps/backend/src/modules/gelato-fulfillment/migrations/Migration20260703000000.ts</read_first>
  <behavior>
    - Inserts concorrentes do mesmo Stripe event/payment_intent respeitam uniques e deixam um fato canônico.
    - Claims concorrentes de CheckoutCompletion têm um vencedor e replay recuperável.
    - Tentativas concorrentes de fulfillment deixam no máximo um ativo por Order.
    - Todas as suites Phase 12 e baselines completas passam sem resíduos.
    - AdminActionLog prova os dois UNIQUE parciais, outcome versus reconciliation, dois workers no mesmo órfão e um único terminal por action_attempt_id.
    - HTTP prova que falha do audit outcome após domínio persistido preserva a resposta de sucesso e não repete o callback.
  </behavior>
  <action>Criar três specs de módulo via harness 12-01, usando conexões/transactions reais e barriers para iniciar concorrência, não Promise mocks. Cada invocação de `medusaIntegrationTestRunner` deve passar `dbName: process.env.DB_TEMP_NAME`, rejeitar nome ausente/não descartável e reportar somente o nome sanitizado que o test-utils realmente cria/remove. Consumir a migration Gelato já materializada no predecessor 12-01 como `Migration20260703000000.ts`, consultar o catálogo para confirmar `IDX_gelato_fulfillment_order_id_unique` e as demais constraints alvo e afirmar linha/estado final. Rodar também provas PG de OperationalAlert/AdminActionLog, incluindo catálogo dos UNIQUE parciais de intent/terminal, outcome concorrendo com reconciliation e dois workers reconciliando o mesmo órfão, mais o unit do job de 15 minutos e as provas HTTP de que outcome audit failure preserva sucesso e não repete domínio. Executar as quatro specs INV, suites completas, lint e build conforme 12-VALIDATION.

Gate PostgreSQL (P12-12-06-R1): cinco specs executados serialmente; um processo descartável por spec via `run-disposable-postgres-tests.mjs` + `--runTestsByPath` de um único path; todos PASS; cleanup após cada processo. Não empilhar os cinco paths no mesmo Jest.

Gate Modules: suíte completa pelo comando normal `TMPDIR=/tmp npm run test:integration:modules -w @dtc/backend`; baseline igual ou superior à vigente; nenhum teste omitido.

Stacked execution: prova diagnóstica apenas — known incompatible `Map.prototype.set`; not required for PASS; no correction attempted in Phase 12. Não classificar stacked como teste aprovado.

Qualquer PG indisponível, teste ignorado, resíduo ou baseline regressiva classifica o gate BLOCKED; não suavizar. Reutilizar obrigatoriamente o `PHASE12_EXECUTION_BASE_SHA` registrado em 12-01-SUMMARY.md e propagado por 12-03-SUMMARY.md para negativas do intervalo, manter `git diff --check` e `git status --short` separados e registrar o diff de medusa-config.ts como evidência allowlisted. Recusar PASS se a evidência usar apenas worktree limpo, o comando legado worktree-only de 12-03 ou ausência total de diff em medusa-config.ts; a config pode ter exatamente os registros allowlisted e precisa ser revisada pelo intervalo base...HEAD. Classificar a evidência em quatro níveis: process-local executado; PostgreSQL transacional executado em processos descartáveis serializados; cross-process/dyno inferido pela constraint compartilhada; cross-dyno real não executado e não alegado.</action>
  <verify><automated>Serial disposable: for each of the five .postgres.spec.ts paths, `cd apps/backend &amp;&amp; node scripts/run-disposable-postgres-tests.mjs -- npm run test:integration:modules -- --runTestsByPath &lt;one-spec&gt; --runInBand` then assert no residual `p12-pg-*`. Modules normal: `TMPDIR=/tmp npm run test:integration:modules -w @dtc/backend`.</automated></verify>
  <acceptance_criteria>Constraints e concorrência são comprovadas em PostgreSQL real, suites completas passam e nenhuma chamada externa/resíduo existe.</acceptance_criteria>
  <done>TEST-01 e o gate técnico final da Phase 12 possuem evidência completa.</done>
</task>

</tasks>

## Data/schema decisions

- Nenhuma migration nova neste plano; os specs consultam/aplicam migrations existentes em DB descartável.
- Concorrência usa múltiplas operações reais e barreira controlada.
- Fixtures recebem IDs únicos e cleanup é responsabilidade do harness.

## Security/redaction constraints

- Fixtures usam valores sintéticos, nunca secrets ou dados de produção.
- Logs/evidence não contêm DSN, signatures reais, client_secret, Pix QR, tracking token, endereço ou documento.
- Providers e Redis ficam dobrados; nenhuma chave externa é necessária.

## Test strategy

- Hybrid per D12-13: quatro specs HTTP nomeados + reuse de helpers.
- D12-14: HTTP/workflow para comportamento; PostgreSQL para unique/claim/concurrency.
- D12-15: DB local descartável, providers/Redis doubles, fail-closed se DB indisponível.

## Exact validation commands

Gate PostgreSQL (serial, um processo por spec — P12-12-06-R1):

```text
Gate PostgreSQL:
- cinco specs executados serialmente;
- um processo descartável por spec;
- todos PASS;
- cleanup após cada processo.
```

1. Recarregar `PHASE12_EXECUTION_BASE_SHA` do valor exato registrado em `12-01-SUMMARY.md`.
2. `cd apps/backend && npm run test:integration:http -- --runTestsByPath integration-tests/http/invariants-inv01-02-order-birth.spec.ts integration-tests/http/invariants-inv03-04-webhook-idempotency.spec.ts integration-tests/http/invariants-inv08-gelato-single-active.spec.ts integration-tests/http/invariants-inv09-10-refund-decoupling.spec.ts --runInBand`
3. Para cada um dos cinco paths `.postgres.spec.ts` (não empilhar no mesmo Jest): `cd apps/backend && node scripts/run-disposable-postgres-tests.mjs -- npm run test:integration:modules -- --runTestsByPath <one-spec> --runInBand` e confirmar `docker ps -a --filter name=p12-pg-` vazio.
4. `TMPDIR=/tmp npm run test:unit -w @dtc/backend`

Gate Modules:

```text
Gate Modules:
- suíte completa pelo comando normal;
- baseline igual ou superior à vigente;
- nenhum teste omitido.
```

5. `TMPDIR=/tmp npm run test:integration:modules -w @dtc/backend`
6. `TMPDIR=/tmp npm run test:integration:http -w @dtc/backend`
7. `HOME=/tmp XDG_CONFIG_HOME=/tmp TMPDIR=/tmp ADMIN_DISABLED=true npm run lint -w @dtc/backend`
8. `HOME=/tmp XDG_CONFIG_HOME=/tmp TMPDIR=/tmp ADMIN_DISABLED=true npm run build -w @dtc/backend`
9. `git diff --check`
10. `git status --short`
11. `git diff --exit-code "$PHASE12_EXECUTION_BASE_SHA"...HEAD -- apps/backend/package.json package-lock.json apps/backend/jest.config.js`
12. `git diff "$PHASE12_EXECUTION_BASE_SHA"...HEAD -- apps/backend/medusa-config.ts` e revisão linha a linha da allowlist.

Stacked (diagnóstico, não gate):

```text
Stacked execution:
known incompatible Map.prototype.set failure;
not required for PASS;
no correction attempted in Phase 12.
```

## Negative proofs

- Confirmar no runner que nenhum teste PG foi skipped e que o alvo era loopback descartável.
- `rtk rg -n "sk_live|api\.stripe\.com|gelatoapis\.com|api\.resend\.com|posthog" apps/backend/integration-tests/http/invariants-*.spec.ts apps/backend/src/modules/*/__tests__/*.postgres.spec.ts` deve retornar vazio.
- `rtk git diff --exit-code "$PHASE12_EXECUTION_BASE_SHA"...HEAD -- apps/backend/package.json package-lock.json apps/backend/jest.config.js`; worktree limpo isoladamente não é prova do intervalo.
- O diff registrado de `apps/backend/medusa-config.ts` aceita somente os registros `operational_alert` e `admin_action_log`; mudanças em Redis, cache, event bus, workflow, locking, database, providers, health ou release migration mode bloqueiam.
- O comando legado worktree-only do 12-03 pode acompanhar seu SUMMARY apenas como prova focada intermediária; não satisfaz nem substitui as duas negativas finais acima.

## Manual review gate

Parar no resultado binário PASS/BLOCKED. O humano revisa comandos, contagens, prova PG, negativas e resíduos. Não iniciar SPEC/SDD, implementation prompt, deploy ou closure automaticamente.

## Rollback/recovery considerations

- Rollback remove apenas os sete specs; nenhum runtime/schema é tocado.
- Harness remove DB/container; resíduo torna gate BLOCKED até cleanup explícito por ID.
- Falha de suite preserva outputs sanitizados para diagnóstico e não autoriza correção fora do gate.

## Completion evidence required

- Resultado individual dos quatro specs HTTP por INV.
- Resultado dos cinco specs PG com catálogo e cardinalidade final.
- Classificação explícita: process-local executado; PostgreSQL transacional executado; cross-process/dyno inferido pela constraint; cross-dyno real não executado/não alegado.
- Contagens completas unit/modules/HTTP, lint error/warning count, build e diff check.
- Lista de arquivos alterados e prova de ausência de package/lockfile/Jest config/providers/resíduos.
- SHA-base herdado do 12-01-SUMMARY, `git diff --check`, `git status --short` e diff allowlisted revisável de medusa-config.ts.

## Artifacts this phase produces

- Quatro suites HTTP nomeadas exatamente por invariantes.
- Três suites PostgreSQL de constraints/concorrência.
- Evidence bundle binário PASS/BLOCKED para revisão humana.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixtures→webhook/API | Payloads sintéticos atravessam validação de assinatura/rotas |
| Jest→PostgreSQL local | Testes concorrentes exercem constraints destrutivas em DB descartável |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06-S | Spoofing | assinatura webhook | mitigate | usar helper de assinatura sintética e secret exclusivamente de teste |
| T-12-06-T | Tampering | evidência de concorrência | mitigate | múltiplas conexões reais, barriers e catálogo PostgreSQL |
| T-12-06-I | Information Disclosure | output Jest | mitigate | fixtures sintéticas e redaction do harness |
| T-12-06-D | Denial of Service | suites/cleanup | mitigate | runInBand, timeouts e teardown fail-closed |
| T-12-06-SC | Tampering | installs | accept | Nenhum pacote novo |
</threat_model>

<verification>
Todas as provas focadas/completas passam, PG é real e local, providers não são chamados e o workspace fica sem resíduos.
</verification>

<success_criteria>
TEST-01 impede regressões nos invariantes centrais e a Phase 12 alcança gate técnico PASS sem evidência simulada indevida.
</success_criteria>

<output>
Create `.planning/phases/12-ops-audit-critical-tests/12-06-SUMMARY.md` when done e pare para revisão humana.
</output>
