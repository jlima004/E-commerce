---
phase: 09
plan: 05
type: validation
wave: 5
depends_on: [09-04]
files_modified:
  - .planning/phases/09-gelato-fulfillment-webhook/09-05-SUMMARY.md
  - .planning/phases/09-gelato-fulfillment-webhook/09-VALIDATION.md
  - .planning/STATE.md
autonomous: false
manual_review_gate: true
requirements: [FUL-01, FUL-02, FUL-03, FUL-04, WHK-03]
---

# 09-05 - Final Validation, Invariant Tests And Negative Proofs

## Objective

Run the final Phase 09 validation battery, document evidence, reconcile `09-VALIDATION.md` if needed, and stop at manual review gate. Do not start Phase 10.

## Scope

Validation/documentary closeout only for Phase 09. No new feature implementation unless required to fix failed Phase 09 validation and approved within the same slice scope.

## Required Decisions Covered

- `D09-01`
- `D09-02`
- `D09-03`
- `D09-04`
- `D09-05`
- `D09-06`
- `D09-07`
- `D09-08`
- `D09-09`
- `D09-10`
- `D09-11`
- `D09-12`
- `D09-13`
- `D09-14`
- `D09-15`
- `D09-16`

## Required Validation Matrix

Run targeted Phase 09 unit suites:

- `gelato-fulfillment` contract/model/helper tests.
- Gelato payload builder tests.
- Eligibility gate tests.
- Gelato dispatch relay tests.
- Gelato webhook route/service tests.

Run filtered HTTP/integration suites:

- accepted Order path with Gelato blocked until e-mail `sent`;
- local fulfillment enqueue once eligible;
- replay/concurrency single-active proof;
- fake dispatch success/failure proof;
- webhook Gelato idempotency/status/tracking proof.

Run build:

```bash
cd apps/backend && HOME=/tmp XDG_CONFIG_HOME=/tmp TMPDIR=/tmp ADMIN_DISABLED=true npm run build
```

Run negative greps from `09-VALIDATION.md`:

- no Store completion public Order creation drift;
- no refund/exchange/tracking public implementation;
- no Stripe CLI smoke;
- no Gelato real call in tests;
- no persisted secrets/raw payload/forbidden data;
- no Phase 10 files/routes.

## Evidence To Record In Summary

`09-05-SUMMARY.md` must include:

- branch decision B (`gsd/phase-09-gelato-fulfillment-webhook`);
- tests run and exact results;
- build result;
- negative grep result;
- proof of `FUL-01..FUL-04` and `WHK-03`;
- proof that Gelato failure does not revert `Order`;
- proof that persistent Gelato failure marks `dead_letter` and `requires_operator_attention = true` on `GelatoFulfillment`;
- proof that eligibility scan creates/reuses `GelatoFulfillment` after e-mail `sent` without Stripe webhook replay;
- proof that e-mail `dead_letter` blocks automatic Gelato;
- proof that e-mail `recorded`, `queued`, `sending` and `failed` block automatic Gelato;
- proof that stale `queued`/`dispatching`/`submitted` Gelato relay rows are recoverable without blind duplicate Gelato dispatch;
- proof that uncertain post-call crash states are reconciled or operator-gated, not automatically redispatched;
- if Gelato webhook authenticity remains unconfirmed, proof that `09-04` stopped blocked and `WHK-03` was not marked complete;
- proof that no public tracking/refund/exchange/Stripe CLI smoke was introduced;
- explicit manual gate before Phase 10.

## Acceptance Criteria

- Phase 09 validation passes or blockers are documented.
- If the Gelato webhook authenticity blocker remains unresolved, `09-04` stays blocked and `WHK-03` is not marked complete.
- All Phase 09 requirements have evidence.
- `STATE.md` is updated only to a manual-review-gated status.
- Phase 10 remains not started.

## Manual Gate

Stop after `09-05-SUMMARY.md`. Do not create `09-CLOSURE.md` or start Phase 10 unless the user explicitly asks for closure/next-phase work.
