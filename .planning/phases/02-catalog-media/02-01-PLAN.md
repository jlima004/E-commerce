---
phase: 02-catalog-media
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/catalog/gelato-metadata.ts
  - apps/backend/src/modules/catalog/types.ts
  - apps/backend/src/modules/catalog/__tests__/gelato-metadata.unit.spec.ts
autonomous: false
requirements:
  - CAT-01
  - CAT-02
  - CAT-04
must_haves:
  truths:
    - "D-01 e D-02: todo acesso a `ProductVariant.metadata.gelato_*` passa por um helper/schema tipado central."
    - "D-03 e D-04: o contrato obrigatorio segue integralmente `docs/DB_MODEL_v1.21.md` e inclui `gelato_product_uid`, `gelato_template_id`, `gelato_variant_options` e `template_mode=fixed`."
    - "D-06 e D-07: draft incompleto e permitido, mas helper deve distinguir claramente variante vendavel de variante incompleta."
    - "Nao ha persistencia em Order ou `LineItem.metadata.gelato_snapshot` nesta slice."
  artifacts:
    - path: "apps/backend/src/modules/catalog/gelato-metadata.ts"
      provides: "Leitura, normalizacao e validacao tipada da metadata Gelato"
      exports: ["readGelatoMetadata", "assertSellableVariantMetadata", "isSellableVariant"]
    - path: "apps/backend/src/modules/catalog/types.ts"
      provides: "Tipos compartilhados do contrato de catalogo Gelato"
    - path: "apps/backend/src/modules/catalog/__tests__/gelato-metadata.unit.spec.ts"
      provides: "Prova RED/GREEN do contrato e dos erros tipados"
---

<objective>
Centralizar o contrato Gelato da variante em um helper tipado e testado.

Purpose: transformar metadata bruta do Medusa em uma superficie unica, segura e reutilizavel pelas slices seguintes.
Output: tipos, parser/validator e predicado `is_sellable` sem acesso espalhado a `metadata.gelato_*`.
</objective>

## Phase Goal

**Como operador**, **quero modelar variantes com contrato Gelato consistente**, **para que publicacao, API publica e snapshot futuro leiam a mesma fonte de verdade**.

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-catalog-media/02-CONTEXT.md
@.planning/phases/02-catalog-media/02-VALIDATION.md
@docs/DB_MODEL_v1.21.md
@docs/PRD_Backend_v1.1.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Escrever a suite RED do contrato Gelato da variante</name>
  <read_first>
    - .planning/phases/02-catalog-media/02-CONTEXT.md - D-01 ate D-07
    - docs/DB_MODEL_v1.21.md - secoes 2.12 e 4.11
    - docs/PRD_Backend_v1.1.md - requisitos de catalogo e metadados Gelato
    - apps/backend/jest.config.js - runner existente
  </read_first>
  <files>apps/backend/src/modules/catalog/__tests__/gelato-metadata.unit.spec.ts</files>
  <action>Criar testes unitarios RED cobrindo: leitura de metadata completa; metadata ausente/incompleta; `template_mode` diferente de `fixed`; `gelato_variant_options` vazia; preco BRL em centavos inteiros nas variantes vendaveis; e erros tipados estaveis para consumidores internos.</action>
  <verify>
    <automated>cd apps/backend && npm run test:unit -- --runTestsByPath src/modules/catalog/__tests__/gelato-metadata.unit.spec.ts -t "parse|sellable|BRL|required"</automated>
  </verify>
  <acceptance_criteria>
    - Os testes falham antes da implementacao.
    - A suite distingue draft incompleto de variante marcada como vendavel/publicavel.
    - O shape exigido e exatamente o do contrato canonico, sem "minimal core".
  </acceptance_criteria>
  <done>Suite RED do helper central escrita.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar helper tipado e erros deterministas</name>
  <read_first>
    - apps/backend/src/modules/catalog/__tests__/gelato-metadata.unit.spec.ts
    - .planning/phases/02-catalog-media/02-CONTEXT.md - D-01 ate D-10
  </read_first>
  <files>apps/backend/src/modules/catalog/gelato-metadata.ts, apps/backend/src/modules/catalog/types.ts</files>
  <action>Implementar tipos e helpers centrais que encapsulam toda leitura de `variant.metadata`. Exportar parser/normalizador, `assertSellableVariantMetadata` para gates estritos e `isSellableVariant` para consumo read-time. Garantir erros tipados claros, sem stack cru no payload publico, e sem qualquer persistencia em Order/LineItem.</action>
  <verify>
    <automated>cd apps/backend && npm run test:unit -- --runTestsByPath src/modules/catalog/__tests__/gelato-metadata.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Nenhum caller futuro precisa acessar `metadata.gelato_*` diretamente.
    - `isSellableVariant` e `assertSellableVariantMetadata` compartilham a mesma fonte de verdade.
    - Variantes incompletas continuam representaveis como draft.
  </acceptance_criteria>
  <done>Contrato Gelato central implementado.</done>
</task>

</tasks>

<verification>
- `cd apps/backend && npm run test:unit -- --runTestsByPath src/modules/catalog/__tests__/gelato-metadata.unit.spec.ts`
- `cd apps/backend && npm run build`
</verification>

<success_criteria>
- O contrato Gelato da variante esta centralizado, tipado e testado.
- Draft incompleto e distinguido de variante vendavel.
- Nenhuma persistencia de Order/LineItem foi introduzida.
</success_criteria>

<output>
Create `.planning/phases/02-catalog-media/02-01-SUMMARY.md` when done.
</output>
