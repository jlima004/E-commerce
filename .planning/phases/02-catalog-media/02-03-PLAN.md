---
phase: 02-catalog-media
plan: "03"
type: execute
wave: 2
depends_on: []
files_modified:
  - apps/backend/package.json
  - package-lock.json
  - apps/backend/medusa-config.ts
  - apps/backend/src/config/env.ts
  - apps/backend/src/config/__tests__/env.unit.spec.ts
autonomous: false
requirements:
  - MEDIA-01
must_haves:
  truths:
    - "D-11 e D-12: usar `@medusajs/file-s3` apontando para o endpoint S3-compatible do Supabase Storage; nao criar provider custom nesta fase."
    - "D-13 e D-14: imagens de catalogo sao publicas no MVP e a API retorna apenas referencias URL, nunca binarios no banco."
    - "D-15: signed URLs e assets privados ficam fora do escopo."
    - "Versoes `@medusajs/*` e pacote de storage devem permanecer alinhadas ao stack atual."
  artifacts:
    - path: "apps/backend/src/config/env.ts"
      provides: "Schema validado das envs de storage"
    - path: "apps/backend/medusa-config.ts"
      provides: "Registro do provider de arquivo para o bucket do Supabase"
    - path: "apps/backend/src/config/__tests__/env.unit.spec.ts"
      provides: "Prova de fail-fast do contrato de env"
---

<objective>
Preparar o backend para armazenar imagens do catalogo no Supabase Storage via provider oficial.

Purpose: garantir que a camada de midia use URL publica estavel e nao introduza provider custom desnecessario.
Output: provider `@medusajs/file-s3` configurado, env validado e gate humano de credenciais externo ao Git.
</objective>

## Phase Goal

**Como operador**, **quero subir imagens de produto para o Supabase Storage**, **para que o catalogo publico use referencias URL publicas e nao binarios persistidos no banco**.

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-catalog-media/02-CONTEXT.md
@.planning/phases/02-catalog-media/02-VALIDATION.md
@.planning/research/STACK.md
@apps/backend/medusa-config.ts
@apps/backend/src/config/env.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING] Aprovar pacote oficial e provisionar bucket/credenciais fora do Git</name>
  <read_first>
    - .planning/research/STACK.md - provider recomendado para storage
    - apps/backend/package.json - conjunto atual de versoes Medusa
  </read_first>
  <files>Sem alteracoes antes do gate.</files>
  <action>Interromper antes de instalar/configurar. Confirmar `@medusajs/file-s3` na mesma linha do conjunto Medusa instalado, criar/selecionar bucket publico do Supabase Storage e provisionar endpoint, region e credenciais em ambiente autorizado, sem registrar valores no Git.</action>
  <verify>
    <automated>npm view @medusajs/file-s3@2.16.0 version repository.url</automated>
    <human-check>Confirmar bucket publico, endpoint S3-compatible e credenciais fora do Git; responder "done".</human-check>
  </verify>
  <acceptance_criteria>
    - O pacote oficial existe e esta alinhado com o stack atual.
    - O bucket e publico no MVP.
    - Endpoint e credenciais ficam apenas no ambiente autorizado.
  </acceptance_criteria>
  <done>Gate externo de supply-chain e credenciais aprovado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Escrever testes RED do contrato de env e wiring de storage</name>
  <read_first>
    - apps/backend/src/config/env.ts
    - apps/backend/medusa-config.ts
    - .planning/phases/02-catalog-media/02-CONTEXT.md - D-11 ate D-15
  </read_first>
  <files>apps/backend/src/config/__tests__/env.unit.spec.ts</files>
  <action>Adicionar casos RED no schema de env cobrindo variaveis obrigatorias de storage em production, ausencia de fallback silencioso, e forma publica das URLs retornadas para catalogo. Nao introduzir signed URLs nem provider custom.</action>
  <verify>
    <automated>cd apps/backend && npm run test:unit -- --runTestsByPath src/config/__tests__/env.unit.spec.ts -t "storage|s3|supabase|public url"</automated>
  </verify>
  <acceptance_criteria>
    - Falta de env obrigatoria de storage falha cedo em production.
    - Nao existe caminho alternativo via provider custom nesta fase.
  </acceptance_criteria>
  <done>Suite RED do contrato de storage escrita.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Registrar provider oficial e validar schema de storage</name>
  <read_first>
    - apps/backend/src/config/__tests__/env.unit.spec.ts
    - apps/backend/medusa-config.ts
    - apps/backend/src/config/env.ts
  </read_first>
  <files>apps/backend/package.json, package-lock.json, apps/backend/medusa-config.ts, apps/backend/src/config/env.ts</files>
  <action>Apos o gate humano, instalar/configurar `@medusajs/file-s3` no backend, ampliar o schema de env com as vars necessarias do Supabase Storage S3-compatible e registrar o provider em `medusa-config.ts` de forma fail-fast. Preservar o contrato de URL publica do catalogo e nao tocar em migrations, deploy ou assets privados.</action>
  <verify>
    <automated>cd apps/backend && npm run test:unit -- --runTestsByPath src/config/__tests__/env.unit.spec.ts -t "storage|s3|supabase" && cd apps/backend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - O backend conhece o provider oficial e suas envs obrigatorias.
    - O contrato aponta para URL publica do bucket.
    - Nenhum binario e persistido em Postgres por este plano.
  </acceptance_criteria>
  <done>Storage de imagens pronto para uso pelo catalogo.</done>
</task>

</tasks>

<verification>
- `cd apps/backend && npm run test:unit -- --runTestsByPath src/config/__tests__/env.unit.spec.ts`
- `cd apps/backend && npm run build`
- Smoke manual de upload em ambiente autorizado apos o gate humano
</verification>

<success_criteria>
- Provider oficial configurado para Supabase Storage.
- Schema de env falha cedo sem vars obrigatorias.
- O contrato da fase continua sendo URL publica, nao binario no banco.
</success_criteria>

<output>
Create `.planning/phases/02-catalog-media/02-03-SUMMARY.md` when done.
</output>
