---
phase: 16-cart-merge-review
plan: 03
type: execute
wave: 2
depends_on: [16-02]
files_modified:
  - apps/backend/src/modules/cart-merge/service.ts
  - apps/backend/src/modules/cart-merge/index.ts
  - apps/backend/src/modules/cart-merge/migrations/
  - docs/DB_MODEL_v1.22.md
autonomous: true
requirements: [MRG-01, MRG-05, MRG-06]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "O wiring `cart_merge` criado na Wave 0 permanece único: `CartMergeModuleService` é o default service, a rota resolve o token no container e nenhuma função solta contorna o módulo."
    - "O config unit e o build validam o wiring existente route → container → module service antes de gerar DDL; 16-03 não cria segunda entrada, token, Module ou injeção manual."
    - "A migration/snapshot são produzidos exclusivamente pela CLI Medusa a partir dos models D16-16..D16-21 e D16-27..D16-33, sem aplicação local persistente ou remota."
    - "O shape aprovado preserva receipt original/ETag per R16-HR-04, retenção coordenada per R16-HR-06 e nenhum branch positivo CUSTOMER_CART_PRESERVED per R16-HR-01."
  artifacts:
    - path: apps/backend/src/modules/cart-merge/service.ts
      provides: "Default CartMergeModuleService consolidado com os models de 16-02, preservando executeCartMerge como entrada pública única"
      exports: [CartMergeModuleService]
    - path: apps/backend/src/modules/cart-merge/index.ts
      provides: "Wiring CART_MERGE_MODULE da Wave 0 consolidado/validado sem duplicação"
      exports: [CART_MERGE_MODULE]
    - path: apps/backend/src/modules/cart-merge/migrations/
      provides: "Migration e snapshot gerados pela CLI para revisão humana"
  key_links:
    - from: apps/backend/src/api/store/customers/me/cart/merge/route.ts
      to: apps/backend/src/modules/cart-merge/index.ts
      via: "16-03 revalida o req.scope.resolve(CART_MERGE_MODULE) já criado na Wave 0"
      pattern: "scope.resolve.*CART_MERGE_MODULE|cart_merge"
    - from: apps/backend/src/modules/cart-merge/index.ts
      to: apps/backend/src/modules/cart-merge/service.ts
      via: "Module registra CartMergeModuleService como service default"
      pattern: "Module.*service: CartMergeModuleService"
    - from: apps/backend/medusa-config.ts
      to: apps/backend/src/modules/cart-merge/index.ts
      via: "16-03 valida o registro único cart_merge já criado na Wave 0"
      pattern: "key:.*cart_merge|src/modules/cart-merge"
---

<objective>
Consolidar os models no motor já traçado e validar o módulo Medusa `cart_merge` registrado na Wave 0, gerando sua identidade de schema para a revisão humana.

Purpose: preservar o route-to-function boundary já executável desde a Wave 0 e impedir que migration/snapshot sejam materializados antes de o wiring existente, models, config e build passarem.
Output: `CartMergeModuleService`/models consolidados, wiring `Module`/config validado sem recriação, DB model documentado e migration CLI gerada ainda não aplicada.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-cart-merge-review/16-CONTEXT.md
@.planning/phases/16-cart-merge-review/16-RESEARCH.md
@.planning/phases/16-cart-merge-review/16-RESEARCH-REVIEW.md
@.planning/phases/16-cart-merge-review/16-PATTERNS.md
@.planning/phases/16-cart-merge-review/16-01-SUMMARY.md
@.planning/phases/16-cart-merge-review/16-02-SUMMARY.md
@apps/backend/src/modules/store-idempotency/index.ts
@apps/backend/src/modules/cart-merge/service.ts
@apps/backend/medusa-config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 16-03-01: Consolidar models e validar wiring cart_merge da Wave 0</name>
  <files>apps/backend/src/modules/cart-merge/service.ts, apps/backend/src/modules/cart-merge/index.ts</files>
  <read_first>16-PATTERNS.md seção 1 e seção 5; `store-idempotency/index.ts` para o export default oficial; `cart-merge/service.ts`/`cart-merge/index.ts` criados no tracer; a merge route já ligada ao token; os três models de 16-02; `medusa-config.ts` e `medusa-config.unit.spec.ts` para validar o registro único e expectedLocalModules</read_first>
  <behavior>
    - "O default export de service.ts continua sendo a classe concreta CartMergeModuleService e index.ts continua registrando exatamente essa classe sob o token já criado."
    - "A merge route continua resolvendo CART_MERGE_MODULE no request scope e invocando executeCartMerge como método; não há import/chamada de função pública solta."
    - "Os três models de 16-02 ficam incorporados ao service/module para o config/build, e o registro cart_merge permanece exatamente uma vez."
  </behavior>
  <action>Partir dos artefatos já produzidos em 16-01: `CART_MERGE_MODULE = "cart_merge"`, `default Module(CART_MERGE_MODULE, { service: CartMergeModuleService })`, import do token na rota, registro único `key: "cart_merge", resolve: "./src/modules/cart-merge"` em `medusa-config.ts` e expectativa correspondente no config unit. Não criar novamente nenhum desses elementos, não adicionar segunda chamada `Module`, segundo token, segunda entrada de config, injeção manual ou bypass de container. Reabrir `service.ts`/`index.ts` somente para consolidar os três models de 16-02 no padrão MedusaService/module exigido pelo build e pela geração de schema, preservando `executeCartMerge` como única entrada pública. Executar o config unit e o build antes de qualquer geração; a prova deve continuar mostrando route → container → service. Se o wiring existente, types, models ou build divergirem, parar BLOCKED e não gerar migration para esconder erro.</action>
  <verify>
    <automated>cd apps/backend && npm run test:unit -- --runTestsByPath src/infrastructure/__tests__/medusa-config.unit.spec.ts && npm run build && test "$(rg -c 'CART_MERGE_MODULE = \"cart_merge\"' src/modules/cart-merge/index.ts)" -eq 1 && test "$(rg -c 'key: \"cart_merge\"' medusa-config.ts)" -eq 1</automated>
  </verify>
  <acceptance_criteria>O config unit e o build passam; o token `CART_MERGE_MODULE = "cart_merge"`, o default `Module(CART_MERGE_MODULE, { service: CartMergeModuleService })` e a entrada `cart_merge` permanecem únicos e foram apenas consolidados/validados, não recriados; os três models de 16-02 estão incorporados; route → container → service method continua sendo o único wiring público; D16-29, D16-34 e D16-36 continuam no mesmo manager.</acceptance_criteria>
  <done>O wiring executável da Wave 0 permanece íntegro, os models estão incorporados e o módulo está pronto para o gate de schema sem duplicação.</done>
</task>

<task type="auto">
  <name>Task 16-03-02: Gerar identidade model/snapshot/migration para checkpoint</name>
  <reversibility rating="costly">Os arquivos ainda são locais, mas qualquer aplicação posterior precisa corresponder byte a byte à identidade revisada.</reversibility>
  <files>apps/backend/src/modules/cart-merge/migrations/, docs/DB_MODEL_v1.22.md</files>
  <read_first>os três models de 16-02; wiring `cart_merge` e config unit da Wave 0; consolidação service/module validada no Task 16-03-01; migration/snapshot analog de store-resource-version; planos 14-07 e 15-02 para audit→generate→disposable; DB_MODEL v1.22</read_first>
  <action>Atualizar DB_MODEL somente com CustomerCartAuthority, CartMergeResult, CartReview, enums, constraints, retenção coordenada e proibições públicas. De `apps/backend`, executar uma única vez `npx medusa db:generate cart_merge`; preservar filename, class e snapshot produzidos e complementar CHECK/unique parcial apenas pelo mecanismo aceito já documentado, sem aplicar a migration. Produzir no SUMMARY a lista exact-set de models, snapshot e todos os arquivos de migration com SHA-256, delimitada literalmente por `SCHEMA_IDENTITY_SHA256_BEGIN` e `SCHEMA_IDENTITY_SHA256_END`, uma linha padrão `sha256sum` por path relativo ao repositório. Se a CLI falhar ou o output não corresponder aos três models, parar BLOCKED e não criar DDL alternativo. Não executar migrate, Supabase CLI, provider ou DSN remoto.</action>
  <verify>
    <automated>cd apps/backend && test -n "$(find src/modules/cart-merge/migrations -maxdepth 1 -type f -print -quit)" && npm run test:unit -- --runTestsByPath src/infrastructure/__tests__/medusa-config.unit.spec.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>Migration/snapshot CLI e DB_MODEL correspondem aos três models; a identidade SHA-256 exact-set fica registrada para 16-04/16-05; nenhum apply, remote action ou push ocorreu.</acceptance_criteria>
  <done>Schema Phase 16 está materializado localmente e identificável byte a byte para revisão.</done>
</task>

</tasks>

## Constraints and planning gates

- Technical draft only: HUMAN PLAN REVIEW REQUIRED; Phase 16 EXECUTION NOT AUTHORIZED.
- Nenhum writer OpenAPI, migration apply, remote DB/Redis, provider, frontend, deploy ou push.
- D16-01..D16-42 e R16-HR-01..R16-HR-08 permanecem vinculantes; MRG-01..MRG-08 permanecem OPEN.

## Artifacts this phase produces

- `apps/backend/src/modules/cart-merge/service.ts` consolidado com os três models de 16-02, preservando a classe default concreta `CartMergeModuleService` e `executeCartMerge` como entrada pública única do motor.
- `apps/backend/src/modules/cart-merge/index.ts` com o wiring `CART_MERGE_MODULE` da Wave 0 validado e sem duplicação.
- Migration e snapshot gerados pela CLI Medusa em `apps/backend/src/modules/cart-merge/migrations/`, ainda não aplicados.
- `docs/DB_MODEL_v1.22.md` atualizado com `CustomerCartAuthority`, `CartMergeResult`, `CartReview`, enums e constraints aprováveis no checkpoint serial 16-04.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route/request scope → module service | O handler não pode contornar DI nem resolver um token divergente. |
| Medusa models → generated DDL | O writer de migration só roda depois de config/build válidos e não aplica banco. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-03-01 | Elevation of Privilege | route/service wiring | high | mitigate | container exact token, concrete default service e no-function-bypass unit. |
| T-16-03-02 | Tampering | module registration | high | mitigate | exact config entry, config unit e build antes da CLI. |
| T-16-03-03 | Tampering | generated schema | high | mitigate | CLI-only output, exact SHA-256 identity e blocking DDL checkpoint. |
| T-16-03-04 | Information Disclosure | models/docs | high | mitigate | safe field allowlists e ausência de capability/JWT/raw key/PII/provider. |
| T-16-03-SC | Tampering | package supply chain | low | accept | nenhuma instalação; CLI Medusa já fixada no workspace. |
</threat_model>

<verification>Config unit, build e existência/identidade do output CLI local; nenhuma migration é aplicada.</verification>
<success_criteria>O módulo Medusa compila e a rota só alcança o motor pelo container; o DDL local fica pronto e identificado para decisão humana.</success_criteria>
<output>Create `.planning/phases/16-cart-merge-review/16-03-SUMMARY.md` and stop at the DDL-review boundary. Do not auto-start 16-04.</output>
