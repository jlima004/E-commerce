---
phase: 16-cart-merge-review
plan: 10
type: execute
wave: 9
depends_on: [16-09]
files_modified:
  - apps/backend/src/api/store/customers/me/cart/attach/route.ts
  - apps/backend/src/modules/checkout/attach-guest-cart.ts
  - apps/backend/src/api/store/carts/merge-review-validators.ts
  - apps/backend/integration-tests/http/cart-merge-review.spec.ts
autonomous: true
requirements: [MRG-01, MRG-02, MRG-05, MRG-08]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Attach não mantém motor ou outcome próprio: quando elegível delega ao mesmo executeCartMerge, per D16-37 e D16-40."
    - "Attach exige BFF, publishable, Customer bearer, capability/replay, Idempotency-Key e guest If-Match; sessão legada isolada retorna erro estável sem efeito, per D16-38 e D16-39."
    - "Native attach continua DENY; adapter/replay retornam os mesmos outcomes/review/ETag e criam zero Order."
  artifacts:
    - path: apps/backend/src/api/store/customers/me/cart/attach/route.ts
      provides: "Fachada deprecated que delega ao merge e nega sessão"
  key_links:
    - from: apps/backend/src/api/store/customers/me/cart/attach/route.ts
      to: apps/backend/src/modules/cart-merge/service.ts
      via: "mesmo validator, authority, operation, service e serializer do merge"
      pattern: "executeCartMerge"
---

<objective>
Converter attach em adaptador controlado do motor único, sem fallback de sessão ou semântica paralela.

Purpose: preservar uma janela de migração explícita sem deixar sessão ou semântica antiga contornar merge/review.
Output: adapter paritário e denial estável para session-only; exact-set/leakage ficam no slice serial 16-11.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-cart-merge-review/16-CONTEXT.md
@.planning/phases/16-cart-merge-review/16-RESEARCH.md
@.planning/phases/16-cart-merge-review/16-RESEARCH-REVIEW.md
@.planning/phases/16-cart-merge-review/16-VALIDATION.md
@.planning/phases/16-cart-merge-review/16-PATTERNS.md
@.planning/phases/16-cart-merge-review/16-09-SUMMARY.md
@apps/backend/src/api/store/customers/me/cart/attach/route.ts
@apps/backend/src/modules/checkout/attach-guest-cart.ts
@apps/backend/src/api/store-surface/manifest.ts
@apps/backend/src/api-docs/coverage/exclusions.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 16-10-01: Substituir attach por delegação semântica única</name>
  <files>apps/backend/src/api/store/customers/me/cart/attach/route.ts, apps/backend/src/modules/checkout/attach-guest-cart.ts, apps/backend/src/api/store/carts/merge-review-validators.ts, apps/backend/integration-tests/http/cart-merge-review.spec.ts</files>
  <read_first>16-CONTEXT.md D16-37..D16-42; 16-PATTERNS.md seção 5 e anti-pattern attach; merge route/validator/service/serializer implementados; attach route/helper atuais; HTTP Phase 16 suite</read_first>
  <behavior>
    - "Request completo no attach -> resposta/effects byte-semanticamente equivalentes ao endpoint merge para outcome, cart, review e ETag."
    - "Session-only ou ausência de capability/replay/Idempotency-Key/If-Match -> stable deprecation/migration error e zero write."
    - "Mesmo key/fingerprint committed via attach ou merge usa o mesmo receipt; não existe operação idempotente paralela."
  </behavior>
  <action>Reduzir a rota attach a um shell deprecated que reutiliza exatamente o parser/body `{guestCartId}`, required headers, dual authority, `CART_MERGE`, `executeCartMerge` e serializers da rota canônica. Remover `buildAttachGuestCartDecision`, session active_cart_id, timestamp selection, transfer/update/supersede separados e legacy outcome mapping como autoridade de execução; o helper histórico pode conservar somente tipos/deprecation mapping não mutável se ainda houver imports, sem motor próprio. Session-only retorna código estável de migração/depreciação com zero capability touch, claim, cart/version/review/Order effect. D16-37, D16-38, D16-39, D16-40, D16-41 e D16-42 são postconditions.</action>
  <verify>
    <automated>cd apps/backend && npm run test:integration:http -- --runTestsByPath integration-tests/http/cart-merge-review.spec.ts</automated>
  </verify>
  <acceptance_criteria>Adapter e merge compartilham service/operation/validator/serializer; parity cases e session-only denial passam; nenhum legacy outcome ou second mutation engine é executável.</acceptance_criteria>
  <done>Attach existe apenas como facade deprecated segura e não como bypass.</done>
</task>

</tasks>

## Constraints and planning gates

- HUMAN PLAN REVIEW REQUIRED
- Phase 16 EXECUTION NOT AUTHORIZED

## Artifacts this phase produces

- Attach adapter que chama `executeCartMerge` e stable deprecation/migration error para sessão.
- Adapter/session parity tests; exact-set, native denial, leakage e zero Order seguem no 16-11.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy client/session → attach adapter | Compatibilidade não pode reintroduzir posse por sessão. |
| runtime route inventory → M1 allowlist | Apenas method/path exatos autorizados podem chegar ao handler. |
| secret-bearing request → observability/persistence | Capability/JWT/raw key não podem atravessar sinks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-10-01 | Spoofing | session-only attach | high | mitigate | same dual-authority validator/service or stable zero-effect denial. |
| T-16-10-02 | Tampering | second attach engine | high | mitigate | one CART_MERGE operation/service and parity assertions. |
| T-16-10-03 | Information Disclosure | adapter error/response | high | mitigate | shared serializer e stable deprecation error; eight-sink proof follows in 16-11. |
| T-16-10-SC | Tampering | package supply chain | low | accept | nenhuma instalação ocorre. |
</threat_model>

<verification>Adapter parity/session-only denial no HTTP Phase 16.</verification>
<success_criteria>Attach permanece controlado e deprecated sem sessão nem motor próprio; os gates de superfície/leakage seguem serialmente.</success_criteria>
<output>Create `.planning/phases/16-cart-merge-review/16-10-SUMMARY.md` when done and stop. Do not auto-start 16-11.</output>
