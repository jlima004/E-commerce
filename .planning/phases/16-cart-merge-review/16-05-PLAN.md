---
phase: 16-cart-merge-review
plan: 05
type: execute
wave: 4
depends_on: [16-04]
files_modified:
  - apps/backend/integration-tests/modules/cart-merge-review.postgres.spec.ts
  - apps/backend/integration-tests/helpers/cart-merge-postgres.ts
  - apps/backend/src/modules/store-idempotency/service.ts
  - apps/backend/src/modules/guest-cart-capability/service.ts
  - apps/backend/src/modules/guest-cart-capability/types.ts
  - apps/backend/src/modules/cart-merge/service.ts
autonomous: true
requirements: [MRG-01, MRG-03, MRG-05, MRG-06]
estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A migration aprovada aplica e reverte somente em PostgreSQL descartável; collision/backfill ambíguo aborta sem escolher cart, per D16-06."
    - "CART_MERGE claim, CartMergeResult, CartReview, cart writes, versions, guest lifecycle e terminal state compartilham um transaction manager, per D16-29, D16-34 e D16-36."
    - "O fingerprint contém operação, Customer, guest, destino/null, guest/source version, destino server-side version e intenção agregada, sem capability plaintext, per D16-27, D16-28, D16-30 e R16-HR-08."
    - "Same-key/same-fingerprint COMMITTED após capability consumed retorna outcome, public cart snapshot, review e ETag originais sem novo efeito, per D16-31, D16-33 e R16-HR-04."
    - "NO_ITEMS pode confirmar receipt mas altera zero carts, versions, review e capability, per D16-05, D16-12 e D16-18."
    - "Retenção do CartMergeResult acompanha StoreIdempotency e CartReview terminal vive pelo menos pelo mesmo período, per R16-HR-06."
    - "Models, snapshot e migration aprovados no 16-04 permanecem byte-identical; qualquer delta estrutural retorna ao 16-03 e repete o 16-04 antes de prosseguir."
  artifacts:
    - path: apps/backend/src/modules/store-idempotency/service.ts
      provides: "Claim/load/complete/fail transaction-aware com result binding"
    - path: apps/backend/src/modules/guest-cart-capability/service.ts
      provides: "Terminal replay lookup hash-only e consume no sharedContext"
    - path: apps/backend/src/modules/cart-merge/service.ts
      provides: "Receipt/replay/fingerprint e fronteira de commit Phase 16"
    - path: apps/backend/integration-tests/modules/cart-merge-review.postgres.spec.ts
      provides: "Constraints, failpoints, replay e retenção em PostgreSQL real"
  key_links:
    - from: apps/backend/src/modules/store-idempotency/service.ts
      to: apps/backend/src/modules/cart-merge/models/cart-merge-result.ts
      via: "terminal COMMITTED aponta result_id 1:1"
      pattern: "result_id|idempotency_record_id"
    - from: apps/backend/src/modules/guest-cart-capability/service.ts
      to: apps/backend/src/modules/cart-merge/service.ts
      via: "consumed capability só é aceita pelo terminal replay lookup após bindings completos"
      pattern: "terminal|replay|touch: false"
    - from: apps/backend/src/modules/cart-merge/service.ts
      to: apps/backend/src/modules/store-resource-version/service.ts
      via: "guest If-Match e destination version FOR UPDATE no fingerprint"
      pattern: "guestVersion|customerVersion"
---

<objective>
Transformar o tracer em uma unidade durável de claim, receipt, replay e rollback, provada em PostgreSQL descartável.

Purpose: eliminar a janela entre cart commit e idempotency/capability terminalization, preservando o resultado público original para retries pós-consumo.
Output: migration local validada, APIs shared-context de idempotência/capability, replay imutável e failpoint ledger.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-cart-merge-review/16-CONTEXT.md
@.planning/phases/16-cart-merge-review/16-RESEARCH.md
@.planning/phases/16-cart-merge-review/16-RESEARCH-REVIEW.md
@.planning/phases/16-cart-merge-review/16-PATTERNS.md
@.planning/phases/16-cart-merge-review/16-04-SUMMARY.md
@apps/backend/src/modules/store-idempotency/service.ts
@apps/backend/src/modules/store-idempotency/models/store-idempotency-record.ts
@apps/backend/src/modules/guest-cart-capability/service.ts
@apps/backend/src/modules/store-resource-version/service.ts
@apps/backend/src/api/store/carts/line-item-mutation.ts
@apps/backend/src/modules/store-resource-version/__tests__/store-resource-version.postgres.spec.ts
@apps/backend/integration-tests/helpers/guest-cart-postgres.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 16-05-01: Aplicar migration congelada somente no PostgreSQL descartável</name>
  <precondition>16-04-SUMMARY.md registra literalmente approve-ddl e seu bloco SHA-256 confere com models/snapshot/migration atuais.</precondition>
  <files>apps/backend/integration-tests/modules/cart-merge-review.postgres.spec.ts, apps/backend/integration-tests/helpers/cart-merge-postgres.ts</files>
  <read_first>16-04-SUMMARY.md decisão/identidade SHA-256 aprovada; migration/snapshot congelados somente para leitura; apps/backend/scripts/run-disposable-postgres-tests.mjs; apps/backend/integration-tests/helpers/guest-cart-postgres.ts; apps/backend/src/modules/store-resource-version/__tests__/store-resource-version.postgres.spec.ts; 16-RESEARCH.md Runtime State Inventory e rollback proof</read_first>
  <behavior>
    - "Migration fresh aplica as três tabelas e constraints no database temporário e cleanup remove somente esse database."
    - "Fixture histórica com >1 Customer cart candidato faz o audit/backfill falhar sanitizado e não cria authority row."
    - "Unique active Customer/cart, result 1:1 e pending review única falham no PostgreSQL, não apenas em TypeScript."
  </behavior>
  <action>Antes de abrir o runner, extrair o bloco SHA-256 aprovado do 16-04-SUMMARY, recomputar o exact-set atual e exigir igualdade; salvar o exact-set validado apenas em arquivo temporário da task. Validar que o alvo fornecido pelo runner é loopback/temporário. Usar o runner Medusa disposable existente para aplicar a migration sem editar models, snapshot ou migration e inspecionar tables, columns, CHECKs, unique/partial unique e indexes. Executar collision audit antes de materializar CustomerCartAuthority; quando houver múltiplos candidatos, retornar relatório sanitizado e abortar sem escolher por `updated_at`. Exercitar apply fresh e cleanup. Ao final, recomputar e comparar novamente com 16-04-SUMMARY. Qualquer delta, inclusive aparentemente comportamental, para model/snapshot/migration para BLOCKED e exige retorno ao 16-03 seguido de repetição do checkpoint 16-04. Não aceitar `DATABASE_URL` remoto, não usar `supabase db push` e não aplicar em ambiente persistente.</action>
  <verify>
    <automated>approved=$(mktemp) && current=$(mktemp) && trap 'rm -f "$approved" "$current"' EXIT && sed -n '/^SCHEMA_IDENTITY_SHA256_BEGIN$/,/^SCHEMA_IDENTITY_SHA256_END$/p' .planning/phases/16-cart-merge-review/16-04-SUMMARY.md | sed '1d;$d' | sort > "$approved" && { sha256sum apps/backend/src/modules/cart-merge/models/customer-cart-authority.ts apps/backend/src/modules/cart-merge/models/cart-merge-result.ts apps/backend/src/modules/cart-merge/models/cart-review.ts; find apps/backend/src/modules/cart-merge/migrations -maxdepth 1 -type f -print0 | sort -z | xargs -0 sha256sum; } | sort > "$current" && test -s "$approved" && diff -u "$approved" "$current" && cd apps/backend && node scripts/run-disposable-postgres-tests.mjs -- npm run test:integration:modules -- --runTestsByPath integration-tests/modules/cart-merge-review.postgres.spec.ts</automated>
  </verify>
  <acceptance_criteria>PostgreSQL real prova três tabelas/constraints e collision abort; migration identity é a aprovada; cleanup loopback passa; nenhum alvo remoto foi aberto ou alterado.</acceptance_criteria>
  <done>Schema Phase 16 está provado em banco descartável e continua não aplicado remotamente.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 16-05-02: Fechar claim, terminal replay e retenção no mesmo manager sem schema drift</name>
  <files>apps/backend/src/modules/store-idempotency/service.ts, apps/backend/src/modules/guest-cart-capability/service.ts, apps/backend/src/modules/guest-cart-capability/types.ts</files>
  <read_first>apps/backend/src/modules/store-idempotency/service.ts canonicalize/hash/claim/complete/lifecycle; store-idempotency model e operations; apps/backend/src/modules/guest-cart-capability/service.ts authorize/consume/locks; cart-merge result model; 16-CONTEXT.md D16-27..D16-36; 16-RESEARCH-REVIEW.md R16-HR-04, R16-HR-06 e R16-HR-08; 16-PATTERNS.md seções 3 e 4</read_first>
  <behavior>
    - "Same key+fingerprint+bindings COMMITTED retorna result original mesmo com capability consumed e não toca TTL."
    - "Same key com guest/destination/version/intent/Customer divergente retorna IDEMPOTENCY_KEY_REUSE_CONFLICT e zero write."
    - "Different key nunca usa o terminal lookup consumed como nova autorização."
    - "Result expiry é igual ao terminal StoreIdempotency associado; review acknowledged não expira antes do receipt reconhecível."
  </behavior>
  <action>Consolidar APIs `claimStoreIdempotency`, `loadCommittedStoreIdempotencyResult`, `completeStoreIdempotency`, `failStoreIdempotency` para receber `sharedContext` e nunca abrir manager paralelo quando fornecido. Usar o binding 1:1 já aprovado em `CartMergeResult.idempotency_record_id` e a metadata escalar allowlisted existente; não alterar nenhum model, snapshot ou migration. Criar lookup da capability consumida específico de replay: hash constant-time, sem touch/renew/reactivate, disponível apenas depois de BFF+Customer e de key/scope/fingerprint/result bindings. Popular por APIs de service a projeção pública original e expiry nos campos já aprovados, sem mudar o shape do model. A fingerprint canônica inclui `CART_MERGE`, customerId, guestCartId, customerCartId/null, guestVersion, customerVersion/null e normalizedGuestIntent explicitamente ordenada; raw capability/raw key/JWT ficam fora. Revalidar os hashes aprovados ao concluir; qualquer necessidade estrutural retorna ao 16-03 e repete 16-04. D16-27, D16-28, D16-29, D16-30, D16-31, D16-32, D16-33, D16-34, D16-35, D16-36, R16-HR-04, R16-HR-06 e R16-HR-08 são postconditions.</action>
  <verify>
    <automated>approved=$(mktemp) && current=$(mktemp) && trap 'rm -f "$approved" "$current"' EXIT && sed -n '/^SCHEMA_IDENTITY_SHA256_BEGIN$/,/^SCHEMA_IDENTITY_SHA256_END$/p' .planning/phases/16-cart-merge-review/16-04-SUMMARY.md | sed '1d;$d' | sort > "$approved" && { sha256sum apps/backend/src/modules/cart-merge/models/customer-cart-authority.ts apps/backend/src/modules/cart-merge/models/cart-merge-result.ts apps/backend/src/modules/cart-merge/models/cart-review.ts; find apps/backend/src/modules/cart-merge/migrations -maxdepth 1 -type f -print0 | sort -z | xargs -0 sha256sum; } | sort > "$current" && test -s "$approved" && diff -u "$approved" "$current" && cd apps/backend && npm run test:unit -- --runTestsByPath src/modules/store-idempotency/__tests__/store-idempotency.unit.spec.ts src/modules/guest-cart-capability/__tests__/guest-cart-capability-service.unit.spec.ts src/modules/cart-merge/__tests__/decision.unit.spec.ts</automated>
  </verify>
  <acceptance_criteria>Unit tests distinguem first/replay/conflict/in-progress; consumed só replaya bindings idênticos; fingerprint exact object/array order é estável; retention timestamps coordenam; source/fixture scans não contêm raw secrets.</acceptance_criteria>
  <done>Idempotência e capability suportam replay pós-consumo sem conceder nova mutação.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 16-05-03: Persistir receipt/terminal state e provar failpoints</name>
  <files>apps/backend/src/modules/cart-merge/service.ts, apps/backend/integration-tests/modules/cart-merge-review.postgres.spec.ts, apps/backend/integration-tests/helpers/cart-merge-postgres.ts</files>
  <read_first>cart-merge service do tracer; APIs sharedContext alteradas nesta plan; apps/backend/src/api/store/carts/line-item-mutation.ts snapshot/transaction boundary; 16-RESEARCH.md Provas Obrigatórias 3 e 4; 16-RESEARCH-REVIEW.md R16-HR-04/R16-HR-06; existing PostgreSQL failpoint helpers</read_first>
  <behavior>
    - "Falha após cada write relevante reverte cart/lines/version/result/review/capability/guest/idempotency e Orders."
    - "Committed replay devolve bytes semânticos do original cart/review/outcome e original ETag depois de ACK ou cart change fixture, sem refetch atual."
    - "NO_ITEMS commit cria receipt original, mantém capability ACTIVE e zero version bumps."
  </behavior>
  <action>Atualizar o método `CartMergeModuleService.executeCartMerge` para criar CartMergeResult pelos campos já aprovados antes de terminalizar idempotência e gravar a projeção original capturada dentro da transação. No replay, ler exclusivamente o result associado e serializar original outcome/cart/review/ETag; não consultar o cart atual para compor a resposta. Para NO_ITEMS, confirmar receipt com snapshot original da tentativa e manter cart/version/review/capability inalterados. Adicionar failpoints após result, review quando aplicável, version, association/supersede, capability consume e completion; cada ponto lança antes do commit. A suite PostgreSQL compara baseline completo, txid/manager, terminal row count, capability state e Orders real. Revalidar ao final o SHA-256 exact-set aprovado; não tocar model/snapshot/migration. Technical failures permanecem errors/rollback e não viram rejected reason.</action>
  <verify>
    <automated>approved=$(mktemp) && current=$(mktemp) && trap 'rm -f "$approved" "$current"' EXIT && sed -n '/^SCHEMA_IDENTITY_SHA256_BEGIN$/,/^SCHEMA_IDENTITY_SHA256_END$/p' .planning/phases/16-cart-merge-review/16-04-SUMMARY.md | sed '1d;$d' | sort > "$approved" && { sha256sum apps/backend/src/modules/cart-merge/models/customer-cart-authority.ts apps/backend/src/modules/cart-merge/models/cart-merge-result.ts apps/backend/src/modules/cart-merge/models/cart-review.ts; find apps/backend/src/modules/cart-merge/migrations -maxdepth 1 -type f -print0 | sort -z | xargs -0 sha256sum; } | sort > "$current" && test -s "$approved" && diff -u "$approved" "$current" && cd apps/backend && node scripts/run-disposable-postgres-tests.mjs -- npm run test:integration:modules -- --runTestsByPath integration-tests/modules/cart-merge-review.postgres.spec.ts</automated>
  </verify>
  <acceptance_criteria>Todos os failpoints deixam baseline idêntico; committed replay original não gera writes; NO_ITEMS altera somente receipt idempotente permitido; capability e Order invariants passam.</acceptance_criteria>
  <done>Receipt, replay e rollback Phase 16 têm evidência PostgreSQL real.</done>
</task>

</tasks>

## Constraints and planning gates

- HUMAN PLAN REVIEW REQUIRED
- Phase 16 EXECUTION NOT AUTHORIZED

## Artifacts this phase produces

- APIs shared-context `claimStoreIdempotency`, `loadCommittedStoreIdempotencyResult`, `completeStoreIdempotency`, `failStoreIdempotency` e binding pelo `CartMergeResult.idempotency_record_id` já aprovado.
- Guest capability terminal-replay input/result types e lookup hash-only sem touch.
- Uso comportamental dos campos CartMergeResult `original_public_cart_snapshot`, `original_review_snapshot`, `original_etag`, `expires_at` e safe bindings congelados no 16-04.
- `executeCartMerge` branches first execution, immutable committed replay, idempotency conflict e NO_ITEMS receipt.
- PostgreSQL failpoint/barrier fixtures para result, review, versions, association, capability e completion.
- Migration proof somente em PostgreSQL descartável.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| idempotency key/capability → terminal replay | Consumed bearer must never authorize a new mutation. |
| transaction manager → multiple modules | Every write must join the Cart-owned PostgreSQL transaction. |
| persisted receipt → replay response | Original projection must be safe, immutable and retention-coordinated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-05-01 | Spoofing | consumed capability replay | high | mitigate | Customer+BFF+key+scope+fingerprint+safe capability binding before terminal lookup. |
| T-16-05-02 | Tampering | partial commit | critical | mitigate | one manager, failpoint after every write and baseline/txid assertions. |
| T-16-05-03 | Repudiation | immutable receipt | high | mitigate | unique 1:1 result and original projection/ETag stored with COMMITTED. |
| T-16-05-04 | Information Disclosure | receipt persistence | high | mitigate | allowlisted snapshot and multi-sink canaries; no raw credentials/provider data. |
| T-16-05-05 | Denial of Service | stale processing claims | medium | mitigate | existing lease/lifecycle semantics retained with transaction-aware terminalization. |
| T-16-05-06 | Tampering | approved schema identity | high | mitigate | SHA-256 exact-set before and after every task; drift returns to 16-03/16-04. |
| T-16-05-SC | Tampering | package supply chain | low | accept | nenhuma instalação; Medusa/Jest já fixados. |
</threat_model>

<verification>Migration disposable, focused units e PostgreSQL failpoint/replay ledger.</verification>
<success_criteria>Merge receipt e idempotência confirmam atomicamente com carts/capability e reproduzem a resposta original sem double effect.</success_criteria>
<output>Create `.planning/phases/16-cart-merge-review/16-05-SUMMARY.md` when done, including the unchanged approved schema identity, and stop. No remote migration; do not auto-start 16-06.</output>
