---
phase: 16-cart-merge-review
plan: 04
type: execute
wave: 3
depends_on: [16-03]
files_modified:
  - apps/backend/src/modules/cart-merge/migrations/
  - apps/backend/src/modules/cart-merge/models/customer-cart-authority.ts
  - apps/backend/src/modules/cart-merge/models/cart-merge-result.ts
  - apps/backend/src/modules/cart-merge/models/cart-review.ts
  - docs/DB_MODEL_v1.22.md
autonomous: false
requirements: [MRG-05, MRG-06]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Nenhuma migration Phase 16 é aplicada antes de uma revisão humana do DDL, snapshot, constraints, backfill e alvo descartável."
    - "A revisão confirma que CustomerCartAuthority não escolhe canonicalidade por updated_at e que colisões >1 falham fechado, per D16-06."
    - "A revisão confirma receipt original imutável, review pending/acknowledged e retenção coordenada, per D16-19, D16-21, D16-32, D16-33, R16-HR-04 e R16-HR-06."
    - "O checkpoint não autoriza DSN remoto, deploy, execução dos planos seguintes nem alteração dos contadores."
  artifacts:
    - path: apps/backend/src/modules/cart-merge/migrations/
      provides: "DDL CLI inspecionado e aprovado ou devolvido para correção"
    - path: docs/DB_MODEL_v1.22.md
      provides: "Contrato de dados comparado ao DDL"
  key_links:
    - from: apps/backend/src/modules/cart-merge/migrations/
      to: apps/backend/src/modules/cart-merge/models/
      via: "model/snapshot/DDL exact-set humano"
      pattern: "customer_cart_authority|cart_merge_result|cart_review"
---

<objective>
Parar na one-way door da migration e obter decisão humana explícita sobre o DDL gerado antes de qualquer aplicação PostgreSQL.

Purpose: impedir que uma migration incompatível, backfill arbitrário ou retenção divergente se torne estado persistido.
Output: decisão `approve-ddl` ou `revise-ddl`, sem write de banco.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-cart-merge-review/16-CONTEXT.md
@.planning/phases/16-cart-merge-review/16-RESEARCH.md
@.planning/phases/16-cart-merge-review/16-RESEARCH-REVIEW.md
@.planning/phases/16-cart-merge-review/16-PATTERNS.md
@.planning/phases/16-cart-merge-review/16-03-SUMMARY.md
@docs/DB_MODEL_v1.22.md
@apps/backend/src/modules/cart-merge/models/customer-cart-authority.ts
@apps/backend/src/modules/cart-merge/models/cart-merge-result.ts
@apps/backend/src/modules/cart-merge/models/cart-review.ts
@apps/backend/src/modules/cart-merge/migrations/
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 16-04-01: Aprovar e congelar DDL, constraints, collision policy e retenção</name>
  <reversibility rating="one-way">Depois de aplicada e consumida por receipts/reviews, desfazer a forma das tabelas exige migration de dados e pode invalidar replay publicado.</reversibility>
  <files>apps/backend/src/modules/cart-merge/migrations/, apps/backend/src/modules/cart-merge/models/customer-cart-authority.ts, apps/backend/src/modules/cart-merge/models/cart-merge-result.ts, apps/backend/src/modules/cart-merge/models/cart-review.ts, docs/DB_MODEL_v1.22.md</files>
  <read_first>16-03-SUMMARY.md com a identidade SHA-256 exact-set; os três models; migration e snapshot gerados; docs/DB_MODEL_v1.22.md; 16-RESEARCH.md Persistência Recomendada; 16-RESEARCH-REVIEW.md R16-HR-03, R16-HR-04 e R16-HR-06; 16-PATTERNS.md seção 1</read_first>
  <decision>Autorizar ou devolver para correção o DDL Phase 16 antes da aplicação em PostgreSQL descartável.</decision>
  <context>O DDL fixa authority Customer, receipt original e review versionada. A decisão não autoriza aplicação remota nem execução automática do plano seguinte.</context>
  <options>
    <option id="approve-ddl">
      <name>Aprovar DDL</name>
      <pros>Permite aplicar somente no PostgreSQL descartável e executar provas de constraints/rollback.</pros>
      <cons>Qualquer mudança posterior de shape exige migration coordenada.</cons>
    </option>
    <option id="revise-ddl">
      <name>Revisar DDL</name>
      <pros>Corrige model/snapshot/migration antes de existir estado persistido.</pros>
      <cons>Bloqueia 16-04 até novo diff humano.</cons>
    </option>
  </options>
  <action>Validar que o bloco `SCHEMA_IDENTITY_SHA256_BEGIN`/`END` do 16-03-SUMMARY corresponde byte a byte ao exact-set atual de `models/customer-cart-authority.ts`, `models/cart-merge-result.ts`, `models/cart-review.ts` e todos os arquivos migration/snapshot. Apresentar diff sanitizado de model, snapshot, migration e DB_MODEL. Conferir: unique ativo por Customer/cart; backfill audit que aborta em múltiplos candidatos sem escolher timestamp; CartMergeResult 1:1 com idempotência e snapshot/review/ETag originais; CartReview pending única, status pending|acknowledged e versão produzida; índices de lookup/retention; nenhuma capability/JWT/raw key/PII/provider; nenhuma FK direta em tabela core de outro módulo. Mostrar o comando planejado para PostgreSQL descartável e provar que nenhum migrate foi executado. Em `approve-ddl`, copiar para 16-04-SUMMARY o mesmo bloco SHA-256 aprovado, sem recalcular identidades divergentes; a partir desse marker, models/snapshot/migration ficam congelados. Qualquer delta estrutural posterior deve parar BLOCKED, retornar obrigatoriamente ao 16-03 e repetir este checkpoint 16-04. Registrar exatamente `approve-ddl` ou `revise-ddl`; `revise-ddl` retorna ao 16-03 e bloqueia sequência.</action>
  <verify>
    <automated>approved=$(mktemp) && current=$(mktemp) && trap 'rm -f "$approved" "$current"' EXIT && sed -n '/^SCHEMA_IDENTITY_SHA256_BEGIN$/,/^SCHEMA_IDENTITY_SHA256_END$/p' .planning/phases/16-cart-merge-review/16-03-SUMMARY.md | sed '1d;$d' | sort > "$approved" && { sha256sum apps/backend/src/modules/cart-merge/models/customer-cart-authority.ts apps/backend/src/modules/cart-merge/models/cart-merge-result.ts apps/backend/src/modules/cart-merge/models/cart-review.ts; find apps/backend/src/modules/cart-merge/migrations -maxdepth 1 -type f -print0 | sort -z | xargs -0 sha256sum; } | sort > "$current" && test -s "$approved" && diff -u "$approved" "$current" && git diff --check -- apps/backend/src/modules/cart-merge docs/DB_MODEL_v1.22.md</automated>
  </verify>
  <acceptance_criteria>A decisão humana nomeia approve-ddl ou revise-ddl; approve-ddl confirma constraints, zero seleção por timestamp, safe fields, snapshot original e alvo somente descartável; revise-ddl bloqueia sem aplicação.</acceptance_criteria>
  <done>One-way door do DDL foi decidida explicitamente, a identidade foi congelada e 16-05 só pode prosseguir com approve-ddl.</done>
  <resume-signal>Select: approve-ddl or revise-ddl.</resume-signal>
</task>

</tasks>

## Constraints and planning gates

- HUMAN PLAN REVIEW REQUIRED
- Phase 16 EXECUTION NOT AUTHORIZED

## Artifacts this phase produces

- Decisão humana `approve-ddl` ou `revise-ddl` vinculada à identidade da migration gerada.
- Checklist factual de `CustomerCartAuthority`, `CartMergeResult`, `CartReview`, indexes, CHECKs, backfill e safe-field set.
- Nenhum novo symbol/runtime path; este plano não altera banco nem contrato HTTP.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated DDL → persistent database | A migration revisável se torna estado durável somente depois da decisão humana. |
| historical carts → authority backfill | Ambiguidade não pode ser resolvida por timestamp. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-04-01 | Tampering | generated migration | high | mitigate | checkpoint blocking com model/snapshot/DDL exact-set antes de apply. |
| T-16-04-02 | Elevation of Privilege | authority backfill | high | mitigate | collision audit fail-closed; nenhuma escolha por updated_at. |
| T-16-04-03 | Information Disclosure | result/review columns | high | mitigate | revisão de allowlist e ausência de secrets/PII/provider fields. |
| T-16-04-SC | Tampering | package supply chain | low | accept | nenhuma instalação ou package-manager write ocorre. |
</threat_model>

<verification>Diff check e existência de migration; a prova decisiva é a escolha humana explícita, não um teste de runtime.</verification>
<success_criteria>DDL recebe decisão humana antes de aplicação, mantendo a autoridade remota e os gates posteriores fechados.</success_criteria>
<output>Create `.planning/phases/16-cart-merge-review/16-04-SUMMARY.md` recording the decision and approved SHA-256 exact-set, then stop. Do not auto-start 16-05.</output>
