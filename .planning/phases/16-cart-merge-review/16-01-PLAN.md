---
phase: 16-cart-merge-review
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/backend/src/modules/store-idempotency/operations.ts
  - apps/backend/src/modules/store-idempotency/service.ts
  - apps/backend/src/modules/cart-merge/service.ts
  - apps/backend/src/api/store/customers/me/cart/merge/route.ts
  - apps/backend/integration-tests/http/cart-merge-review.spec.ts
  - apps/backend/src/modules/cart-merge/__tests__/decision.unit.spec.ts
  - apps/backend/integration-tests/modules/cart-merge-review.postgres.spec.ts
  - apps/backend/integration-tests/helpers/cart-merge-postgres.ts
  - apps/backend/integration-tests/modules/guest-cart-order-invariants.postgres.spec.ts
autonomous: true
requirements: [MRG-01, MRG-02, MRG-03, MRG-05]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "O primeiro slice real aceita um Customer+BFF+capability, promove o próprio guest quando não existe destino e retorna GUEST_CART_ATTACHED, per D16-04."
    - "O If-Match do merge referencia exclusivamente a versão do guest source e a versão de destino é server-authoritative, per D16-20, D16-27, D16-28 e R16-HR-08."
    - "Claim CART_MERGE, associação do cart, bump, consumo da capability e terminalização confirmam no mesmo transaction manager ou revertem juntos, per D16-29, D16-30, D16-31, D16-34 e D16-36."
    - "O tracer cria zero Orders e preserva o webhook payment_intent.succeeded como única autoridade de nascimento de Order."
    - "Os três arquivos Wave 0 existem com testes executáveis para o slice entregue; nenhum substitui PostgreSQL real por Redis, memória ou fixture Order."
    - "O commit base anterior à primeira mudança de execução Phase 16 é validado e registrado uma única vez como PHASE16_BASE_COMMIT para o range gate final."
  artifacts:
    - path: apps/backend/src/modules/cart-merge/service.ts
      provides: "Default CartMergeModuleService com executeCartMerge como único método público do motor"
      exports: [CartMergeModuleService]
    - path: apps/backend/src/api/store/customers/me/cart/merge/route.ts
      provides: "POST canônico autenticado de merge"
      exports: [POST]
    - path: apps/backend/integration-tests/http/cart-merge-review.spec.ts
      provides: "Prova HTTP do tracer e arquivo Wave 0 da Phase 16"
    - path: apps/backend/integration-tests/modules/cart-merge-review.postgres.spec.ts
      provides: "Prova PostgreSQL do commit único e rollback do tracer"
  key_links:
    - from: apps/backend/src/api/store/customers/me/cart/merge/route.ts
      to: apps/backend/src/modules/cart-merge/service.ts
      via: "POST resolve CART_MERGE_MODULE no container e chama CartMergeModuleService.executeCartMerge uma única vez"
      pattern: "scope.resolve.*CART_MERGE_MODULE|executeCartMerge"
    - from: apps/backend/src/modules/cart-merge/service.ts
      to: apps/backend/src/modules/store-idempotency/service.ts
      via: "CART_MERGE claim/complete recebe sharedContext do Cart"
      pattern: "CART_MERGE|sharedContext"
    - from: apps/backend/src/modules/cart-merge/service.ts
      to: apps/backend/src/modules/guest-cart-capability/service.ts
      via: "preflight touch:false e consume no transaction manager comum"
      pattern: "touch: false|consumeGuestCartCapability"
---

<objective>
Provar a arquitetura Phase 16 de ponta a ponta com um tracer de produção: o caso autorizado sem Customer cart, com intenção guest integralmente preservada, promove o próprio guest e confirma `GUEST_CART_ATTACHED` em uma única transação.

Purpose: detectar cedo qualquer incompatibilidade entre rota BFF/Customer/capability, transaction manager Medusa, idempotência, versão guest e autoridade real de Order.
Output: endpoint canônico fino, operação `CART_MERGE`, serviço transacional inicial e a fundação executável das três suites definidas em 16-VALIDATION.md.

Spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip). O init devolveu `phase_req_ids=null`, mas MRG-01..MRG-08 continuam sendo a autoridade vinculante e permanecem OPEN/UNCHANGED.
</objective>

<execution_context>
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/workflows/execute-plan.md
@/home/jlima/Projetos/ecommerce/Backend/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-cart-merge-review/16-CONTEXT.md
@.planning/phases/16-cart-merge-review/16-RESEARCH.md
@.planning/phases/16-cart-merge-review/16-RESEARCH-REVIEW.md
@.planning/phases/16-cart-merge-review/16-VALIDATION.md
@.planning/phases/16-cart-merge-review/16-PATTERNS.md
@apps/backend/src/api/store/customers/me/cart/attach/route.ts
@apps/backend/src/api/store/carts/line-item-mutation.ts
@apps/backend/src/modules/store-idempotency/operations.ts
@apps/backend/src/modules/store-idempotency/service.ts
@apps/backend/src/modules/guest-cart-capability/service.ts
@apps/backend/src/modules/store-resource-version/service.ts
@apps/backend/integration-tests/modules/guest-cart-order-invariants.postgres.spec.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 16-01-01: Tracer GUEST_CART_ATTACHED sob um commit PostgreSQL</name>
  <files>apps/backend/src/modules/store-idempotency/operations.ts, apps/backend/src/modules/store-idempotency/service.ts, apps/backend/src/modules/cart-merge/service.ts, apps/backend/src/api/store/customers/me/cart/merge/route.ts, apps/backend/integration-tests/http/cart-merge-review.spec.ts</files>
  <read_first>apps/backend/src/api/store/customers/me/cart/attach/route.ts para o shell HTTP e os blocos legados proibidos; apps/backend/src/api/store/carts/line-item-mutation.ts para transaction.call, sharedContext, lock, CAS e snapshot; apps/backend/src/modules/store-idempotency/operations.ts e service.ts para operação/fingerprint/claim; apps/backend/src/modules/guest-cart-capability/service.ts e types.ts para preflight touch:false, ordem cart-lock-before-capability e consume; apps/backend/src/modules/store-resource-version/service.ts para load/CAS; apps/backend/src/modules/checkout/active-cart.ts para associação/superseding; 16-CONTEXT.md D16-04, D16-20, D16-27..D16-36; 16-RESEARCH-REVIEW.md R16-HR-08; 16-PATTERNS.md seções 2–5 e 9</read_first>
  <behavior>
    - "Customer+BFF válidos, capability ACTIVE, guest vN com itens integralmente aceitos e zero Customer carts utilizáveis -> 200 GUEST_CART_ATTACHED, o mesmo cart associado ao Customer, ETag N+1, capability consumed e Orders=0."
    - "If-Match stale do guest -> 412 CART_VERSION_MISMATCH, snapshot/ETag coerentes e zero write."
    - "Falha injetada antes do commit -> associação, versão, capability e idempotência retornam ao baseline."
  </behavior>
  <action>Antes da primeira mudança de execução, resolver `BASE=$(git rev-parse HEAD)`, exigir formato lowercase de 40 hex, provar `git cat-file -e "${BASE}^{commit}"` e conservar esse valor sem recalcular para a linha única `PHASE16_BASE_COMMIT=<BASE>` do 16-01-SUMMARY; não inferir fallback. Primeiro escrever o caso HTTP do happy path e fazê-lo falhar. Adicionar `CART_MERGE` ao vocabulário fechado de operações e tornar os métodos de claim/terminalização usados pelo tracer capazes de receber o `sharedContext` existente, sem abrir transação própria quando o manager é fornecido. Criar `CartMergeModuleService` como classe concreta exportada por default em `service.ts`, com `executeCartMerge` como único método público do motor. A rota nunca importa/chama uma função solta: resolve o token estável `cart_merge` por `req.scope.resolve`, tipa o resultado como `CartMergeModuleService` e chama o método uma única vez; o Plan 16-03 materializa o `Module`/config para esse mesmo token antes do build/config gate. O método exige BFF já autorizado, Customer JWT resolvido, `guestCartId`, capability, `Idempotency-Key` e `If-Match`; trata o body como binding e nunca como autoridade do destino. Sob `cartModule.transaction`, adquirir advisory lock de Customer, confirmar zero destinos utilizáveis, travar o guest e sua StoreResourceVersion, reautorizar capability sem touch prévio, recomputar fingerprint `{operation:CART_MERGE, customerId, guestCartId, customerCartId:null, guestVersion, customerVersion:null, normalizedGuestIntent}` ordenado por variantId, claimar, associar o mesmo guest ao Customer, invalidar estado dependente de cart, incrementar a versão exatamente uma vez, consumir capability e completar idempotência usando o mesmo manager. Capturar cart/versão antes de sair da transação e responder com serializer allowlisted existente, outcome `GUEST_CART_ATTACHED`, review false/null/[] e ETag do mesmo snapshot. Exceção técnica propaga rollback sanitizado; nenhuma ramificação chama workflow de Order, payment completion, Stripe ou Gelato. Implementar somente a ramificação de sucesso do tracer e erros fail-closed para estados que os planos seguintes expandem, sem criar semântica alternativa.</action>
  <verify>
    <automated>cd apps/backend && npm run test:integration:http -- --runTestsByPath integration-tests/http/cart-merge-review.spec.ts</automated>
  </verify>
  <acceptance_criteria>O teste HTTP executa a rota real através de `scope.resolve("cart_merge")` e do método `CartMergeModuleService.executeCartMerge`, observa exatamente um cart canônico promovido, uma versão avançada, capability terminal e zero Order; não existe export/chamada pública solta `executeCartMerge`; stale/rollback deixam todos os estados invariantes; o request não aceita Customer destino nem segundo ETag; o BASE original validado permanece disponível para a linha única do SUMMARY. D16-04, D16-20, D16-27, D16-28, D16-29, D16-30, D16-31, D16-34, D16-35, D16-36 e R16-HR-08 estão materializados no slice.</acceptance_criteria>
  <done>Um BFF autenticado consegue promover integralmente o guest pelo endpoint canônico com commit único, ETag coerente e zero Order.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 16-01-02: Materializar Wave 0 unitária, HTTP e PostgreSQL</name>
  <files>apps/backend/src/modules/cart-merge/__tests__/decision.unit.spec.ts, apps/backend/integration-tests/http/cart-merge-review.spec.ts, apps/backend/integration-tests/modules/cart-merge-review.postgres.spec.ts, apps/backend/integration-tests/helpers/cart-merge-postgres.ts, apps/backend/integration-tests/modules/guest-cart-order-invariants.postgres.spec.ts</files>
  <read_first>16-VALIDATION.md Wave 0 Requirements e Per-Task Verification Map; apps/backend/src/modules/checkout/__tests__/attach-guest-cart.unit.spec.ts para fixtures/equality sem copiar outcomes legados; apps/backend/integration-tests/helpers/guest-cart-postgres.ts e auth multiprocess helpers para DSN loopback, cleanup e child process; apps/backend/src/modules/store-resource-version/__tests__/store-resource-version.postgres.spec.ts para txid/rollback; apps/backend/integration-tests/modules/guest-cart-order-invariants.postgres.spec.ts para contagem Medusa/PostgreSQL real</read_first>
  <behavior>
    - "A suite unitária fixa o enum público com os cinco literais e cobre a decisão integral da promoção do tracer; não existe fixture positiva CUSTOMER_CART_PRESERVED per R16-HR-01."
    - "A suite PostgreSQL prova manager/txid comum, rollback do tracer, constraint/lock aplicáveis e zero Order real."
    - "O helper cria apenas PostgreSQL descartável/loopback, registra os recursos criados e limpa exatamente esses recursos."
  </behavior>
  <action>Criar os três arquivos requeridos por 16-VALIDATION.md com casos executáveis do tracer já entregue. Na unit, declarar o enum fechado e a promoção integral sem inventar branch positivo para o literal reservado. Na HTTP, manter segurança/headers/outcome/ETag e zero-effect negatives. Na suite module, usar o runner PostgreSQL descartável existente, capturar transaction IDs e failpoint antes do commit; estender a autoridade real de Order para contar rows Medusa antes/depois. Criar `cart-merge-postgres.ts` apenas para lifecycle loopback, fixtures sanitizadas, barrier/failpoint e cleanup registrado; Redis permanece ausente ou explicitamente não autoritativo. Não adicionar provider real, Order in-memory, mocks de persistência ou DSN remoto.</action>
  <verify>
    <automated>cd apps/backend && npm run test:unit -- --runTestsByPath src/modules/cart-merge/__tests__/decision.unit.spec.ts && node scripts/run-disposable-postgres-tests.mjs -- npm run test:integration:modules -- --runTestsByPath integration-tests/modules/cart-merge-review.postgres.spec.ts integration-tests/modules/guest-cart-order-invariants.postgres.spec.ts</automated>
  </verify>
  <acceptance_criteria>Os três paths Wave 0 existem e são descobertos pelo Jest; unit/HTTP/PG do tracer passam; PG demonstra txid comum, rollback, cleanup e Order delta zero; `CUSTOMER_CART_PRESERVED` aparece no enum e em nenhum caso positivo, per D16-07, D16-08 e R16-HR-01.</acceptance_criteria>
  <done>Wave 0 possui feedback unitário, HTTP e PostgreSQL real para o primeiro slice.</done>
</task>

</tasks>

## Constraints and planning gates

- Esta é uma technical draft. HUMAN PLAN REVIEW REQUIRED; Phase 16 EXECUTION NOT AUTHORIZED.
- `phase_req_ids=null` não reduz a cobertura: MRG-01..MRG-08 permanecem OPEN e serão distribuídos no conjunto.
- Nenhuma instalação de package, provider, DB/Redis remoto, OpenAPI writer ou deploy pertence a este plano.

## Artifacts this phase produces

- Path `apps/backend/src/modules/cart-merge/service.ts`.
- Default class `CartMergeModuleService` e método público `executeCartMerge` resolvido exclusivamente pelo token de container `cart_merge`; o Module/config correspondente é o slice serial 16-03.
- Enum member/operation `CART_MERGE` em StoreIdempotency.
- Shared-context aware claim/complete seam no `StoreIdempotencyModuleService`.
- Route export `POST` em `/store/customers/me/cart/merge`.
- Response fields `outcome`, `cart`, `review.requiresReview`, `review.reviewRef`, `review.rejectedItems`; header `ETag`.
- Paths Wave 0 `decision.unit.spec.ts`, `cart-merge-review.spec.ts`, `cart-merge-review.postgres.spec.ts` e helper `cart-merge-postgres.ts`.
- Casos reais de zero-Order para o tracer em `guest-cart-order-invariants.postgres.spec.ts`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BFF/Customer/capability → merge route | Quatro autoridades independentes cruzam a borda HTTP antes de qualquer mutação. |
| merge service → PostgreSQL | Cart, versão, capability e idempotência precisam compartilhar um commit. |
| response snapshot → BFF | Body e ETag devem vir do mesmo snapshot sem capability/JWT/raw key. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01-01 | Spoofing | merge route | high | mitigate | BFF exact operation + publishable authority + Customer bearer + capability binding antes do serviço. |
| T-16-01-02 | Tampering | transaction/idempotency | high | mitigate | sharedContext único, advisory/row locks, fingerprint server-authoritative e rollback PostgreSQL. |
| T-16-01-03 | Information Disclosure | response/error | high | mitigate | serializer allowlisted, no-store e canários sem secrets em body/headers/errors. |
| T-16-01-04 | Denial of Service | Customer/cart locks | medium | mitigate | ordem Customer→cart lexical→version→capability→idempotency e transação curta. |
| T-16-01-05 | Elevation of Privilege | Order authority | critical | mitigate | ausência de imports/workflows de completion + contagem real de Orders antes/depois. |
| T-16-01-SC | Tampering | package supply chain | low | accept | nenhuma instalação npm/pip/cargo ocorre; somente dependências já fixadas são usadas. |
</threat_model>

<verification>HTTP tracer, unit enum/decision, PostgreSQL transaction/rollback e Order real; todos os comandos usam o workspace local e não acessam infra remota.</verification>
<success_criteria>O menor caminho arquitetural de merge funciona ponta a ponta, com GUEST_CART_ATTACHED real, commit único, snapshot/ETag coerente, capability consumida somente no commit e zero Order.</success_criteria>
<output>Create `.planning/phases/16-cart-merge-review/16-01-SUMMARY.md` with exactly one validated `PHASE16_BASE_COMMIT=<40-hex-lowercase>` line, then stop. Do not auto-start 16-02.</output>
