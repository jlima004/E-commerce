---
id: 260802-o5a
quick_task: 260802-o5a
phase: quick-260802-o5a
plan: 01
type: execute
wave: 1
depends_on: []
status: ready
created: 2026-08-02
scope: pr-20-review-remediation
autonomous: true
branch: gsd/api-docs-wave-6-global-closure
reviewed_head: 1700ad822c8955a7c85ca084491a288446b4980e
pr: https://github.com/jlima004/E-commerce/pull/20
files_modified:
  - .github/workflows/api-docs.yml
  - apps/backend/src/api-docs/example-safety.ts
  - apps/backend/src/api-docs/registry.ts
  - apps/backend/src/api-docs/generation/validate.ts
  - apps/backend/src/api-docs/__tests__/generation.unit.spec.ts
must_haves:
  truths:
    - "O workflow API Docs executa explicitamente generation, coverage, security, exposure, money-units, native-extensions e as três suítes Store/Admin/Webhooks."
    - "ContractRegistryBundle.registerOperation(...) e validateDocument(...) rejeitam authorization com qualquer valor não nulo e strings de exemplo contendo cpf ou cnpj."
    - "Os dois gates de exemplos sensíveis compartilham a mesma política fail-closed sem remover casos já protegidos."
    - "Os artefatos OpenAPI permanecem byte a byte inalterados e todos os gates locais obrigatórios passam."
    - "As quatro threads só são respondidas e resolvidas depois do push e do workflow API Docs verde; novos reviews do Codex e Copilot são solicitados por último."
  artifacts:
    - path: .github/workflows/api-docs.yml
      provides: "Lista explícita completa das suítes unitárias de API Docs no CI"
    - path: apps/backend/src/api-docs/example-safety.ts
      provides: "Política interna única de detecção de exemplos sensíveis"
    - path: apps/backend/src/api-docs/__tests__/generation.unit.spec.ts
      provides: "Regressões equivalentes nas fronteiras registry e documento final"
    - path: .planning/quick/260802-o5a-corrigir-todos-os-reviews-validos-do-cod/260802-o5a-SUMMARY.md
      provides: "Relatório binário PASS ou BLOCKED com evidência local e de publicação"
  key_links:
    - from: apps/backend/src/api-docs/registry.ts
      to: apps/backend/src/api-docs/example-safety.ts
      via: "validação de exemplos antes do registro da operação"
    - from: apps/backend/src/api-docs/generation/validate.ts
      to: apps/backend/src/api-docs/example-safety.ts
      via: "validação integral do documento OpenAPI gerado"
    - from: .github/workflows/api-docs.yml
      to: apps/backend/src/api-docs/__tests__/generation.unit.spec.ts
      via: "comando Jest explícito do job Global closure gate"
---

# Quick Task 260802-o5a — Remediar todos os reviews válidos da PR #20

## Objetivo

Corrigir as quatro threads abertas da PR #20 com o menor diff relacionado: fechar a lacuna de cobertura do workflow, unificar a política fail-closed de exemplos sensíveis nas duas fronteiras, provar os quatro casos solicitados com regressões equivalentes e publicar somente após uma auditoria independente e todos os gates verdes.

## Escopo e invariantes

- Trabalhar exclusivamente na branch `gsd/api-docs-wave-6-global-closure`, partindo do HEAD revisado `1700ad822c8955a7c85ca084491a288446b4980e`. Se a branch, PR, base revisada ou conjunto de quatro threads não corresponder ao contrato, classificar `BLOCKED` antes de editar.
- Preservar integralmente os casos sensíveis já cobertos, os contratos HTTP e os três JSON gerados. Não executar o gerador para “limpar” o checkout antes de `openapi:check`.
- Não fazer merge, deploy, migration, alteração de env/secrets/providers, mudança de package/lockfile, skip, waiver, suppression, redução de testes ou edição de arquivo não relacionado.
- O agente principal é o único autorizado a integrar mudanças, criar commits, fazer push, editar o corpo da PR, responder/resolver threads e solicitar reviews. Subagentes não podem executar mutações Git/GitHub nem criar commits.
- Resultado binário: qualquer gate obrigatório vermelho, artefato gerado alterado sem necessidade demonstrada, divergência de escopo ou ausência de prova por thread encerra o quick task como `BLOCKED`; não suavizar para “PASS com ressalvas”.

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Auditar em paralelo e implementar as duas causas-raiz</name>
  <files>.github/workflows/api-docs.yml, apps/backend/src/api-docs/example-safety.ts, apps/backend/src/api-docs/registry.ts, apps/backend/src/api-docs/generation/validate.ts, apps/backend/src/api-docs/__tests__/generation.unit.spec.ts</files>
  <behavior>
    - Caso 1: `{ authorization: "******" }` é rejeitado por `ContractRegistryBundle.registerOperation(...)` e `validateDocument(...)`.
    - Caso 2: `{ authorization: "redacted" }` é rejeitado pelas duas fronteiras, provando que a chave é sensível independentemente do valor.
    - Caso 3: `{ note: "cpf informado" }` é rejeitado pelas duas fronteiras.
    - Caso 4: `{ description: "cnpj presente" }` é rejeitado pelas duas fronteiras.
    - Caso 5: todos os exemplos inseguros e seguros já existentes mantêm seu comportamento.
  </behavior>
  <action>Após confirmar branch, HEAD, PR e quatro threads por leitura thread-aware, iniciar três subagentes simultâneos com ownership não sobreposto: (1) CI coverage, somente leitura de `.github/workflows/api-docs.yml`, para confirmar a omissão e recomendar a menor lista explícita estável; (2) security, somente leitura integral de `registry.ts` e `generation/validate.ts`, para comparar padrões, normalização, recursão e fronteiras de `example/examples` e reportar toda divergência adicional dentro do mesmo contrato; (3) tests, proprietário apenas de `generation.unit.spec.ts`, para escrever primeiro as regressões negativas equivalentes dos quatro payloads sem apagar casos existentes. O principal deve consolidar os achados, adicionar as três suítes `store-contract.unit.spec.ts`, `admin-contract.unit.spec.ts` e `webhooks-contract.unit.spec.ts` à lista explícita do workflow e centralizar a política duplicada em `example-safety.ts`, importada pelo registry e pelo validador final. O helper deve preservar todas as detecções atuais, acrescentar `authorization` aos segmentos de chave e `cpf|cnpj` aos padrões textuais comuns; os chamadores preservam seus erros públicos atuais. Não ampliar a detecção fora de exemplos OpenAPI nem alterar contratos/serialização. Se a auditoria security encontrar divergência adicional válida dentro desses mesmos detectores, cobri-la simetricamente e com regressão; se exigir arquivo ou comportamento fora do escopo autorizado, parar como `BLOCKED`.</action>
  <verify>
    <automated>rtk npm run test:unit -w @dtc/backend -- --runTestsByPath src/api-docs/__tests__/generation.unit.spec.ts</automated>
  </verify>
  <done>Os três pareceres estão registrados para o relatório; o workflow contém as nove suítes explícitas; os quatro payloads falham nas duas fronteiras; a política está compartilhada sem perda de casos anteriores; somente os cinco arquivos allowlisted foram tocados.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Executar a bateria obrigatória e obter auditoria final independente</name>
  <files>.planning/quick/260802-o5a-corrigir-todos-os-reviews-validos-do-cod/260802-o5a-SUMMARY.md</files>
  <action>Antes de qualquer commit ou push, capturar o diff dos três JSON gerados contra o HEAD revisado e executar, nessa ordem, `openapi:check`, `openapi:lint`, a suíte focada completa com as nove paths, lint, build e `git diff --check`. Confirmar também que package/lockfile, contratos OpenAPI gerados e qualquer arquivo fora da allowlist não mudaram. Depois dos gates do principal, iniciar o quarto subagente como auditor final, sem permissão de editar/commit/push/GitHub: ele deve revisar o diff completo, reexecutar os gates obrigatórios, conferir a matriz quatro threads → correção → teste e confirmar que os achados dos três subagentes foram integrados. Divergência entre auditor e principal é `BLOCKED` até correção e nova auditoria; o principal não pode autoaprovar o gate final.</action>
  <verify>
    <automated>rtk npm run openapi:check &amp;&amp; rtk npm run openapi:lint &amp;&amp; rtk npm run test:unit -w @dtc/backend -- --runTestsByPath src/api-docs/__tests__/generation.unit.spec.ts src/api-docs/__tests__/coverage.unit.spec.ts src/api-docs/__tests__/security.unit.spec.ts src/api-docs/__tests__/exposure.unit.spec.ts src/api-docs/__tests__/money-units.unit.spec.ts src/api-docs/__tests__/native-extensions.unit.spec.ts src/api-docs/__tests__/store-contract.unit.spec.ts src/api-docs/__tests__/admin-contract.unit.spec.ts src/api-docs/__tests__/webhooks-contract.unit.spec.ts &amp;&amp; rtk npm run lint -w @dtc/backend &amp;&amp; rtk npm run build -w @dtc/backend &amp;&amp; rtk git diff --check</automated>
  </verify>
  <done>Todos os comandos passam no principal e no auditor final; os JSON gerados estão byte a byte inalterados; o diff contém apenas a allowlist; as quatro threads têm evidência local específica; o auditor final retorna PASS explícito.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Publicar, confirmar CI e encerrar as threads na ordem autorizada</name>
  <files>.planning/quick/260802-o5a-corrigir-todos-os-reviews-validos-do-cod/260802-o5a-SUMMARY.md</files>
  <action>O agente principal deve cumprir estritamente esta sequência: (1) criar commit objetivo, preferencialmente `fix(api-docs): address Wave 6 review gaps`, contendo somente a allowlist; (2) fazer push para `gsd/api-docs-wave-6-global-closure`; (3) aguardar o workflow `API Docs` da PR #20 concluir com `success`, sem resolver ou responder threads enquanto estiver pending/failed; (4) corrigir o corpo da PR removendo a afirmação de draft e confirmar que a PR continua ready for review; (5) responder individualmente às quatro threads com commit e evidência do gate correspondente e então resolvê-las, verificando via leitura GraphQL/thread-aware `isResolved=true` em 4/4; (6) somente depois solicitar novos reviews do Codex e Copilot. Use o conector GitHub para metadados/patch e o script thread-aware do skill `gh-address-comments` via `gh` para anchors, `isOutdated` e `isResolved`; não trate comentários flat como prova de resolução. Se autenticação, push, workflow ou qualquer escrita GitHub falhar, registrar o ponto exato e encerrar `BLOCKED`, sem pular ou reordenar etapas.</action>
  <verify>
    <automated>rtk proxy gh pr checks 20 --repo jlima004/E-commerce &amp;&amp; rtk proxy gh pr view 20 --repo jlima004/E-commerce --json isDraft,headRefName,headRefOid,reviewRequests</automated>
  </verify>
  <done>Commit e push estão comprovados; o workflow API Docs do SHA publicado está verde; o corpo não afirma draft; as quatro threads têm reply e `isResolved=true`; novos reviews de Codex e Copilot foram solicitados por último. Qualquer item ausente produz SUMMARY `BLOCKED`.</done>
</task>

</tasks>

<threat_model>
## Fronteiras de confiança

| Fronteira | Risco | Mitigação do plano |
|---|---|---|
| Exemplo OpenAPI → registry/documento final | Credencial ou dado pessoal sintético escapar para artefato público | Política única fail-closed, regressões equivalentes e `openapi:check` sem writer anterior |
| Checkout local → GitHub PR #20 | Publicar diff alheio, responder thread errada ou resolver sem prova | allowlist, HEAD/PR pinados, leitura thread-aware e auditor final independente |
| Push → execução GitHub Actions | Resolver reviews antes de o CI provar o commit publicado | ordem rígida push → workflow `success` → replies/resolução → novos reviews |

## Registro STRIDE

| ID | Categoria | Componente | Disposição | Mitigação |
|---|---|---|---|---|
| T-Q260802-01 | Information Disclosure | exemplos OpenAPI | mitigate | rejeitar chave `authorization` e strings `cpf|cnpj` nas duas fronteiras com testes negativos |
| T-Q260802-02 | Tampering | branch/diff publicado | mitigate | validar branch/HEAD, limitar arquivos, provar JSONs inalterados e auditar o diff antes do commit |
| T-Q260802-03 | Repudiation | threads de review | mitigate | resposta individual com SHA/evidência e confirmação GraphQL de resolução 4/4 |
| T-Q260802-04 | Elevation of Privilege | operações GitHub | mitigate | somente o principal faz mutações; sem merge, deploy, secrets ou providers |
| T-Q260802-SC | Tampering | dependências | accept | nenhuma instalação ou alteração de package/lockfile pertence ao escopo |
</threat_model>

## Relatório obrigatório

Criar `260802-o5a-SUMMARY.md` usando exatamente a estrutura pedida no briefing: decisão `API-DOCS WAVE 6 REVIEW REMEDIATION: PASS | BLOCKED`; base/head anterior; resultado dos quatro subagentes; estado individual das quatro threads; testes adicionados e contagem focada; OpenAPI check/lint; lint; build; diff de artefatos; arquivos alterados; SHA; push; corpo da PR; replies e resoluções; workflow; novos reviews. Não declarar PASS se qualquer campo obrigatório estiver pendente, falho ou sem evidência.

## Critérios de sucesso

- As quatro observações válidas possuem correção e regressão rastreável.
- CI local, workflow remoto e auditor final estão verdes para o mesmo commit publicado.
- Artefatos gerados e escopo proibido permanecem inalterados.
- PR body está correto, threads estão respondidas/resolvidas 4/4 e novos reviews foram solicitados na ordem autorizada.
