---
quick_id: 260821-r4s
slug: b15-07-hr-01-alinhar-security-contract-d
description: "B15-07-HR-01: alinhar security contract do Cart M1 ao runtime BFF"
status: planned
must_haves:
  truths:
    - "As seis operações Cart M1 exigem bffServiceCredential e publishableApiKey em todas as três alternativas."
    - "A alternativa guest não contém customerBearer nem customerSession."
    - "Runtime, manifest e artefatos Admin/Webhooks permanecem inalterados."
  artifacts:
    - path: "apps/backend/src/api-docs/components/security-schemes.ts"
      provides: "Política de segurança específica do Cart M1."
    - path: "apps/backend/src/api-docs/operations/store/carts.ts"
      provides: "Aplicação da política às seis operações Cart M1."
    - path: "apps/backend/src/api-docs/__tests__/store-contract.unit.spec.ts"
      provides: "Regressão do exact-set de segurança Cart M1."
    - path: "apps/backend/src/api-docs/generated/store.openapi.json"
      provides: "Artefato Store produzido pelo writer."
  key_links:
    - from: "apps/backend/src/api-docs/operations/store/carts.ts"
      to: "STORE_CART_M1_BFF_OPTIONAL_CUSTOMER"
      via: "registry security metadata"
---

# Plano rápido — B15-07-HR-01

## Objetivo

Remediar exclusivamente o drift do contrato OpenAPI das seis operações Cart M1,
exigindo `bffServiceCredential` e `publishableApiKey` em todas as três
alternativas de segurança, sem alterar o runtime ou a superfície publicada.

## Escopo autorizado

- `apps/backend/src/api-docs/components/security-schemes.ts`
- `apps/backend/src/api-docs/operations/store/carts.ts`
- `apps/backend/src/api-docs/__tests__/store-contract.unit.spec.ts`
- `apps/backend/src/api-docs/generated/store.openapi.json` somente por writer
- `.planning/phases/15-guest-cart-capability-concurrency/15-07-SUMMARY.md`

## Tarefas

### 1. Corrigir registry e teste regressivo

**Files:** `apps/backend/src/api-docs/components/security-schemes.ts`,
`apps/backend/src/api-docs/operations/store/carts.ts`,
`apps/backend/src/api-docs/__tests__/store-contract.unit.spec.ts`

**Action:** Criar a política Cart M1 e aplicá-la somente às seis operações;
adicionar o teste do exact-set.

**Verify:** Unit focado passa e cobre os locks anteriores.

**Done:** Seis operações, três alternativas exatas e nenhum publishable-only.

- Criar uma política Cart M1 específica, profundamente readonly, com exatamente
  as alternativas guest, customer bearer e customer session, todas contendo
  `bffServiceCredential` e `publishableApiKey`.
- Aplicar a política somente às seis operações Cart M1 em `carts.ts`.
- Adicionar teste explícito que selecione exatamente seis operações e valide o
  exact-set das três alternativas, inclusive ausência de credencial Customer no
  guest e ausência de mistura bearer/session.

**Verificação:** o teste unitário focado deve passar e continuar cobrindo os
locks anteriores do Cart M1.

### 2. Regenerar e validar somente a superfície Store

**Files:** `apps/backend/src/api-docs/generated/store.openapi.json`

**Action:** Rodar o unit focado, gerar apenas Store pelo writer, inspecionar o
JSON, comparar hashes Admin/Webhooks e rodar lint.

**Verify:** Unit `23/23` (ou total real), writer PASS, lint PASS e hashes
Admin/Webhooks inalterados.

**Done:** JSON Store reflete o exact-set e `openapi:check` não foi executado.

- Executar o teste unitário focado.
- Gerar somente `store.openapi.json` com o writer.
- Inspecionar as seis operações no JSON para confirmar as três alternativas e
  zero alternativa publishable-only.
- Executar `openapi:lint`.
- Comparar hashes/diff dos artefatos Admin e Webhooks para confirmar invariância.

**Verificação:** Store writer PASS, lint PASS, Admin/Webhooks inalterados e
nenhuma execução de `openapi:check`.

### 3. Registrar a remediação e fechar o checkpoint técnico

**Files:** `.planning/phases/15-guest-cart-capability-concurrency/15-07-SUMMARY.md`

**Action:** Acrescentar a seção factual da remediação, resultados, não-ações e
estado de revisão humana pendente.

**Verify:** `git diff --check`, status/log e allowlist final.

**Done:** Summary preserva `15-07 HUMAN REVIEW: PENDING` e `15-08` não iniciado.

- Atualizar `15-07-SUMMARY.md` com a seção factual B15-07-HR-01, resultados
  reais, `git diff --check`, commit e `openapi:check: NOT RUN`.
- Preservar `15-07 HUMAN REVIEW: PENDING`, `15-08: NOT STARTED / NOT AUTHORIZED`
  e `DEPLOY: NOT AUTHORIZED`.
- Executar validação final de status/log sem alterar runtime, manifest ou
  demais superfícies.

**Verificação:** `git diff --check` PASS e allowlist final confirmada.

## Restrições

- Não alterar runtime, middleware, manifest, exact-set, capability domain,
  idempotência, CAS/concurrency, migrations, env ou dependencies.
- Não gerar Admin/Webhooks OpenAPI.
- Não executar `npm run openapi:check`.
- Não iniciar 15-08, fazer push, PR, merge ou deploy.
