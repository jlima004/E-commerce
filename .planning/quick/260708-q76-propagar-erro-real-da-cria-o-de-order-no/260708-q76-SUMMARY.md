---
quick_id: 260708-q76
status: complete
completed_at: 2026-07-08
---

# Quick Task 260708-q76: Propagar erro real da criacao de Order no fluxo payment_intent.succeeded

## Resultado

PASS local.

## Correcoes

- `create-order-from-confirmed-attempt` agora descreve falhas reais com `error_name`, `error_code`, `error_message`, `cause.message`, tipo e string segura para erro desconhecido.
- `webhook-order-entrypoint` persiste a falha real no `CheckoutCompletionLog`, com codigo especifico e metadata diagnostica:
  - `order_creation_error_name`
  - `order_creation_error_code`
  - `order_creation_error_message`
  - `order_creation_error_cause_message`
  - `order_creation_error_step`
  - `cart_id`
  - `payment_attempt_id`
  - `payment_intent_id`
- A rota Stripe propaga o erro enriquecido para `WebhookEventLog.error_code/error_message` e adiciona metadata de Order quando disponivel.
- A rota Stripe emite log estruturado sanitizado para falha de criacao de Order, com `event_id`, `event_type`, `payment_intent_id`, `payment_attempt_id`, `cart_id`, `checkout_completion_log_id`, `error_name`, `error_code` e `error_message`.
- Allowlists de metadata foram ampliadas somente para as chaves diagnosticas desse gate.
- Nenhuma migration, package file ou provider real foi alterado/chamado.

## Validacoes

- Comando solicitado no root:
  - `TMPDIR=/tmp npm run test:unit -- --runTestsByPath apps/backend/src/workflows/order/__tests__/webhook-order-entrypoint.unit.spec.ts apps/backend/src/api/hooks/stripe/__tests__/stripe-webhook-route.unit.spec.ts`
  - Resultado: FAIL esperado/local, root sem script `test:unit`.
- Equivalente no workspace:
  - `TMPDIR=/tmp npm run test:unit -w @dtc/backend -- --runTestsByPath src/workflows/order/__tests__/webhook-order-entrypoint.unit.spec.ts src/api/hooks/stripe/__tests__/stripe-webhook-route.unit.spec.ts`
  - Resultado: PASS, 2 suites / 20 testes.
- Suite order:
  - `TMPDIR=/tmp npm run test:unit -w @dtc/backend -- --runInBand src/workflows/order`
  - Resultado: PASS, 6 suites / 50 testes.
- Sanitizers e allowlists tocados:
  - `TMPDIR=/tmp npm run test:unit -w @dtc/backend -- --runTestsByPath src/modules/checkout-completion/__tests__/checkout-completion-log.unit.spec.ts src/modules/webhooks/__tests__/webhook-event-log.unit.spec.ts src/observability/__tests__/logger.unit.spec.ts`
  - Resultado: PASS, 3 suites / 44 testes.
- Build:
  - `HOME=/tmp XDG_CONFIG_HOME=/tmp TMPDIR=/tmp ADMIN_DISABLED=true npm run build -w @dtc/backend`
  - Resultado: PASS.
- Diff:
  - `git diff --check`
  - Resultado: PASS, sem saida.
- Package files:
  - `git diff --name-only package.json package-lock.json apps/backend/package.json`
  - Resultado: PASS, sem saida.

## Escopo respeitado

- Sem Phase 12.
- Sem refund smoke.
- Sem refund Stripe.
- Sem `sk_live`.
- Sem Gelato real.
- Sem Correios.
- Sem Order manual no banco.
- Sem migration nova.
- Sem alteracao em `package.json`, `package-lock.json` ou `apps/backend/package.json`.
- Sem delecao de `WebhookEventLog`.
- Sem chamadas reais via conectores Stripe/Supabase.
