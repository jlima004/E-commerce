---
quick_id: 260708-q76
status: planned
created_at: 2026-07-08
---

# Quick Task 260708-q76: Propagar erro real da criacao de Order no fluxo payment_intent.succeeded

## Escopo

Gate tecnico aprovado para eliminar `error_message` vazio/generico no fluxo `payment_intent.succeeded -> webhook-order-entrypoint -> create-order-from-confirmed-attempt -> completeCart`.

## Tarefas

1. Propagar erro terminal de Order
   - files: `apps/backend/src/workflows/order/webhook-order-entrypoint.ts`, `apps/backend/src/workflows/order/steps/create-order-from-confirmed-attempt.ts`, `apps/backend/src/modules/checkout-completion/service.ts`
   - action: extrair `name`, `message`, `code`, `cause.message` e fallback seguro; persistir mensagem real sanitizada, codigo especifico e metadata diagnostica no `CheckoutCompletionLog`.
   - verify: testes focados de order entrypoint/creation.

2. Registrar falha estruturada no webhook Stripe
   - files: `apps/backend/src/api/hooks/stripe/route.ts`, `apps/backend/src/modules/webhooks/service.ts`
   - action: manter `WebhookEventLog.error_message` nao-nulo quando houver mensagem, enriquecer metadata permitida e emitir log estruturado sanitizado sem body completo, `client_secret`, PAN ou payload sensivel.
   - verify: teste focado da rota Stripe.

3. Fechar gate e provas negativas
   - files: `.planning/quick/260708-q76-propagar-erro-real-da-cria-o-de-order-no/260708-q76-SUMMARY.md`, `.planning/STATE.md`
   - action: registrar validacoes, escopo respeitado e nao-acoes.
   - verify: `git diff --check` e prova de package files sem diff.

## Proibido neste gate

- Nao iniciar Phase 12.
- Nao executar refund smoke, criar refund Stripe ou usar `sk_live`.
- Nao chamar Gelato real, Correios ou criar Order manualmente no banco.
- Nao aplicar migration nova.
- Nao alterar `package.json`, `package-lock.json` ou `apps/backend/package.json`.
- Nao apagar `WebhookEventLog`.
